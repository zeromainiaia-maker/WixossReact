# round4 型台帳（1バッチ1行）★止め時の判定に使う唯一のカウンタ

**運用は PLAN §2.6。** 監査の目的は「カードの網羅」ではなく**「まだ知らない“型”の発見」**。
🔴**止め時＝連続3バッチで「新型 0」**（残枚数では判断しない）。
🔑**真バグを引いたら、直す前に `grep` で同型の全数を数える**（1枚だけ直して満足しない）。

| バッチ | 枚数 | findings | 新型 | 型の内容（新しく分かった壊れ方） | 状態 |
|---|---|---|---|---|---|
| r4-01 | 10 | 0 | 0 | — | 済 |
| r4-02 | 10 | 2 | 1 | **クロス宣言の接頭辞が捨てられ `crossOnly` が付かない**（→ grep で**10枚**に展開） | 済（第197バッチで修正） |
| r4-03 | 10 | 4 | 1 | **「この方法で〜した1枚につき」が固定 `count:1`**／同時に**バースト抑制 STUB の順序と向き**（`WX08-010`） | 済（第197バッチで修正） |
| r4-04 | 10 | 0 | 0 | — | 済 |
| r4-05 | 10 | 0 | 0 | — | 済 |
| r4-06 | 10 | 2 | **2** | ①**受け皿はあるのに表現が総称 timing へ落ち、engine の別経路が食って意味が反転**（`ON_FRONT_SIGNI_ATTACK`→`ON_ATTACK_SIGNI`）②**id 名の範囲 ≠ engine の適用範囲**（FP の派生で発見＝`IGNORE_LRIG_RESTRICTION_ARTS` が**シグニ召喚まで**無視する＝PLAN §5.3 `O-268`） | 済（第199バッチ・①は修正／②は登録） |
| r4-07 | 10 | 0 | 0 | — | 済 |
| r4-08 | 10 | 1 | 0 | 「〜まで」クラッシュが固定 count:1＝**r4-03 と同じ「既定値のある枚数 regex が外れて1へ落ちる」族**（受け皿 `upToCount` は新設したが**壊れ方の型は既知**） | 済（第199バッチで修正） |
| r4-09 | 10 | 1 | 済 | `WX11-034` パワー修正の対象フィルターに `hasCharm:true` が欠落 | 済（第207で triage） |
| r4-10 | 10 | 2 | 済 | `WX07-017` owner が `self` になっている（本来は対戦相手／各プレイヤー） | 済（第207で triage） |
| r4-11 | 10 | 2 | 済 | `WX03-002` 除外条件が包含条件に反転／`WX11-025` バーストの最終処理が丸ごと欠落 | 済（第207で triage） |
| r4-12 | 10 | 2 | 済 | `WX07-039` 位置限定（正面）欠落／`WX03-024` ルリグタイプ限定欠落＝**この2件は claude -p が自由形式(markdown)で応答し JSON 契約を守らなかったため手動で findings.jsonl へ復元**（下記「踏んだ罠」参照） | 済（第207で triage） |
| r4-13 | 10 | 2 | 済 | `WX04-037` パワー減少に期間指定(duration)が無い／`WX07-033` 直前バニッシュ対象への絞り込み欠落 | 済（第206〜207で全数 triage） |
| r4-14 | 10 | 3 | 済 | `WX09-037` 前半アクション丸ごと欠落＋対象条件欠落／`WX10-002` did-it ゲート疑い（規則13対象かは要確認） | 済（第207で triage） |
| r4-15 | 10 | 5 | 済 | `WX08-005` 自己限定フィルター欠落／`WX10-015` 対象が相手シグニに誤り／`WX05-005` timing が MAIN 固定／`WX11-021` コスト軽減の誤複製＋条件限定欠落 | 済（第206〜207で全数 triage） |
| r4-16 | 10 | 4 | 済 | `WX06-019` 発生源限定・自己除外・duration の3点欠落／`WX09-028` 発生源限定欠落 | 済（第206〜207で全数 triage） |
| r4-17 | 10 | 3 | 済 | `WX07-030` isUp フィルター欠落／`WX07-021` 「好きな数」が count:ALL 固定／`WX06-014` 並び順選択の未表現 | 済（第207で triage） |
| r4-18 | 10 | 1 | 済 | `WX07-032` 2つの別対象アクションのうち1つが欠落 | 済（第207で triage） |
| r4-19 | 10 | 0 | 0 | — | 済 |
| r4-20 | 10 | 1 | 済 | `WX09-020` クロス相手の限定（特定カード名）が crossOnly のみで未表現 | 済（第207で triage） |
| r4-21 | 10 | 2 | 済 | `WX05-007` did-it 分岐欠落（規則13対象かは要確認）／`WX10-051` バーストのパワー修正が duration:INSTANT | 済（第206〜207で全数 triage） |
| r4-22 | 10 | 3 | 済 | `WX11-006` SEARCH と TRASH の count 不一致／同 STUB id 名の疑い／`WX04-103` duration 欠落 | 済（第206〜207で全数 triage） |
| r4-23 | 10 | 4 | 済 | `WX09-032` 代替コストの発動条件が丸ごと未表現／`WX03-028`・`WX11-036` 自己限定フィルター欠落2件／`WX11-036` activeCondition の条件違い | 済（第206〜207で全数 triage） |
| r4-24 | 10 | 2 | 済 | `WX05-015` duration:INSTANT／`WX04-004` 追加コスト(手札破棄)が payload に無い | 済（第206〜207で全数 triage） |
| r4-25 | 10 | 5 | 済 | `WX05-030` 名前一致条件欠落／`WX10-028`・`WX08-023` cardType フィルター誤り2件／`WX04-049` 自己限定フィルター欠落／`WX10-031` 手札に戻す処理が丸ごと欠落 | 済（第206〜207で全数 triage） |
| r4-26 | 2 | 1 | 済 | `WX05-025` 「そのシグニ」が特定カードに固定されず任意対象＝**claude -p が閉じ括弧を書き忘れ、JSON.parse が失敗**（手動で復元。`extractJson` に括弧補完のフォールバックを追加済み） | 済（第207で triage） |

🔴🔑**2026-09-07（第205バッチ・Sonnet 5・S-1）＝18バッチ一括実行で歩留まりが急変した**＝
**r4-01〜08（80枚・8バッチ）は findings 9件（1.1件/バッチ）だったのに、r4-09〜26（172枚・18バッチ）は findings 43件（2.4件/バッチ）**。
🏁**2026-09-07（第206〜207バッチ）に全数 triage した結果＝真バグ26 / 偽陽性17（precision 60%）**＝
**「頻度2倍」は偽陽性率の上昇ではなく、真バグも偽陽性も両方増えていた**（当時の「相当数が偽陽性だろう」という予測は**半分だけ当たり**）。
🔑**残数を予測しない**＝この行は「triage するまで意味は不明」と書いた点だけが正しかった。
🔑**この回で踏んだ罠2つ**＝
①**claude -p が JSON 契約を守らず自由形式(markdown)で応答することがある**（r4-12＝バッチ04）＝`engine ソースまで確認した` と自称する詳細な散文レポートを返し、`{"results":[...]}` を一切出力しなかった。**自動リトライは実装していない**（頻度が低く複雑）＝手動で findings.jsonl へ書き戻した。
②**claude -p が JSON の閉じ括弧を書き忘れて打ち切ることがある**（r4-26＝バッチ18・`stop_reason:"end_turn"` で正常終了なのに構文が不完全）＝`extractJson` に**開き括弧と閉じ括弧の数を比較して不足分を補うフォールバック**を追加した（`scripts/semanticAuditRun.mjs`）。

### 🔵 第206バッチ（2026-09-07・Opus・O-A triage 第2回）＝9件を triage → **新型 0**

未triage は **43 → 33**（この回で9件）。**残り33件は第207バッチで全数 triage した**（下節）。

| 判定 | 件数 | 型 | 新型か |
|---|---|---|---|
| **FP** | **5** | 🆕**パワー修正に `duration` が無いのは正準形**＝engine は `temp_power_mods` に積み**ターン終了時に必ずクリア**する（`effectExecutor.ts:2188`／`turnScopedState.ts:444`）。`duration` は**ターン終了より長い**ものを表す語彙。効果トップの `duration:"INSTANT"` はパワー修正の寿命と無関係 | **FP の新型**（→ 規則18） |
| **FP** | **3** | 🆕**【常】の `target.count:1` は「このシグニ自身」が正準形**＝`POWER_SET`（`effectEngine.ts:2482`）／`SET_BASE_LEVEL`（`applyContinuousBaseLevelOverride`）／`GRANT_PROTECTION`（`collectProtectedSigni:6008`・`collectEffectImmuneSigni:6500`）が揃って `count!=='ALL'` を効果元 top として解決する。`thisCardOnly` は不要 | **FP の新型**（→ 規則19） |
| 🔴**BUG** | **1** | `WX11-036-E1`＝**助詞「は」＋読点なしで先頭条件節 regex が外れ `IS_SELF_UP` が丸ごと落ちる**（ダウン中も効果耐性が立つ過剰実行） | ❌**既知の型**＝§5d-0 のミラー表コメント③「このシグニ**は**アクセ」と同族の**綴りの穴** |

🔑**この回の収穫は「バグ 1件」より「FP 型 2つ」**＝規則18/19 は
**当時の残33件の再読を止める**うえ、Sheet2 以降 356枚の全バッチに効く。
🔴**新型 0**＝止め時カウンタ（§2.6 決定1「連続3バッチで新型0」）は**1本目**。
⚠ただし**ここでの「バッチ」は監査バッチ（10枚）ではなく triage 回**なので、
**r4-01〜08 の 済 8バッチのうち新型0は r4-01/04/05/07/08 の5つ**＝**残 33件の triage を終えるまで止め時は判定しない。**

🔴🔑**この回で踏んだ罠（LESSONS §4.2 へ還元済み）**＝**先頭条件節の regex を緩めると主語まで食う**。
`^このシグニ[はが]…であるかぎり、?` と読点を optional にした瞬間、
後段の「対戦相手の効果を受けない」が主語を失って **`target.count` が `1` → `'ALL'`（自分の全シグニ）**へ広がった。
**過小（条件欠落）を直して過剰（対象拡大）を作った**＝LESSONS §4.2「1つの根から過剰実行と過小実行が同時に出る」の実例。
⇒ **読点なしの形は主語を `rest` へ戻す**。⇒ **golden は「条件が載ったか」と「対象が広がっていないか」を両方 assert する。**

**偽陽性から得た型（プロンプト規則へ還元済み＝規則13〜19）**：
did-it ゲート（`DID_IT_GATED_TYPES`）／`OPPONENT_PAY_OPTIONAL` の既定の極性／STUB の id 名と payload の食い違い／
🆕**id 名に入っている種別（ARTS/SPELL/SIGNI）は適用範囲を表さない**（規則17・第199バッチ）／
🆕**パワー修正の `duration` 省略は正準形**（規則18・第206バッチ）／🆕**【常】の `count:1` は自身**（規則19・第206バッチ）。

### 🔵 第207バッチ（2026-09-07・Opus・O-A triage 第3回）＝**残33件を全数 triage** → 新型 0

**未 triage 0**（52件すべて判定済み）。**BUG 26 / FP 7**（この回だけなら BUG 26 / FP 7＝**precision 79%**）。

> 🔴🔑**第206バッチ（precision 11%）との差は「取る順」だけ**＝206 は同型が固まっている FP クラスタを先に抜いたので、
> **残りは真バグの密度が上がった**。⇒ 🔑**precision は母集団の性質ではなく、triage の順序で動く指標**＝
> **「バッチごとの precision」を歩留まりの指標として読んではいけない。**
> 🔑**round4 全体の precision＝32/52（62%）**＝パイロット30枚の 50% から**規則13〜19 で改善している**。

**新しく分かった FP 型5つ（→ 規則20〜24）**＝どれも **engine／UI／カードデータの受け皿を読まないと判定できない**：

| 規則 | 型 | 受け皿 |
|---|---|---|
| 20 | **キーワードは本文に無くても展開される** | 【チェイン】→ `COST_REDUCTION{targetCardType:'アーツ'}`（`effectParser.ts:23630`） |
| 21 | **「アップ状態の〜をダウンする」コストに `isUp` は要らない** | `useTimeCost.ts:97` が既ダウンを候補から外す |
| 22 | **`crossOnly:true` はクロス相手を問わない印ではない** | `getCrossConditionText(card)`＋`evaluateCrossCondition` がカードデータから解決 |
| 23 | **STUB ハンドラが自前の did-it ゲートを持つことがある** | `LRIG_UNDER_TO_TRASH`（`effectExecutor.ts:5294`） |
| 24 | **`SEARCH` の `then` は選択カード1枚ずつに適用** | `effectExecutor.ts:11041` の `for (const id of picked)` |

**新型（＝新しい壊れ方）は 0**＝26件の BUG はすべて既知の型（表現の穴／丸ごと欠落／限定の欠落／engine が別の意味で読む）。
🔴**止め時カウンタは連続2本目**（206＝新型0／207＝新型0）。**あと1回 新型0 なら §2.6 決定1 により監査を止める。**
⚠**ただしこの2回は「新規監査バッチ」ではなく triage 回**＝止め時の判定は **Sheet2 の新規バッチ**で取り直すのが筋。

**この回で直したのは3件**（受け皿が既にあったもの）＝`WX03-002-E1`（否定 → `cardClassExclude`）／
`WX11-034-BURST`（「その後、」節の `hasCharm`）／`WX09-028-E1`（`discardCauseCardTypes`＋`byWatcherEffect`）。
**残 23件は PLAN §5.0 の実装キュー（15件）と §5.3 の機構項目 `O-272`〜`O-279`（8件）へ登録した。**
🔴🔑**「BUG」と triage した瞬間、この台帳からも `semanticAuditPool.mjs` からも `census:cards` からも消える**
（3つとも数えているのは*未 triage*であって*未修正*ではない）＝**追跡先は PLAN §5.0 の表だけ。**

---

## 累計

- 🏁**Sheet1（round4）は 2026-09-07（第205バッチ）に監査完了＝252枚 / 26バッチ全消化**。
  `node scripts/archive/semanticAuditGap.mjs --sheet 1` の未監査は **0**。**次に取るのは Sheet2（未監査356枚）**。
- 🏁**監査済＝252枚 / findings 52件＝全数 triage 済み（真バグ32 / 偽陽性20＝precision 62%）／未triage 0**。
- 🏁**r4-01〜26 は全バッチ triage 済み**（第206バッチで9件・第207バッチで33件）。**次に取るのは Sheet2（未監査356枚）。**
- **全52件の precision＝32/52（62%）**。⚠**バッチ単位の precision は triage の順序で動く**（第206＝11%／第207＝79%）＝**歩留まりの指標として読まない**。
  ⇒ 上の「finding 頻度が2倍」は**偽陽性率の上昇ではなく、単に真バグも偽陽性も両方増えていた**（r4-09〜26 の真バグ26件は本物）。
- 🔑**FP も無駄ではない**＝今回の唯一の FP（`WX05-006-E2`）が、**JSON だけを読む監査員には原理的に見えない別バグ**
  （適用範囲の過剰）を engine 読解の過程で炙り出した。⇒ **FP を「読み損」と数えない。**
- 🔑**プロンプト規則は 24本**（第207バッチで規則20〜24＝キーワード展開／`signi_down` コスト／`crossOnly`／STUB 自前ゲート／`SEARCH` の `then`）。
- ⚠**r4-09〜26 の finding 頻度は r4-01〜08 の2倍以上**（1.1件/バッチ→2.4件/バッチ）。**triage するまで意味は不明**
  （偽陽性率が上がっただけの可能性が高い＝did-it ゲート・duration 既定値・STUB id 名疑いに該当しそうな finding が複数混在）。
