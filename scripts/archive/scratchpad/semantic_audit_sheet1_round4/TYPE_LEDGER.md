# round4 型台帳（1バッチ1行）★止め時の判定に使う唯一のカウンタ

**運用は PLAN §2.6。** 監査の目的は「カードの網羅」ではなく**「まだ知らない“型”の発見」**。
🔴**止め時＝連続3バッチで「新型 0」**（残枚数では判断しない）。
🔑**真バグを引いたら、直す前に `grep` で同型の全数を数える**（1枚だけ直して満足しない）。

| バッチ | 枚数 | findings | 新型 | 型の内容（新しく分かった壊れ方） | 状態 |
|---|---|---|---|---|---|
| r4-01 | 10 | 0 | 0 | — | 済 |
| r4-02 | 10 | 2 | 1 | **クロス宣言の接頭辞が捨てられ `crossOnly` が付かない**（→ grep で**10枚**に展開） | 済（第197バッチで修正） |
| r4-03 | 10 | 4 | 1 | **「この方法で〜した1枚につき」が固定 `count:1`**／同時に**バースト抑制 STUB の順序と向き**（`WX08-010`） | 済（第197バッチで修正） |
| r4-04 | 10 | 0 | 0 | — | 済 |
| r4-05 | 10 | 0 | 0 | — | 済 |
| r4-06 | 10 | 2 | **2** | ①**受け皿はあるのに表現が総称 timing へ落ち、engine の別経路が食って意味が反転**（`ON_FRONT_SIGNI_ATTACK`→`ON_ATTACK_SIGNI`）②**id 名の範囲 ≠ engine の適用範囲**（FP の派生で発見＝`IGNORE_LRIG_RESTRICTION_ARTS` が**シグニ召喚まで**無視する＝PLAN §5.3 `O-268`） | 済（第199バッチ・①は修正／②は登録） |
| r4-07 | 10 | 0 | 0 | — | 済 |
| r4-08 | 10 | 1 | 0 | 「〜まで」クラッシュが固定 count:1＝**r4-03 と同じ「既定値のある枚数 regex が外れて1へ落ちる」族**（受け皿 `upToCount` は新設したが**壊れ方の型は既知**） | 済（第199バッチで修正） |
| r4-09 | 10 | 1 | ? | `WX11-034` パワー修正の対象フィルターに `hasCharm:true` が欠落 | **未triage**（O-A待ち） |
| r4-10 | 10 | 2 | ? | `WX07-017` owner が `self` になっている（本来は対戦相手／各プレイヤー） | **未triage**（O-A待ち） |
| r4-11 | 10 | 2 | ? | `WX03-002` 除外条件が包含条件に反転／`WX11-025` バーストの最終処理が丸ごと欠落 | **未triage**（O-A待ち） |
| r4-12 | 10 | 2 | ? | `WX07-039` 位置限定（正面）欠落／`WX03-024` ルリグタイプ限定欠落＝**この2件は claude -p が自由形式(markdown)で応答し JSON 契約を守らなかったため手動で findings.jsonl へ復元**（下記「踏んだ罠」参照） | **未triage**（O-A待ち） |
| r4-13 | 10 | 2 | ? | `WX04-037` パワー減少に期間指定(duration)が無い／`WX07-033` 直前バニッシュ対象への絞り込み欠落 | **未triage**（O-A待ち） |
| r4-14 | 10 | 3 | ? | `WX09-037` 前半アクション丸ごと欠落＋対象条件欠落／`WX10-002` did-it ゲート疑い（規則13対象かは要確認） | **未triage**（O-A待ち） |
| r4-15 | 10 | 5 | ? | `WX08-005` 自己限定フィルター欠落／`WX10-015` 対象が相手シグニに誤り／`WX05-005` timing が MAIN 固定／`WX11-021` コスト軽減の誤複製＋条件限定欠落 | **未triage**（O-A待ち） |
| r4-16 | 10 | 4 | ? | `WX06-019` 発生源限定・自己除外・duration の3点欠落／`WX09-028` 発生源限定欠落 | **未triage**（O-A待ち） |
| r4-17 | 10 | 3 | ? | `WX07-030` isUp フィルター欠落／`WX07-021` 「好きな数」が count:ALL 固定／`WX06-014` 並び順選択の未表現 | **未triage**（O-A待ち） |
| r4-18 | 10 | 1 | ? | `WX07-032` 2つの別対象アクションのうち1つが欠落 | **未triage**（O-A待ち） |
| r4-19 | 10 | 0 | 0 | — | 済 |
| r4-20 | 10 | 1 | ? | `WX09-020` クロス相手の限定（特定カード名）が crossOnly のみで未表現 | **未triage**（O-A待ち） |
| r4-21 | 10 | 2 | ? | `WX05-007` did-it 分岐欠落（規則13対象かは要確認）／`WX10-051` バーストのパワー修正が duration:INSTANT | **未triage**（O-A待ち） |
| r4-22 | 10 | 3 | ? | `WX11-006` SEARCH と TRASH の count 不一致／同 STUB id 名の疑い／`WX04-103` duration 欠落 | **未triage**（O-A待ち） |
| r4-23 | 10 | 4 | ? | `WX09-032` 代替コストの発動条件が丸ごと未表現／`WX03-028`・`WX11-036` 自己限定フィルター欠落2件／`WX11-036` activeCondition の条件違い | **未triage**（O-A待ち） |
| r4-24 | 10 | 2 | ? | `WX05-015` duration:INSTANT／`WX04-004` 追加コスト(手札破棄)が payload に無い | **未triage**（O-A待ち） |
| r4-25 | 10 | 5 | ? | `WX05-030` 名前一致条件欠落／`WX10-028`・`WX08-023` cardType フィルター誤り2件／`WX04-049` 自己限定フィルター欠落／`WX10-031` 手札に戻す処理が丸ごと欠落 | **未triage**（O-A待ち） |
| r4-26 | 2 | 1 | ? | `WX05-025` 「そのシグニ」が特定カードに固定されず任意対象＝**claude -p が閉じ括弧を書き忘れ、JSON.parse が失敗**（手動で復元。`extractJson` に括弧補完のフォールバックを追加済み） | **未triage**（O-A待ち） |

🔴🔑**2026-09-07（第205バッチ・Sonnet 5・S-1）＝18バッチ一括実行で歩留まりが急変した**＝
**r4-01〜08（80枚・8バッチ）は findings 9件（1.1件/バッチ）だったのに、r4-09〜26（172枚・18バッチ）は findings 43件（2.4件/バッチ）**。
⚠**まだ triage していないので precision は不明**。過去の実測（30枚6件→precision50%／50枚3件→精度向上）からは
**43件のうち相当数が偽陽性の可能性がある**（did-it ゲート／duration の既定挙動／STUB id 名の疑いなど、規則13〜17に該当しそうな finding が複数混ざっている）。
⇒ **次のセッションは `/model opus` で O-A triage から入る**（§5.0）。**5〜8件ずつ triage し、真バグは grep で母集団を数えてから直す。**
🔑**この回で踏んだ罠2つ**＝
①**claude -p が JSON 契約を守らず自由形式(markdown)で応答することがある**（r4-12＝バッチ04）＝`engine ソースまで確認した` と自称する詳細な散文レポートを返し、`{"results":[...]}` を一切出力しなかった。**自動リトライは実装していない**（頻度が低く複雑）＝手動で findings.jsonl へ書き戻した。
②**claude -p が JSON の閉じ括弧を書き忘れて打ち切ることがある**（r4-26＝バッチ18・`stop_reason:"end_turn"` で正常終了なのに構文が不完全）＝`extractJson` に**開き括弧と閉じ括弧の数を比較して不足分を補うフォールバック**を追加した（`scripts/semanticAuditRun.mjs`）。

**偽陽性から得た型（プロンプト規則へ還元済み＝規則13〜17）**：
did-it ゲート（`DID_IT_GATED_TYPES`）／`OPPONENT_PAY_OPTIONAL` の既定の極性／STUB の id 名と payload の食い違い／
🆕**id 名に入っている種別（ARTS/SPELL/SIGNI）は適用範囲を表さない**（規則17・第199バッチ）。

---

## 累計

- 🏁**Sheet1（round4）は 2026-09-07（第205バッチ）に監査完了＝252枚 / 26バッチ全消化**。
  `node scripts/archive/semanticAuditGap.mjs --sheet 1` の未監査は **0**。**次に取るのは Sheet2（未監査356枚）**。
- **監査済＝252枚 / findings 52件（うち triage 済み9件＝真バグ5件・影響13枚・偽陽性4件）／未triage 43件（r4-09〜26）**。
- 🔴**r4-09〜26（18バッチ・172枚）は triage 前**＝上表の「新型」列は全部 `?`。**次セッションは Opus で O-A から入る。**
- **triage 済み9件の precision＝5/9（56%）**。改良前（パイロット30枚）は 6件中3件（50%）＝**規則13〜17 は効いている**。
- 🔑**FP も無駄ではない**＝今回の唯一の FP（`WX05-006-E2`）が、**JSON だけを読む監査員には原理的に見えない別バグ**
  （適用範囲の過剰）を engine 読解の過程で炙り出した。⇒ **FP を「読み損」と数えない。**
- 🔑**プロンプト規則は 17本**（第199バッチで規則17＝「id 名の種別は適用範囲を表さない」を追加）。
- ⚠**r4-09〜26 の finding 頻度は r4-01〜08 の2倍以上**（1.1件/バッチ→2.4件/バッチ）。**triage するまで意味は不明**
  （偽陽性率が上がっただけの可能性が高い＝did-it ゲート・duration 既定値・STUB id 名疑いに該当しそうな finding が複数混在）。
