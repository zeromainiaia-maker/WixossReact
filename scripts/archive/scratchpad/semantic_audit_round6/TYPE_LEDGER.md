# round6 型台帳（原文 × 実行結果）

**round6＝effects JSON を見せず、engine で効果を解決した結果（初期盤面・選択・盤面差分・ログ）を原文と並べる**（2026-09-16 ユーザー決定＝案1・試行30枚）。
round5 総括「同じ読み方の2周目は新型が出にくい／JSON を読む監査員は engine の読み替えを判定できない」への回答。

- トレース＝`npx tsx scripts/behaviorAudit.ts --ids <…> | --ids-file <file> --json-out <out>/traces.json`（非CONTINUOUS 効果ごとに「断る／受ける」の2通り）
- プロンプト＝`node scripts/semanticAuditTraceExtract.mjs --out <out> --traces <traces.json> [--batch-size 10]`
- 実行＝`CODEX_HOME="C:/Users/zerom/.codex-work" node scripts/semanticAuditRunCodex.mjs --out <out>`（round5 と同じランナー）
- 対象の並び＝`card_order.txt`（`changed_since_round5.txt`＝round5 開始 `40940e1f3` 以降に live JSON が変わった 2,272枚を seed 42 でシャッフル）

| 回 | 枚数 | findings | 判定 | 内容 |
|---|---|---|---|---|
| 試行 v1 | 30（59効果） | 4 | **FP 4（全部ハーネス）** | ①自動操縦が `REARRANGE_SIGNI` の候補（`signiNums`）を読まず入れ替えが常に空振り（2件）②アシストルリグの効果元をシグニゾーンに置き、パワー「-」＝NaN で `powerLtAnyAlly` が外れた ③盤面差分が `negated_attacks` 等の予約を映さない。⇒ 3点とも `behaviorAudit.ts` を直した（出力は `rejected_v1/`） |
| 試行 v2 | 30（59効果） | 0 | — | 差分なし9効果を目視＝全部「条件不成立で正しく空振り」 |
| 再現率 | 16（修正前 `40940e1f3` の既知バグ） | 10 | **既知 16枚中 6枚を再検出（うち1枚は ASK）**＋**新規の実バグ1系統**＋軽微1＋FP 1 | 再検出＝`O-391`×2（辞退しても後続）／`O-461`（全体付与が1体）／`O-383`（ダメージ防止の回数）／`O-398`（条件不成立でも実行）／`WXDi-P13-074`（ASK）。**見逃し**＝候補の取り違え・対象宣言の順序・タイミング・シャッフル順など（汎用盤面と「先頭から選ぶ」自動操縦では再現しない形）。🔴**新規＝場に出す先の空きゾーンが無いとカードが消える**（下） |
| **R6-0**（2026-09-16） | 16（同じ再現率セット・変種つき） | 13 | **既知 16枚中 6枚（前回と同じ・9/16 に届かず）**＋修正前に残っていた実バグ 6件（`O-524`×5・`O-526`×1）＋FP 1 | 変種で `O-524` を3枚分多く拾えた（満杯／枯渇）。**見逃しの原因は変わらずハーネスの盤面**＝例 `WX22-001-E2`「自分の＜遊具＞をトラッシュに置く。そうした場合」はどの変種でも＜遊具＞が場に居て失敗経路を踏まない。FP＝`WXK03-049-E1`（任意の探索は 0枚選択で表す＝規則6）。出力＝`recall_r60/` |

## R6-0 ハーネス整備（2026-09-16）

- ①**位置の死角**＝チャーム／アクセ／裏向き付け／マジックボックス／シード／裏向きシグニ／除外／ルリグデッキ／ルリグゾーン裏向き／`check_rest`／キー2枚目／クラッシュ待ちを位置として追跡。
  (消滅) の行には `［残存: 自.<key>］`（state のどのキーに id がまだ残っているか）を添える。
  スペル等の効果元は `check` に置かない（ライフバースト確認用の1枚スロットで、効果内のクラッシュが上書きしていた）。敗北（ライフ0）は1行に畳む。
- ②**盤面の変種**＝`--variants base,full,empty`（既定は3つ）。JSON は結果が基本と違う変種だけ `variants` に載せる（全 9,374効果のうち 満杯で変わる 638／枯渇で変わる 3,367）。
- ③**候補の全列挙**＝「候補N件[全ラベル]からM件: …」。
- **完了条件①**＝全 5,976枚 × 3変種で (消滅) **0件**（`vanish_all_cards_r60.txt`）。最後の1件だった `WX11-034-BURST`（【チャーム】付きのシグニへ新しい【チャーム】を付けると古い方が消える）は
  同日ユーザー判断（読みA＝付けられない）で `O-528` を直して 0 になった。
  死角をふさいだ途中で **engine の実バグ2件**が出た（直した）＝`PLACE_TRAP_FROM_REVEALED` の残り札（3効果）／`OPP_TRASH_TO_OPP_SIGNI_UNDER` のゾーン選択をまたぐと消える（1効果）。
- **完了条件②**＝**6/16 で未達** ⇒ 計画どおり **R6-2（LLM 監査）はやらず、R6-1（不変条件センサス）で閉じる**。

## R6-1 不変条件センサス（2026-09-16）

- 道具＝`npm run census:traceinv`（`scripts/censusTraceInvariants.mjs`・明細 `docs/_census_trace_invariants.txt`）。全 5,976枚 × 5変種（基本／満杯／枯渇／**場が空**＝R6-1 で追加）× 断る/受ける。約2分。
- `behaviorAudit.ts` に足したもの＝変種 `nofield`（両者のシグニ0体・効果元自身は残す）／**二重存在**の検出（2か所以上に居るカード）。

| 不変条件 | 初回 | 判定した標本 | 精度（実バグ） | 結果 |
|---|---|---|---|---|
| **I1** カード保存則 | 16 | 16（全数） | **4/16**（残り12は `puppet_signi` を位置として数えたハーネスの誤り） | 実バグ4件＝`TRIGGER_LIFE_BURST`（3効果）がチェックゾーンに置いても元の領域から抜かない／`PLACE_CARD_UNDER_SIGNI{processed}` がデッキから抜かない。**直して 0 → `gates` にラチェット同梱（BASELINE 0）** |
| **I2** 任意の不履行 | 269 | 20 | **0/20**（軽微候補1＝`WX04-096-E2`） | 任意性は別の層が持つ＝スペルの使用コスト／トリガーの「発動しますか」／「対象とし〜てもよい」は0体選択（規則6）／付与する能力の文面。**計器として使えない** |
| **I3** 失敗・辞退後の後続 | 85 | 20 | **5/20（25%）** | 系統B＝手札から場に出してもよい。そうした場合（満杯で出せなくても後続）＝**直した**（`WX20-034-CB-E1`／`WX20-039-CB-E1`）。系統A＝【トラップ】が無くても後続＝`O-527`（3効果）。偽陽性＝失敗が「そうした場合」より後の段（8）／ログ誤爆（4＝「不可解」・「」内の「できない」→計器を直した）。85 → 77 |
| **I4** ログと差分の枚数 | 419 | 20＋全数の型分け | 挙動の実バグ **0**／**ログの実バグ1根（約285効果）** | ドロー／エナチャージのログが**実際に動いた枚数ではなく要求枚数**（デッキが足りない枯渇変種）＝直した。残り132は「引いた直後に捨てた」「シャッフル後に引いた」「探索で手札へ」を移動として数え損ねる計器の誤爆 |
| **I5** 側跨ぎ | 35 | 35（全数） | **0/35** | 原文が持ち主を限定しない（「すべてのシグニ」「正面のシグニ」「シグニ１体」）／使用したスペル・ライフバーストの中身。**計器として使えない** |

- 🔑**ゲートにしたのは I1 だけ**＝I2〜I5 は候補出し（ゲートにしない）。I3 は精度25%で worklist の発見器としては働いた。
- 🔑**R6-2（LLM 監査）はやらない**（R6-0 の再現率 6/16 で未達）＝この台帳の次は R6-3（締め）。

## 🔴 新規の実バグ（再現率の回で発見・HEAD でも再現）

- **`ADD_TO_FIELD` が元の領域から取り除いた後で空きゾーンを判定する**（`src/engine/effectExecutor.ts` の applyDirectAction `ADD_TO_FIELD`・「空きシグニゾーンなし（〜配置不可）」の return）＝**場が埋まっているとカードがデッキ／トラッシュ／エナから消えてどこにも置かれない**。
- 全数（機械・LLM 不要）＝**132効果 / 128枚**（`vanish_all_cards.txt` の「空きシグニゾーンなし」行）。例＝`WX19-001-E2`（デッキから探して場に出す）／`WX22-001-E1`（トラッシュから2体まで場に出す・空き1つで2体目が消える）。
- ⚠`vanish_all_cards.txt` の残り108件は**ハーネスの死角**（チャーム・除外領域・シード・【マジックボックス】・トラップを snapshot が位置として追跡していない）＝バグ数ではない。

## 試行で分かったこと

- 🔑**LLM より先に機械で数えられる不変条件がある**＝「カードが消える」はトレースの差分だけで全数が取れた（5,976枚・約100秒）。
- 🔑**この軸が拾うのは「盤面に結果が出る」バグだけ**＝再現率の見逃しの大半は、汎用盤面が失敗経路（場が満杯・手札0・条件成立）を踏まないこと。次に足すなら**盤面の変種**（場満杯／手札0／デッキ0／条件成立）。
- 🔑精度＝修正前の版で **10件中 既知 BUG 5＋解釈待ち(ASK) 1＋新規 2（同一の根）／軽微1（ログ文言）／FP 1**。
