# round4 型台帳（1バッチ1行）★止め時の判定に使う唯一のカウンタ（Sheet3）

**運用は PLAN §2.6。** 監査の目的は「カードの網羅」ではなく**「まだ知らない“型”の発見」**。
🔴**止め時＝連続3バッチで「新型 0」**（残枚数では判断しない）。
🔑**真バグを引いたら、直す前に `grep` で同型の全数を数える**（1枚だけ直して満足しない）。
⚠**この回（S-1・Sonnet レーン）は抽出・実行・簿記のみ**＝新型判定（「新型」列）は Opus の O-A triage で埋める。

| バッチ | 枚数 | findings | 新型 | 型の内容（finding の主張・未triage） | 状態 |
|---|---|---|---|---|---|
| s3-01 | 10 | 3 | ? | `WX22-005` スペル打ち消し後続処理が CHOOSE の外＝①②選択でも発動／`WX22-010` チェックゾーン後の「場に出す」欠落／`WX22-014` エクシード無償使用が PLAY_FREE STUB で対応不確実 | **未triage**（O-A待ち） |
| s3-02 | 10 | 1 | ? | `WX22-022` BURST の＜遊具＞2枚「異なる色を持つ」制約が filter に欠落 | **未triage**（O-A待ち） |

🔴🔑**2026-09-07（続き・Sonnet 5・S-1）＝Sheet3 の先頭2バッチ（20枚）を実行**＝
**findings 4件（2.0件/バッチ）**＝Sheet2（1.8件/バッチ）とほぼ同水準・逓減の兆候なし。
⚠**全件未 triage**（この回は S-1＝抽出・実行・簿記のみ。新型判定は O-A へ）。
🔑**実行時トラブルなし**（2バッチとも JSON 契約どおり応答）。
⇒ **止め時判定（連続3バッチで新型0）はまだ出せない**＝O-A triage 待ちのまま。

## 累計（Sheet3・2/37バッチ消化）

- **消化＝20枚 / 37バッチ中2バッチ**（`audited_cards_cumulative.txt`＝20件・`semanticAuditGap.mjs --sheet 3` で残349枚を確認）。**残 35バッチ / 349枚**。
- **findings 4件・全件未 triage**。
- **実行時トラブル 0件**。
