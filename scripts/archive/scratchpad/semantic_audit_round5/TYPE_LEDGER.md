# round5 型台帳（1バッチ1行）

**round5＝効果テキストと effects JSON を持つ全 5,976 枚の読み直し**（2026-09-15 ユーザー決定）。
- 並び＝`card_order.txt`（seed 42 の mulberry32 でシャッフル）／598バッチ（10枚/バッチ）。JSON 未登録の56枚は対象外。
- 実行＝`CODEX_HOME="C:/Users/zerom/.codex-work" node scripts/semanticAuditRunCodex.mjs --out scripts/archive/scratchpad/semantic_audit_round5 --batches N`
- 抽出（作り直すとき）＝`node scripts/semanticAuditExtract.mjs --out scripts/archive/scratchpad/semantic_audit_round5 --cards-file scripts/archive/scratchpad/semantic_audit_round5/card_order.txt --batch-size 10`

| バッチ | 枚数 | findings | 新型 | 内容 | 状態 |
|---|---|---|---|---|---|
| r5-001 | 10 | 0（初回は1） | 0 | 初回の1件（`WXDi-D01-011-E1`「無色エナ8個の支払いが余分」）は**偽陽性**＝抽出スクリプトが `meta` に `Cost`/`GrowCost` を入れておらず、監査員はコストを知らなかった。**抽出側を直して再実行し0件**（初回の出力は `rejected_v1/`） | 済 |
| r5-002〜021 | 200 | 7 | 1（＋解釈待ち3） | FP 3（→ 規則35〜37）／BUG 1＝**「対戦相手のスペルを使用したとき」の持ち主限定が engine に無い**（`WX14-027-E2` → `O-376`）／**解釈待ち3**＝「その後」が「そうした場合」の内側か（`O-377`・同文型14件）・「他の」の基準（`O-378`）・「1体につき〜てもよい」の単位（`O-379`）。詳細は `triaged.txt` | triage 済・登録済 |
| r5-022〜081 | 600 | 30 | **5型＋単発17** | **既定 codex（`~/.codex`）で60バッチ＝失敗0・所要 約20分**。型＝①**did-it ゲートの外に帰結**（`WXK09-TK-01A-E1`／`WXK08-037-E1`／`WX25-CP1-080-E1`＝`O-377` の同族だが**「そうした場合」が無い／CHOOSE の中**で汎用パスが届かない形）②**対象宣言の時点ズレ**（先に固定すべき「それ」を後で選び直す＝`WXK02-028-E1-G2`／`WX18-068-E1`／`WXK05-043-E1`／`-E2`）③**原文は強制なのに `OPTIONAL_COST`**（`WX21-004-E3`／`WX11-006-E3`）④**リコレクト条件がアーツだけを数える**（`WX24-D3-25-E1`／`WX24-D4-20-E1`）⑤**カード名の部分一致**（`WX25-CP1-TK2A-E1`／`-E2`＝「（正月）」が落ちる）。単発17件に HIGH 6（シャッフル順・軽減の母数・`lastProcessed` 上書き・代替コストの移動先・**【ライズ】丸ごと欠落**・ダメージ防止の回数）| **triage 済＝BUG 12（→ `O-380`〜`O-390`）／FP 18（→ 規則41〜44）**。🔴**最大の収穫は `O-380`**＝括弧つきカード名《X（Y）》の（Y）が原文ブロック分割で落ち、`cardName` が**別カードを指す／存在しない名前になる**（8効果）。**`docs/_effect_srctext.json` にも同じ欠落がある＝§2.6 ②の原文コーパス照合の死角**（この巡で実際に踏んだ） |
| r5-082〜141 | 600 | 23 | **1型（実質）** | **`.codex-work` で60バッチ＝失敗0**。⚠**プロンプトは規則41〜44 を入れる前のもの**＝既知 FP 型（`OPTIONAL_COST` の後にゲート無し／【ライズ】／対象宣言の時点ズレ）が再出現した（**還元が間に合わないと同じものを読まされる**の実例）。triage＝**BUG 10 / FP 13**。🔴**実質の新型は1つ＝「コストではない任意行動は辞退しても後続を止めない」**（`execMill` の skip は空 SEQUENCE／後続を止めるのは `OPTIONAL_COST` の Pattern ⑤だけ）＝`O-391`（4効果）。他は `O-392`〜`O-397`。**triage 後にプロンプトを作り直した**（142〜 は規則41〜47 入り） | triage 済・登録済 |
| r5-142〜201 | 600 | 25 | **1型（実質）＋既知型の母集団増** | `.codex-work`・失敗0。triage＝**BUG 19 / FP 6**。🔴**最大の発見＝`O-398`**＝`CONDITIONAL{IS_MY_TURN}` の**前段が `TRASH`** の形が **60箇所**（`TRASH` は `DID_IT_GATED_TYPES` に無いので did-it ゲートにならず literal 判定＝**自分ターンなら失敗しても走り、相手ターンなら成功しても落ちる**）。既知型の母集団も増えた（`O-391` 4→5／`O-392` 1→3／`O-385`・`O-395`・`O-397` は各2）。新規 `O-399`〜`O-407`。FP は規則48〜49 へ還元（engine の包み解き／JSON の入れ子の誤読） | triage 済・登録済 |
| r5-202〜301 | 1,000 | 53 | **0（全部既知型か単発）** | `.codex-work`・失敗0。🔴**新基準（FP にしてよいのは `file:line` か実測値を示せるときだけ）で判定＝FP は 1件だけ / BUG 52**。既知型の母集団が大きく増えた＝`O-413` +8（計19）／`O-398` +6（計9）／`O-391` +5（計10）／`O-399` +4（計5）／`O-397`・`O-394`・`O-395`・`O-406`・`O-411`・`O-381` が各+1〜2。新規 `O-420`〜`O-441`（22件）。**新型が出ていない**＝型の発見は飽和しつつあり、いまは**同じ型の母集団が積み上がる段階**（§2.6 の止め時の判断材料） | triage 済・登録済 |
| r5-302〜331 | 300 | 21 | **4型** | `.codex-work`・失敗0。新基準で **FP 0 / BUG 21**。既知型の母集団＝`O-391` +6（計16）／`O-398` +4（計13）／`O-413` +1（計20）。🆕**新型4つ**＝①**遅延トリガーが設置されず即時実行**（`O-442`）②**コストの色指定が無色に落ちる**（`O-445`）③**`triggerScope:self` で相手のフェイズに発動しない**（`O-446`）④**対戦相手が選べる任意行動が強制**（`O-448`＝`O-391` の相手側版）。⇒ **止め時（連続3バッチで新型0）にはまだ達していない** | triage 済・登録済 |
| r5-332〜531 | 2,000 | 105 | **2型（どちらも回帰・engine 側）** | 既定 codex（332〜431）＋`.codex-work`（432〜531）の**2系統並行**＝失敗0。⚠既定 codex は 372 で利用上限に当たり停止（ユーザーがリセットして再開）。triage＝**BUG 92 / FP 13**。🔴**FP 13件は全部「任意行動を辞退しても後続が走る」型＝規則45 が stale だった**（`O-391(a)` を第349バッチで直したので、いまは `selectOrInteract` → `resumeSelectTarget` → `stripDidItConditional`（`effectExecutor.ts:12065`/`:12074`）が「そうした場合」を落とす）⇒ **規則45 を書き換え（規則53）**。🆕**新型2つはどちらも直近の自分の修正の回帰／死角**＝①`O-452`＝**第352バッチ（`O-413`）の対象宣言引き上げが行きすぎ**＝原文が「〈条件〉の**場合**、〜を**対象とし**」の順のものまで条件の外へ出した（**実測36効果**）②`O-453`＝**自分の `TRASH` 空振りの粗ゲート（`effectExecutor.ts:7348`）が独立文の後続まで丸ごと消す**（**実測34効果**＝原文に「（手札を捨てられなくてもカードを引ける）」と明記された11効果を含む）。新規 `O-452`〜`O-495`（44件）／既知型の母集団＝`O-451`(+8)／`O-399`(+7)／`O-445`(+3)／`O-418`(+3)／`O-391(b)`(+3)／`O-385`(+2)／`O-386`(+2)／`O-397`・`O-383`・`O-421`・`O-436`・`O-382`(+1) | triage 済・登録済 |
| r5-532〜561 | 300 | 10 | **0型（全部既知型か単発）** | 既定 codex（上限リセット後）・失敗0。**規則45 の改訂＋規則53〜55 を入れた最初のラウンド**＝findings 率が **5.3%（105/2,000）→ 3.3%（10/300）** に落ちた（既知型の抑制が効いている）。triage＝**BUG 10 / FP 0**。既知型＝`O-451` +3（対象宣言が条件の内側＝`O-413` の正しい向き）／`O-445` +2（計6効果）／`O-391`(b) +1。新規 `O-496`〜`O-499`（4件）＝クラフト除外の欠落／手札に戻す対象の取り違え／「ダウン状態で場に出し【出】を発動させない」後続の欠落／アップ対象がトリガー元に固定されていない | triage 済・登録済 |
