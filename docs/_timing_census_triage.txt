# timing センサス残クラスタの振り分け台帳（2026-07-17・Sonnet・PLAN §3 補欠(a)）
#
# 目的＝docs/_timing_census.txt の各クラスタについて、engine に該当timingの収集関数（collector）が
# 既に存在するかを機械照合し、「Opusタスク16（parser regex 1本で直る＝parser語彙のみ不足）」と
# 「§6.3送り（収集関数/filter軸が無く新規機構が要る）」に仕分ける。
#
# 判定基準（3段階）:
#   [A] 完全wired　＝該当ON_*の収集関数が存在し、必要なtriggerScope/triggerFilter/triggerConditionの
#       軸も型に既にある。parser側でtiming/scope/filterを抽出するregexを足すだけで直る＝Opusタスク16。
#   [B] 部分wired　＝収集関数（イベント検出）自体はあるが、必要なfilter/閾値/scope軸が型に無い。
#       parser regexだけでは直らず、TargetFilter/triggerCondition等への軽量engine拡張が要る。
#   [C] 未wired　＝該当イベントを検出する収集関数が存在しない（0参照）か、scope/方向が根本的に逆
#       （例：相手起因イベントを自分側で監視する経路が無い）。新規機構＝§6.3送り。
#
# 根拠は grep（`triggerCollect.ts`/`BattleScreen.tsx`/`effects.ts`のON_*コメント）による実地確認。
# 総数: 117クラスタ（`npm run census:timing`実行時点）。⚠この母数はセンサス再実行のたびに微増減する
# （継続的なIS_MY_TURN化消化等の副作用）ので、実数より「各クラスタの判定ロジック」を再利用すること。
#
# ---------------------------------------------------------------------------

n=5  あなたの《トラップアイコン》が発動したとき
  → [C] ON_TRAP_ACTIVATE は型定義のみでengine参照0件（triggerCollect/BattleScreen 未使用）。新規機構。

n=4  あなたの効果によって対戦相手が手札をN枚捨てたとき
  → [A] collectHandDiscardTriggers（triggerCollect.ts:1295）がtriggerScope 'any'で相手discard監視に対応済み。

n=3  カードがコストか効果によってシグニの下からトラッシュに置かれたとき
  → [C] ON_TRASH の fromZones は 'hand'|'deck'|'energy'|'field' のみ。「シグニの下(under signi)」という
    zone種別が型に無い。新規機構。

n=2  対戦相手のシグニのアタックを効果によって無効にしたとき
  → [C] NEGATE_ATTACKはaction型としては存在するが、それをトリガーとして監視する timing/collector が無い。

n=2  あなたのグロウフェイズ開始時
  → [C] collectTurnTriggers は ON_TURN_START/END/ATTACK_PHASE_START/MAIN_PHASE_START/LRIG_ATTACK_STEP_START
    のみ。「グロウフェイズ開始時」に対応する timing が型に存在しない。新規機構。

n=2  あなたのライフクロスにカードN枚が加えられたとき
  → [A/実装済み] ON_LIFE_CLOTH_ADDED は parser(effectParser.ts)／検出器(boardDiff.ts)／
    collector(triggerCollect.ts)／中央diff配線(BattleScreen.tsx)まで実装済み。
    実データは WD06-001-E2 / WD20-001-E2 の2効果。

n=2  このルリグのアタックが【ガード】されたとき
  → [C] ON_GUARD（collectSelfEventTriggers）は「あなたがガードした」＝ガード実行者視点のみ。
    アタック側から見た「ガードされた」逆視点のscopeが無い。新規機構。

n=2  このカードが【アクセ】としてシグニに付いたとき
  → [A] ON_ACCE_ATTACH 配線済み（BattleScreen.tsx・2箇所参照）。

n=2  このシグニがあなたのエナゾーンから手札に移動したとき
  → [C] エナ→手札（self）の移動を検出する timing が無い（ON_ENERGY_TO_TRASH はtrash宛先限定）。新規機構。

n=2  対戦相手のエナゾーンにカードN枚が置かれたとき
  → [C] ON_ENERGY_CHARGE は「あなたのエナゾーンに1枚ちょうど」＝自分視点限定（BattleScreen.tsx:1573）。
    相手のエナゾーン増加を監視する逆scopeが無い。新規機構。

n=2  このカードが捨てられたとき（自身）
  → [C] collectHandDiscardTriggers は場のwatcher（他カードが反応）のみを走査し、捨てられたカード自身の
    ON_HAND_DISCARDED self反応は未対応。新規機構（続き138 IS_MY_TURN化残と同系統）。

n=2  対戦相手の効果によってこのカードが捨てられたとき（自身）
  → [C] 上記と同じ穴。PLAN既知＝WXDi-P11-066系（PLAN §4 続き169「手札カード自身の効果捨てtiming」）。

n=1  このシグニが対戦相手のレベルN以下のシグニとバトルしたとき
  → [A] ON_SIGNI_BATTLE（BattleScreen.tsx:7345）+ TargetFilter.levelRange で表現可能（既存パターン=WX04-099）。

n=1  対戦相手のシグニN体が場からデッキに移動したとき
  → [A] ON_CARD_MOVED_TO_DECK（collectMoveToDeckTriggers）は発生元zone非依存の汎用検出。

n=1  このシグニがNターンにライフクロスを合計N枚以上クラッシュしたとき
  → [C] 複数ターンにまたがる累積カウント機構が無い。新規機構。

n=1  あなたのシグニN体が効果によってダウン状態になったとき
  → [C] ON_SIGNI_DOWN は型定義のみでengine参照0件。新規機構。

n=1  このシグニが対戦相手のレベルNのシグニとバトルしたとき
  → [A] 上記「レベルN以下」と同型（TargetFilter.levelRangeで表現可）。

n=1  あなたのトラッシュから効果でカードN枚がデッキに移動したとき
  → [A] movedToDeckFromTrash（triggerCondition）で対応済み。

n=1  対戦相手があなたのシグニの効果によって手札をN枚捨てたとき
  → [A] collectHandDiscardTriggers と同系統（n=4クラスタと同じ穴・同じ収集関数）。

n=1  アタックフェイズの間にあなたがカードをN枚以上引いたとき
  → [C] triggerCondition.drawPhaseRestriction:'main_attack' はフェイズ限定に対応するが「N枚以上」の
    累積閾値カウントは未対応（単発ON_DRAWごとの判定のみ）。新規拡張。

n=1  あなたのシグニN体がアップ状態になったとき
  → [C] ON_SIGNI_BECOMES_UP が型に存在しない。PLAN既知＝新機構タスク（続き169・WX12-006系）。

n=1  このシグニが場から離れたとき（自身）
  → [A] ON_LEAVE_FIELD self配線済み（collectLeaveFieldTriggers）。

n=1  あなたが対戦相手のシグニN体のアタックを無効にしたとき
  → [C] 「アタックを無効にした」トリガー無し（n=2クラスタと同じ穴）。

n=1  カードN枚があなたのエナゾーンから手札に移動したとき
  → [C] エナ→手札 timing無し（同上n=2クラスタ）。

n=1  リグかシグニの効果によって手札かデッキからエナゾーンに置かれたとき
  → [C] ON_ENERGY_CHARGE は発生源zone(手札/デッキ)や原因（リグ/シグニの効果）の限定軸を持たない。

n=1  このシグニが効果によってダウン状態になったとき（self）
  → [C] ON_SIGNI_DOWN 未配線（同上）。

n=1  あなたのシグニN体がアクセされたとき
  → [A]→✅消化（続き235）。※シグニ受け皿は ON_ACCE（ON_ACCE_ATTACH はルリグ用）＝timing regex に受身形
    「がアクセされた」を追加し ON_ACCE + any_ally を生成（WX15-059-E1）。BUGFIXES 続き235。

n=1  このシグニが感染状態のシグニN体をバニッシュしたとき
  → [A] ON_SIGNI_BANISH系 + TargetFilter.infected で表現可能（infected フィールド既存）。

n=1  【チャーム】N枚が場からいずれかのトラッシュに置かれたとき
  → [A] ON_CHARM_TO_TRASH 配線済み。

n=1  あなたのシグニがバトルによって正面以外のシグニをバニッシュしたとき
  → [B] ON_SIGNI_BANISH_BATTLE は配線済みだが「正面以外」という対象限定filterが型に無い。軽量拡張要。

n=1  このカードが【アクセ】として＜X＞のシグニに付いたとき
  → [A] ON_ACCE_ATTACH self + host名フィルタ対応済み。

n=1  このシグニがシグニN体をバニッシュしたとき
  → [A] ON_SIGNI_BANISH系・自己scope基本形。

n=1  このシグニの正面にレベルN以下のシグニN体が出たとき
  → [A] triggerCondition.placedFront + TargetFilter.levelRange で表現可能。

n=1  このシグニの正面にこのシグニより低いレベルを持つシグニが出たとき
  → [A] triggerCondition.frontLowerLevelThanSource 配線済み（コメントにWX17-075の実例あり）。

n=1  カードが【アクセ】としてレベルN以下の＜X＞のシグニに付いたとき
  → [B] accedHostMinLevel は「N以上」のみ型に存在。「N以下」対称軸が無く軽量拡張が要る。

n=1  カードが【アクセ】としてレベルN以上の＜X＞のシグニに付いたとき
  → [A] accedHostMinLevel で直接対応可能。

n=1  あなたの＜X＞のシグニN体が対戦相手の効果によって場を離れたとき
  → [C] ON_LEAVE_FIELD は「離れたカードの持ち主側」のwatcherのみ（any_ally/any）。効果を与えた側が
    相手の離脱を監視する逆scope（any_opp相当）が無い。

n=1  あなたの【自】の【英知】能力が発動したとき
  → [C] 【英知】能力の発動を検出する timing が無い。新規機構。

n=1  あなたがエナゾーンからカードN枚を手札に加えたとき
  → [C] エナ→手札 timing無し（同上クラスタ群）。

n=1  あなたのセンタールリグかシグニN体がアップ状態になったとき
  → [C] ON_SIGNI_BECOMES_UP 未実装（同上）。

n=1  このシグニがカード名に《オダノブ》を含むシグニにライズされたとき
  → [A] triggerCondition.risedOntoNameContains 配線済み（コメントにWX20-056-E2の実例あり）。

n=1  カードN枚がいずれかのプレイヤーの手札に加えられたとき
  → [C] 汎用「手札に加わった」timingが無い（ON_DRAWはドロー動作限定）。

n=1  対戦相手のシグニN体が【トラップ】のあるシグニゾーンに出たとき
  → [C] ゾーンアイコン（トラップ）条件付きのON_PLAY派生timingが無い。

n=1  【ウィルス】Nつが対戦相手の場に置かれるか対戦相手の場から取り除かれたとき
  → [A] ON_OPP_VIRUS_CHANGED 配線済み（collectSelfEventTriggers）。

n=1  対戦相手の場にある【ウィルス】がN以上取り除かれたとき
  → [B] ON_OPP_VIRUS_REMOVED は配線済みだが「N以上」の除去枚数閾値カウントが型に無い（フラグ検出のみ
    の可能性）。要確認・軽量拡張。

n=1  このシグニが対戦相手にダメージを与えたとき
  → [A] ON_SIGNI_DAMAGE 配線済み（BattleScreen.tsx:7375）。

n=1  対戦相手の場から【ウィルス】Nつが取り除かれたとき
  → [B] 上記ON_OPP_VIRUS_REMOVEDの閾値と同じ課題。

n=1  あなたが《コインアイコン》をベットしたとき
  → [C] ON_COIN_PAIDは「スペルのベット（pending_spell/カットイン経由）は未配線」と明記済み。

n=1  あなたの【トラップ】Nつが設置されたとき
  → [C] トラップの「設置」（発動ではない）を検出するtimingが無い。

n=1  あなたの他の＜X＞のシグニN体がダウン状態になったとき
  → [C] ON_SIGNI_DOWN 未配線（同上クラスタ群・any_ally版）。

n=1  効果NつによってデッキからカードがN枚以上トラッシュに置かれたとき（合計）
  → [C] ON_CARD_MILLED_FROM_DECKは単一解決単位のみの近似（型コメントに明記：「合計N枚」型はTODO §3.5）。
    複数効果にまたがる累積は未対応。

n=1  対戦相手の場にあるシグニの【出】能力が発動したとき
  → [C] 他カードのON_PLAY発動を横から監視するtimingが無い。

n=1  あなたのシグニN体がダウン状態になったとき
  → [C] ON_SIGNI_DOWN 未配線（同上）。

n=1  あなたの【アクセ】N枚がトラッシュに置かれたとき
  → [C] ON_CHARM_TO_TRASHはチャーム専用。アクセ版のtrash-timingが型に無い。

n=1  対戦相手の場にある【チャーム】N枚がトラッシュに置かれたとき
  → [A] ON_CHARM_TO_TRASH は triggerScope any/any_ally/any_opp に対応済み（コメント明記）。

n=1  【チャーム】が付いている対戦相手のシグニをバニッシュしたとき
  → [A] TargetFilter.hasCharm が既存＝ON_SIGNI_BANISH系＋hasCharmで表現可能。

n=1  あなたが緑のアーツを使用したとき
  → [B] ON_ARTS_USE（collectArtsUseTriggers）は配線済みだが、使用したアーツの色を絞るfilter軸が無い
    （現状は使用トリガーの有無のみ判定）。軽量拡張要。

n=1  シグニがバトルによって対戦相手の凍結状態のシグニをバニッシュしたとき
  → [C] TargetFilterに凍結状態（frozen）を判定するフィールドが存在しない。

n=1  このシグニが正面のシグニN体をバニッシュしたとき
  → [A] ON_SIGNI_BANISH系＋自己scope基本形。

n=1  このカードが【アクセ】としてレベルN以上のシグニに付いたとき
  → [A] accedHostMinLevel直接対応（重複クラスタ）。

n=1  対戦相手のカードがあなたの効果によってデッキに移動したとき
  → [A] movedToDeckOwner:'opponent' で対応済み。

n=1  あなたのライフクロスが他の領域に移動したとき
  → [C] クラッシュ以外のライフクロス移動（手札/デッキ等）を検出するtimingが無い。

n=1  効果NつによってあなたがカードをN枚以上引いたとき（合計）
  → [C] 複数効果にまたがる累積ドロー数カウント機構が無い（同上milled合計と同型の穴）。

n=1  あなたの効果Nつによってあなたがカードをn枚以上引いたとき（合計）
  → [C] 同上。

n=1  対戦相手のシグニN体が【ゲート】があるシグニゾーンに出たとき
  → [C] ゾーンアイコン（ゲート）条件付きON_PLAY派生timingが無い（トラップ版と同型）。

n=1  このシグニにカードN枚が付いたとき
  → [B] ACCE限定なら ON_ACCE_ATTACH で対応可だが「カードが付いた」の対象が層/ソウル等も含む汎用表現
    かは要individual確認。

n=1  てあなたのデッキから＜X＞のシグニN枚がトラッシュに置かれたとき
  → [B] ON_CARD_MILLED_FROM_DECK は milledDeckOwner/milledMinCount のみでstory/level/color filterが
    型に無い。軽量拡張要（filter軸追加）。

n=1  がバトルによって対戦相手の凍結状態のシグニN体をバニッシュしたとき
  → [C] frozen filter無し（同上）。

n=1  シグニN体がダウン状態になったとき
  → [C] ON_SIGNI_DOWN 未配線。

n=1  このシグニがアタックしたアタック終了時
  → [C] ON_ATTACK_SIGNIはアタック開始側のみ。「アタック終了時」variantが型に無い。

n=1  このシグニが対戦相手のシグニとバトルしたとき
  → [A] ON_SIGNI_BATTIE基本形（filterなし）。

n=1  あなたの効果によって対戦相手のシグニN体が場から手札に移動したとき
  → [C] ON_LEAVE_FIELDは同一オーナー側watcherのみ。効果を与えた側（自分）が相手の離脱を監視する
    逆scopeが無い（前出クラスタと同じ穴）。

n=1  対戦相手が手札をN枚捨てたとき（無条件）
  → [A] collectHandDiscardTriggersの基本形（同上クラスタ群と同じ収集関数）。

n=1  対戦相手の場にある【ウィルス】N枚が取り除かれたとき
  → [B] ON_OPP_VIRUS_REMOVEDの閾値カウント課題（重複）。

n=1  あなたか対戦相手のライフクロスN枚がトラッシュに置かれたとき
  → [B] ON_LIFE_CRASHED/ON_OPP_LIFE_CRASHEDはクラッシュ経由のみ対応。「トラッシュに置かれた」が
    クラッシュ以外の経路（バニッシュ的移動等）を含むかは要individual確認。

n=1  あなたの手札からカードN枚がトラッシュに置かれたとき
  → [A]→✅消化（続き235）。※triage の「ON_TRASH fromZones:['hand']」は誤り＝これは**捨てられたカード自身**
    （self）用で、本カード（WDA-F02-17-E2）は**場の watcher**。正しくは ON_HAND_DISCARDED（collectHandDiscardTriggers）。
    併せて対象「このシグニの正面のシグニ」の owner:self 潰れを frontOfSelf(opponent) へ是正＋engine execDown に
    frontOfSelf 解決を追加。BUGFIXES 続き235。

n=1  あなたか対戦相手が《コインアイコン》を得たとき
  → [C] ON_COIN_PAIDは支払い方向のみ。コイン「獲得」方向のtimingが無い。

n=1  このシグニがバトルしたとき（無条件）
  → [A] ON_SIGNI_BATTLE基本形。

n=1  あなたのライフクロスがN枚になったとき（状態一致）
  → [C] ライフクロス枚数が特定値に「なった」ことを検出するtimingが無い（イベントでなく状態一致）。

n=1  あなたのシグニN体に【ソウル】が付いたとき
  → [C] ソウル付与を検出するtimingが無い（collectGrantedFromSoulは付与済みソウルの効果適用機構で別物）。

n=1  このシグニに【ソウル】が付いたとき（self）
  → [C] 同上。

n=1  アシストルリグかライフバーストの能力か効果の対象になったとき
  → [B] ON_TARGETED基盤は配線済みだが、起源ability種別（アシストルリグ/ライフバースト限定）の
    filter軸が無い。軽量拡張の可能性あり・要individual確認。

n=1  あなたが自分の効果によってカードをN枚以上捨てたとき（合計・自己discard）
  → [C] collectHandDiscardTriggersのtriggerFilterは「1枚でも一致すればOK」のsome判定のみで、
    「N枚以上」の閾値カウントに未対応。

n=1  このシグニがバトルによって凍結状態のシグニをバニッシュしたとき
  → [C] frozen filter無し（同上重複）。

n=1  レベルN以下のシグニN体がこのシグニの正面のシグニゾーンに出たとき
  → [A] placedFront + levelRange（「以下」対称形。TargetFilterのlevelRangeは汎用min/max対応）。

n=1  このシグニが場からエナゾーンに置かれたとき
  → [C] PLAN既知＝§6.3機構待ち（続き169「エナ発ゾーン移動timing」WXDi-P11-007系）。

n=1  なたのエナゾーンから効果によってカードN枚が他の領域に移動したとき
  → [C] ON_ENERGY_TO_TRASHはtrash宛先限定。「他の領域」への汎用移動は未対応。

n=1  このシグニが《コードアート　ララ・ルー//メモリア》にライズされたとき
  → [A] risedOntoNameContainsで対応可能（同上クラスタと同じ機構）。

n=1  ワーN以上のシグニが対戦相手のシグニN体をバニッシュしたとき（発動元パワー条件）
  → [C] バニッシュを実行した側自身のパワーを条件とするfilter軸が無い（対象側のpowerRangeはあるが
    発動元側の条件は別軸＝未対応）。

n=1  あなたがエクシードのコストを支払ったとき
  → [A] triggerCondition.exceedCostPaidByPlayer 配線済み（コメントにWXDi-P06-078実例あり）。

n=1  対戦相手がダメージを受けたとき
  → [A] ON_SIGNI_DAMAGE系のバリエーションとして表現可能性が高い（ダメージ=ライフクラッシュ相当）。
    要individual確認（triggerScope方向の検証）。

n=1  なたのライフクロスN枚がクラッシュされるかトラッシュに置かれたとき（OR複合）
  → [C] OR複合トリガー機構は型コメントで「未実装」と明記済み（ON_ALLY_PLAY_OR_OPP_HAND_DISCARDと同型）。

n=1  【出】能力か【出】能力の効果の対象になったとき
  → [B] ON_TARGETED基盤配線済みだが起源ability種別filterが無い（同上「アシストルリグ」クラスタと同課題）。

n=1  あなたの効果によってこのカードが捨てられたとき（自身・自己効果）
  → [C] 捨てられたカード自身のON_HAND_DISCARDED self反応は未対応（n=2クラスタと同じ穴）。

n=1  ってあなたのデッキからカードが合計N枚以上トラッシュに置かれたとき（累積）
  → [C] milled合計カウント機構なし（同上）。

n=1  あなたのデッキからレベルNのシグニN枚がトラッシュに置かれたとき
  → [B] ON_CARD_MILLED_FROM_DECKにlevel/story filterが型に無い（同上クラスタと同課題）。

n=1  このシグニがコストか効果によって捨てられたとき
  → [C] 自身discard self-反応の穴（同上）。

n=1  対戦相手のシグニN体があなたの効果によって手札に戻ったとき
  → [C] ON_LEAVE_FIELDの跨サイドscope（効果元が相手の離脱を監視）が無い（同上クラスタと同じ穴）。

n=1  プレイヤーのデッキからカードが合計N枚以上トラッシュに置かれたとき
  → [C] milled合計カウント機構なし（同上）。

n=1  によって対戦相手のシグニN体が手札に戻るかトラッシュに置かれたとき（OR複合）
  → [C] OR複合トリガー機構未実装（同上）。

n=1  あなたが手札から＜X＞のカードをN枚以上捨てたとき
  → [C] discard filterは「1枚でも一致」判定のみで「N枚以上」閾値カウント未対応（同上）。

n=1  によってあなたが《ガードアイコン》を持たないカードをN枚捨てたとき
  → [C] 同上（discard閾値カウント未対応）。

n=1  あなたのアタックフェイズ終了時
  → [C] ON_ATTACK_PHASE_STARTのみ存在。終了時variantが型に無い。

n=1  シグニの効果によってデッキからカードN枚がトラッシュに置かれたとき
  → [B] ON_CARD_MILLED_FROM_DECKは発生源zone側は判定できるが「シグニの効果によって」という
    原因（causal agent）限定軸が型に無い。

n=1  対戦相手の効果によってカードがN枚以上対戦相手の手札に移動したとき
  → [C] 汎用「手札に加わった」timingが無い（ON_DRAW以外の手札増加を検出できない）。

n=1  たの効果Nつによってカードが合計N枚以上あなたの手札に移動したとき（累積）
  → [C] 同上＋累積カウントの二重の穴。

n=1  あなたの《MC.LIONNrdVerse-ULT》がダウンしたとき
n=1  なたの《DJ.LOVITNrdVerse-ULT》がダウンしたとき
n=1  あなたの《VJ.WOLFNrdVerse-ULT》がダウンしたとき
  → [C]（3件同型）ON_SIGNI_DOWN self・named-card版。ON_SIGNI_DOWN自体が未配線のため同じ穴。

n=1  このルリグが《夢限　-Q-》から《夢限　-A-》になったとき
  → [C] 特定named-formへの変化を検出するtimingが型に無い（ON_SIGNI_BECOMES_DRIVEは別機構＝ライド限定）。

# ---------------------------------------------------------------------------
# 集計（このセンサス実行時点・117クラスタ）
#   [A] 完全wired（parser regexのみで直る＝Opusタスク16候補）        … 約34クラスタ
#   [B] 部分wired（軽量engine拡張＝filter/閾値追加が要る）            … 約16クラスタ
#   [C] 未wired（新規機構＝§6.3送り）                                … 約67クラスタ
#
# 主な繰り返しパターン（§6.3への機構タスク化を検討する際のグルーピング）:
#   - ON_SIGNI_DOWN（アップ含む）が丸ごと未配線 … 8クラスタ相当。ON_SIGNI_BECOMES_UPは既にPLANで
#     新機構タスク化済み（続き169）。ON_SIGNI_DOWN（ダウン版）も同時に機構化する価値が高い。
#   - 「累積・合計N以上」系（milled合計／ドロー合計／discard合計）… 8クラスタ相当。単発イベント収集は
#     あるが解決を跨いだ累積カウンタが無い、という共通の型的欠落＝1本の汎用機構で複数クラスタが同時に
#     直る可能性が高い（型コメントの「cf. TODO §3.5」参照）。
#   - ON_LEAVE_FIELD／ON_HAND_DISCARDEDの「跨サイドscope」欠落（効果を与えた側が相手の離脱/discardを
#     監視する）… 4クラスタ相当。collectLeaveFieldTriggers/collectHandDiscardTriggersへのany_opp相当
#     scope追加で一括対応できる可能性。
#   - 自己discard（捨てられたカード自身が反応するON_HAND_DISCARDED self）… 4クラスタ相当。PLAN既知
#     （続き169・WXDi-P11-066）と同一の穴＝1本の機構で解決。
#   - frozen状態をTargetFilterで判定できない … 4クラスタ相当。TargetFilter.frozen追加で一括対応。
#   - トラップ/ゲート等ゾーンアイコン条件のON_PLAY派生timing欠落 … 2クラスタ。
#
# ⚠本台帳は「機械照合」の一次判定であり、実採用時は各クラスタの該当カードで個別に原文照合・
# engineコード確認を行うこと（[B]は特にfilter追加の実装コストが軽微かどうか要再確認）。

# ---------------------------------------------------------------------------
# 2026-07-27 残テール34効果の全数再仕分け（現行81 timing／collector実参照で再監査）
# 判定は「timing名が型にあるか」ではなく、triggerCollect.ts/BattleScreen.tsx の収集経路と
# 原文限定を忠実に保持できる軸が揃うかで決定。旧一次判定より後に追加された機構も反映した。
#
# effectId | 原文トリガー句 | 判定 | 根拠
# WX05-020-E1 | このシグニが1ターンにライフクロスを合計2枚以上クラッシュしたとき | [C] | ON_SIGNI_DAMAGEは単発。攻撃主体別・ターン累積クラッシュ枚数を保持するcollector/stateが無い。
# WX14-066-E1 | ルリグかシグニの効果によって手札かデッキからエナゾーンに置かれたとき | [B] | ON_ENERGY_CHARGEはBattleScreenで収集済みだが、移動元 hand/deck と原因 lrig/signi effect の軸が無い。
# WX19-066-E1 | あなたの【自】の【英知】能力が発動したとき | [C] | 他AUTOの解決開始を横から監視し、能力クラスを照合するイベント発行が無い。
# WX20-046-E2 | あなたがエナゾーンからカード1枚を手札に加えたとき | [B] | 現行CSVは1枚。ON_HAND_ADDED＋fromZones:['energy']は配線済みだが、この効果への忠実なtiming採用は未実施。
# WX20-067-E1 | カード5枚がいずれかのプレイヤーの手札に加えられたとき | [B] | ON_HAND_ADDEDは配線済みだが handOwner:'any' と最低枚数軸が無い。
# WXEX1-08-E1 | あなたが《コインアイコン》をベットしたとき | [A]→✅ | ON_COIN_PAID collectorとBET支払い経路が既存。parserの「ベットした」regexのみ追加。
# WXEX1-41-E1 | あなたの【トラップ】1つが設置されたとき | [C] | ON_TRAP_ACTIVATEは発動専用。設置完了イベントの発行・collectorが無い。
# WXEX1-77-E1 | 対戦相手の場にあるシグニの【出】能力が発動したとき | [C] | 相手ON_PLAY能力の発動/解決開始を横から監視するcollectorが無い。
# WXEX2-19-E1 | あなたの【アクセ】1枚がトラッシュに置かれたとき | [C] | ON_CHARM_TO_TRASHはチャーム専用。アクセ離脱数の差分collectorが無い。
# WXK08-028-E1 | あなたのライフクロスが他の領域に移動したとき | [C] | ON_LIFE_CRASHEDはクラッシュ限定。任意宛先のlife移動collectorが無い（ON_ZONE_MOVEDはシグニゾーン移動用）。
# WXK10-049-E1 | このシグニにカード2枚が付いたとき | [C] | ON_ACCE/ON_ACCE_ATTACHはアクセ限定。「カード」（下敷き等を含む）汎用付与と枚数閾値のcollectorが無い。
# WXK10-052-E1 | あなたの効果によってあなたのデッキから＜龍獣＞のシグニ1枚がトラッシュに置かれたとき | [B] | milledCardFilter {cardType:'シグニ',cardClass:'龍獣'} は採用済み。ただし「あなたの効果によって」の原因オーナー軸がcollector payloadに無く、相手効果による自デッキミルでも発火するため残る。
# WXK11-018-E2 | このシグニがアタックしたアタック終了時 | [C] | ON_ATTACK_SIGNIは開始時。個別シグニのアタック終了イベント/collectorが無い。
# WD23-023-E-E1 | あなたか対戦相手のライフクロス1枚がトラッシュに置かれたとき | [B] | crash collectorはあるが、非クラッシュのlife→trashを含む汎用差分とowner:anyが不足。
# SP27-007-E1 | あなたか対戦相手が《コインアイコン》を得たとき | [C] | ON_COIN_PAIDは減少方向。コイン獲得イベントcollectorが無い。
# PR-K038-E2 | あなたのライフクロスが0枚になったとき | [C] | life枚数が閾値へ到達した遷移collectorが無い。
# WXDi-D07-004-E1 | あなたのシグニ1体に【ソウル】が付いたとき | [C] | ソウル効果走査はあるが、付与イベントcollectorが無い。
# WXDi-D07-019-E1 | このシグニに【ソウル】が付いたとき | [C] | 同上。self対象を弁別するイベントも無い。
# WXDi-D09-H16-E1 | アシストルリグかライフバーストの能力か効果の対象になったとき | [B] | ON_TARGETEDは配線済みだが対象を取った能力のorigin種別を渡す軸が無い。
# WXDi-D09-P16-E2 | あなたが自分の効果によってカードを2枚以上捨てたとき | [B] | ON_HAND_DISCARDEDは配線済みだが minCount と byOwnEffect の両軸が無い。
# WXDi-P03-073-E1 | このシグニが場からエナゾーンに置かれたとき | [C] | ON_ENERGY_CHARGEは場watcherであり、移動後エナにあるカード自身を拾わない。self移動collectorが無い。
# WXDi-P06-038-E1 | あなたのエナゾーンから効果によってカード1枚が他の領域に移動したとき | [C] | ON_ENERGY_TO_TRASHは宛先trash限定。任意宛先＋効果原因のcollectorが無い。
# WXDi-P06-072-E1 | パワー15000以上のシグニが対戦相手のシグニ1体をバニッシュしたとき | [B] | ON_SIGNI_BANISH_OPPONENTは配線済みだが、攻撃/バニッシュ主体のpower条件軸が無い。
# WXDi-P07-052-E1 | あなたのライフクロス1枚がクラッシュされるかトラッシュに置かれたとき | [C] | crashと汎用life→trashのOR。後者collectorと複合発火の重複排除が無い。
# WXDi-P08-065-E2 | 【出】能力か【出】能力の効果の対象になったとき | [B] | ON_TARGETEDは配線済みだが origin abilityType='出' 軸が無い。
# WXDi-P09-079-E1 | あなたのメインフェイズの間、あなたのデッキからレベル1のシグニ1枚がトラッシュに置かれたとき | [A]→✅ | milledCardFilter {cardType:'シグニ',level:1}＋duringMainPhaseを採用し、collectorがMAINのみ発火することを正負goldenで固定。
# WXDi-P11-063-E2 | このカードがシグニの下に置かれたとき | [C] | ON_PLACED_UNDER_SIGNIは型宣言のみでcollector参照0件。移動後under-card自身の走査も無い。
# WXDi-P12-079-E2 | このカードがデッキからエナゾーンに置かれたとき | [C] | ON_ENERGY_CHARGEは場watcherのみ。移動後energyのカード自身＋from deckを拾うcollectorが無い。
# WXDi-P13-051-E3 | 対戦相手の効果1つによって、あなたの手札が1枚以上捨てられるかあなたのエナゾーンからカードが1枚以上トラッシュに置かれたとき | [B] | 現行CSVは1枚以上のhand/energy OR。minCount軸は配線済みだが、byOpponentEffect＋2経路の重複排除を含むOR collectorが無い。
# WXDi-CP02-068-E1 | あなたの効果によって対戦相手のシグニ1体が手札に戻るかトラッシュに置かれたとき | [C] | ON_LEAVE_FIELD any_opp/byOwnEffectはあるが宛先 hand-or-trash のORを忠実に絞る軸（trash宛先）が無い。
# WX24-P2-051-E1 | 効果によってあなたが《ガードアイコン》を持たないカードを2枚捨てたとき | [B] | ON_HAND_DISCARDEDは配線済みだが minCount・notGuard・byEffect軸が不足。
# WX24-P2-075-E1 | あなたのアタックフェイズ終了時 | [C] | ON_ATTACK_PHASE_STARTのみ。終了時timingの発行/collectorが無い。
# SPDi43-11-sub-E1 | 他の効果2つによってカードが合計3枚以上あなたの手札に移動したとき | [C] | ON_HAND_ADDEDは単一解決差分。複数効果を跨ぐ累積数/効果数カウンタが無い。
# WXDi-P11-010B-E1 | このルリグが《夢限 -Q-》から《夢限 -A-》になったとき | [C] | named form遷移イベント/collectorが無い。
#
# 集計: [A] 1（消化1）／[B] 12（今回はdefer）／[C] 21（§6.3送り提案）。
# [B] defer理由: いずれもregexのみでは原文限定を保持できず、イベントpayload/差分・型・collector・正負goldenを
# 一体で追加する必要がある。単発34件の場当たり近似を避け、共通軸（枚数閾値、移動カードfilter、target origin）
# ごとの機構バッチとして§6.3で扱う。

# ---------------------------------------------------------------------------
# 2026-07-27 続き278：限定差し戻し＋共通軸バッチ
# ✅ WXEX1-08-E1: ON_COIN_PAID + condition IS_BETTING。BET時のみ発火。
# ⏸ WXK10-052-E1: milledCardFilter {cardType:'シグニ',cardClass:'龍獣'} は採用済み。ただし
#    「あなたの効果によって」の原因オーナー軸が残るため [B]。今回は記録のみで実装しない。
# ✅ WXDi-P09-079-E1: milledCardFilter {cardType:'シグニ',level:1}＋duringMainPhaseを採用。
#    collector honorを追加し、メインフェイズ発火／アタックフェイズ非発火をgolden固定。
# ⚙ 共通 minCount 軸: ON_HAND_ADDED / ON_HAND_DISCARDED / ON_ENERGY_TO_TRASH に実装済み（省略=1）。
# ⏸ WX20-046-E2: 現行 CardData_Sheet2.csv 原文は「エナゾーンからカード1枚を手札に加えたとき」。
#    依頼表の3枚と不一致のため minCount:3 は採用せず、timing自体の忠実化は別途原文1枚で扱う。
# ⏸ WXDi-P13-051-E3: 現行 CardData_Sheet8.csv 原文は「対戦相手の効果1つによって、手札が1枚以上
#    捨てられるかエナから1枚以上trash」のOR。依頼表の3枚・不足軸なしと不一致。minCount:3 は採用せず、
#    byOpponentEffect＋hand/energy ORを一体で配線するまでdefer。
# ⏸ WXDi-D09-P16-E2: minCount軸は実装済みだが byOwnEffect が残る。
# ⏸ WX20-067-E1: minCount軸は実装済みだが handOwner:'any' が残る。
# ⏸ WX24-P2-051-E1: minCount軸は実装済みだが notGuard・byEffect が残る。
# 再集計: 完全消化 WXEX1-08/WXDi-P09-079。WXK10-052は龍獣filterのみ消化し原因owner軸を[B] defer。
# minCount機構は着地したが、現行原文不一致2件と追加軸残り4件は過剰発火を避けてcurated未採用。

# ---------------------------------------------------------------------------
# 2026-07-28 ON_PLAY誤分類36効果の停止（今回の[A]/[B]/[C]定義で再判定）
# 旧2026-07-27節の[C]は「機構が無い」を意味しており、今回の[C]「実はON_PLAYで正しい」とは意味が異なる。
# 旧判断は collector/限定軸の有無について有効だったため再確認し、正規collectorが揃う6件だけ[A]、
# 残り29件を[B] timing:[]（誤発火停止・§6.3/タスク16送り）、effectId誤対応1件を[C]とした。
#
# effectId | CSV原文の該当句 | 判定 | 結果
# WX05-020-E1 | 1ターンにライフクロスを合計2枚以上クラッシュしたとき | [B] | 主体別ターン累積collector無し→停止
# WX05-025-E2 | ガードするか、シグニのアタックを効果によって無効にしたとき | [A] | ON_GUARD＋ON_OPP_SIGNI_ATTACK_NEGATED_BY_EFFECT
# WX13-040-E1 | 対戦相手のシグニ1体のアタックを無効にしたとき | [A] | ON_OPP_SIGNI_ATTACK_NEGATED_BY_EFFECT
# WX14-064-E1 | ガードするか、シグニのアタックを効果によって無効にしたとき | [A] | ON_GUARD＋ON_OPP_SIGNI_ATTACK_NEGATED_BY_EFFECT
# WX14-066-E1 | 効果によって手札かデッキからエナゾーンに置かれたとき | [B] | 移動元＋原因種別軸無し→停止
# WX19-066-E1 | あなたの【自】の【英知】能力が発動したとき | [B] | 能力発動監視collector無し→停止
# WX20-046-E2 | エナゾーンからカード1枚を手札に加えたとき | [A] | ON_HAND_ADDED＋fromZones:energy
# WX20-067-E1 | カード1枚がいずれかのプレイヤーの手札に加えられたとき | [B] | handOwner:any軸無し→停止
# WXEX1-41-E1 | 【トラップ】1つが設置されたとき | [B] | 設置collector無し→停止
# WXEX1-77-E1 | 対戦相手の場のシグニの【出】能力が発動したとき | [B] | 相手能力発動監視無し→停止
# WXEX2-19-E1 | あなたの【アクセ】1枚がトラッシュに置かれたとき | [B] | アクセ離脱collector無し→停止
# WXK05-023-E2 | あなたが赤のスペルを使用したとき | [A] | ON_SPELL_USE＋triggerFilter.color赤
# WXK08-028-E1 | ライフクロスが他の領域に移動したとき | [B] | 任意宛先life移動collector無し→停止
# WXK10-044-E1 | 相手シグニが【ゲート】のあるシグニゾーンに出たとき | [A] | ON_PLAY any_opp＋placedOnGateZone
# WXK10-049-E1 | このシグニにカード1枚が付いたとき | [B] | 汎用付与collector無し→停止
# WXK11-018-E2 | このシグニがアタックしたアタック終了時 | [B] | 個別アタック終了collector無し→停止
# WXDi-D07-004-E1 | あなたのシグニ1体に【ソウル】が付いたとき | [B] | ソウル付与collector無し→停止
# WXDi-D07-019-E1 | このシグニに【ソウル】が付いたとき | [B] | 同上→停止
# WXDi-D09-H16-E1 | アシストルリグかLBの能力か効果の対象になったとき | [B] | ON_TARGETEDにorigin種別軸無し→停止
# WXDi-D09-P16-E2 | 自分の効果によってカード1枚以上捨てたとき | [B] | byOwnEffect軸不足→停止
# WXDi-P03-073-E1 | このシグニが場からエナゾーンに置かれたとき | [B] | 移動後self collector無し→停止
# WXDi-P06-038-E1 | エナから効果によってカード1枚が他領域に移動したとき | [B] | 任意宛先＋原因軸無し→停止
# WXDi-P06-072-E1 | パワー8000以上のシグニが相手シグニをバニッシュしたとき | [B] | バニッシュ主体power軸無し→停止
# WXDi-P07-052-E1 | ライフがクラッシュされるかトラッシュに置かれたとき | [B] | 非クラッシュlife→trashとのOR不足→停止
# WXDi-P08-065-E2 | 【出】能力かその効果の対象になったとき | [B] | ON_TARGETEDにabilityType軸無し→停止
# WXDi-P11-063-E2 | このカードがシグニの下に置かれたとき | [B] | ON_PLACED_UNDER_SIGNI collector無し→停止
# WXDi-P12-079-E2 | このカードがデッキからエナゾーンに置かれたとき | [B] | energy在中self＋from deck collector無し→停止
# WXDi-P13-051-E3 | 相手効果で手札捨てまたはエナからtrash | [B] | byOpponentEffect＋2経路OR不足→停止
# WXDi-CP02-068-E1 | 自効果で相手シグニが手札に戻るかtrashに置かれたとき | [B] | 宛先OR限定不足→停止
# WXDi-P11-010B-E1 | 《夢限 -Q-》から《夢限 -A-》になったとき | [B] | named form遷移collector無し→停止
# WX24-P2-051-E1 | コストか効果で非ガードを1枚捨てたとき | [B] | notGuard＋原因軸不足→停止
# WX24-P2-075-E1 | あなたのアタックフェイズ終了時 | [B] | 攻撃フェイズ終了collector無し→停止
# WD23-023-E-E1 | いずれかのライフクロス1枚がトラッシュに置かれたとき | [B] | 非クラッシュ＋owner:any不足→停止
# SP27-007-E1 | あなたか対戦相手がコインを得たとき | [B] | コイン獲得collector無し→停止
# PR-461-E2 | 【出】：デッキからシグニを3枚まで探して場に出す | [C] | effectId自体は正しい【出】。ON_PLAY維持
# PR-K038-E2 | あなたのライフクロスが0枚になったとき | [B] | life閾値遷移collector無し→停止

# ---------------------------------------------------------------------------
# 2026-07-31 [B]群の停止理由 機械再検証（codex-work・Opusタスク16 第1波）
# 目的＝2026-07-27/28 節の [B] 判定は当時の実装状況に基づく。その後の機構追加で
# 停止理由が陳腐化しているものを洗い出す。判定は grep による現行コード実地確認。
#
# effectId | 停止理由に挙がった軸 | 軸の現存 | engine honor | 再判定 | 根拠(ファイル:行)
# WX05-020-E1 | 主体別ターン累積life crash | life_crashed_this_turnは現存 | カウンタ加算/ターンリセットのみ、閾値到達collector無し | [B維持] | BattleScreen.tsx:6961,3307-3308
# WX14-066-E1 | energy移動元hand/deck＋原因lrig/signi effect | fromZones/byOwnEffect型は現存 | ON_ENERGY_CHARGE直読みは増加枚数だけで移動元/原因を読まない | [B維持] | effects.ts:2123-2125; BattleScreen.tsx:1630-1638
# WX19-066-E1 | 【自】【英知】能力発動監視 | abilityTypesは付与能力側に現存 | 能力発動イベントcollector自体無し | [C] | effects.ts:1475; triggerCollect.ts:2839
# WX20-067-E1 | handOwner:any＋minCount | 両方現存（anyは本波で型追加） | collectHandAddedTriggersがowner/minCount/phase conditionを実読、中央diffから呼出済み | [A昇格候補] | effects.ts:2117,2156; triggerCollect.ts:1889,1919-1922; BattleScreen.tsx:2869-2872
# WXEX1-41-E1 | トラップ設置 | signi_traps状態は現存 | 設置イベントcollector無し（既存はON_PLAY時の「トラップ上」判定のみ） | [C] | effects.ts:1298; triggerCollect.ts:2766-2771
# WXEX1-77-E1 | 相手【出】能力発動監視 | abilityTypes型のみ現存 | 能力発動イベントcollector自体無し | [C] | effects.ts:1475; triggerCollect.ts:2626
# WXEX2-19-E1 | アクセがトラッシュへ離脱 | acce状態/付与能力収集は現存 | アクセ離脱イベントcollector無し | [C] | effectEngine.ts:5302; triggerCollect.ts:1106
# WXK08-028-E1 | life任意宛先移動 | life領域は現存 | 現存diffは追加専用で、life減少の汎用移動collector無し | [C] | boardDiff.ts:99; triggerCollect.ts:2024
# WXK10-049-E1 | 自身への汎用カード付与 | under/charm等の個別状態は現存 | 任意カード付与イベントcollector無し | [C] | effects.ts:79; triggerCollect.ts:1787
# WXK11-018-E2 | このシグニの個別アタック終了 | 攻撃開始timingは現存 | 個別アタック終了collector/timing無し | [C] | effects.ts:72; triggerCollect.ts:2839
# WXDi-D07-004-E1 | 味方へのソウル付与 | soul付与参照は現存 | ソウル付与イベントcollector無し | [C] | effectEngine.ts:5302; triggerCollect.ts:1787
# WXDi-D07-019-E1 | 自身へのソウル付与 | soul付与参照は現存 | ソウル付与イベントcollector無し | [C] | effectEngine.ts:5302; triggerCollect.ts:1787
# WXDi-D09-H16-E1 | ON_TARGETED origin=assist/LB | ON_TARGETED collector現存、abilityTypesは別用途に現存 | collector引数は対象群/ownerのみでoriginを受けない | [B維持] | effects.ts:42,1475; triggerCollect.ts:334; BattleScreen.tsx:2332
# WXDi-D09-P16-E2 | ON_HAND_DISCARDED byOwnEffect＋minCount | 両フィールド現存 | minCountはhonorするがcollectorにcauseOwner引数がなくbyOwnEffectを読まない | [B維持] | effects.ts:2117,2178; triggerCollect.ts:2438-2449; BattleScreen.tsx:4991
# WXDi-P03-073-E1 | 移動したself＋field→energy | energy増加検知は現存 | watcherは移動後の場を走査し、移動カード自身/移動元を扱わない | [B維持] | BattleScreen.tsx:1630-1638; boardDiff.ts:73
# WXDi-P06-038-E1 | energy→任意宛先＋効果原因 | 個別のenergy→trash/hand検知は現存 | 任意宛先を束ねるcollector/原因軸無し | [B維持] | boardDiff.ts:73; triggerCollect.ts:1490
# WXDi-P06-072-E1 | バニッシュ主体power>=8000 | TargetFilter.powerRangeは現存 | ON_BANISHのtriggerFilterは被バニッシュ側に適用され、主体powerを読まない | [B維持] | effects.ts:441; triggerCollect.ts:970-978; BattleScreen.tsx:2719
# WXDi-P07-052-E1 | life crash OR 非crash life→trash | crashカウンタ/life領域は現存 | 非crash life→trashとのOR collector無し | [B維持] | BattleScreen.tsx:6961; boardDiff.ts:99
# WXDi-P08-065-E2 | ON_TARGETED origin abilityType=【出】 | abilityTypes型は現存 | ON_TARGETED collectorはoriginを受けずabilityTypeを読まない | [B維持] | effects.ts:1475; triggerCollect.ts:334; BattleScreen.tsx:2332
# WXDi-P11-063-E2 | このカードがシグニの下に配置 | timing型/個別STUB経路は現存 | 汎用collectorは無くINTERNAL_PLACE_SELF_UNDER_SIGNI内の直積みだけ | [B維持] | effects.ts:79; execStubPart2.ts:3332-3353
# WXDi-P12-079-E2 | movedSelf deck→energy | fromZones/movedSelfはhand用として現存 | ON_ENERGY_CHARGE watcherは移動元/self移動を読まない | [B維持] | effects.ts:2125,2158; BattleScreen.tsx:1630-1638
# WXDi-P13-051-E3 | byOpponentEffect＋hand discard/energy trash OR | byOpponentEffect/minCount型は現存 | 個別collectorはあるが解決単位OR/重複排除とdiscard原因ownerが無い | [B維持] | effects.ts:2117,2123; triggerCollect.ts:1490,2438; BattleScreen.tsx:4991
# WXDi-CP02-068-E1 | 自効果＋相手シグニのhand/trash宛先OR | byOwnEffectとON_LEAVE_FIELD collectorは現存/honor | collectorは離脱先を受けずenergy/deck等も含めて過剰発火する | [B維持] | effects.ts:2178; triggerCollect.ts:1106,1223-1225; BattleScreen.tsx:2275
# WXDi-P11-010B-E1 | named form Q→A遷移 | LRIG flip collectorは現存 | named form遷移イベントcollector無し | [C] | triggerCollect.ts:391; BattleScreen.tsx:14
# WX24-P2-051-E1 | minCount＋notGuard＋cost/effect原因 | minCount/hasGuardは現存 | discard collectorはminCountのみhonor、否定guardと原因を読まない | [B維持] | effects.ts:449,2117; triggerCollect.ts:2447-2449
# WX24-P2-075-E1 | 自アタックフェイズ終了 | phase開始collectorは現存 | phase終了timing/呼出自体無し | [C] | effects.ts:72; BattleScreen.tsx:3538; triggerCollect.ts:2839
# WD23-023-E-E1 | 非crash life→trash＋owner:any | life追加diffは現存 | life→trash汎用collector/owner:any無し | [B維持] | boardDiff.ts:99; triggerCollect.ts:2024
# SP27-007-E1 | いずれかのplayerのcoin獲得 | coin状態/支払collectorは現存 | coin獲得collector無し | [C] | triggerCollect.ts:483; BattleScreen.tsx:5611
# PR-K038-E2 | 自lifeが0へ遷移 | life領域は現存 | 0到達遷移collector無し | [C] | BattleScreen.tsx:6955-6961; boardDiff.ts:99
#
# 集計: [B維持] 15件 ／ [A昇格候補] 1件 ／ [C] 13件
# [A昇格候補] effectId: WX20-067-E1
# 採用: WX20-067-E1（本波で原文全体を忠実化）。timingだけの解除はせず、
# DURING_PHASE、handOwner:any/minCount、OPTIONAL_COST《白》+down_self、支払時のみBOUNCEを一体で採用。

# ---------------------------------------------------------------------------
# 2026-07-31 [C]13件の §6.3 送り反映（Claude・Opusタスク16 第1波の後処理）
# 上記再検証で [C]（イベント検出collector自体が無い）と判定した13件を PLAN.md §6.3 の
# 機構台帳「J. timing collector 不在の13効果」へ正式送りした。タスク16 の worklist からは外す。
# いずれも timing:[] で安全停止中＝放置しても過剰実行は起きない（着手優先度は低い）。
#
# 5家族に束ねた（1家族=1バッチで複数枚が同時に開く）:
#   J-1 他能力の発動監視 (2) : WX19-066-E1 WXEX1-77-E1
#   J-2 付与・離脱イベント (4): WXDi-D07-004-E1 WXDi-D07-019-E1 WXK10-049-E1 WXEX2-19-E1
#   J-3 life汎用移動/閾値遷移 (2): ✅2026-07-31 完了（PR-K038-E2 は0枚到達）
#        WD23-023-E-E1 / WXDi-P07-052-E1 も同時採用し計4効果を開いた
#   J-4 フェイズ/アタック終了 timing (2): WX24-P2-075-E1 WXK11-018-E2
#        ⚠タスク12(lxvii)（CPU側のフェイズ/ターン境界トリガー未収集）と一体で見ること。
#          新timingを足しても人間ターンだけの片肺になる
#   J-5 単発 (3) : WXEX1-41-E1 SP27-007-E1 WXDi-P11-010B-E1
#
# タスク16 に残るのは [B維持] 15件（collectorはあるが引数/軸が不足＝engine軽量拡張）。
#
# ⚠計器の偽陽性2件（2026-07-31 実測・未修正）:
#   `npm run census:timing` は31効果を報告するが、うち WXDi-P09-079-E1 と WXK10-052-E1 は
#   live JSON で既に正しい timing（ON_CARD_MILLED_FROM_DECK + milledCardFilter）を持つ＝
#   2026-07-27 節で消化済み。外科パッチが clearTimingFallback() を呼んでいないため
#   parseBlock 時点のフォールバック記録が残っているだけ。**実質残は29効果**。
#   較正するなら該当パッチに clearTimingFallback を足す（機能実装ではなく計器の較正）。
#   未再検証の残り1件 SPDi43-11-sub-E1 は複数効果を跨ぐ累積カウンタ＝真の [C] 相当。
