===== 原文エコー・センサス（§5.3 O-356・逆翻訳が原文を切り出してそのまま返す箇所） =====
生成: npm run census:srcecho   1 id の内訳: node scripts/censusSrcEcho.mjs --id <STUB_ID>

走査対象: scripts/decompileEffects.ts
  🔴A ECHO  （原文が戻り値に乗る＝本命の worklist）: 0行 / 0id / live 0ノード / 0カード
    うち原文を返してよいと登録済み（ALLOWED）     : 10行 / 2id
  B READ/GUARD（原文を読むが戻り値には乗らない）  : 10行
  C COMMENT （コメント）                          : 34行

⚠候補出しであって判定ではない＝1件ずつ「payload から同じ意味を組み立てられるか」で判定する。
  組み立てられる → payload から描く分岐へ置換／組み立て不能（引用そのものが中身） → ALLOWED に理由つきで登録。
🔑置換すると逆翻訳が原文と「ずれる」カードが出る＝**退化ではなく可視化**（engine の欠落が初めて見える）。
   原文へ寄せて隠さないこと（逆翻訳は engine が何をするかを描く）。
⚠live 0 の id は parser に生成元が無い＝**触らない・消さない**（安全網）。

── A群（live 出現の多い順）──

── B群（原文を読むが戻り値には乗らない＝照合は生きている）──
  scripts/decompileEffects.ts:124  [(非STUB＝汎用ヘルパー)]  const scope = currentEffectText || currentCardText;
  scripts/decompileEffects.ts:125  [(非STUB＝汎用ヘルパー)]  for (const sent of scope.split(/[。\n]/)) {
  scripts/decompileEffects.ts:2349  [SEARCH]  if (a.maxCount?.$ref === 'last_processed_count' && /捨てたカード[１1]枚につき/.test(currentCardText)) {
  scripts/decompileEffects.ts:3995  [GRANT_QUOTED_AUTO_ABILITY]  if (gm && /【(?:自|常|起|出)/.test(gm[3])) {
  scripts/decompileEffects.ts:4003  [GRANT_QUOTED_AUTO_ABILITY]  if (gm2 && /【(?:自|常|起|出)/.test(gm2[2])) {
  scripts/decompileEffects.ts:5556  [GRANT_QUOTED_ACTIVATE_ABILITY]  ?? currentCardText.match(/[^。：]*?は「[\s\S]+?」を得る(?:。（[^）]*）)?/);
  scripts/decompileEffects.ts:6736  [SELF_PLACE_COIN_COST]  const noun = /このルリグが[^。]{0,100}ライフ(?:クロス)?/.test(currentEffectText) ? 'ルリグ' : 'シグニ';
  scripts/decompileEffects.ts:6737  [SELF_PLACE_COIN_COST]  s = `この${noun}が対戦相手のライフクロス1枚をクラッシュしたとき`;
  scripts/decompileEffects.ts:7220  [SELF_PLACE_COIN_COST]  const noun = milled?.cardClass ? `＜${milled.cardClass}＞のシグニ` : milled?.cardType === 'シグニ' ? 'シグニ' : 'カード';
  scripts/decompileEffects.ts:7221  [SELF_PLACE_COIN_COST]  s = `${source}${who}デッキから${noun}が${mc}枚以上トラッシュに置かれたとき`;
