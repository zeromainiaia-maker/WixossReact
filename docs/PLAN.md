# PLAN — 開発計画（唯一の正）

> **このファイルの読み方**＝新セッション（cold start）は **§1 現在地 → §2 作業の流れ → §5 作業キュー** の順に読む。設計方針は [DESIGN.md](./DESIGN.md)、修正記録は [BUGFIXES.md](./BUGFIXES.md)（新しいものを上）。
> **ここには「現在地・流れ・ルール・教訓・生きている worklist」だけを置く。** 消化済みの経緯・完了項目の全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md)、過去のセッション要約は [PLAN_PROGRESS.md](./PLAN_PROGRESS.md)、一次記録は [BUGFIXES.md](./BUGFIXES.md) にある。**クローズした項目はこのファイルから消す**（ID も残さない＝退避先と git 履歴が正）。

---

## 1. 現在地（直近1セッション）

> **運用**＝この節には**直近1件の要約だけ**を残す（入れ替え式）。新しく作業したら ①いまの要約を [PLAN_PROGRESS.md](./PLAN_PROGRESS.md) の先頭へ移す ②この節を今回の要約へ書き換える。**溜めない**（溜めると cold start が最初に読む節が一番古くなる）。
🏁**直近＝2026-09-15（第357バッチ）＝`O-452` をクローズ（対象宣言を条件の内側へ沈めた・22効果）**（全文は [BUGFIXES.md](./BUGFIXES.md)）
🔴**訂正＝`O-452` は「第352バッチ（`O-413`）の回帰」ではなかった**＝`git show 1bf358353^` で live が**修正前後で同一**と確認。出どころは `applyDroppedTargetDesignation` / `O-96` 系の**もっと古い平坦挿入**。
🔑**直し方＝全 pass の最後に `sinkTargetDeclIntoLeadingCondition` を置いた**（`hoistTargetBeforeCondition` と対）。**早い段（`applyDroppedTargetDesignation` の条件先枝）を広げる案は退化した**＝任意コスト STUB が素の `TRASH{HAND_CARD}`（＝強制の手札破棄）に化けた（`WX24-P1-042-E1` で実測）。
🔑**実測は 36 → 29 → 22**＝検出器の偽陽性7件（`。` を跨ぐコスト軽減の前置き／`「` を跨ぐ引用付与の外側条件）を落とし、さらに**条件そのものが JSON に無い7効果**を `O-508` へ分離した。

| 軸 | いまの値 |
|---|---|
| 🔥**次に取るもの** | ①索引A `O-453`（34効果）→ `O-399`（12効果）／②索引B `O-391`(b) 9効果／③索引G 122件（`O-451` 18効果・🆕`O-508` 7効果を含む）。🏁**round5 は完走**＝次の発見器を回すなら round6 の設計から |
| 📊**進捗3計器** | Sheet1 要対応 **11 / 863**（全件 mech＝**即着手可能 0**）／台帳 残 OPEN **0**／census 高シグナル **1 / BASELINE 1** |
| 📦**在庫** | 機構 worklist 🔥**125**（索引A **2**／索引B 1／索引G 122）／**実機 🏁0**／実装キュー 🔥**77 効果**／round5 🏁**598 / 598 バッチ（5,976枚＝100%）** |
| 🔧**ゲート** | `npm run gates` **全緑**（golden **4185/4185**＝`O-452` の形・挙動・反転確認で +3本）＋実機 `V-228` 2本 PASS（対象宣言UIの番人） |
| 🔴**運用の決定** | 🔴**live の木の形を変えると「isolation ハーネス」が黙って壊れる**＝内側のノードだけ取り出して単体で回すテストは、新しく載った `targetsStored` のせいで**候補0**になる。**shape assert とハーネスを別のヘルパーで直す**（`stripO413TargetDecl` / `withoutStoredBinding`） |

---

## 3. 不変の運用ルール

- **`effects_*.json` の curated 値は上書きされない**＝`build:effects` は ①`parseStatus` が `MANUAL`/`PARTIAL` を含むカードを**カード単位で温存**（`PRESERVE_STATUSES`）②それ以外も fresh が curated の純粋な上位集合でなければ**温存して held へ送る**（`buildEffectsJson.ts:158-162`）。**変わるのは `heldReview --adopt` で明示採用したものだけ。**
- **逆翻訳を直したらエンジン実装までセット**（乖離＝偽陰性を作らない）。語彙が無ければ §5.3 の機構として実装するか、`engineUnwiredTimings` に登録し逆翻訳へ `【※engine未配線】` を付けて明示する。[[decompile-engine-parity]]
- **ゲートは `npm run typecheck`（＝`tsc -b --noEmit`）**＝plain `tsc --noEmit` は project references を見ず CI が拾うエラーを見逃す。⚠`scripts/` は型チェックの対象外＝**`scripts/` を直したら実際に `npm run golden` を走らせるまで「直った」と言わない。**
- 🔴**一括置換は禁止**（全再生成系も、**ソースの文字列置換も**）＝無検証置換で約90枚退化／`manualEffects.ts` への `s.replace('"usageLimit":"once_per_turn"', '')` で**無関係な21効果から《ターン1回》を消した**前例がある。⇒ **行番号を指定した1行だけの置換にする。** 🔑**`git diff --stat` が「1行のはずが22行」と言ったら、そこで止まる。** 🔑**ゲートが2本同時に落ちたら、engine を読み始める前に自分の直前の置換範囲を疑う。**
- **系統ごとの直し方**＝機構を1回確立 → 同パターンに適用 → **各カードを verify**。
- **日本語を含むスクリプトは `scratchpad` に `.mjs` を書いて `node <path>` 実行**（Git Bash 経由の `node -e` は文字化けする）。使い捨ては `tmp_*` 名（gitignore 済み）。
- **CSV の順番を必ず維持する**（スクリプト内の `sorted` ロジックで対応済み）。
- **件数メトリクスを完了指標にしない**＝逆翻訳器は複数効果を1行に圧縮するので、内容を直しても件数は減らない。判断は必ず**該当カードの逆翻訳が原文と一致するか（目視／grep）**で行う。

### 進捗の報告の仕方 ★2026-08-27 ユーザー決定・2026-09-01 改定

**「3計器＋在庫3本」を併記する。どれか1本を「進捗指標」に固定しない。**

| | 指標 | 取り方 |
|---|---|---|
| ① | Sheet1 の要対応カード数 | `npm run census:cards -- --sheet 1` |
| ② | 意味照合 段2 台帳の残 OPEN | `node scripts/archive/semanticAuditLedger.mjs` |
| ③ | census 高シグナル数 | `npm run census` |
| ④ | 機構 worklist の残項目数 | §5.3 の索引 |
| ⑤ | 実機の残 `V-nn` 数 | §5.1 |
| ⑥ | 実装キューの残効果数 | §5.0 |

- 🔴**①〜③は 2026-09-01 に底を打った**（即着手可能 0／残りは全件 UI か新機構待ち）＝**動かないことを「停滞」と読まない**（22効果を直して3計器とも据置だった実績）。**以後の進捗指標は ④〜⑥の在庫。**
- ⚠**在庫も「減れば前進」とは限らない**＝母集団を実測すると**項目が割れて増える**ことがある。**減った理由／増えた理由を必ず1行書く。**
- **3計器は別のものを測っている**＝①**カード単位の網羅**（1枚に findings が複数あると1つ直しても落ちない）②**指摘単位の消化**（標本なので、同じ parser 規則で直った標本外の実バグは数に出ない）③**語彙の欠落**（STUB/MANUAL を免除するので STUB を解体すると**増えて見える**）。⇒ **単独の計器は「進んだのに動かない」と「動いていないのに動く」の両方を起こす。**
- ⚠**バッチの着手時に「今回どれが動く見込みか」を宣言する**。**動かない計器があること自体は失敗ではない。動かない理由を書けないことが失敗。**

### 横展開（系統バグ）のときの追加手順

①**抽出**＝全シート走査で「同じ壊れ方」を機械抽出 → ②**分類**＝偽陽性（付録B）・既知の複雑札を除外してクリーンな系統を確定 → ③**パッチ**＝`effectId` をアンカーにした置換（他カードを巻き込まない。MANUAL 化するなら `parseStatus:'MANUAL'`）→ ④**検証**＝§2.1 ④〜⑤ → ⑤**記録**＝`BUGFIXES.md` へ追記（新しいものを上）→ §1 を入れ替え → commit / push。

### 機構実装の「型」
1. `src/types/effects.ts`（アクション/条件/timing の型）→ 2. `src/types/index.ts`（`PlayerState` 状態フィールド）→ 3. `src/engine/effectExecutor.ts`（実行）/`execUtils.ts`（`evalCondition`/`matchesFilter`）/`effectEngine.ts`（CONTINUOUS収集）→ 4. `src/screens/BattleScreen.tsx`（状態読み取り＋**ターン境界リセット3箇所**：PvP通常終了・PvP確認後・CPU）→ 5. `scripts/decompileEffects.ts`（表示）→ 6. JSON 配線 → 検証。

### 主要ファイル
- 語彙: `src/types/effects.ts` / `src/types/index.ts`（PlayerState）
- エンジン: `src/engine/effectExecutor.ts`・`execUtils.ts`（`evalCondition`/`matchesFilter`）・`effectEngine.ts`（CONTINUOUS収集・`checkActiveCondition`）
- UI/ルール: `src/screens/BattleScreen.tsx` ＋ `src/screens/battle/`（コスト計算・バトル・ターン境界リセット）
- 逆翻訳器: `scripts/decompileEffects.ts`、グルーピング: `scripts/group{Similar,BySentence}.mjs`（`--all` で全10シート統合）
- 監査: `scripts/behaviorAudit.ts`（`npm run audit` / `audit:queue` / `audit:html`）

---

## 4. 教訓集 ★**本体は別ファイル**（2026-09-06 に分離）

> 🔴**この節に教訓を書かない。** 2026-09-06 時点で PLAN.md は **1,313行中 634行（52%）が §4**で、
> 「計画表」として読めなくなっていた（うち §4.4 だけで 368行＝**全体の30%**）。⇒ 2ファイルへ分離した。
> 🔴**節番号 `§4.1`〜`§4.6` は住所として維持している**＝BUGFIXES・PLAN・`scripts/`・`src/` から**数百箇所**が
> 「§4.2」「§4.4-2c」の形で参照している（実測＝`§4.4` だけで `scripts/` から **216箇所**）。**詰め直さない。**

| ファイル | 中身 | 読むタイミング |
|---|---|---|
| **[LESSONS.md](./LESSONS.md)** | **§4.1** 着手前（見立てと母集団）／**§4.2** 実装（型・parser・engine）／**§4.3** 計器の読み方／**§4.5** 修正を live へ届ける経路／**§4.6** Codex へ委譲するときの作法／🆕**§4.7** 意味照合ラウンドと triage の読み方／🆕**§4.8** 監視だけしている項目 | 🔑**§5 のどの項目に着手するときも先に読む** |
| **[DRIVE_TRAPS.md](./DRIVE_TRAPS.md)** | **§4.4** 実機シナリオの罠（番号つき92項） | 🔑**実機シナリオを書く／直す回だけ**（`scripts/verifyBattleDrive.mjs`） |

⚠**新しい教訓はこの2ファイルへ足す**（PLAN へ書き戻さない）。
⚠**踏んだ罠は必ず残す**＝この2ファイルが生まれた理由は、教訓が**クローズ済みタスクの本文に埋まっていて**
**完了行を消すと巻き添えで消えていた**こと（2026-08-23 の §4 新設時の経緯）。

---

## 5. 作業キュー（単一・上から取る）

> **取り方**＝**上から1件**。並行して別の節から取らない。1巡の手順は §2、着手前に読むのは [LESSONS.md](./LESSONS.md)。
> **`V-nn`／`O-nn` は固定の住所**＝消化しても詰めない・再利用しない。**消化したら行ごと消す**（全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md) と [BUGFIXES.md](./BUGFIXES.md) が正）。
> ⚠**クローズの経緯をこの節に書き溜めない**＝書くのは**いまの残数だけ**（過去に「残」列がクローズ履歴2,251字に膨らみ、しかも3節とも数字が stale だった）。
> ⚠**残数は毎回コマンドで数え直して書く**（表の数字を足し引きしない＝triage 済み32件を二重に足して「77件」と書いた実績がある）。

### 残作業の全体像 ★まずここを見る

| 順 | キュー | 残 | 中身 | 測り直すコマンド |
|---|---|---|---|---|
| **①** | **§5.1 実機 `V-nn`** | 🏁**0件** | `src/screens/` を触った回の返済先＝**溜める前に返す** | §5.1 の表 |
| **②** | **§5.3 機構 worklist `O-nn`** | 🔥**1項目**（索引G `O-376`）＋**索引H 解釈待ち 3項目**（`O-377`〜`O-379`＝**ユーザーが判断**・Claude は取らない） | 新しい型・評価器・engine が要るもの | §5.3 の索引（母集団は着手時に実測し直す） |
| **③** | **§5.0 実装キュー** | 🏁**0効果**（2026-09-12 に全数照合） | triage で真バグと確定した未修正バグ | `node scripts/archive/semanticAuditBugList.mjs` |
| — | §5.2 意味照合 | 🏁**round5 完走 598 / 598 バッチ**（全5,976枚を読み直し） | **「受け皿の名前を知らない穴」を拾える唯一の発見器** | `semantic_audit_round5/TYPE_LEDGER.md` |
| — | §5.4 構造混線 | 🏁**0** | 新しく見つけたときだけ足す | — |
| — | §5.3 末尾「個別カードの機構待ち」 | 参照 **3件** | 🆕**2026-09-14 に全項目を再実測**＝**5件が stale**（第323で3件・**第328で2件**＝残りの defer 2件も実装済みだった）／残り**3件は有効** | — |

**取る順とその理由（依存関係）**

1. 🔥**①実機（§5.1）を先に0にする**＝寝かせるほど「バグかシナリオの腐りか」の切り分けが高くつく（§2.2）。**いま残0**＝`src/screens/` を触ったら同じ巡で返す。
2. **②機構（§5.3）**＝索引の上から取る。**新しく機構待ちを見つけたときの登録先**でもある。
3. **③実装キュー**＝**機構不要と判定できる候補だけ**を取る（`semanticAuditQueue.mjs --take N`）。**②を消化すると候補が戻る。**
4. **④低優先**は①〜③が尽きたときだけ。**⑤§5.2 round5** は③の在庫が尽きたときに開始を判断する。
   🆕🔥**2026-09-15 に round5（全カード読み直し）を開始した**（第338＝21 / 598 バッチ）。
   **round5 が出した BUG は②（§5.3）へ `O-nn` で入る**＝round5 を回すほど②の在庫が戻る。解釈待ち（索引 H）はユーザーの判断が出るまで取らない。
   🔑round5 の性質＝**意味照合は「受け皿の名前を知らない穴」も拾える唯一の発見器**（逆翻訳・census・golden は
   知っているキーしか見ない）。⚠ただし**監査の止め時は「残枚数」ではなく「1バッチあたりの新型数」**（§2.6）。

⚠**「§5 の上から1件」は §5.3 の索引にもそのまま当てる**（索引が母集団順に並んでいる）。

---

### 5.0 実装キュー（triage 済みの確定バグ）

🏁**残0**（`node scripts/archive/semanticAuditBugList.mjs` で測り直す＝在庫カウンタ／`semanticAuditQueue.mjs` は候補プール）。
**この節から新規に取るものは無い。** 新しい確定バグが triage で出たらここへ戻る。

🆕🔴**2026-09-12 の実測＝この節は「残0」ではなく「残5効果」だった**（`WX09-032-E1`／`SP26-002-E1`／`PR-305-E1`／
`WX25-P3-057`／`WXDi-P13-004B-E3`）。**5件とも第291〜292バッチ（`O-334`／`O-337`／`O-338`+`O-342`／`O-339`／`O-340`）で
既に実装済み**（live JSON・engine 消費点・golden を1件ずつ照合）で、**`semantic_bug_fixed.txt` への記録を落としていただけ**。
🔑**教訓＝「機構を実装して `O-nn` をクローズする」ことと「その機構を待っていた効果を消化済みにする」ことは別作業**。
`semantic_bug_deferred.txt` の `MECH` 行は**在庫から引かれない**（＝直っていないのが事実の間はそれが正しい）ので、
**`O-nn` をクローズした回に、その登録票が引用している effectId を `semantic_bug_fixed.txt` へ必ず書く**。
落とすと**在庫カウンタが永久に残ったまま**になり、§1 の「次の一手」が閉じた項目を指し続ける。

🔴**per-effect の正本は各ラウンド dir の `triaged.txt` の `:: BUG ::` 行**＝この節はそこから系統を引き出した取り掛かり表。
🔴🔑**この節が唯一の追跡先**＝finding を「BUG」と triage した瞬間、`semanticAuditPool.mjs` からも
`census:cards` の「未消化 findings」からも消える（どちらも数えているのは*未 triage*であって*未修正*ではない）。
⇒ **行を消してよいのは (a) 直して golden を張った (b) 偽陽性と確定した (c) §5.3 へ `O-nn` で登録し直した ときだけ。**

#### 1バッチの回し方（Codex 委譲・道具はリポジトリ内。スクラッチに置かない）

```
# ① 候補を組む（残数・優先度の確認だけなら引数なし）
node scripts/archive/semanticAuditQueue.mjs --take 30 --out <scratchpad>
#    → batch.json（原文・live JSON つき）と batch_table.md（指示書に貼る表）

# ② 指示書を作る（テンプレをコピーして <NNN>/<BASELINE_COMMIT>/<GOLDEN>/<プール残>/<残数> を埋め、
#    batch_table.md を「今回のスコープ」節へ丸ごと貼る）
scripts/archive/scratchpad/codex_batch_template.md

# ③ 投入（⚠先に git status --porcelain を空にする）
CODEX_HOME=/c/Users/zerom/.codex-work codex exec -C "C:/Users/zerom/WixossReact" \
  -c model_reasoning_effort="high" -o <report> - < <指示書> > <log> 2>&1
```

- 🔑**候補の母集団は3ファイルの引き算だけ**＝`triaged.txt` の `:: BUG ::` − `semantic_bug_fixed.txt` − `semantic_bug_deferred.txt`。**外部状態に依存しないので、セッションが変わっても同じ候補が再現する。**
- ⚠**`pri=2`（判定文が「機構が要る」と明言）は既定で候補から外れる**＝入れても Codex が見送りを書くだけ。
- 🔑**委譲バッチの既定形＝1項目終わるたびに `BUGFIXES.md` へ追記させる**（§2.6）。**Claude は `git diff`／`typecheck`／`golden`／`gates` で独立検証してから採用する**（Codex の「実装した」を信用しない）。

#### 記録の付け方（これを怠ると在庫カウンタが永久に減らない）

- **直した** → `scripts/archive/scratchpad/semantic_bug_fixed.txt` に `<effectId> :: <日付> :: <一言>`。
- **直さないと判定した** → `scripts/archive/scratchpad/semantic_bug_deferred.txt`（`MECH`＝機構待ち／`STALE`＝既に直っていた／`FP`＝偽陽性／`REGISTERED`＝§5.3 登録済み）。🔴**在庫からは引かない**（直っていないのは事実）＝**次バッチの候補から外すためだけ**。
- 🔴🔑**`MECH` と書くときは必ず `O-nn` を併記する**（無ければ同じ回に §5.3 へ新設）＝併記を怠ると §5.3 が在庫を映さなくなる。
- ⚠**`triaged.txt` の行は消さない**（「何を直したか」の履歴）＝消化は上の2ファイルで引き算する。
- ⚠**モデルを1セッション内で切り替えない**（プロンプトキャッシュはモデル別＝切替後の最初のターンで全コンテキストを full price で読み直す）＝**簿記 → commit → `/clear` → `/model`** の順にする。

---

### 5.1 実機で確かめる（`V-nn`）★①＝溜める前に返す

> ⚠**着手前に [DRIVE_TRAPS.md](./DRIVE_TRAPS.md) を読む**（番号つきの罠 123項）。
> ⚠**`verifyBattleDrive.mjs` は必ず明示シナリオIDで実行する**（引数なしのフルバッチはフリーズ報告あり）。
> **FAIL の切り分け3分類**＝(a)**シナリオの腐り**（[DRIVE_TRAPS.md](./DRIVE_TRAPS.md) の 26）はその場で直す (b)**engine/parser のバグ**もその場で直す（§2.4） (c)**未実装**は §5.3 へ登録。

🏁**残0**（`V-228` は第353バッチ、`V-227` は第349バッチ、`V-209`〜`V-212` は第293バッチ、**`V-213` は第296バッチ**、**`V-214` は第310バッチ**、**`V-215` は第315バッチ**、**`V-216` は第316バッチ**、**`V-217` は第317バッチ**、**`V-218` は第318バッチ**、**`V-219`〜`V-221` は第320バッチ**、**`V-222` は第326バッチ**、**`V-223`・`V-224` は第327バッチ**で返済）。
> 🆕`V-215`＝`O-353` Part B の全カード名宣言（`node scripts/verifyBattleDrive.mjs v215DeclareAllCardsHit v215DeclareAllCardsMiss`）＝**一致／不一致の2シナリオとも PASS**。
> 🆕`V-216`＝`O-345` の名前指定つき無償グロウ（`node scripts/verifyBattleDrive.mjs v216FreeGrowNamedOnly`）＝**候補が指定2枚だけ・囮は出ない・エナ0枚のままグロウ成立**で PASS。
> 🆕`V-217`＝`O-344` の全領域色喪失（`node scripts/verifyBattleDrive.mjs v217LoseColorCondOn v217LoseColorCondOff`）＝**条件成立で「エナ不足」／チーム3体で支払える**の両方向 PASS。
> 🆕`V-218`＝`O-363`③ の強制対象の種別限定（`node scripts/verifyBattleDrive.mjs v218ForceTargetSigniSrc v218ForceTargetArtsSrc`）＝**シグニの効果では絞られ／アーツでは絞られない**の両方向 PASS。

> 🆕`V-219`＝`O-362`① パワー－2倍の解除（`node scripts/verifyBattleDrive.mjs v219PowerMinusExpires`）＝**同一ターンは P3000−1000×2＝P1000／ターン境界後は同じ−1000で P2000**（＝倍化が残らない）で PASS。
> 🆕`V-220`＝`O-362`② ライフバースト2回発動の**2種類**（`node scripts/verifyBattleDrive.mjs v220LifeBurstAllTurn v220LifeBurstNextOnly`）＝🔑**2本が互いの対照**＝全ターン版は手札 **0→2→4**（2回目も倍化）／「次に」版は **0→2→3**（2回目は素）で両方 PASS。
> 🆕`V-221`＝`O-362`③ ゾーン消去の期間（`node scripts/verifyBattleDrive.mjs v221RemoveZoneOppTurn`）＝**発動時 active＋予約／相手ターン中も配置不可・予約消費／次の境界で解除**で PASS。
> 🆕🔑`V-222`＝`O-366` ルリグの再アタック（`node scripts/verifyBattleDrive.mjs v222LrigReattackUp v222LrigReattackControl`）＝**アップ後に2回アタックできてライフが2枚クラッシュ／対照（`WX19-021` を抜くだけ）は2回目の「アタック」が提示されない**で両方 PASS。🔴**反転確認も実機で取った**＝旧の門（`lrig_has_attacked`）を戻すと `upped=true` なのに `attacks=1` で止まる。
> 🆕🔑`V-223`/`V-224`＝`O-367`「そのアタックの間」（`node scripts/verifyBattleDrive.mjs v223DoubleCrushOneAttackOnly v224CrashRedirectOneAttackOnly`）＝**1回目だけ【ダブルクラッシュ】（7→4）／1回目だけクラッシュ先がトラッシュ**で両方 PASS。🔑**対照は別シナリオではなく「同じターンの1回目と2回目」**（盤面も操作も同一＝1ビットの対照）。🔴**実機の反転確認を3方向**で取った。

🆕🔑**`V-227`（第349バッチ・2026-09-15）＝`O-391`(a) の任意ダウン**（`node scripts/verifyBattleDrive.mjs v227OptionalDownSkip v227OptionalDownTake`）＝**辞退すればダウンもエナチャージも起きない／実行すれば両方起きる**で**両方 PASS**。🔑**対照は「同じ盤面・同じ操作で選択だけ1ビット違う」**（罠3）。🔴**反転確認を実機で取った**＝修正を戻すと `energy=1 down=[true,…]`（強制ダウン＋帰結が走る）で FAIL する。⚠**採番に注意**＝`V-225`/`V-226` は既に使用済みだった（罠103 を新設）。

🆕🔑**`V-228`（第353バッチ・2026-09-15）＝`O-413` の対象宣言UI**（`node scripts/verifyBattleDrive.mjs v228DeclareTargetCondFalse v228DeclareTargetCondTrue`）＝**両方 PASS**。
カード＝`WX22-011`（【自】アタックフェイズ開始時、相手シグニ1体を**対象とし**、場に緑と白の＜美巧＞が**ある場合**、手札に戻す）／
相手の観測札＝`WXDi-P03-067`（ON_TARGETED《ターン1回》＝カードを1枚引く）。
🔑**対照は「盤面1文字」**（罠3）＝自分の場に＜美巧＞2体を置くかどうかだけ。⇒ **条件不成立** は guest 手札 **0→1**（宣言で `ON_TARGETED` が1回発火・帰結は起きずシグニは場に残る）／
**条件成立** は **0→2**（ドロー1＋手札に戻った1）で、**帰結側の候補は宣言した1体だけ**（`["WXDi-P03-067#39114"]`）＝`targetsStored` が効いている。
🔴**反転確認を実機で取った**＝live を `O-413` 前の形（`CONDITIONAL` の内側に対象）へ戻すと**宣言UIが出ず guest 手札は 0 のまま**＝`ON_TARGETED` が発火しない。

⚠**新しい観測点はここへ `V-<次番号>` で足す**（機構項目は §5.3 へ）。
🚀**リリースゲートの通し対戦スモークは道具になった**＝`node scripts/verifyFullMatch.mjs`（`cpu` / `pvp` で片方だけも可）。
   **`src/screens/` や engine の再入経路を触ってリリースする前に1回**回す（CPU 約4分／PvP 約33分）。


🏁**返済済みの全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md)「§5.1 実機返済の完了報告」と [BUGFIXES.md](./BUGFIXES.md)。**
🔑**`order` に入れたシナリオは「壊れたら気づく」ための番人**＝返済後も外さない（例＝`o267CutinResonaResolvesBeforeSpell` は「`effect_stack` を空にしてからスペルを解決する」ガードの唯一の番人）。

---

### 5.2 意味照合監査（semantic audit）★🏁**round5 完走＝全カード読み直し（598 / 598 バッチ・5,976枚）**

🏁**round4＝全11シート・効果あり6,032枚を監査完了**。findings は全数 triage 済み（確定433／偽陽性70 → §5.0）。
🏁**段2 台帳＝残 OPEN 0**（`node scripts/archive/semanticAuditLedger.mjs` で測り直す＝内訳を PLAN に書き写さない）。

🔥**round5（2026-09-15 ユーザー決定）＝効果テキストと effects JSON を持つ全 5,976 枚を読み直す**（JSON 未登録の56枚は対象外）。
置き場＝`scripts/archive/scratchpad/semantic_audit_round5/`（並び `card_order.txt`＝seed 42 固定／台帳 `TYPE_LEDGER.md`／判定 `triaged.txt`）。
**🏁済＝r5-001〜598（5,976枚＝100%・失敗0）**・findings 累計255件を全数 triage（内訳と型は `TYPE_LEDGER.md` が正）。
🔴**2026-09-15（第354バッチ）の学び＝「読み方ルールは engine を直した瞬間に stale になる」**＝`O-391(a)` を第349バッチで直したのに規則45 を直さなかったため、**その後の200バッチで同じ偽陽性を13件読まされた**。
⇒ **engine を直したら、その挙動を根拠にしている読み方ルールを必ず同じ巡で書き換える**（`grep -n "実測" scripts/semanticAuditExtract.mjs` で根拠つきの規則を洗い出せる）。

**続きの回し方**（codex-work＝Claude の月額枠を使わない。1バッチ 11〜29 秒）：
```
CODEX_HOME="C:/Users/zerom/.codex-work" node scripts/semanticAuditRunCodex.mjs \
  --out scripts/archive/scratchpad/semantic_audit_round5 --batches 22,23,...
# 読み方ルールを足したらプロンプトを作り直す（処理済みバッチは raw/ があるので再実行されない）
node scripts/semanticAuditExtract.mjs --out scripts/archive/scratchpad/semantic_audit_round5 \
  --cards-file scripts/archive/scratchpad/semantic_audit_round5/card_order.txt --batch-size 10
```
- 消化したら `audited_cards_cumulative.txt` へカード番号を追記し、`TYPE_LEDGER.md` に1行。
- 🔴**triage の出口は §2.6 の3つだけ**（BUG＝`O-nn` 登録／解釈待ち＝索引 H／FP＝engine の事実で説明できるものだけ）。
- ⚠**round1〜4 は監査員にコスト列を渡していなかった**（2026-09-15 に抽出を修正）＝過去の「支払いが余分」系の判定は、このことを知ったうえで読む。
- 🔴🔑**findings をそのまま直さない**＝偽陽性は「engine が JSON の見た目を裏で読み替えている」型（did-it ゲート／`OPPONENT_PAY_OPTIONAL` の既定の極性）。**引き当てたら engine の受け皿を必ず読んでから triage する。**
- 🔴**止め時は「1バッチあたりの新型数」＝連続3バッチで新型0**（§2.6）。

---

### 5.3 機構 worklist（新しい型・評価器・engine が要るもの）★**新機構の登録先**

> **二層構造**＝**索引（ここ）＝残数と一行の説明だけ／登録票の全文＝[PLAN_DETAIL.md](./PLAN_DETAIL.md)**。
> 🔴**ID を決めたら、着手前に PLAN_DETAIL の同じ ID の登録票を読む**（経緯・実測・受け皿の在処・罠・取り方がそこにある）。**索引だけで実装を始めない。**

**この節の使い方**
1. 索引 **A → B → G**（母集団の大きい順）に取る。**索引の並び順がそのまま取る順**（旧「取る順」表は廃止）。
   ⚠**欠番の C/D/F は閉じた枠＝文字も住所なので再利用しない**（`索引 C` は歴史側に約108箇所の参照があり、`goldenTest.ts`・`manualEffects.ts` のコメントも旧 C を指している）。
2. **②母集団を実測し直す**（§2.1 ②）。索引の数字は**登録時点の上限値**であって、いま正しいとは限らない。⚠**「受け皿が無い」は連続12項目外れている**＝**別名で grep し直す**（型のコメント・golden・`manualEffects.ts` まで見る）。
3. **②' レーンを決める**（§2.0）＝索引 G（1〜2効果）は速いレーンが既定、A/B は遅いレーン。

**登録のルール**
- **新規登録は「索引に1行（表）」＋「PLAN_DETAIL に登録票1項目」**。索引に長い経緯を書かない（それが旧構造の破綻原因）。
- 🔑**登録時に母集団を実測して書く。** ⚠`S`/`M`/`L` は**実装量の見立てであって母集団ではない**（この混同で「S・1効果」の項目に34枚・19枚・15枚の塊が隠れていた）。
- **消化したら索引の行ごと消す**（ID は再利用しない）。🔑**クローズは「実装が終わった」ではなく「索引から行を消し、§1／§6／この節の残数を直す」まで**（落とすと §1 の「次の一手」が閉じた項目を指し続ける）。
- **実機で確かめるだけの項目はここに書かない**＝§5.1 へ `V-<次番号>`（守らなかった結果、旧 `O-13` が8,185字に肥大した）。

> 🔴🔑**書式を守る（`census:cards` の `mech` フラグがこの節を機械で読む）**
> - **開いている項目は必ず `| \`O-nn\` |` で始まる表の行**（`cardProgressCensus.mjs:176`）＝箇条書きで足すと**計器から丸ごと消える**。🔴**ID セルの先頭に絵文字を置かない**（`| 🆕\`O-281\`` で実際に2項目が消えていた）。
> - **クローズ注記をこの節に残さない・カード番号を書かない**＝計器は §5.3 の本文からカード番号を拾う（`:196`）ので、**クローズ注記の引用カードまで「機構待ち」に化ける**（項目を1つ閉じたのに `mech` が 38→40 に増えた）。**経緯は BUGFIXES.md と登録票へ。**
> - **索引を全部消化したら見出しの下に `🏁**残0**` と書く**（「書式が壊れた」と「本当に空」を区別している）。
> - **編集したら `npm run census:cards` で差分を見る。**

**母集団の測り直し方**＝`node scripts/archive/censusMechPopulation.mjs [--id O-nn]`（原文 regex で拾う → live の受け皿 regex で絞る2段）。
🔴**この測り方には構造的な限界がある**＝「原文にフレーズがあるのに live にキーが無い」は**受け皿の"別名"を全部知らないかぎり必ず過大に出る**（`census:wiring` の最大セル24件が実際は1件だった例がある）。⇒ **索引と `census:wiring` は「着手前のふるい」＝"どれだけ残っているか"の答えには使わない。**

#### 🆕 1〜3枚の項目の取り方（型を足す前に読む）

1. 🔑**まず受け皿を疑う。** 探し方＝①原文の言い回しで `src/` を grep ②同じ言い回しで `scripts/goldenTest.ts` を grep（**実装済みなら golden が張ってあることが多い**）。
2. **アクション側は「型」ではなく `STUB` ハンドラ1本で書く**（`execStubPart*.ts` に923本の実績・1本平均17行）。`census:stubs` A群からも消える。
3. 🔴**条件側には STUB の道が無い**＝`COND_STUB` は `execUtils.ts:2413` が **`return true`＝無条件成立**。条件は**型＋`CONDITION_TYPES`＋`evalCondition`＋`checkActiveCondition`＋golden＋parser の6箇所**を必ず揃える（[LESSONS.md](./LESSONS.md) §4.2）。
4. **1枚のために機構を作らないと決めてよい**＝そのときは**無言 no-op にせず** id を `DEFERRED_*` へ改名し、理由を散文でこの節に書く。

#### 索引 A'. 実機が出した配線ギャップ

🏁**残0**。⚠**golden が緑のまま実機だけが出した穴**をここへ足す。

#### 索引 A. 母集団2桁（**ここから取る**・遅いレーン）

🔥**残2**（`O-398`／`O-450`／`O-413` は第350〜352バッチ、🏁`O-452` は第357バッチで消化済み）。

| ID | 規模 | 何が無いか（一行） |
|---|---|---|
| `O-453` | M・**34効果** | 🔴**自分の `TRASH` が空振りしたときの粗ゲート（`effectExecutor.ts:7348` の `return done()`）が、原文で独立している後続文まで丸ごと消す**＝「手札を１枚捨てる。**カードを３枚引く**。**（手札を捨てられなくてもカードを引ける）**」が手札0枚で1枚も引けない。実測34効果（うち**原文に「捨てられなくてもカードを引ける」と明記されたものが11効果**）。⚠粗ゲートが正しいのは後続が `CONDITIONAL{IS_MY_TURN}`（＝「そうした場合」）の形だけ＝**細ゲート（`:7371`）へ寄せて粗ゲートを畳む**のが筋。⚠`O-398` の細ゲート追加と同じ場所を触る |
| `O-399` | S・**12効果** | 付与の寿命が原文と違う＝`PERMANENT`（`WX05-014-E1`／`WX15-010-E1`）／`INSTANT`＝期間指定なし（`PR-204-E1`／`PR-257-E1`／`PR-258-E1`／`WXK10-054-BURST`）／「次の自分のターンまで」が当ターン終了で失効（`WXK08-075-E1`）／「次の対戦相手のターン終了時まで」が当ターン終了（`SPDi43-01-E2`／`WXDi-P16-044-E2`）／期間指定の欠落（`WX11-038-E2`／`WX13-029-E1`／`WX22-Re04-E2`）。**round5 r5-149/223/242/256×2/284＋332〜531 で +7** |

⚠**新しく母集団2桁の項目が出たらここへ足す。**

#### 索引 B. 母集団 3〜8効果

⚠**新しく母集団 3〜8効果の項目が出たらここへ足す。登録するときは、その場で受け皿を grep して実装済みでないことを確かめる。**

| ID | 規模 | 何が無いか（一行） |
|---|---|---|
| `O-391` | S・残**9効果** | 🏁**下位型(a)＝`optional` が消費されない＝2026-09-15 に修正済み**（`execDown` の `thisCardOnly` 早期 return が `a.optional` より手前にあった＝27ノード）。**残るのは下位型(b)＝did-it ゲートが構造的に届かない形**＝①`MILL` は任意性を `CHOOSE{置かない}`（`execMill:10126`）で表すので**空の SEQUENCE が後続を止めない**（`WXDi-P13-088-E1`／`WX24-P2-009-E1`）②**後続が `CONDITIONAL{IS_MY_TURN}` でない**＝`stripDidItConditional` が剥がせない（`WX24-P1-040-E1`＝次が `STUB`／`-E2`＝次が `GRANT_KEYWORD`。⚠`-E2` は**シャドウのレベル既定値が「無条件シャドウ」に落ちる**副次バグ付き＝`effectExecutor.ts:5687`）③`ADD_TO_FIELD{thisCardOnly}`→兄弟（`WD22-035-G-E1`）④`TRANSFER_TO_DECK` に `optional` が無い（`WX14-030-E1`）⑤`GROW_BY_EFFECT` 系（`SP38-001-E1`）。🔑**対象を選ばせる型（DOWN/BOUNCE/REVEAL/TRANSFER_TO_DECK…）は既に gate 済み**＝`selectOrInteract`→`resumeSelectTarget`→`stripDidItConditional`（`:12065`/`:12074`）（🆕第355バッチ +1＝`WXDi-P08-031-E1`＝`ADD_TO_FIELD{upToCount}` の空振りで後続 `BANISH` が素の兄弟＝`powerLtLastProcessed` の基準が消える） |

#### 索引 G. 母集団 1〜2効果（速いレーンが既定）

| ID | 規模 | 何が無いか（一行） |
|---|---|---|
| `O-451` | S・残**18効果** | 🔴**`O-413` の対象宣言引き上げが届かない形。**(a)**`then` と `else` で対象が違う**（原文も2回「対象とし」と書く）＝`WXK09-081-E1`／`WXK04-025-CB-E2`＝**枝ごとに宣言を作る機構**が要る。(b)🆕**中間動作の後ろでしか対象を選んでいない**（第354バッチ＝round5 r5-332〜531 で +8）＝`WXDi-D07-004-E3`（ピース除外の後）／`WXDi-P05-003-E1`（3回の支払いの後）／`WX18-001-E3`・`WXDi-P02-040-E2`（コスト支払いの後）／`WXDi-P05-043-E1`（スペル除外の後）／`WD22-016-UG-E1`（ルリグトラッシュ送りの後）／`WX05-024-BURST`（エナチャージの後）／`WX25-P2-090-E1`（自己をエナに置いた後＝**自分自身が候補に混じる**） 。🆕第355バッチ +3（`WXK03-029-E1`／`WXK09-090-E1`／`WXDi-P01-085-E1`） 。🆕第356バッチ +5（`WX22-048-E1`／`WX05-081-E1`／`WXEX1-49-E2`／`WXEX2-70-E1`／`WXDi-P06-007-E1`） |
| `O-376` | S・1効果 | 「あなたが**対戦相手の**スペルを使用したとき」の持ち主限定が無い＝`spellUseTriggerMatches` は使ったスペルの持ち主を見ず、自分のスペルでも発火する（round5 r5-012）。`triggerFilter` の新キー＋`src/screens/`＝**遅いレーン・実機必須** |
| `O-381` | S・要実測 | `SEARCH` の `afterSearch:SHUFFLE_DECK` が `then`（デッキ上の一番目／二番目へ置く）の**後**に走り、置いた位置が壊れる（round5 r5-033/257/324・**3効果**）。原文は「デッキをシャッフルし、**その後**置く」 |
| `O-382` | S・要実測 | `ENERGY_CHARGE_FROM_DECK` が `lastProcessedCards` を上書きするため、「N枚以上公開した場合」の**2段目以降の分岐が全部死ぬ**（round5 r5-053・`WX21-023-E1`＝3枚以上のエナ破棄と4枚のアサシンが不発）。engine 実測＝`effectExecutor.ts` の `execEnergyChargeFromDeck` が `lastProcessedCards: took` を返す （🆕第354バッチで +1＝`WXEX1-69-E1`＝2枚以上公開時のエナ→手札の移動で `lastProcessedCards` が上書きされ「3枚公開した場合」が死ぬ＝**計2効果**） |
| `O-383` | S・1効果 | 「このターン、〜からダメージを受けない」が `PREVENT_NEXT_DAMAGE{count:1}`＝**次の1回だけ**（round5 r5-073・`WX25-P3-051-E1`）。回数無制限の受け皿 `PREVENT_DAMAGE` に**発生源フィルタが無い** （🆕第354バッチで +1＝`WXDi-P03-077-BURST`＝**計2効果**） |
| `O-384` | S・1効果 | 「バニッシュされた**か**効果によって場からトラッシュに置かれたとき」が `timing:[ON_TRASH]`＋`byEffect:true` の1本に畳まれ、**バニッシュ経路で発火しない**（round5 r5-036・`WX19-029-E1`） |
| `O-385` | S・1効果 | 「場にあるこのシグニが**他のシグニゾーンに移動した**とき」の移動先限定が無く、自分の効果による別のゾーン移動でも発火する（round5 r5-049/177・`WXDi-P00-063-E2`／`WX11-002-E1`＝**2効果**） （🆕第354バッチで +2＝`WX24-P2-091-E1`／`WXDi-P05-071-E1`＝**計4効果**） |
| `O-386` | S・1効果 | 「対戦相手の**選んだ**シグニ1枚を場に出す」の `opponentSelects` が `ADD_TO_FIELD` に無く、使用者が選ぶ（round5 r5-048・`PR-242-E1`）。受け皿は `TRASH` 側に実在 （🆕第354バッチで +2＝`WXK11-006-E1-G2`（ライフに加えるカードの選択者）／`WX08-006-E3`（バニッシュするシグニを相手が選ぶ）＝**計3効果**） |
| `O-387` | S・1効果 | 「対戦相手の**センタールリグ**1体」の限定が無く `LRIG` 全般が対象＝アシストルリグを凍結できる（round5 r5-070・`WX17-020-E1` 選択肢③） |
| `O-388` | S・1効果 | 「このシグニの下に《X》が**無い場合**」の実行条件が `PLACE_CARD_UNDER_SIGNI` に無く、何個でも置ける（round5 r5-057・`WXDi-CP02-061-E2`） |
| `O-389` | S・1効果 | 選択肢の**発動可否**には別名条件があるのに、実際に捨てる手札の対象に別名制約が無い（round5 r5-037・`WX04-034-E1`）。受け皿 `selectionConstraint:{distinct:"name"}` は実在 |
| `O-390` | S・1効果 | 「【ダブルクラッシュ】**によって**2枚以上クラッシュされたとき」の発生原因の限定が無い（round5 r5-041・`WX16-Re07-E1`）。⚠**着手前に他経路（【トリプルクラッシュ】等）で2枚以上同時クラッシュが起きるかを実測する**。起きるなら実挙動差 |
| `O-392` | S・1効果 | 「それをトラッシュから場に出す」の「それ」が直前にミルしたカードへ束縛されず、**トラッシュの別のシグニも選べる**（round5 r5-098/145/178・`WXK03-040-E1`／`WXK10-009-E1`／`WXK03-079-E1`＝**3効果**）。受け皿 `targetsLastProcessed` は実在 （🆕第356バッチ +1＝`PR-K072-E1`＝`REMOVE_ABILITIES` が直前に場に出したカードへ束縛されない＝**計4効果**） |
| `O-393` | S・1効果 | `STUB{OPTIONAL_COST}` に**コストが1つも入っていない**＝原文「手札から青のカードを２枚捨ててもよい」がタダになり【アサシン】が無償で付く（round5 r5-101・`SPDi43-30-E1` 選択肢②） |
| `O-394` | S・1効果 | 「**バトルによって**凍結状態のシグニをバニッシュしたとき」の発生原因の限定が無い＝効果バニッシュでも発火（round5 r5-136/267＝**2効果**・`triggerCondition` は `banishedFilter` だけ） |
| `O-395` | S・1効果 | 「シグニが場から手札に戻っていた場合」に `owner:"self"` の**余計な限定**が付き、相手のシグニが戻った場合に成立しない（round5 r5-138/185/271・**3効果**） |
| `O-396` | S・1効果 | 「アーツの効果を受けない」に `sourceOwner:"opponent"` の**余計な限定**が付き、自分のアーツの効果は通る（round5 r5-104・`WX04-064-E1`） |
| `O-397` | S・1効果 | 「デッキの上から４枚**見る**」が `REVEAL_AND_PICK{revealCount:4}`＝**4枚とも公開**になっている（原文は選んだ1枚だけ公開＝非公開情報の差・round5 r5-124/197/268＝**3効果**） （🆕第354バッチで +1＝`WX12-049-E1`＝**計4効果**） |
| `O-400` | S・1効果 | 「対戦相手のライフクロスが**０枚になったとき**」が `timing:["ON_OPP_LIFE_CRASHED"]`＝**クラッシュのたびに発火**し、0枚前の空振りで《ゲーム１回》を使い切る（round5 r5-155・`WX25-P2-009-E1`） |
| `O-401` | S・1効果 | 「他の＜水獣＞が対戦相手の効果によって**場を離れる場合**」の置換が `BANISH_SUBSTITUTE`＝**バニッシュだけ**に限定され、かつ `excludeSelf` が無く**自身も対象**になる（round5 r5-196・`WX06-019-E1`） |
| `O-402` | S・1効果 | 追加エクシード時の「**代わりに**上限2枚」が**置換ではなく加算**になり、合計10枚見て最大3枚回収できる（round5 r5-165・`WXDi-P03-054-E1`） |
| `O-403` | S・1効果 | 「ライフクロスがクラッシュされたとき追加で－2000」が**任意コストを支払った分岐の内側**にあり、支払わないと付与されない（round5 r5-158・`WX25-CP1-065-E1`） |
| `O-404` | S・1効果 | 「レイヤーアイコンの**総数**」を数えるべきところが「レイヤーアイコンを**持つシグニの枚数**」になっている（round5 r5-168・`WXEX1-59-E1`） |
| `O-405` | S・1効果 | 「対象としたカードの順番を**無作為化して**デッキの一番下に置く」の無作為化が無い（`shuffle:false` のまま・round5 r5-169・`WXK09-067-E1`） |
| `O-406` | S・1効果 | 先頭の `STUB{OPTIONAL_ACTIVATE}` が**後続全体を任意化**するため、原文で**必ず行う**1枚ドローまで辞退できる（round5 r5-153/287・`WX19-059-BURST`／`WXK03-023-E1`＝**2効果**）。⚠`O-391` の**逆向き** |
| `O-407` | S・1効果 | 「シグニを対象とし」に `owner:"self"` の**余計な限定**が付き、相手のシグニを対象にできない（round5 r5-195・`WX11-030-E2`）。⚠`O-395`/`O-396` と同型の「多い」側 |
| `O-408` | S・1効果 | 「**シグニゾーン以外のあなたの領域**にあるカードは対戦相手の効果でトラッシュとデッキに移動しない」の `zoneMoveImmunity.zones` が `[hand,energy,deck,trash,life]` だけ＝**ルリグの下／ルリグゾーン／チェックゾーン／シグニの下**が抜けている（round5 r5-036・`WXK03-011-E1b`）。⚠**2026-09-15 に FP と誤判定していたものを差し戻した**（原文を読まずに「シグニが在れない領域」と決めつけた） |
| `O-409` | S・1効果 | 「（デッキの一番下に）**置く順番は対戦相手が決める**」の指定が無い＝相手の次のドロー順を使用者が決めてしまう（round5 r5-096/307・`WXDi-CP02-036-E1`／`WDK09-013-E2`＝**2効果**）。⚠同じく FP からの差し戻し |
| `O-410` | S・1効果 | 選択肢②の「《無》×5を**支払い**」（＝その枝を選んだら必須）が `STUB{OPTIONAL_COST}`＝**選んでから辞退できる**（round5 r5-120・`WXDi-P03-004-E1`）。⚠FP からの差し戻し |
| `O-411` | S・1効果 | 原文は「あなたのセンタールリグは以下の能力を**得る**」だが、【自】が**キー自身の能力として**定義されている＝ルリグの能力喪失との相互作用が原文と違う（round5 r5-074/266・`WXK06-005-E3`／`WXK01-028-E3`＝**2効果**）。⚠FP からの差し戻し |
| `O-412` | S・1効果 | グロウコスト軽減の条件が「**この方法で公開した**シグニ」ではなく**手札全体**を数える（`HAND_COUNT_FILTER`）＝公開しなくても軽減される（round5 r5-044・`WD13-002-E1`）。⚠FP からの差し戻し |
| `O-414` | S・母集団要実測 | 「代わりに〈コスト〉を支払って**もよい**」のダメージ置換（`DAMAGE_REPLACE_BY_COST`）が**自動で支払う**＝被害側が選べない（消費地点 `crashOneLife`／ルリグアタック応答に対話窓が無い）。⚠**2026-09-15 まで LESSONS §4.8 で「近似」として放置していた**＝差し戻し |
| `O-415` | S・母集団要実測 | 「**その効果によってトラッシュに置かれたカードの中から**」の対象が `lastProcessedCards[0]` → 無ければ `trash.at(-1)`＝**1回の解決で複数枚置かれると「最後の1枚」に丸まる**（`TRASHED_CARD_TO_HAND_OR_ENERGY`）。⚠§4.8 からの差し戻し |
| `O-416` | S・3効果 | 原文「対戦相手のシグニの**【自】**能力」の支払いゲートが**【出】も巻き込む**＝`CardEffect` に【出】と【自】を分ける情報が無い（どちらも `effectType:'AUTO'`）。⚠§4.8 からの差し戻し（`BLOCK_OPP_SIGNI_AUTO` 系3枚） |
| `O-417` | S・2効果 | 「そうでない場合、**その**カードをデッキの一番下に置く」等の **else 節が丸ごと据置**＝行き先を枝ごとに変える受け皿（`remainder` の枝別指定）が無い（`WXDi-P09-068-E1`／`WXK03-050-E1`）。⚠§4.8 からの差し戻し |
| `O-418` | S・2効果 | 原文が**強制**の手札破棄（「手札から〜を１枚捨てる。そうした場合、」）を `OPTIONAL_COST` で表しており、**払える盤面で辞退できる**（`WX21-004-E3`／`WX11-006-E3`）。根治は `TRASH` を `DID_IT_GATED_TYPES`（`effectExecutor.ts:6163`）へ足すこと＝既存の「そうした場合」全体に影響するので母集団を測ってから。⚠§4.8 からの差し戻し （🆕第354バッチで +3＝`WX08-022-E1`／`WD02-007-E1`（原文は解決時の効果なのに使用コスト化）／`WX21-Re18-E1`（強制の追加コストが `OPTIONAL_COST`）＝**計5効果**） |
| `O-419` | S・1効果 | 「**好きな順番で**デッキの一番**上**に置く」が `TRANSFER_TO_DECK{position:"top"}` で**選択順が逆になる**（各札を先頭へ prepend するため。bottom は append なので正しい・`WXK01-102-E1`）。⚠**2026-09-15 まで LESSONS §4.8 に「潜在」として置いていたが、実測で live 1効果が該当していた**＝差し戻し |
| `O-420` | S・1効果 | 盤面条件が**任意コストだけ**を囲み、対の本体が `CONDITIONAL` **ではない**（`CHOOSE`）ため、engine の読み飛ばし（`effectExecutor.ts:6269` `OPT_IDS_WRAP` は次の `CONDITIONAL{IS_MY_TURN/PAID_ADDITIONAL_COST}` しか探さない）が効かず、**条件不成立でも本体が走る**（round5 r5-259・`WXDi-CP02-080-E1`） |
| `O-421` | S・2効果 | `ON_TARGETED` に**「対戦相手の」能力・効果に限定する条件が無い**＝自分の効果で自分のシグニを対象にしても発火する（round5 r5-206/224・`WX24-P1-045-E1`／`WXDi-P02-043-E2`） （🆕第354バッチで +1＝`WXDi-P13-089-E2`＝**計3効果**） |
| `O-422` | S・1効果 | 【絆】＝「好きな生徒1人との絆を獲得して宣言したカード名の【絆】能力を有効にする」処理が JSON に無い（round5 r5-218・`WX25-CP1-035-E1`） |
| `O-423` | S・1効果 | 「ルリグデッキにある間の移動を防ぐ」**常在**能力が独立した `CONTINUOUS` ではなく `ACTIVATED` の内側に置かれ、移動防止の判定対象にならない（round5 r5-219・`WX25-P1-TK1-E1`） |
| `O-424` | S・1効果 | 「指定された4種の能力**だけ**を失わせ取得も禁止」が、STUB で**相手シグニの能力すべてを消去**＝適用範囲が過剰（round5 r5-219・`SP27-016-E1`） |
| `O-425` | S・1効果 | リフレッシュ判定が「設置後の最初」だけを見ており、**使用前に同ターンの最初のリフレッシュを済ませていた場合にも誤ってライフクロスをクラッシュする**（round5 r5-243・`WX09-Re06-E1`） |
| `O-426` | S・1効果 | 「このターンにセンタールリグがグロウしていない」使用条件が無く、**グロウ済みでも使用できる**（round5 r5-252・`WXDi-P16-001A-E1`） |
| `O-427` | S・1効果 | 「公開したカードと**共通する色のルリグが自分の場にいる場合だけ**下に置く」の条件がどこにも無く無条件に置く（round5 r5-267・`WX25-P3-110-E1`） |
| `O-428` | S・1効果 | `gameGrants.handSizeBonus` と `HAND_SIZE_INCREASE` が**同じ上限増加を二重に表し合計＋4**になるおそれ（round5 r5-202・`WX25-P2-005-E1`） |
| `O-429` | S・1効果 | 戻す先が「デッキの一番上」でなく `position:"any"`（round5 r5-204・`WD06-007-E1`） |
| `O-430` | S・1効果 | 手札に加えられる対象が**黒のシグニだけ**に絞られ、原文が個別指定する3種を黒以外でも選べる条件が欠落（round5 r5-221・`WXDi-P10-077-E1`） |
| `O-431` | S・1効果 | `declareNamePool` が `self_deck` 限定＝**自分のデッキに無いシグニ名を宣言できない**（round5 r5-224・`WX18-028-E1`） |
| `O-432` | S・1効果 | 対象に「**このターンにアタックした**シグニ」の条件が無く、未アタックの相手シグニもバニッシュできる（round5 r5-229・`PR-K076-BURST`） |
| `O-433` | S・1効果 | 公開が「**自分の効果による**」ことの限定が無く、他の原因の手札公開でも誘発する（round5 r5-241・`WXK04-004-E3`） |
| `O-434` | S・1効果 | 宣言後に「**対戦相手の手札全体を見る**」処理が JSON に無い（round5 r5-242・`PR-257-sub-E3`） |
| `O-435` | S・1効果 | エナからトラッシュに置くコストに《ディソナアイコン》限定が無く、**任意のカードで払える**（round5 r5-261・`WXDi-P12-048-E2`） |
| `O-436` | S・1効果 | 「最初の1枚は**必ず**選ぶ」が2枚まで一括の任意選択になり**0枚も選べる**（round5 r5-275・`WXDi-P12-039-E1`） （🆕第354バッチで +1＝`WX25-P1-003-sub-E1`＝**計2効果**） |
| `O-437` | S・1効果 | 選択肢**全体の後**に行う手札1枚の破棄が選択肢④の内側にあり、④を選ばないと実行されない（round5 r5-275・`WX13-003-E1`） |
| `O-438` | S・1効果 | 自分のライフクロスのクラッシュに `optional` が無く、原文が任意のところが**強制**になっている（round5 r5-288・`WX24-P4-005-E1`） |
| `O-439` | S・1効果 | 【アクセ】にする対象から**元のシグニ自身を除外**する条件が無い（round5 r5-289・`WXEX2-19-E2`） |
| `O-440` | S・1効果 | 宣言できるカード名が**対戦相手の公開済みシグニ**に限定され、任意のシグニ名を宣言できない（round5 r5-289・`WXK03-002-E2`） |
| `O-441` | S・1効果 | 効果耐性の発生源が**アーツだけ**に限定され、原文が含む**キー**の効果への耐性が欠落（round5 r5-295・`WXEX1-37-E1`） |
| `O-442` | S・1効果 | 「このターン中に〜したとき」の**遅延トリガー**が設置されず、選択した瞬間に**即座に**パワー－7000が走る（round5 r5-307・`WD22-011-G-E1`） |
| `O-443` | S・1効果 | 原文「正面のシグニとの**バトルだけ**を回避（対戦相手へのダメージは与える）」が `STUB{NEGATE_ATTACK_ON_TRIGGER}`＝**アタック自体の無効化**で意味が違う（round5 r5-321・`WX25-P1-TK6-E1`） |
| `O-444` | S・1効果 | 「ライフクロス2枚以下の場合」の**条件の内外が逆**＝相手のシグニとエナのトラッシュが条件外で常時実行され、条件内には手札破棄だけがある（round5 r5-323・`WDK17-009-E2`） |
| `O-445` | S・1効果 | 使用コスト「**白か黒**で2エナ」が**無色2エナ**として表現され、色の制約が消えている（round5 r5-330・`WXK10-002-E1`） （🆕第354バッチで +3＝`WXK10-001-E1`《白/赤》×6／`WXK10-003-E1`《赤/青》×3／`WXK10-004-E1`《青/緑》×5＝**計4効果**） （🆕第355バッチで さらに +2＝`PR-380-E1`《白/黒》×1／`WXK10-005-E1`《緑/黒》×4＝**計6効果**） |
| `O-446` | S・1効果 | 原文は**両プレイヤーの**アタックフェイズ開始時に発動するのに `triggerScope:"self"`＝**自分のターンにしか発動しない**（round5 r5-303・`WXDi-P04-049-E1`） |
| `O-447` | S・1効果 | 「【ウィルス】を取り除いてからパワーを－5000」の**処理順が逆**（パワー減少が先）＝ルールによるバニッシュを挟む場合に結果が変わる（round5 r5-307・`WX15-115-E1`） |
| `O-448` | S・1効果 | **対戦相手**が行うか選べるエナチャージが**辞退肢なしの強制**になっている（round5 r5-308・`WXDi-P06-011-E1`）。⚠`O-391` の**相手側**版 |
| `O-449` | S・1効果 | 「白のカード**と**赤・青・緑・黒のカード」が**別々の2枚**である要求が無く、**同じ多色カード1枚**が両方の条件を満たしうる（round5 r5-329・`WX24-P4-026-E1`） |

| `O-454` | S・1効果 | 「〜を合計**３回**行う。そうした場合」の**回数達成ゲートが無い**＝`CONDITIONAL{IS_MY_TURN}`（常時成立）で攻撃無効が走り、エナ＋手札が合計3枚未満でも適用される（round5 r5-453・`WXDi-P05-003-E1`） |
| `O-455` | S・1効果 | 「対戦相手の**すべての**ルリグ」の対象が `CENTER_LRIG_OR_SIGNI`＝**アシストルリグが外れる**（round5 r5-436・`WXDi-P16-005-E1`） （🆕第356バッチ +1＝`WX25-P3-002-E1`＝**計2効果**） |
| `O-456` | S・1効果 | 公開・選択・移動を担う STUB が**バニッシュ成否ゲートの外**にあり、バニッシュできなくても主要処理が走る（round5 r5-341・`WX16-Re04-E1`） |
| `O-457` | S・1効果 | 色判定は最初に対象としたシグニに対して行うのに、後続の `TRASH` が `targetsStored` を参照せず**別のシグニを選べる**（round5 r5-346・`WX21-007-E1`） |
| `O-458` | S・1効果 | 「ガードされたアタックを行った**センタールリグ**をアップする」が常に**自分の**ルリグ固定＝相手のセンタールリグがアタックした場合に対象が違う（round5 r5-443・`WXK11-006-E4`） |
| `O-459` | S・1効果 | 選択肢①の対象に**【チーム】を持つ**条件が無く、トラッシュの任意のシグニを場に出せる（round5 r5-446・`WXDi-P16-094-E1`） |
| `O-460` | S・2効果 | 付与の**対象範囲が過剰**＝このシグニだけのはずの耐性が**自分の全シグニ**に掛かる（`WXEX1-35-E1`）／付与能力内の `GRANT_PROTECTION{count:"ALL"}`（`WXK10-080-E2`）＝round5 r5-349/498 |
| `O-461` | S・1効果 | 逆向き＝**全体付与が `count:1`**＝選択肢①「あなたのすべてのシグニ」を1体しか守れない（round5 r5-350・`WX13-029-E1`） |
| `O-462` | S・1効果 | 「このシグニの下から**好きな枚数**」が**最大9枚**に固定（round5 r5-447・`WX17-026-E2`） |
| `O-463` | S・1効果 | 「**この方法でトラッシュに置かれたカード**の中から」の限定が無く、デッキのカードを手札に加える指定になっている（round5 r5-352・`WX24-P2-058-E1`） |
| `O-464` | S・1効果 | 「公開したデッキの一番上のカード**自身**を引く」が `REVEAL_AND_PICK` で取り除いた後の**別カードの `DRAW`**になっている（round5 r5-356・`WX25-P1-082-E1`） |
| `O-465` | S・1効果 | 非ライフバースト時に能力を与える**自分のルリグ1体の対象指定そのものが無い**（round5 r5-359・`WX24-P3-069-E1`） |
| `O-466` | S・2効果 | 「シグニゾーンにある**カード**」の対象が**シグニ限定**＝シグニ以外の表向きのカード（【アクセ】【チャーム】等）を処理できない（round5 r5-362/484・`WDK13-001-E3`／`WXK06-030-E1`） |
| `O-467` | S・1効果 | 「レベルを＋１するか－１して**もよい**」の**「しない」肢が無く**必ずどちらかを選ぶ（round5 r5-459・`WX16-067-E2`） |
| `O-468` | S・1効果 | カード名条件が**部分一致**＝《防衛者Ｄｒ．タマゴ》の指定が「タマゴ」を含む任意のルリグで成立（round5 r5-365・`WXDi-P15-058-E2`）。⚠`O-380` と同族だが**こちらは別名を含む方向** |
| `O-469` | S・1効果 | 【アンチェイン】の条件・処理が JSON のどこにも無い（round5 r5-465・`WX25-P2-018-E2`） |
| `O-470` | S・1効果 | 場に戻すトリガー元から**《羅星　アルファード》を除外する**条件が無い（round5 r5-471・`WX07-033-E2`） |
| `O-471` | S・1効果 | 発動条件は「場に出たシグニのパワー」を見るのに、`BANISH` の対象が**トリガー元に固定されていない**（round5 r5-482・`WX14-025-E1`） |
| `O-472` | S・1効果 | 「ルリグデッキが3枚以上なら**必ず3枚**選び公開する」が `upToCount:true`＝**0枚も選べる**（round5 r5-376・`WX15-001-E3`） |
| `O-473` | S・2効果 | 「手札を２枚捨てない**かぎり**」の本体（相手の手札2枚破棄／対象をデッキの一番下）が**対象を取る STUB しかない**（round5 r5-377・`WXDi-P01-028-E1`／`-E2`） |
| `O-474` | S・1効果 | 【ゲート】の存在が**配置先の制限**ではなく**効果全体の発動条件**になっており、無いと5枚を見る処理ごと走らない（round5 r5-461・`WXDi-P15-079-E1`） |
| `O-475` | S・1効果 | 「《無》×5を**支払わないかぎり**」の極性が逆＝`PAID_ADDITIONAL_COST` 成立時にダメージを与える（round5 r5-464・`WX24-P3-050-E1`） |
| `O-476` | S・1効果 | レベル1なら先に `DRAW` が走るため、後続の `LAST_PROCESSED_MATCHES` が**公開したカードではなく引いたカード**を判定する（round5 r5-465・`WXDi-P04-008-E1`）。⚠`O-382` と同族（`lastProcessedCards` の上書き） |
| `O-477` | S・1効果 | 「手札以外の領域から」の発動元一覧に**チェックゾーンが無い**（round5 r5-466・`WX14-024-E2`） |
| `O-478` | S・1効果 | ターン終了時にライフクロスを「**トラッシュに置く**」が「**クラッシュする**」になっている（＝ライフバーストが誘発する差）（round5 r5-468・`WD06-008-E1`） |
| `O-479` | S・1効果 | ドロー元を「**あなたの場にある**＜凶蟲＞のシグニ」に限定する条件が無い（round5 r5-474・`WX20-026-E3`） |
| `O-480` | S・1効果 | 「**共通するクラスを持つ**シグニが5種類以上」が `distinctClasses`＝「異なるクラスが5種類以上」になっている（round5 r5-475・`WXK10-047-E1`） |
| `O-481` | S・1効果 | 「3以外の数字1つ」の宣言が**1・2・4・5に固定**＝0や6以上を宣言できない（round5 r5-476・`WX17-039-E1`） |
| `O-482` | S・1効果 | 「そうした場合、このメインフェイズを終了する」が**条件・エナチャージの外**にあり、条件不成立でも終了できる（round5 r5-477・`WXK06-078-E1`） |
| `O-483` | S・1効果 | ライフクラッシュ・カウンターの**発動源が「対戦相手のルリグ」に限定されていない**／**ブースト時の追加カウンターが丸ごと欠落**（round5 r5-481・`WX25-P1-004-E1`＝2 findings） |
| `O-484` | S・2効果 | 「その中から**1枚**をトラッシュに置き」が `canTrash:true` だけで**枚数の限定が無い**（round5 r5-483・`WXK07-055-CB-E1`／`-E2`） |
| `O-485` | S・1効果 | 領域移動耐性の対象から**ルリグデッキ・ルリグトラッシュが欠落**＝「場以外のあなたの領域」を守れない（round5 r5-485・`WXK10-004-E1`） |
| `O-486` | S・1効果 | 「そのスペルを使用して**もよい**」が `PLAY_FREE_FROM_TRASH{mandatory:true}`＝**強制使用**（round5 r5-487・`WX09-012-E2`） |
| `O-487` | S・1効果 | 【英知】が参照するレベルを1・2・3として扱う常在が `STUB{ATTACK_PHASE_LEVEL_OVERRIDE}`＝**複数レベル扱いの payload が無い**（round5 r5-490・`WX20-044-CB-E1`） |
| `O-488` | S・1効果 | 能力を得るのは**このルリグ**なのに付与先が `SIGNI`（round5 r5-491・`WXDi-P16-039-E2`） （🆕第356バッチ +1＝`WXDi-P06-007-E3`＝`GRANT_EFFECT` の付与先＝**計2効果**） |
| `O-489` | S・1効果 | 「このクラッシュで**ライフバーストは発動しない**」の抑制 STUB が `LIFE_CRASH{triggerBurst:true}` の**後**にあり、バーストが先に発動する（round5 r5-492・`WXK07-006-E4`） |
| `O-490` | S・1効果 | 「その**バトル終了時**に」の遅延が無く、バトルしたときの解決中に**即座**にデッキ下へ置く（round5 r5-495・`PR-305-E1`） |
| `O-491` | S・1効果 | 「センタールリグが＜エルドラ＞の**場合**」の外にライフ追加がある（条件不成立でも走る）／同カードの `LIFE_CRASH` に `optional` が無く**強制クラッシュ**（round5 r5-499・`WX12-014-E1`＝2 findings。⚠後者は `O-438` と同型） |
| `O-492` | S・1効果 | 「**すべての領域で**このカードを＜プリオケ＞として扱う」常在が無く、選択肢②内の `RULE_REMINDER_TEXT` だけになっている（round5 r5-424・`WX26-CP1-100`） |
| `O-493` | S・1効果 | 「アタックしたシグニ」の所有者を限定していない原文に対し、対象を**対戦相手のシグニだけ**に絞っている（round5 r5-503・`WDK06-R09-E1`） |
| `O-494` | S・1効果 | 「それのパワーを＋4000」の「それ」がトラッシュから場に出したシグニなのに、`targetsTriggerSource` が**【出】能力の発生源（このカード自身）**を指す（round5 r5-504・`WXDi-P07-042-E3`） |
| `O-495` | S・1効果 | `OPTIONAL_COST` を辞退すると**後続ステップ全体がスキップ**される（`effectExecutor.ts:7094` Pattern ⑤）＝支払わなくても行うべきミル2枚と通常の回収まで消える（round5 r5-505・`WXDi-CP01-003-E1`）。⚠第345バッチで `WX25-P3-001-E1` を同じ理由で FP にしたが、**§2.6 ③の新基準（挙動差があるなら載せる）で BUG へ差し戻し**＝**計2効果** |

| `O-496` | S・1効果 | ルリグデッキからトラッシュに置くコストに「**クラフトではない**アーツ」の限定が無い（round5 r5-534・`WXK10-006-E3`） |
| `O-497` | S・1効果 | 「それを手札に戻す」の対象が**トラッシュに置いた【トラップ】持ちのカード**ではなく**正面の対戦相手のシグニ**になっている（round5 r5-536・`WX16-028-E1`） |
| `O-498` | S・1効果 | 「公開したシグニを**ダウン状態で**場に出し、その**【出】能力を発動させない**」後続処理が JSON に無い（round5 r5-542・`WX25-P1-053-E1`） |
| `O-499` | S・1効果 | 「その（アタックした＜遊具＞の）シグニをアップする」がトリガー元に固定されず、**自分のシグニ1体を選べる**（round5 r5-553・`WXEX2-18-E1`） |

| `O-500` | S・1効果 | 「ルリグが3体いなくても使用できる」の**使用条件の緩和**が payload を持たない `RULE_REMINDER_TEXT` だけになっている（round5 r5-562・`WXDi-P16-TK01-E1`） |
| `O-501` | S・1効果 | 「このカードの上にある〈指定〉のシグニのパワーを＋2000」の**常在**修正が、使用時に1回適用してターン終了で消える `POWER_MODIFY` になっている（round5 r5-589・`WXDi-P05-060-E1`） |
| `O-502` | S・1効果 | 「シグニをデッキの一番下に置く → **追加で**ライフクロス1枚」の**実行順が逆**（round5 r5-566・`SPDi47-03-E2`） |
| `O-503` | S・2効果 | 「**対戦相手の**シグニ1体が凍結状態になったとき」の持ち主限定が無く、**自分のシグニの凍結でも発火**する（round5 r5-595/572・`WXEX2-02-E2`／`WX08-039-E1`） |
| `O-504` | S・1効果 | 「このシグニを**エナゾーンからトラッシュに置く**」代替コストが `banish_self`＝**場からのバニッシュ**になっている（round5 r5-584・`WX21-044-E1`） |
| `O-505` | S・1効果 | 「**手札から**場に出た場合」の移動元条件が無く、＜遊具＞の効果で出た条件しか見ていない（round5 r5-584・`WX21-044-E2`） |
| `O-506` | S・1効果 | 「**アタックフェイズの間**にエナチャージした場合」のフェイズ条件が無く、他フェイズのエナチャージでも発動する（round5 r5-585・`WXK11-039-E2`） |
| `O-507` | S・1効果 | 「**手札とエナゾーンから合計3枚まで**選ぶ」処理が無く、`PLACE_CARD_UNDER_SIGNI` が処理済みカードを下に置く指定しか持たない（round5 r5-586・`WX24-P4-046-E1`） |

| `O-508` | S・**7効果** | 🔴**原文の「〜の場合」条件が JSON のどこにも無い**（`CONDITIONAL` にも `triggerCondition` にも）＝**無条件に発動する**。`O-452`（対象宣言の位置）の実測中に分離した別バグ。`WXDi-P00-033-E1`（このシグニのパワーが20000以上）／`WXDi-P11-060-E2`（左右にダウン状態の＜アーム＞）／`WXDi-P15-057-E2`・`WXDi-P15-058-E2`（【ゲート】／《防衛者Ｄｒ．タマゴ》）／`WX24-P2-054-E2`（《参式　一衣》）／`WX25-P3-061-E1`（《ウリス》＋この能力での場出し回数）／`SPDi43-17-E2`（このターンにスペルを使用していた）。⚠**受け皿の有無を1件ずつ実測してから取る**（条件型が既存かどうかで速い／遅いレーンが変わる） |

⚠**新しく母集団 1〜2効果の項目が出たらここへ足す**（速いレーンが既定＝§2.0・登録票の全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md)）。

🔴**着手の1手目は登録票の grep をやり直す**（§2.1 ②）。自分で書いた登録票も実測でやり直す。

#### 索引 H. ルール解釈待ち（**コーパスで決着しなかったものだけ**・Claude は取らない）

🆕🔴**2026-09-15 ユーザー決定で新設 → 同日に定義を狭めた。**＝監査の finding のうち判定がゲームルールの解釈に依存するものを登録する節。
🔑**登録の前に必ず原文コーパスを裁定器として試す**（2026-09-15 ユーザー指摘＝「逆翻訳ではなく原文を読むことで判断できないか」）。
**方法**＝`docs/_effect_srctext.json`（効果単位の原文10,768件）で**同じ言い回しを使う全カードを集め、片方の読みだと矛盾する実例（minimal pair）を探す**。
初回の適用で**登録3件が3件とも決着した**（`O-377` は BUG／`O-378`・`O-379` は FP）＝**ここへ登録してよいのは「minimal pair が見つからなかった」ものだけ**。
**ユーザーの判断が出たら**＝BUG なら索引 G/B/A へ移し（母集団を実測し直す）、FP なら行を消して `semanticAuditExtract.mjs` の読み方ルールへ還元する。

🔑**書き方**＝表の行（`census:cards` が読む）の下に、**項目ごとに「カード・原文・いまの実装の読み・読みA/B・コーパスで試した minimal pair とその結果」を必ず書く**。

| ID | 規模 | 何を判断するか（一行） |
|---|---|---|

🏁**残0**（`O-377`／`O-378`／`O-379` は 2026-09-15 にコーパス実測で決着＝経緯の全文は [BUGFIXES.md](./BUGFIXES.md)）。

#### 索引 E. 計器の較正・掃除（**機構ではない**＝カードの挙動は変わらない）

🏁**残0**。

⚠**新しく「計器が嘘をつく」形を見つけたらここへ足す。** ⚠**挙動を直したい回にここから取らない**（索引 E の性質）。
⚠**消化しても実機の挙動は1件も変わらない**＝効くのは「**計器が嘘をつかなくなる**」ことだけ。**計器主導でバッチを選ぶ前**に取ると後続が楽になる（逆に、挙動を直したい回にここから取ってはいけない）。

#### 個別カードの機構待ち・監視項目（**worklist ではない**＝着手前の参照用）

> 🔴🔑**ここに実作業を置かない。** 残ってよいのは (a) 母集団を実測したうえでの根拠つき defer と (b) 監視だけの既知の近似。
> **stale が判明した記述はその場で消すか、索引へ `O-nn` で出す。**

**■ 根拠つき defer（着手前にこの理由が今も有効か再判定する）**

- **CPU 盤面評価 v2＝着手不要**＝カードの正しさの穴ではない（v1＝`src/screens/battle/cpuBoardEval.ts`）。
- **`doPhaseAdvance` の pure 抽出＝やらない**＝抽出しないと動かないものが無い。必要になった部分だけ切り出す。
- **BEHAVIOR_AUDIT キューの消化＝⛔休眠（2026-09-15 に再検討して据え置き）**＝高シグナル **80 / 682**（真 no-op 0）。
  **測り直す＝`npx tsx scripts/behaviorAudit.ts --queue > docs/_behavior_queue.txt`（Bash で）→ `node scripts/_bqTriage.mjs`**。
  **再開の前提**＝①`_bqTriage.mjs` の動詞・条件語を効果単位で判定する ②シナリオビルダーが対象条件を満たすトークンを置く。
  🔑**2026-09-15 の検討結果（据え置きの根拠）**＝①を試作すると高シグナルは **80 → 108 に増える**（カード全文の別効果の条件語が候補を隠していた）。
  内訳＝**AUTO 75**（監査はトリガー文脈＝アタック者・バトル相手・バニッシュされた札を作らない）／**非トリガー 33**（コスト未払いの「この方法で」約10・
  `staticPart` が `hasIcon` を捨てるのでアイコン／レゾナ対象が置かれない約10・トラッシュ枚数依存の上限4・エナ発動源）。
  代表3件（`WX10-066-E1`／`WX18-030-BURST`／`WX06-018-BURST`）を**条件を満たす盤面で engine に直接かけると3件とも正しく動いた**＝シナリオの空振り。
  ⇒ 再開しても**バグ収量はほぼ0の見込み**（続き133 の22件→0件と整合）。発見器としては §5.2 round5 が優先。

**■ 監視だけしている項目（着手不要・壊れたら気付く）**

🔑**[LESSONS.md](./LESSONS.md) §4.8 にある**＝挙動が正しいと確認済みの近似・表示だけの不足・
恒久的な偽陽性の一覧＝**worklist ではなく「触るときに読む知識」**。⚠**ここへ書き戻さない**
（`cardProgressCensus.mjs` は §5.3 の本文からカード番号を拾うので、正しく動いているカードが `mech` に化ける）。

**■ 測り直す計器（異常が出たら索引へ出す）**

- **同型★**＝`node scripts/groupSimilar.mjs --all`（いま **0**）。★は同じ文型の片方にだけ修正が届いた兆候＝母集団を測って直すか索引へ出す。
- **BURST丸ごと欠落**＝**0**（CSV の BurstText あり × live に `LIFE_BURST` 無し）。
- **保護系キーワードの owner 誤り**＝測るコマンドが無い（測るなら計器を先に作る）。

### 5.4 原文照合テール（構造混線）★🏁**残0＝新規に取る節ではない**

🏁**残0**。**経緯の全文は [BUGFIXES.md](./BUGFIXES.md)。**

⇒ **新しく「原文と JSON の構造がズレた効果」を見つけたときだけここへ足す。**
🔑**足すときの既定＝着手前に必ず現在の逆翻訳を読み直す**（登録時点の逆翻訳で立てた項目は腐る）。
**測り直し方**＝`parseCardEffects` → `mergeManualEffects` を回して `parseStatus==='PARTIAL'` と `"type":"UNKNOWN"` を数える使い捨て（`tmp_*`）で十数秒。live 側は `public/data/effects_*.json` を直接読む＝fresh と live の差が「配送が止まっている件数」。
⚠**完了判定について**＝**census が0になっても「すべてのカードが完璧」にはならない**（死角4つ＝参照解決の誤り／効果単位の粒度／JSON は正しいが engine 実装が違う／文間の実行順序）。**最終的な判定器は BEHAVIOR_AUDIT と §5.1 実機検証。**

### 5.5 （廃止）低優先 → 🏁**2026-09-12 に全項目を §5.3／§5.1／LESSONS へ移した**

> 🔴**この節は無くした。** 「低優先」という置き場があると、**測り直すコマンドの無い数字が溜まる**＝
> 実績で ①系統別内訳が**2か月古いファイルの数字**のまま3〜10倍に膨らんでいた
> ②「CPU AI は (a)〜(f) 消化済み」が実際は **(a)〜(g) 全消化でクローズ済み**だった
> ③「323箇所」という**測り直しても減ったか分からない数字**を worklist として持っていた。
> ⇒ **取る順は §5.3 索引の並び（母集団順）だけで表す。優先度を節の名前で表さない。**
> **節番号は住所なので消さない**（他ファイルからの参照が残っている）。

**移設先**（全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md)「2026-09-12 登録：`O-348`〜`O-351`」）

| 旧 §5.5 の項目 | 移設先 |
|---|---|
| payload キー未描画 56種/145ノード | **§5.3 索引A `O-348`**。🏁**型つき22種/58ノードは 2026-09-13 第303/304バッチで全数クローズ**（実バグ2件）。🔥**残 34種/87ノードは全部 `[STUB]` 側** |
| `[STUB:…]` マーカー 323箇所 | 🏁**`O-351` クローズ（2026-09-13 第300）＝`npm run census:stublabel` を新設**。🔴**件数ではなく「検出器が無い」問題に置き換えたのが正解だった**＝316箇所のうち検出できたのは **A11/B17/C26**（A群の実測精度＝実バグ62%）。残りのラベル整備は **§5.3 索引E `O-354`** |
| 明示 defer（`DEFERRED_*`＝26種/28箇所。残1効果） | **§5.3 索引G `O-349`** |
| リリース判定（fuzz 重め） | 🏁**CRASH 0**（2026-09-12＝`O-350` で再帰2種を修正。seed 12648430/1/7/99/4242/777777 の6本で 0） |
| リリース判定（実機 PvP/CPU 通し対戦） | 🏁**両方 PASS**（2026-09-13＝`node scripts/verifyFullMatch.mjs`。CPU 8ターン／PvP 56ターンで決着） |
| CPU 盤面評価 v2 ／ `doPhaseAdvance` pure 抽出 ／ BEHAVIOR_AUDIT キュー | **§5.3 末尾「根拠つき defer」**（3件とも着手不要と実測で判定） |
| 計器の空振り一覧（`census:timing`／`census:wiring` ほか） | **[LESSONS.md](./LESSONS.md) §4.8**（在庫ではない＝着手前に読む知識） |

## 6. 恒久指標（最新1ブロックのみ）

> **運用**＝この節は**「いまの数字」だけ**を置く。新しく作業したら ①上のブロックを [PLAN_DETAIL.md](./PLAN_DETAIL.md) の恒久指標アーカイブへ移す ②今回の値へ書き換える。⚠**溜め始めたら破綻する**（過去に計測行15本＋ポインタ37本まで膨れ、cold start が最初に読む節が一番古い状態になった）。
> 🆕🔴**2026-09-01 改定＝3計器だけでは進捗が表示できなくなったので「在庫2本」を併記する**（理由は §3 の同日改定）。**3計器は底を打った＝これ以上は下がらないので、動かないことを「停滞」と読まない。**

- **2026-09-15 時点（本ブロックが直近の正）**＝第354〜355バッチ（round5 を 561 / 598 バッチ＝93.8% まで＋triage 115件）
  📊**進捗3計器**＝**Sheet1 要対応 11 / 863**（全件 mech＝**即着手可能 0**）｜**意味照合 段2 台帳 残 OPEN 0**｜**census 高シグナル 1 / BASELINE 1**。
  📦**在庫**＝**機構 worklist 🔥117**（索引A **3**＝`O-452` 36効果／`O-453` 34効果／`O-399` 12効果｜索引B 1｜索引G 113）｜**実機 🏁0**｜
  **実装キュー 🔥77 効果**｜**round5 561 / 598 バッチ**｜`census:numberdrift` **32**。
  🔧**ゲート**＝`npm run gates` **全緑**（golden **4182/4182**）。`src/` は無変更（BUG は登録のみ）。
  ✅**実機**＝不要と判定（§2.2＝`src/engine/` `src/screens/` を触っていない）。



## 付録B. 偽陽性パターン（脱落疑いに出るが**直さない**）— 毎回まず除外

1. **使用条件＋本体**（「このカードは〜の場合にしか使用できない」が前置き）＝条件として正しく表現済み。【真の偽陽性】
2. **CHOOSE/チェインの1文圧縮**＝「以下の[N]つから[M]つ（まで）を選ぶ」で改善済み（BUGFIXES③）。択肢が全部出ていれば機能的には正しい。
3. **REVEAL_AND_PICK / LOOK_AND_REORDER の文法崩れ**＝主要系統は是正済（BUGFIXES上部）。残りは§5.5の低優先テール。
4. **ルール注記**（「（コストのない【出】能力は発動しないことを選べない）」等）＝効果ではない。
5. **アンコール/ベット注記のみ**訳に出ない＝本体が合っていれば正しい。
6. **BET_MECHANIC STUB**＝§5.5（機構待ち）。
7. **owner:any の一括変換は禁止**：POWER_MODIFY/BANISH の `owner:'any'` は大半が正当（「シグニ1体を対象とし±N」＝自他選択／「すべてをバニッシュ」）。原文に明示主語があるものだけ個別是正。
8. **`[STUB:id]` を含むからとスキップしない**：実装済みハンドラのタグ表示。ただしハンドラがカード全体を覆うか（◎）／断片だけで残りを落としたか（実バグ）はタグでは区別不可＝各外れは個別検証。[[stub-means-implemented]]
9. **LIFE_BURST 内 `CONDITIONAL{IS_MY_TURN}`** は実害なし（常時true＋「そうした場合」特別処理）。修正不要。

---
**関連**：[DESIGN.md](./DESIGN.md)（設計方針）／[PLAN_DETAIL.md](./PLAN_DETAIL.md)（消化済み履歴）／[PLAN_PROGRESS.md](./PLAN_PROGRESS.md)（過去セッション要約）／[BUGFIXES.md](./BUGFIXES.md)（修正記録）／[BEHAVIOR_AUDIT.md](./BEHAVIOR_AUDIT.md)／[SEMANTIC_AUDIT.md](./SEMANTIC_AUDIT.md)／[CODEX_GUIDE.md](./CODEX_GUIDE.md)／[BATTLE_CONTROLLER.md](./BATTLE_CONTROLLER.md)／[effects-json-guide.md](./effects-json-guide.md)／[STUBS.md](./STUBS.md)
