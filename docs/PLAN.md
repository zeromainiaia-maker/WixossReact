# PLAN — 開発計画（唯一の正）

> **このファイルの読み方**＝新セッション（cold start）は **§1 現在地 → §2 作業の流れ → §5 作業キュー** の順に読む。設計方針は [DESIGN.md](./DESIGN.md)、修正記録は [BUGFIXES.md](./BUGFIXES.md)（新しいものを上）。
> **ここには「現在地・流れ・ルール・教訓・生きている worklist」だけを置く。** 消化済みの経緯・完了項目の全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md)、過去のセッション要約は [PLAN_PROGRESS.md](./PLAN_PROGRESS.md)、一次記録は [BUGFIXES.md](./BUGFIXES.md) にある。**クローズした項目はこのファイルから消す**（ID も残さない＝退避先と git 履歴が正）。

---

## 1. 現在地（直近1セッション）

> **運用**＝この節には**直近1件の要約だけ**を残す（入れ替え式）。新しく作業したら ①いまの要約を [PLAN_PROGRESS.md](./PLAN_PROGRESS.md) の先頭へ移す ②この節を今回の要約へ書き換える。**溜めない**（溜めると cold start が最初に読む節が一番古くなる）。
- **セッション（2026-09-11・第260バッチ・Opus 5 単独）＝索引 A を 残2 → **残1**（🏁`O-298` クローズ／`O-299` を 残8 → 残5）**
  ユーザー指示は「索引 A を行う」。
  **①🏁`O-298`（自分の任意コストの空払い・残4）**＝実測は **(a) 1 / (b) 3**。
  `WXK10-080-E1` は🔴**任意コストが空だった**（`costColors:[]` と `costText` だけ＝手札に＜水獣＞が無くても払えた）、
  `WXDi-P16-048-E1` は `CHOOSE` の枝の中で3点契約が抜けていた、
  `WX16-029-TRAP` は🔴**【トラップ】が `ACTIVATE_TRAP` からも撃てる**＝アタッカーが居なくてもコストを払わされていた。
  ✅**受け皿は既存の `TargetFilter.isAttacking`**（`fieldCandidates`）＝**parser 規則1本・engine 0行**で `WX17-044-TRAP` も一緒に閉じた。
  🔴**`WX26-CP1-006-E1` だけは軸が違う**（ルリグ候補は常に1体）＝`O-326` として分離登録した。
  **②`O-299`（離場置換・残8 → 残5）**＝**バトル経路にしか無かった3軸を `collectLeaveSubstituteOptions` へ持ち上げた**
  （`selfDown`／`selfExile`／`resonaSelfTrash`）。`selfExile` は旧実装の**「≈トラッシュ近似」も同時に修正**（`excluded` へ）。
  ⚠**`BattleScreen.tsx` の重複はまだ残っている**＝funnel へ委譲するのが残作業で、そこは実機まで必須。

  🔴🔑**教訓①＝1つの項目の中に別の軸が混ざる。** `O-298` の残4は「空払い」で束ねられていたが、
  `WX26-CP1-006-E1` だけは**候補は在るのに帰結が既に成立している**（相手ルリグが既にダウン）という別の空振りだった。
  ⇒ **1件ずつ「なぜ空振りするのか」を言葉にする**と軸違いが落ちる。束ねたまま機構を作ると過剰になる。
  🔴🔑**教訓②＝「型に無いフィールドを刻む」誘惑を止めるのは既存コメントだった。**
  `NEGATE_ATTACK` を `O96_STORABLE_OUTCOMES` へ足すと3点契約が自動で付くが、あの型は `targetsStored` を**読まない**＝
  **無言 no-op** になる。配列のコメントに書かれた「型にあり かつ `FREEZABLE` にもある」の2条件ルールが効いた。
  ⇒ **1点の事前ゲートだけ**にした（対象の同一性は `attackingOnly` が担保する）。
  🔑**受け皿は「同じ概念の別名」で探す**＝今回も engine を1行も足さずに閉じた（`O-298` 側）。

  📦**在庫**＝実装キュー **123効果**（据置）｜機構 worklist **26項目**（**A 1**／B 0／G 25）｜実機 **残0**。

  **次の一手**
  ① 🔥**索引 A の最後の1件**＝`O-299` 残5。**まず `WXDi-P09-TK03A-E1`**（`ACCE_BANISH_SUBSTITUTE` をバニッシュ限定から
     全離場へ）→ `WXEX1-30-E1`／`WXDi-P04-037-E1`（どちらも「宣言はあるが離場 collector が読まない」形）。
     ⚠**`BattleScreen.tsx` の重複を funnel へ委譲する回は実機まで必須**（`V-nn` を登録する）。
  ② A が空いたら**索引 G（残25項目 / 上限92効果）**＝速いレーンが既定・**着手は1件ずつ**。
     ⚠`O-318`〜`O-322` の9効果は登録理由が1行しか無いので**再 triage が先**。

## 2. 作業の流れ（1巡の定義）★このプロジェクトの唯一の作業単位

> 🆕**2026-09-10 に簡素化**（221行 → 約60行）＝制定の経緯・実測の根拠・母集団計器の較正表は
> [PLAN_DETAIL.md](./PLAN_DETAIL.md) の「§2 作業の流れ — 簡素化前の全文」へ**無改変で退避**した。ここには手順だけを置く。
> **節番号は住所なので変えない**（§2.0〜§2.6 は他ファイルから約400箇所参照される）。

### 2.0 実装レーンの選び方 ★③に入る前に必ず決める

**先に「どちらのレーンで書くか」を決める。** これを決めずに parser へ手を伸ばすのが「1効果1時間」の主因だった。

| レーン | 使う条件 | 書く先 | 検証 | 目安 |
|---|---|---|---|---|
| **速い（既定）** | **同型が2枚以下** | **`manualEffects.ts` に手書き** | `build:effects` → **逆翻訳を目視** → `golden -- --only` ＋ `smoke`（計 約5秒） | **1件 5〜10分** |
| **遅い（例外）** | **同型が3枚以上** ／ 新しいアクション型・条件型を足す ／ `src/engine/` を触る ／ `src/screens/` を触る | `effectParser.ts` ほか | 現行フル（§2.1 ③〜⑤） | 1件 30分〜数時間 |

- **根拠（次に読む人はここを消さないこと）**＝`npm run census:clusters` の実測で **1テンプレあたり 1.14効果**（2026-08-29）→ 再測 **1.03**（2026-08-30・母数が 571→340 に減っても比は改善しない）。⇒ **parser に regex を1本足しても平均1効果しか直らない。** ⚠**在庫が戻れば（新カードで同型が増えれば）遅いレーンへ戻す。**
- 🔴**「移設だけ」の manual 化は禁止**＝parser の出力を中身そのままコピーしても ①**何も直らない** ②`vocabCensus.ts` が STUB/MANUAL を高シグナルから免除するので**間違いが計器から消える** ③収穫マージが MANUAL/PARTIAL を効果単位で不可侵にするので**以後の parser 改善が永久に届かない**。**書くのは「原文を読み直して正しい JSON を手で書く」こと。** ⚠トップレベル効果の `parseStatus` は `MANUAL`／`PARTIAL` のみ（`manual-fields` ゲートが強制）。
- **母集団は「§5.3 の索引」そのものから取る**（旧版の3計器＝census 高シグナル／`census:cards --sheet 1`／意味照合台帳 は 2026-09-01 に**同時に底を打った**＝母集団としては使えない）。**索引を上から取り、着手前にその項目の母集団だけを実測する。**
- 🔴**在庫の総量を測る手段は、いま無い**（「原文にフレーズがあるのに live にキーが無い」型の計器は受け皿の別名を全部知らないかぎり必ず過大に出る＝9回連続で外した）。⇒ **「あと何割か」に答えようとせず、残項目数だけを報告する。**

### 2.1 1巡＝1バッチの手順

| # | 工程 | やること |
|---|---|---|
| ① | **取る** | §5 から**1件**。並行して別の節から取らない。⚠`§5.3` は登録順ではなく**索引 A→B→C の順**に取る |
| ② | **数える** | 着手前に**母集団を実測**する（[LESSONS.md](./LESSONS.md) §4.1）。🔑**worklist の件数も登録票の見立ても仮説であって事実ではない**（実測で9回連続外れた）＝**別名で grep し直す** |
| ②' | **レーンを決める** | §2.0 の表で速い／遅いを決める |
| ③ | **実装** | 決めたレーンで書く。**型を足すなら「型＋両評価器＋golden」の3点セット**（[LESSONS.md](./LESSONS.md) §4.2）＝欠けると**無条件成立**に落ちて全ゲート緑のまま意味が壊れる |
| ④ | **ゲート** | **速いレーン＝1件ごとに `golden -- --only` ＋ `smoke`（約5秒）だけ。`gates` は10件たまってから1回。** 遅いレーン＝`npm run gates`。parser を触ったら `npm run regen` ＋ live の A/B 差分で「意図した件数だけ変わった」を機械確認 |
| ⑤ | **実機** | §2.2 の表で要ると出た回だけ `node scripts/verifyBattleDrive.mjs <シナリオID>`（新規1本＋関連する回帰）。⚠**着手前に [DRIVE_TRAPS.md](./DRIVE_TRAPS.md) を読む** |
| ⑥ | **簿記** | **速いレーンは10件まとめて1回**（BUGFIXES は**真因1行／影響枚数／検証コマンド／反転確認の有無**の約10行）。遅いレーン・機構変更は `/baton` フル |
| ⑦ | **通知** | **手が止まったら停止通知メール**（§2.5）。⚠**push が要る作業をしたなら push を終えてから** |

- 🔴**`golden -- --only` の PASS/FAIL は全件実行と等価ではない**（`goldenTest.ts:155` の POOL カーソルがテスト間で共有される可変状態＝スキップすると後続が引くカードが変わり**両方向に化けうる**）＝**1巡を閉じる前に必ずフィルタなしで全件を回す**。
- ⚠**`BASELINE_HIGH` を更新する回は `gates` を2回回さない**＝先に **`npm run census` 単独（約5秒）**で実数へ直してから `gates` を1回（gates の所要は golden 約102〜170秒が支配的＝回し直すと golden をまるごと余計に払う）。

### 2.2 完了の定義 ★実機検証の要否はここで決まる（旧 §2.3 を統合）

**「⑤実機まで毎回」はやらない。触ったディレクトリで機械的に決める。**

| 触ったもの | 完了に要るもの |
|---|---|
| **`src/screens/` を触った**（UI・支払い・提示ゲート・モーダル・タイミング） | 🔴**⑤実機まで。** golden で反転確認を取っていても**省かない** |
| **新しいアクション型・条件型・機構を足した** | 🔴**⑤実機まで**（型は足したが engine のどこも消費していない、を実機で炙る） |
| **`src/data/` `src/engine/` `public/data/` だけ** | **④まで**（`gates` 緑＋ live の A/B 差分）。実機は不要＝`V-nn` の登録も要らない |

- **golden/smoke/fuzz が守れない層が実在する**＝`persist.commit` と React 反映のタイミング差／UI の選択リセット／モーダルの候補絞り込み。**この層の唯一の防御が実機シナリオ**（回帰トリップワイヤとして残す）。実例＝`O-123` は `artsUseGate.ts` の1行が壊れており、**JSON にも逆翻訳にも現れず3計器がどれも動かなかった**。
- **`src/screens/` を触ったのに省略してよい唯一の条件**＝**実機から原理的に観測できない**とき（例＝`optional`「〜してもよい」の【自】は CPU が自動応答して画面に出ない）。このときは **golden で両方向を固定し、理由を §5.1 に `V-<次番号>` で登録**する。**「時間がないから次回」は該当しない。**
- **④まで（実機不要と判定した回）は、それが「完了」**＝「要実機検証」と書いて次セッションへ送らない（判定した理由を BUGFIXES に1行書く）。
- 1巡のスコープは**⑤まで1セッションで回せる大きさ**に切る＝**速いレーン10件／バッチ**、**遅いレーン15〜20効果／バッチ**が目安。

### 2.3 （旧・実機検証の要否）→ §2.2 へ統合

### 2.4 作業中にバグを見つけたとき

**その場で直すのが既定。** **登録して先送りしてよいのは次の2つだけ**：
1. **新しい機構が要る**（型・評価器・engine の新設を伴う）＝§5.3 に `O-<次番号>` で登録。
2. **母集団が別**（いま回しているバッチと違う語彙・別の入口）＝§5 の該当節へ1行で登録。

⚠**登録するときは「見立て」ではなく実測値を書く**（[LESSONS.md](./LESSONS.md) §4.1）。

### 2.5 停止通知メール（工程⑦）★どのアカウントの Claude でも共通

🔴**目的は「1巡の完了報告」ではない。リーダーは離席している。手が止まって指示待ちになったことを知らせ、PC に戻ってくるためのトリガーである。**

- **送る（手が止まったら毎回）**＝⑥まで終わった／**測定・調査だけで終わった**／**質問・判断待ち**／**エラー・ブロッカー**／**ユーザーの指示がそこで終わっている**。
- **送らない**＝まだ作業を続けるとき（途中経過では送らない）。

```bash
node C:/Users/zerom/.claude-shared/notify-mail.mjs --subject "[WixossReact] <状態> — <一行主題>" --body "<本文>"
node C:/Users/zerom/.claude-shared/notify-mail.mjs --subject "件名" --body-file <path>   # 長文はファイルで
node C:/Users/zerom/.claude-shared/notify-mail.mjs --check                                # 送信せず設定だけ確認
```

- **件名の先頭で状態が分かるようにする**＝`完了` ／ `指示待ち` ／ `要判断` ／ `ブロック`（リーダーは件名だけ見て「今すぐ戻るか」を決める）。
- **本文に必ず入れる**（メールだけ読んで次の指示が出せる粒度）＝①何が終わって何が終わっていないか ②次の選択肢（推奨があれば書く）③リーダーの判断が要る点（無ければ「なし」）④commit SHA と push 済みか。**実装まで進んだ回は加えて**＝⑤**真因**（症状ではなく）⑥ゲート数値と3計器＋在庫2本（§3・§6）⑦実機シナリオ名・PASS 数・反転確認の有無 ⑧登録した `O-nn`。⚠**監査／委譲ラウンド中は5行以内でよい**（§2.6）。
- ⚠**踏んだ落とし穴（再発させない）**＝①宛先は **`zeromain.iaia@gmail.com`（ドット入り）が正**（Resend はドメイン verify まで文字列一致の宛先にしか送れず `zeromainiaia@` は **403**）＝**`MAIL_TO` を書き換えない** ②`notify-mail.mjs` は **`process.exit()` を呼ばない形**（`fetch` の後に呼ぶと exit code が 127 に化けて失敗を検知できない）＝崩さない ③`--body-file` のパスに **`$TMPDIR` を使わない**（Git Bash では未設定＝ルート直下に化けて Permission denied）＝スクラッチパッドの絶対パスを使う ④置き場は `C:\Users\zerom\.claude-shared\`＝**`notify.env` をリポジトリ内へコピーしない**（push で漏れる）。

### 2.6 監査ラウンド・委譲バッチの軽量運用 ★2026-09-06 ユーザー決定

> **経緯**＝第197バッチ（30枚監査＋2系統修正＋簿記）で**月額プランの 40% を1時間で消費した**。⚠**`claude -p` も同じ月額枠を食う**（1バッチ10枚 ≒ 35〜40kトークン／2.3分）＝**枚数を目標にしない**。支配的なのは検出ではなく**opus セッションのターン数 × コンテキスト量**。

- 🔑**LLM は「まだ知らない型」を見つけるためだけに使い、その型の全数は grep / census で取る**（第197バッチで効いたのは枚数ではなく **1 finding が10枚の系統に化けた**こと＝その10枚を見つけたのは grep＝無料）。真バグを引いたら**直す前に母集団を数える**（§2.1 ②）。
- 🔴**止め時は「残枚数」ではなく「1バッチあたりの新型数」＝連続3バッチで新型0 なら止める**（台帳＝`scripts/archive/scratchpad/semantic_audit_sheet1_round4/TYPE_LEDGER.md`）。
- 🔴**偽陽性は必ず `scripts/semanticAuditExtract.mjs` の「追加の読み方ルール」へ還元する**（現在 規則1〜16）＝還元しないと**次の50枚で同じものを読まされる**。⚠偽陽性は全部「engine が JSON の見た目を裏で読み替えている」型（did-it ゲート／`OPPONENT_PAY_OPTIONAL` の既定の極性／STUB の id 名）。
- **固定費は「バッチごと」ではなく「セッションごと」**＝作業中は `golden -- --only` ＋ `smoke` だけ／full `gates` はセッション末1回／簿記もセッション末1回（バッチ中は台帳1行）／メールは5行以内。
- **委譲バッチの既定形**＝**1項目終わるたびに `BUGFIXES.md` へ追記させる**（第251で最終レポート生成前に報告が丸ごと消えた）。**判定（triage・parser/engine 修正）だけを Opus に残し**、監査実行・grep 展開・`manualEffects.ts` の手書き・ゲート・簿記は Sonnet／Codex でよい。
- ⚠**この軽量運用は監査・委譲ラウンド中限定**＝`src/engine/` `src/screens/` を触る回は §2.1〜§2.5 のフル手順に戻る。

---

## 3. 不変の運用ルール

- **`effects_*.json` の curated 値は上書きされない**＝`build:effects` は ①`parseStatus` が `MANUAL`/`PARTIAL` を含むカードを**カード単位で温存**（`PRESERVE_STATUSES`）②それ以外も fresh が curated の純粋な上位集合でなければ**温存して held へ送る**（`buildEffectsJson.ts:158-162`）。**変わるのは `heldReview --adopt` で明示採用したものだけ。**
- **逆翻訳を直したらエンジン実装までセット**（乖離＝偽陰性を作らない）。語彙が無ければ §5.3 の機構として実装するか、`engineUnwiredTimings` に登録し逆翻訳へ `【※engine未配線】` を付けて明示する。[[decompile-engine-parity]]
- **ゲートは `npm run typecheck`（＝`tsc -b --noEmit`）**＝plain `tsc --noEmit` は project references を見ず CI が拾うエラーを見逃す。⚠`scripts/` は型チェックの対象外＝**`scripts/` を直したら実際に `npm run golden` を走らせるまで「直った」と言わない。**
- 🔴**一括置換は禁止**（全再生成系も、**ソースの文字列置換も**）＝無検証置換で約90枚退化／`manualEffects.ts` への `s.replace('"usageLimit":"once_per_turn"', '')` で**無関係な21効果から《ターン1回》を消した**前例（2026-09-04 第127バッチ）。⇒ **行番号を指定した1行だけの置換にする。** 🔑**`git diff --stat` が「1行のはずが22行」と言ったら、そこで止まる。** 🔑**ゲートが2本同時に落ちたら、engine を読み始める前に自分の直前の置換範囲を疑う。**
- **系統ごとの直し方**＝機構を1回確立 → 同パターンに適用 → **各カードを verify**。
- **日本語を含むスクリプトは `scratchpad` に `.mjs` を書いて `node <path>` 実行**（Git Bash 経由の `node -e` は文字化けする）。使い捨ては `tmp_*` 名（gitignore 済み）。
- **CSV の順番を必ず維持する**（スクリプト内の `sorted` ロジックで対応済み）。
- **件数メトリクスを完了指標にしない**＝逆翻訳器は複数効果を1行に圧縮するので、内容を直しても件数は減らない。判断は必ず**該当カードの逆翻訳が原文と一致するか（目視／grep）**で行う。

### 進捗の報告の仕方 ★2026-08-27 ユーザー決定・2026-09-01 改定

**「3計器＋在庫3本」を併記する。どれか1本を「進捗指標」に固定しない。**

| | 指標 | 取り方 |
|---|---|---|
| ① | Sheet1 の要対応カード数 | `npm run census:cards -- --sheet 1` |
| ② | 意味照合 段2 台帳の残 OPEN | `node scripts/archive/semanticAuditLedger.mjs` |
| ③ | census 高シグナル数 | `npm run census` |
| ④ | 機構 worklist の残項目数 | §5.3 の索引 |
| ⑤ | 実機の残 `V-nn` 数 | §5.1 |
| ⑥ | 実装キューの残効果数 | §5.0 |

- 🔴**①〜③は 2026-09-01 に底を打った**（即着手可能 0／残りは全件 UI か新機構待ち）＝**動かないことを「停滞」と読まない**（22効果を直して3計器とも据置だった実績）。**以後の進捗指標は ④〜⑥の在庫。**
- ⚠**在庫も「減れば前進」とは限らない**＝母集団を実測すると**項目が割れて増える**ことがある。**減った理由／増えた理由を必ず1行書く。**
- **3計器は別のものを測っている**＝①**カード単位の網羅**（1枚に findings が複数あると1つ直しても落ちない）②**指摘単位の消化**（標本なので、同じ parser 規則で直った標本外の実バグは数に出ない）③**語彙の欠落**（STUB/MANUAL を免除するので STUB を解体すると**増えて見える**）。⇒ **単独の計器は「進んだのに動かない」と「動いていないのに動く」の両方を起こす。**
- ⚠**バッチの着手時に「今回どれが動く見込みか」を宣言する**。**動かない計器があること自体は失敗ではない。動かない理由を書けないことが失敗。**

### 横展開（系統バグ）のときの追加手順

①**抽出**＝全シート走査で「同じ壊れ方」を機械抽出 → ②**分類**＝偽陽性（付録B）・既知の複雑札を除外してクリーンな系統を確定 → ③**パッチ**＝`effectId` をアンカーにした置換（他カードを巻き込まない。MANUAL 化するなら `parseStatus:'MANUAL'`）→ ④**検証**＝§2.1 ④〜⑤ → ⑤**記録**＝`BUGFIXES.md` へ追記（新しいものを上）→ §1 を入れ替え → commit / push。

### 機構実装の「型」
1. `src/types/effects.ts`（アクション/条件/timing の型）→ 2. `src/types/index.ts`（`PlayerState` 状態フィールド）→ 3. `src/engine/effectExecutor.ts`（実行）/`execUtils.ts`（`evalCondition`/`matchesFilter`）/`effectEngine.ts`（CONTINUOUS収集）→ 4. `src/screens/BattleScreen.tsx`（状態読み取り＋**ターン境界リセット3箇所**：PvP通常終了・PvP確認後・CPU）→ 5. `scripts/decompileEffects.ts`（表示）→ 6. JSON 配線 → 検証。

### 主要ファイル
- 語彙: `src/types/effects.ts` / `src/types/index.ts`（PlayerState）
- エンジン: `src/engine/effectExecutor.ts`・`execUtils.ts`（`evalCondition`/`matchesFilter`）・`effectEngine.ts`（CONTINUOUS収集・`checkActiveCondition`）
- UI/ルール: `src/screens/BattleScreen.tsx` ＋ `src/screens/battle/`（コスト計算・バトル・ターン境界リセット）
- 逆翻訳器: `scripts/decompileEffects.ts`、グルーピング: `scripts/group{Similar,BySentence}.mjs`（`--all` で全10シート統合）
- 監査: `scripts/behaviorAudit.ts`（`npm run audit` / `audit:queue` / `audit:html`）

---

## 4. 教訓集 ★**本体は別ファイル**（2026-09-06 に分離）

> 🔴**この節に教訓を書かない。** 2026-09-06 時点で PLAN.md は **1,313行中 634行（52%）が §4**で、
> 「計画表」として読めなくなっていた（うち §4.4 だけで 368行＝**全体の30%**）。⇒ 2ファイルへ分離した。
> 🔴**節番号 `§4.1`〜`§4.6` は住所として維持している**＝BUGFIXES・PLAN・`scripts/`・`src/` から**数百箇所**が
> 「§4.2」「§4.4-2c」の形で参照している（実測＝`§4.4` だけで `scripts/` から **216箇所**）。**詰め直さない。**

| ファイル | 中身 | 読むタイミング |
|---|---|---|
| **[LESSONS.md](./LESSONS.md)** | **§4.1** 着手前（見立てと母集団）／**§4.2** 実装（型・parser・engine）／**§4.3** 計器の読み方／**§4.5** 修正を live へ届ける経路／**§4.6** Codex へ委譲するときの作法／🆕**§4.7** 意味照合ラウンドと triage の読み方／🆕**§4.8** 監視だけしている項目 | 🔑**§5 のどの項目に着手するときも先に読む** |
| **[DRIVE_TRAPS.md](./DRIVE_TRAPS.md)** | **§4.4** 実機シナリオの罠（番号つき92項） | 🔑**実機シナリオを書く／直す回だけ**（`scripts/verifyBattleDrive.mjs`） |

⚠**新しい教訓はこの2ファイルへ足す**（PLAN へ書き戻さない）。
⚠**踏んだ罠は必ず残す**＝この2ファイルが生まれた理由は、教訓が**クローズ済みタスクの本文に埋まっていて**
**完了行を消すと巻き添えで消えていた**こと（2026-08-23 の §4 新設時の経緯）。

---

## 5. 作業キュー（単一・上から取る）

> **取り方**＝**上から1件**。並行して別の節から取らない。1巡の手順は §2、着手前に読むのは [LESSONS.md](./LESSONS.md)。
> **`V-nn`／`O-nn` は固定の住所**＝消化しても詰めない・再利用しない。**消化したら行ごと消す**（全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md) と [BUGFIXES.md](./BUGFIXES.md) が正）。
> ⚠**クローズの経緯をこの節に書き溜めない**＝書くのは**いまの残数だけ**（過去に「残」列がクローズ履歴2,251字に膨らみ、しかも3節とも数字が stale だった）。
> ⚠**残数は毎回コマンドで数え直して書く**（表の数字を足し引きしない＝triage 済み32件を二重に足して「77件」と書いた実績がある）。
> 🆕**2026-09-10 に全面改修**＝残作業が §5.0〜§5.5 に散って見えなくなっていたので、**下の「残作業の全体像」1枚で全部見える**ようにした。改修前の全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md) へ無改変で退避。

### 残作業の全体像（2026-09-10 実測）★まずここを見る

| 順 | キュー | 残 | 中身 | 測り直すコマンド |
|---|---|---|---|---|
| **①** | **§5.1 実機 `V-nn`** | 🏁**0件**（2026-09-10 返済） | `src/screens/` を触った回の返済先＝**溜める前に返す** | §5.1 の表 |
| **②** | **§5.3 機構 worklist `O-nn`** | 🔥**32項目**（上限 約180効果）＝索引 **A 4項目/上限56効果**・**B 4項目/22効果**・**G 24項目/92効果** | 新しい型・評価器・engine が要るもの＝**いまの本線** | §5.3 の索引（母集団は着手時に実測し直す） |
| **③** | **§5.0 実装キュー** | 🔥**123効果**（うち系統 6件・残りは一点物）。⚠**候補プールは残7＝実質枯渇** | triage で真バグと確定した未修正バグ | `node scripts/archive/semanticAuditBugList.mjs` |
| **④** | **§5.5 低優先・保留** | **7件** | CPU AI／逆翻訳テール／リリース判定ほか | — |
| — | §5.2 意味照合 | 🏁**0**（round4 全11シート完走・段2台帳 残 OPEN 0） | **「受け皿の名前を知らない穴」を拾える唯一の発見器**＝③が尽きたら round5 の判断 | `node scripts/archive/semanticAuditGap.mjs` |
| — | §5.4 構造混線 | 🏁**0** | 新しく見つけたときだけ足す | — |
| — | §5.3 末尾「個別カードの機構待ち」 | 参照 **6件** | 根拠つき defer 3・既知の潜在結合 3（**着手不要**） | — |

**取る順とその理由（依存関係）**

1. 🔥**①実機（§5.1）を先に0にする**＝寝かせるほど「バグかシナリオの腐りか」の切り分けが高くつく（§2.2）。**いま残0**＝`src/screens/` を触ったら同じ巡で返す。
2. 🔥**②機構（§5.3）が本線**＝**③の123効果のうち、いま着手できない分の大半が「機構待ち」**なので、②を入れないと③が動き出さない。索引 **A → B → G**（母集団の大きい順）。
3. **③実装キュー**＝**機構不要と判定できる候補だけ**を取る（`semanticAuditQueue.mjs --take N`）。⚠候補プール **7**（`pri=2` が4）＝**Codex 委譲バッチ路線は 2026-09-10 第252 で一区切り**。**②を消化すると候補が戻る。**
4. **④低優先**は①〜③が尽きたときだけ。**⑤§5.2 round5** は③の在庫が尽きたときに開始を判断する。

⚠**「§5 の上から1件」は §5.3 の索引にもそのまま当てる**（索引が母集団順に並んでいる）。

---

### 5.0 実装キュー（triage 済みの確定バグ）

🔥**残123効果**（確定433 − 消化済み337・2026-09-10 実測）。**per-effect の正本は各ラウンド dir の `triaged.txt` の `:: BUG ::` 行**＝この節はそこから系統を引き出した取り掛かり表。

| 軸 | 内訳（2026-09-10 実測・残数） |
|---|---|
| **壊れ方** | `MISSING` **68** ／ `WRONG` **42** ／ `SUSPECT_STUB` **12** ／ `EXTRA` **1** |
| **深刻度** | HIGH **71** ／ MED **46** ／ LOW **6** |
| **シート** | Sheet3 **28** ／ Sheet8 **20** ／ Sheet7 **17** ／ Sheet9 **16** ／ Sheet2 **13** ／ Sheet4 **10** ／ Sheet1 **8** ／ Sheet6 **6** ／ Sheet10 **2** ／ Sheet5 **2** ／ TK **1** |

🔴🔑**この節が唯一の追跡先**＝**finding を「BUG」と triage した瞬間、`semanticAuditPool.mjs` からも `census:cards` の「未消化 findings」からも消える**（どちらも数えているのは*未 triage*であって*未修正*ではない）。
⇒ **行を消してよいのは (a) 直して golden を張った (b) 偽陽性と確定した (c) §5.3 へ `O-nn` で登録し直した ときだけ。**
🔴**severity では取らない**＝残 HIGH 71 の大半は §5.3 登録済みの機構待ち。**「機構不要と判定できるか」で選ぶ。**

#### 残っている系統（一点物より先に取る＝1件あたりのコストが桁で安い）

| 分類 | 効果 | 壊れ方 | 受け皿 | レーン |
|---|---|---|---|---|
| 系統 | 遅延誘発が即時実行に化けている（S-2 実測 A 8効果＋M 2＝10） | 「次の〜時に」が解決時に走る | ✅実在（`INSTALL_DELAYED_TRIGGER` ほか6経路） | 遅い。🆕**2026-09-09 第234バッチで1効果採用**（`WX24-D5-05-E1`）＝**残2**（`node scripts/archive/semanticAuditBugList.mjs --list --grep 遅延誘発` で再測） |
| 系統 | 「1枚をデッキ上・残りを下」／「残りをシャッフルして下」（S-2 実測 A 5効果＋M 3＝8） | 積み込みが効かない／無作為化されない | ✅実在（`first_top_rest_bottom`／`split_top_bottom`／`remainder.shuffle`／`deckBottomShuffled`） | 遅い（未着手・再測要） |
| 系統 | 【ライド】が2つの起動能力に重複（S-2 実測 2効果＝登録どおり・両方 MANUAL） | 同じ能力が2回提示され使用回数も別管理 | ✅本線は `RIDE_ON`（`effectParser.ts:27030`／UI `battleUtils.ts:126`） | 速い（`manualEffects.ts:10109`・未着手） |
| 系統 | 色付きルリグ／シグニ対象の色フィルタ欠落（S-2 実測 A 2＋B 2＋M 1＝5効果） | 色条件を無視して対象にできる＝**不発すべき効果が通る** | ✅実在（`filter.color`／`anyOf[].color`／`colorMatchesLrig`） | 遅い（A2）＋速い（M1・未着手） |
| 系統 | グロウ時「公開した場合」が「手札にある」判定（S-2 実測 2効果＝登録どおり・両方 MANUAL） | 公開せずに軽減を受けられる | △通常の公開受け皿はあるがグロウ支払いフローへの配線が無い | 速い（`manualEffects.ts:4566`/`:4578`）＋engine（未着手） |
| 系統 | 「この【起】能力でまだ選ばれていない」「ゲーム中1回だけ」が無条件（S-2 実測 2効果・旧 `O-289`） | 同じ選択肢を何度でも選べる | ✅実在（`CHOOSE.noRepeat` ＋ 永続 `taken_choice_keys`＝`execStubPart1.ts:922-930`） | 遅い（parser・engine 新機構は不要・未着手） |

⚠**系統の効果数は上限値**＝着手時に §2.1 ② で割り直す（[LESSONS.md](./LESSONS.md) §4.7）。**残り（約110効果）は上の内訳表が全数＝一点物。**
🔴**着手前に必ず S-2（grep 展開）を回す**＝2026-09-08 実測で系統の母集団は **2.7〜3.7倍**になった（`usageLimit` 3→11／`OPPONENT_PAY_OPTIONAL` 12→32／`reorder:false` 4→12／`commonClass` 9→27）＝**監査員が気づくのは同型の約1/3**。⚠grep 実測も上限値（別軸で表現済みのものを「欠落」と数える）＝着手時に1件ずつ割る。

#### 1バッチの回し方（Codex 委譲・道具はリポジトリ内。スクラッチに置かない）

```
# ① 候補を組む（残数・優先度の確認だけなら引数なし）
node scripts/archive/semanticAuditQueue.mjs --take 30 --out <scratchpad>
#    → batch.json（原文・live JSON つき）と batch_table.md（指示書に貼る表）

# ② 指示書を作る（テンプレをコピーして <NNN>/<BASELINE_COMMIT>/<GOLDEN>/<プール残>/<残数> を埋め、
#    batch_table.md を「今回のスコープ」節へ丸ごと貼る）
scripts/archive/scratchpad/codex_batch_template.md

# ③ 投入（⚠先に git status --porcelain を空にする）
CODEX_HOME=/c/Users/zerom/.codex-work codex exec -C "C:/Users/zerom/WixossReact" \
  -c model_reasoning_effort="high" -o <report> - < <指示書> > <log> 2>&1
```

- 🔑**候補の母集団は3ファイルの引き算だけ**＝`triaged.txt` の `:: BUG ::` − `semantic_bug_fixed.txt` − `semantic_bug_deferred.txt`。**外部状態に依存しないので、セッションが変わっても同じ候補が再現する**（道具化前は第243 の候補生成で、除外したはずの12効果がまるごと復活した）。
- ⚠**`pri=2`（判定文が「機構が要る」と明言）は既定で候補から外れる**＝入れても Codex が見送りを書くだけ。
- 🔑**委譲バッチの既定形＝1項目終わるたびに `BUGFIXES.md` へ追記させる**（§2.6）。**Claude は `git diff`／`typecheck`／`golden`／`gates` で独立検証してから採用する**（Codex の「実装した」を信用しない）。

#### 記録の付け方（これを怠ると在庫カウンタが永久に減らない）

- **直した** → `scripts/archive/scratchpad/semantic_bug_fixed.txt` に `<effectId> :: <日付> :: <一言>`。
- **直さないと判定した** → `scripts/archive/scratchpad/semantic_bug_deferred.txt`（`MECH`＝機構待ち／`STALE`＝既に直っていた／`FP`＝偽陽性／`REGISTERED`＝§5.3 登録済み）。🔴**在庫からは引かない**（直っていないのは事実）＝**次バッチの候補から外すためだけ**。
- 🔴🔑**`MECH` と書くときは必ず `O-nn` を併記する**（無ければ同じ回に §5.3 へ新設）＝2026-09-10 に突き合わせたら**21効果がどの `O-nn` にも登録されていなかった**（`§5.3` が在庫を映していなかった）。
- ⚠**`triaged.txt` の行は消さない**（「何を直したか」の履歴）＝消化は上の2ファイルで引き算する。
- ⚠**モデルを1セッション内で切り替えない**（プロンプトキャッシュはモデル別＝切替後の最初のターンで全コンテキストを full price で読み直す）＝**簿記 → commit → `/clear` → `/model`** の順にする。

---

### 5.1 実機で確かめる（`V-nn`）★①＝溜める前に返す

> ⚠**着手前に [DRIVE_TRAPS.md](./DRIVE_TRAPS.md) を読む**（番号つきの罠 98項）。
> ⚠**`verifyBattleDrive.mjs` は必ず明示シナリオIDで実行する**（引数なしのフルバッチはフリーズ報告あり）。
> **FAIL の切り分け3分類**＝(a)**シナリオの腐り**（[DRIVE_TRAPS.md](./DRIVE_TRAPS.md) の 26）はその場で直す (b)**engine/parser のバグ**もその場で直す（§2.4） (c)**未実装**は §5.3 へ登録。

🏁**残0**（2026-09-10＝第253で `V-184`／`V-185`、第255で `V-186` を**いずれも同じ巡で返済**）＝**6本を `order` に常設した**
（`v184DriveCrasherFires` / `v184NonDriveCrasherSilent` / `v185FieldDownExcludesSelfOffered` / `v185FieldDownExcludesSelfBlocked`
／`v186LeaveSubstituteDeckBottom` / `v186LeaveSubstitutePlainControl`）。
🔑**`V-186` は「golden では原理的に緑になる」型の返済**＝golden は funnel（`collectLeaveSubstituteOptions`）を直接叩くので、
**funnel が実戦の離場経路から呼ばれていなくても通る**。実機で初めて「代わりに〜」の対話が出ることを確認した。

⚠**新しい観測点はここへ `V-<次番号>` で足す**（機構項目は §5.3 へ）。**返済したら行ごと消す。**
🔑**`order` に入れたシナリオは「壊れたら気づく」ための番人**＝返済後も外さない（例＝`o267CutinResonaResolvesBeforeSpell` は「`effect_stack` を空にしてからスペルを解決する」ガードの唯一の番人）。
🔑**返済済み `V-04`〜`V-183` の全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md) の「§5.1 実機返済の完了報告」と [BUGFIXES.md](./BUGFIXES.md)。**

---

### 5.2 意味照合監査（semantic audit）★🏁**残0＝いまは取らない。③が尽きたら round5 を判断する**

🏁**round4＝全11シート・効果あり6,032枚を監査完了**（`node scripts/archive/semanticAuditGap.mjs` で残0）。findings は全数 triage 済み（確定433／偽陽性70 → §5.0）。
🏁**段2 台帳＝残 OPEN 0**（1,444 findings。`node scripts/archive/semanticAuditLedger.mjs` で測り直す＝内訳を PLAN に書き写さない）。

🔑**意味照合は「受け皿の名前を知らない穴」も拾える唯一の発見器**（逆翻訳・census・golden は**知っているキーしか見ない**）＝**§5.0 の在庫が尽きたら、まずこの性質がまだ必要かを検討する。**

**round5 を始めるときの手順**（歩留まり・罠は [PLAN_DETAIL.md](./PLAN_DETAIL.md) の「round4 完走記録」と [LESSONS.md](./LESSONS.md) §4.7）：
```
node scripts/semanticAuditRun.mjs --out scripts/archive/scratchpad/semantic_audit_round5 \
  --model sonnet --batches <N>,<N+1>,...
```
- シャッフルは seed 42 の mulberry32 固定。1バッチ10枚・所要87〜443秒。消化したら `audited_cards_cumulative.txt` へ追記（キー名は `num`）。
- 🔴🔑**findings をそのまま直さない**＝偽陽性は「engine が JSON の見た目を裏で読み替えている」型（did-it ゲート／`OPPONENT_PAY_OPTIONAL` の既定の極性）。**引き当てたら engine の受け皿を必ず読んでから triage する。**
- 🔴**止め時は「1バッチあたりの新型数」＝連続3バッチで新型0**（§2.6）。

---

### 5.3 機構 worklist（新しい型・評価器・engine が要るもの）★②＝いまの本線・**新機構の登録先**

> **二層構造**＝**索引（ここ）＝残数と一行の説明だけ／登録票の全文＝[PLAN_DETAIL.md](./PLAN_DETAIL.md)**。
> 🔴**ID を決めたら、着手前に PLAN_DETAIL の同じ ID の登録票を読む**（経緯・実測・受け皿の在処・罠・取り方がそこにある）。**索引だけで実装を始めない。**

**この節の使い方**
1. 索引 **A → B → G**（母集団の大きい順）に取る。**索引の並び順がそのまま取る順**（旧「取る順」表は廃止）。
   ⚠**欠番の C/D/F は過去に使って閉じた枠**（C＝旧「母集団1〜2効果」2026-09-02 全件クローズ／D＝母集団未計測・解消／F＝事実上クローズ）＝**文字も住所なので再利用しない**（`索引 C` は歴史側に約108箇所の参照があり、`goldenTest.ts`・`manualEffects.ts` のコメントも旧 C を指している）。
2. **②母集団を実測し直す**（§2.1 ②）。索引の数字は**登録・再計測時点の上限値**であって、いま正しいとは限らない。⚠**2026-09-10 に4項目連続で「受け皿が無い」が誤りだった**（`O-288`／`O-287`／`O-324`／`O-304`＝実測 登録27→3／12→3 など）＝**別名で grep し直す**（型のコメント・golden・`manualEffects.ts` まで見る）。
3. **②' レーンを決める**（§2.0）＝索引 G（1〜2効果）は速いレーンが既定、A/B は遅いレーン。

**登録のルール**
- **新規登録は「索引に1行（表）」＋「PLAN_DETAIL に登録票1項目」**。索引に長い経緯を書かない（それが旧構造の破綻原因）。
- 🔑**登録時に母集団を実測して書く。** ⚠`S`/`M`/`L` は**実装量の見立てであって母集団ではない**（この混同で「S・1効果」の項目に34枚・19枚・15枚の塊が隠れていた）。
- **消化したら索引の行ごと消す**（ID は再利用しない）。🔑**クローズは「実装が終わった」ではなく「索引から行を消し、§1／§6／この節の残数を直す」まで**（落とすと §1 の「次の一手」が閉じた項目を指し続ける）。
- **実機で確かめるだけの項目はここに書かない**＝§5.1 へ `V-<次番号>`（守らなかった結果、旧 `O-13` が8,185字に肥大した）。

> 🔴🔑**書式を守る（`census:cards` の `mech` フラグがこの節を機械で読む）**
> - **開いている項目は必ず `| \`O-nn\` |` で始まる表の行**（`cardProgressCensus.mjs:176`）＝箇条書きで足すと**計器から丸ごと消える**。🔴**ID セルの先頭に絵文字を置かない**（`| 🆕\`O-281\`` で実際に2項目が消えていた）。
> - **クローズ注記をこの節に残さない・カード番号を書かない**＝計器は §5.3 の本文からカード番号を拾う（`:196`）ので、**クローズ注記の引用カードまで「機構待ち」に化ける**（項目を1つ閉じたのに `mech` が 38→40 に増えた）。**経緯は BUGFIXES.md と登録票へ。**
> - **索引を全部消化したら見出しの下に `🏁**残0**` と書く**（「書式が壊れた」と「本当に空」を区別している）。
> - **編集したら `npm run census:cards` で差分を見る。**

**母集団の測り直し方**＝`node scripts/archive/censusMechPopulation.mjs [--id O-nn]`（原文 regex で拾う → live の受け皿 regex で絞る2段）。
🔴**この測り方には構造的な限界がある**＝「原文にフレーズがあるのに live にキーが無い」は**受け皿の"別名"を全部知らないかぎり必ず過大に出る**（2026-08-30 に9回連続で過大。`census:wiring` の最大セル24件は実際は1件だった）。⇒ **索引と `census:wiring` は「着手前のふるい」＝"どれだけ残っているか"の答えには使わない。**

#### 🆕 1〜3枚の項目の取り方（型を足す前に読む）

1. 🔑**まず受け皿を疑う。** 探し方＝①原文の言い回しで `src/` を grep ②同じ言い回しで `scripts/goldenTest.ts` を grep（**実装済みなら golden が張ってあることが多い**）。
2. **アクション側は「型」ではなく `STUB` ハンドラ1本で書く**（`execStubPart*.ts` に923本の実績・1本平均17行）。`census:stubs` A群からも消える。
3. 🔴**条件側には STUB の道が無い**＝`COND_STUB` は `execUtils.ts:2413` が **`return true`＝無条件成立**。条件は**型＋`CONDITION_TYPES`＋`evalCondition`＋`checkActiveCondition`＋golden＋parser の6箇所**を必ず揃える（[LESSONS.md](./LESSONS.md) §4.2）。
4. **1枚のために機構を作らないと決めてよい**＝そのときは**無言 no-op にせず** id を `DEFERRED_*` へ改名し、理由を散文でこの節に書く。

#### 索引 A'. 実機が出した配線ギャップ

🏁**残0**。⚠**golden が緑のまま実機だけが出した穴**をここへ足す（過去2件とも真因は収集器やゲートではなく**その手前の配線**だった）。

#### 索引 A. 母集団2桁（**ここから取る**・遅いレーン）

🔥**残1項目 / 上限5効果**（2026-09-11 第260バッチ＝🏁**`O-298` をクローズ**（残4の実測は **(a)1 / (b)3**＝軸違い1件は `O-326` へ分離）／`O-299` を 残8 → **残5**）。

| ID | 母集団 | 何が無いか |
|---|---|---|
| `O-299` | 🔥**残5**（2026-09-11 第260バッチで**バトル経路にしか無かった3軸を funnel へ持ち上げた**＝母集団24効果は据置。内訳は **(a) 既に正しく動く8 ／ (b) 配線済み11 ／ (c) 機構が要る5**） | **離場置換（「代わりに」）の受け皿**＝`collectLeaveSubstituteOptions` の軸列。✅**第260で `selfDown`／`selfExile`／`resonaSelfTrash` の3軸を追加**（`src/screens/` は無傷・engine のみ）。🔥**残5の内訳と各々に要る機構は [PLAN_DETAIL.md](./PLAN_DETAIL.md) の `O-299` 登録票**（カード番号もそちら＝この節に書くと `census:cards` が「機構待ち」に化ける）。⚠**バトル経路の重複はまだ残っている**＝`BattleScreen.tsx` が同じ意味を自前で持っており、funnel へ委譲するのが残作業（そこを触る回は §2.2 により実機まで必須） |

⚠**新しく母集団2桁の項目が出たらここへ足す。**

#### 索引 B. 母集団 3〜8効果

🏁**残0**（2026-09-11 第259バッチで4項目とも消化）。
⚠**うち2件（`O-294`／`O-302`）は登録票が stale で、受け皿は最初から実装済みだった**
（`O-294` は**登録コミットの祖先**に実装が入っていた／`O-302` は engine ではなく**逆翻訳だけ**が穴だった）。
🔑**新しく母集団 3〜8効果の項目が出たらここへ足す。登録するときは、その場で受け皿を grep して実装済みでないことを確かめる。**

#### 索引 G. 母集団 1〜2効果（速いレーンが既定）

🔥**残25項目 / 上限92効果。** ⚠**単発の集まりなので、着手は1件ずつ**（`O-317`／`O-322` は互いに無関係な効果を束ねただけ）。
⚠🔴**`O-318`〜`O-322` の9効果は登録時の理由が1行しか残っておらず、着手前に再 triage が要る**（各登録票に明記）。

| ID | 母集団 | 何が無いか |
|---|---|---|
| `O-326` | **1効果**（`WX26-CP1-006-E1`） | **「帰結が既に成立している」ときの空払いを事前に見る軸が無い**＝原文「追加で対戦相手のルリグ１体を対象とし、手札から＜プリオケ＞のカードを１枚捨ててもよい。そうした場合、それをダウンする。」に対し、**相手のセンタールリグが既にダウンしていても支払いを提示する**（`execDown` の LRIG 枝が「ルリグは既にダウン状態」で降りる＝払い損）。🔴**`O-298` の軸（候補0体）とは別**＝ルリグ候補は常に1体あるので `SELECT_TARGET_ONLY{abortIfNoCandidate}` では止まらない。⚠**`SELECT_TARGET_ONLY` の LRIG 枝は `filter` を1つも適用しない**（`cands = lrigTop ? [lrigTop] : []`）＝`filter.isUp` を書いても無視される。⚠**シグニ側の `DOWN` も同じ**（ダウン済みシグニを候補から外していない）＝**軸を作るなら両方に効かせる**
| `O-325` | **12効果**（「〜**と**共通するクラスを持つ」＝`PR-K070-E1`／`WX25-P1-058-E2`／`WX25-P1-089-E1`／`WX25-P1-093-E1`／`WXDi-CP02-025-E1`／`WXDi-P00-021-E2`／`WXDi-P13-027-E1`／`WXK10-023-E1`／`WXK10-029-E2`／`WXK10-033-E2`／`WXK10-038-E1`／`WXK10-056-E2`） | ❌**受け皿が無い**＝**参照カードを基準にした動的比較**（「この方法で捨てたシグニと」「このシグニの下にあるシグニと」「あなたの場にあるいずれかのシグニと」）。`sharesClassWithPrev` が近いが**直前ピック限定**で、①コストで捨てた札 ②下敷き ③場の任意の1体 を基準にできない。⚠**`O-311`（「見た／選んだ集合」を後段へ渡す carrier が無い）と同族**＝**先に `O-311` を読む**。⚠parser 側は `(?<!と)` の lookbehind で**この軸を意図的に除外している**（`parseSentencePart1.ts`）＝混ぜると軸①が壊れる |
| `O-290` | 🆕**S-2 実測 3カード**（登録4＝stale。`WXK03-014` は `coinReduction` で処理済み） | **キーを場に出すときのコスト条件・軽減**＝「場に出すためのコストは《コイン×0》になる」2枚（`WXK10-015`/`WXK11-012`）と「エナの色が3種類以上ある場合にしか出せない」1枚（`PR-K060`）。⚠**この3文は effectId が切り出されていない**（`_effect_srctext.json` に無い＝CSV 全文が正本）。専用 `PLACE_KEY_FROM_LRIG_DECK.coinReduction` はあるが**通常キープレイ経路（`BattleScreen.tsx:9074`／`KeyUseModal.tsx:35-56`）は印刷コインを直読み**する |
| `O-291` | 🆕**S-2 実測 1効果＝登録どおり**（`WXK11-020-E1`・MANUAL） | **`STRIP_OPP_ENA_MULTI_ENA` の後半**＝「対戦相手のエナゾーンのカードは対戦相手の効果を受けない」。消費地点は `costs.ts:1233` と `artsUseGate.ts:71` の2箇所だけで【マルチエナ】剥奪しか実装していない |
| `O-292` | 🆕**S-2 実測 3効果＝登録どおり**（`WXDi-CP01-006-E2`/`-007-E2`/`-008-E2`） | **通常効果からのコラボ**＝起動コスト「コラボライバー1人とのコラボ」。実行機構は `STUB{INTERNAL_DO_COLLAB}` にしかなく、`execStubPart3.ts:1311-1314` が「通常効果からの生成元が無い」と明記している |
| `O-296` | 🆕 **live 4効果中1件**（`WXDi-D09-H15-E1`）＋**原文にあって JSON に無い2件**（`WX11-051-BURST` / `WXDi-P01-039-E1`） | **`SET_BASE_LEVEL.until` が `'END_OF_TURN'` しか取れない**＝原文「次の対戦相手のターン終了時まで、基本レベルは3になり基本パワーは12000になる」の前半だけが**恒久化**する（隣の `POWER_SET` は `UNTIL_OPP_TURN_END` を持てるので**同じ文の2つのアクションで寿命が食い違う**）。🔑`O-293`（「次のエナフェイズ終了まで」）と**同じ「寿命の語彙が足りない」族**＝まとめて取ると安い |
| `O-297` | 🆕 **1効果**（`WXDi-P16-074-E2`） | **ON_BANISH のトリガー元シグニのゾーンを参照する軸が無い**＝原文「**同じシグニゾーンに【ゲート】がある**あなたのシグニ1体がバニッシュされたとき」。⚠**受け皿は2つ在るがどちらも軸が違う**＝`SAME_ZONE_HAS_GATE`（`execUtils.ts:2858`）は **`sourceCardNum`＝効果元**のゾーンしか見ず、`filter.inGateZone`（`effectEngine.ts:1207`）は**いま場に在るシグニ**の状態フィルタ＝**バニッシュ後のトリガー元**には当たらない（`prevOwnerState` が要る）。🔑**`FIELD_HAS_GATE` を使う live 10効果のうち 9件は原文が「あなたの場に【ゲート】がある場合」＝正しい**（誤用はこの1件だけ） |
| `O-308` | **6効果**（2026-09-09 第239バッチで Codex が「条件型が無い」と申告した分をまとめた） | **AUTO の発動条件に使える `Condition` 型が5つ足りない**＝①**正面のシグニが凍結状態**（`WXDi-D05-017-E1`。`FRONT_SIGNI` は **`ActiveCondition` 側にしか無い**＝`effectEngine.ts:99`）②**左右の隣接ゾーンにダウン状態の＜X＞がいる**（`WXDi-P11-060-E1`/`-E2`）③**このターンにコストか効果で自分のエナへカードが置かれた**（`WXDi-CP02-086-E1`。既存 `SELF_DECK_TO_ENERGY_THIS_TURN` は**デッキ由来だけ**）④**＜X＞限定のパワー合計**（`WXDi-P10-055-E1`。`FIELD_LEVEL_SUM{metric:'power'}` は全シグニ合計のみ）⑤**同じシグニゾーンに【マジックボックス】がある**（`WX24-P3-066-E1`。`SAME_ZONE_HAS_GATE/SEED/TRAP` の兄弟が無い）。🔑**条件側には STUB の道が無い**（`COND_STUB` は `execUtils.ts:2413` で無条件成立）＝**型＋`CONDITION_TYPES`＋`evalCondition`＋`checkActiveCondition`＋golden＋parser の6箇所**を必ず揃える（CLAUDE.md）。⚠**5つは互いに独立**＝1つずつ取ってよい（①③⑤は既存の兄弟条件のコピーで済む見込み） |
| `O-309` | **5効果**（`WXK06-025-E2`／`WXK10-044-E1`／`WXK10-091-E1`／`WXK06-028-E1`／`WXEX2-12-E4`） | **「対戦相手が選ぶ」の配線が一部の型・分岐にしか無い**＝`opponentSelects` は `BANISH`/`BOUNCE`/`SEND_TO_ENERGY`/`TRASH`/`TRANSFER_TO_DECK` の**型宣言には在る**が、🔴**Claude 実測（2026-09-09）＝`execTransferToDeck` の `HAND_CARD` 分岐は `selectOrInteract(..., false, …)` と第8引数がハードコード**で `opponentSelects` を無視する。⇒ **これは大機構ではなく「型は在るが分岐だけ抜けている」型**（今セッション4回踏んだ形）＝**1分岐ずつ潰せる**。⚠`SEARCH` の応答者（`WXK10-091-E1`＝相手デッキを相手が探す）と「置く順番を相手が決める」（`WXK06-028-E1`）は別軸で、そちらは新設が要る |
| `O-310` | **4効果**（`WX24-P4-022-E3`／`WXEX2-08-E4`／`WD20-006-E1`／`WXK07-018-E1`） | **両プレイヤー・複数ゾーンを横断した候補 pool が作れない**＝「対戦相手のシグニゾーン**と**エナゾーンから各色1枚まで」「両プレイヤーのエナから2枚」「自分と相手のシグニを1体ずつ」。`owner:'any'` は**同じゾーン種**の両側までで、**ゾーン種を跨いだ1つの選択**にできない。⚠`fieldCandidatesByOwner('any')` の拡張ではなく**選択 UI（`TargetScope`）側の新設**が要る |
| `O-311` | **5効果**（`WXDi-P00-037-E1`／`WXK04-003-E2`／`WXDi-P10-052-E2`／`WX24-P4-048-E2`／`WXEX2-29-E3`） | **「見た／選んだ集合」を後段の別処理へ渡す carrier が無い**＝`lastProcessedCards`／`storedTargetCards` は**直前ステップ限定**で、①同じ5枚を2つの行き先へ振り分ける ②E1 で選んだシグニを E2 が恒久参照する ③移動した札の**パワー／レベル**を後段の対象条件へ渡す、が書けない。🔑**`WXEX2-54-E2` は E2E で `lastProcessedCards` が空になることを実測済み**（Codex が採用を撤回した根拠）＝**この族は「JSON に書いても無言 no-op」になる**ので、必ず E2E で確かめる |
| `O-312` | **6効果**（`WXDi-D09-P04-E3`／`WXK07-033-E1`／`WXEX2-81-E2`／`WXK02-027-E1`／`WXEX2-54-E2`／`WXK05-029-E3`） | **動的な値を上限・一致条件へ渡す filter が無い**＝「宣言できる数字は**相手センタールリグのレベル以下**」「レベルが**自分の＜天使＞が持つ色の種類数**以下」「**自センタールリグのレベル+1/-1** ちょうど」「**場からトラッシュしたシグニのレベル-1**」「選ぶ4枚の**能力が同じ**」。🔑**`O-304`（「〜の数以下の」動的上限）と同族**＝**軸ごとに専用キーを増やす形が限界に来ている**という同じ結論。**まとめて「参照値で上限を決める」汎用型を1つ作るのが安い** |
| `O-313` | **4効果**（`WXK08-024-E2`／`WXK07-003-E1`／`WXK10-018-E2`／`WX24-P3-018-E1`） | **シグニゾーン内の「非シグニ札」を対象にできない**＝付属札（チャーム／アクセ／【マジックボックス】）・下敷きを**一般カードとして**選ぶ軸が無い。原文は「シグニゾーンにある**カード**1枚を対象とし」「指定1ゾーンの**非シグニ札すべて**」「付属札**か**下のカード1枚をトラッシュ（コスト）」。⚠既存は種類ごとの専用 action（`REMOVE_CHARM`／`TAKE_FROM_UNDER_SIGNI`／`OPEN_MAGIC_BOX`）に割れており、**「ゾーンの中身を一様に扱う」入口が無い** |
| `O-314` | **4効果**（`WXDi-P00-038-E1`／`WXK02-002-E3`／`WXK05-001-E2`／`WXK04-033-E1`） | **複数ターンにまたがる遅延状態が無い**＝「**次の次の**あなたのメインフェイズ開始時に同じゾーンが空なら表向きにする」「ターン終了時に**宣言値とアーツ使用回数を比較**して敗北させる」「**追加ターンの**メインフェイズ中だけ手札から出せない」「ターン終了時に**この効果でアクセにした札だけ**を手札へ戻す（最大3組）」。🔑**`INSTALL_DELAYED_TRIGGER` は「次の1回」までで、条件つき・複数ターン・対象追跡つきの遅延が書けない** |
| `O-315` | **3効果**（`WXK04-038-E1`／`WXK01-045-E1`／`WXEX2-39-E3`） | **ターン内の履歴条件が足りない**＝「このターンにあなたのエナゾーンへ**＜植物＞のシグニ**が置かれていた場合」「**このターンに場に出た**相手シグニ」「**コスト か ＜凶蟲＞の効果によって**」。🔑**`O-308` ③（エナへ置かれた履歴）と同族**＝既存の履歴カウンタは**デッキ由来・相手効果由来**など軸が固定で、**フィルタ付きの履歴**が取れない |
| `O-316` | **3効果**（`WXDi-P03-035-E1`／`WXDi-D09-H11-E1`／`WXDi-P16-002-E1`） | **ルリグ側の対象・条件がセンター固定**＝`UP`／`REMOVE_ABILITIES.targetsTriggerSource` は**シグニ専用**でアタックしたアシストルリグを扱えない／「**レベル3の**ルリグ1体を対象」（センター＋アシストから選ぶ）／「**全員が**レベル1以上」（`LRIG_LEVEL` はセンターしか見ない）。⚠**`triggerCondition.centerLrigOnly` の逆**＝アシストを含めて数える・選ぶ軸が無い |
| `O-317` | **7効果**（単発＝互いに無関係。着手は1件ずつ） | `WXK03-003A`＝**デッキ構築時**のアーツ上限（実行時ではない）／`WX16-002-E4`＝同一能力の【出】/【起】二重経路／`WX16-003-E1`＝「そのターン最初のアーツ」条件＋二択キャリア／`WX25-P2-008-E1`＝**攻撃元パワー限定**のターン中ダメージ防止（既存は次の1回のみ）／`WX25-P3-032-E2`＝ライフクラッシュを**トラッシュ移動**へ置換する `kind`／`WXEX2-10-E2`＝全領域への動的カード名変更／`WX14-003-E2`＝**CONTINUOUS なのに `action` が `ADD_TO_FIELD`**（CONTINUOUS は `executeAction` を通らない＝構造の取り違え） |
| `O-323` | **1効果**（`WX24-P2-050-E1`） | **did-it 連動の `usageLimit` が無い**＝原文「この能力で**実際にカードをトラッシュへ置くまで**再度誘発できる」に対し、live は素の `once_per_turn`＝**置けなかった場合も1回で打ち切られる**。🔑**「1回使った」ではなく「処理が実際に起きた」で数える軸**が要る（`STORE_LAST_PROCESSED_TARGETS` の有無で判定できる可能性がある＝着手時に確かめる）。2026-09-10 第247 で利用上限により未着手 |
| `O-318` | **5効果**（`WXEX1-72-E2`／`PR-305-E1`／`WXDi-P05-025-E2`／`WXDi-P07-006-E1`／`WXDi-P08-044-E2`。2026-09-11 に `WXDi-P04-007-E3` を `O-301` 側で解決＝6 → 5） | **遅延・置換の受け皿が足りない**＝`WXEX1-72-E2` は「**次にクラッシュされる相手ライフのバーストを無効化**」する遅延置換が無い。⚠🔴**残り4効果は登録時の理由が「遅延・履歴・置換機構が足りない」の1行しか残っていない**＝**着手前に再 triage が必須**（`triaged.txt` の該当行と原文を読み直す）。🔑`O-314`（複数ターンにまたがる遅延状態）と同族の可能性が高いので、**先に `O-314` を読んでから母集団を測る** |
| `O-319` | **4効果**（`PR-422-E1`／`WXDi-D02-19LAT-E1`／`WXDi-P09-045-E1`／`WXDi-P14-040-E1`） | **盤面を集計する条件型が足りない**＝`WXDi-D02-19LAT-E1`＝**全ルリグのレベル条件**（`LRIG_LEVEL` はセンターしか見ない＝**`O-316` と同族**）／`WXDi-P14-040-E1`＝「対戦相手の場に**凍結状態のルリグとシグニが合計3体以上**」（ルリグとシグニを**跨いで数える**軸が無い）。⚠**`PR-422-E1`／`WXDi-P09-045-E1` は理由が「条件型・収集機構が足りない」の1行のみ＝再 triage が要る** |
| `O-320` | **6効果**（`SPDi43-22-E1`／`WXEX2-13-E1`／`WDK10-015-E1`／`SP26-002-E1`／`WXDi-P05-086-E1`／`WXDi-P06-002-E1`） | **対象・参照・全域抑止の受け皿不足**＝`WXDi-P05-086-E1`＝**デッキ／トラッシュにある間もレベル1として扱う**（領域をまたぐ属性の読み替えが無い）／`WXDi-P06-002-E1`＝**レベル3のルリグ選択**（**`O-316` と同族**）／`WDK10-015-E1`＝**キー配置コスト**（**`O-290` と同族**＝同カードの `WXK10-015` は既に `O-290` に登録済み）。⚠**`SPDi43-22-E1`／`WXEX2-13-E1`／`SP26-002-E1` は理由が1行のみ＝再 triage が要る** |
| `O-321` | **3効果**（`WXDi-P11-046-E2`／`WXDi-CP02-009-E1`／`WXDi-P13-085-E1`） | **フィルタ付きのターン内履歴が無い**＝「**このターンにあなたがピースを使用していた**場合」／「**このターンにあなたのエナゾーンにカードが2枚以上置かれていた**場合」（既存 `SELF_DECK_TO_ENERGY_THIS_TURN` は**デッキ由来だけ**）／「**あなたの《ディソナアイコン》のカードの効果によって**ミルされた場合」（`triggerCondition.milledSourceStory` が無い＝`triggerCollect.ts:2216-2223`）。🔑🔴**`O-315`／`O-308`③ と完全に同族**＝**3項目を1バッチで取るのが桁で安い**（既存の履歴カウンタは軸が固定で、フィルタ付きの履歴が取れない、という同じ結論） |
| `O-322` | **2効果**（単発＝互いに無関係） | `WXDi-P14-061-E1`＝**宣言値の上限（相手センタールリグのレベル以下）＋相手手札の全走査＋対象が指定カードのときだけ覚醒**の複合（`AWAKEN_SIGNI` は `effectExecutor.ts:8546-8553` で効果元＝スペルを対象にして空振りする）／`WXDi-CP01-033-E1`＝**デッキ下のカード名を条件に効果全体を反復する制御フロー**（`DEFERRED_REPEAT_ON_REVEALED_NAME` に専用ハンドラが無く `execStub.ts:20` のログだけの既定処理へ落ちる） |
| `O-306` | 🆕 **1効果**（`WXEX2-10-E2`。第234バッチ後半の re-triage で発見） | **`DECLARE_CARD_NAME`×2 だけで「宣言されたカード名のカードを全領域で《サーバント　ＺＥＲＯ》に変更する」主要処理が無い**＝`DECLARED_NAME_TO_SERVANT_ZERO` 相当のハンドラは存在するが、既存実装は現存インスタンスへの永続 snapshot（`card_identity_overrides`）で、原文の「このターン」という期限にも「発動後に領域へ来たカード」への継続適用にも応えない＝ turn-scoped な PlayerState 規則が要る |
| `O-307` | 🆕 **1効果**（`WXEX2-12-E4`。第234バッチ後半の re-triage で発見） | **原文「対戦相手は自分のルリグデッキを裏向きで2つの束に分ける。あなたはどちらかの束を見て、その中からアーツ1枚をルリグトラッシュに置く」の秘匿分割 interaction が engine に無い**＝現在の JSON は `STUB{CAST_FROM_OPP_TRASH}`×2（相手ルリグトラッシュのカードを唱える別機構の流用）で意味が全く違う。相手が自分のルリグデッキを2束に分け、効果使用者が片方だけを見て1枚選ぶ pending/UI 自体が存在しない |

⚠**新しく母集団 1〜2効果の項目が出たらここへ足す**（速いレーンが既定＝§2.0）。
🔑**登録票の「受け皿が無い」は stale になる**＝着手前に必ず `grep` で消費地点を読み直す（`O-289` は S-2 実測で「機構不要」と確定して §5.0 の系統行へ降ろした）。

#### 索引 E. 計器の較正・掃除（**機構ではない**＝カードの挙動は変わらない）

🏁**残0**。⚠**消化しても実機の挙動は1件も変わらない**＝効くのは「**計器が嘘をつかなくなる**」ことだけ。**計器主導でバッチを選ぶ前**に取ると後続が楽になる（逆に、挙動を直したい回にここから取ってはいけない）。

#### 個別カードの機構待ち・監視項目（**worklist ではない**＝着手前の参照用）

> 🔴🔑**この節の使い方**（2026-09-06 に再整理）＝**ここに実作業を置かない。**
> 「worklist ではない」と書いた節に作業を置くと**索引にもキューにも計器にも出ない**＝
> 第182・第184 の2回とも、ここに埋もれた記述が**実測すると30効果／10効果の実バグ**だった。
> ⇒ **残ってよいのは (a) 母集団を実測したうえでの根拠つき defer と (b) 監視だけの既知の近似**。
> **stale が判明した記述はその場で消すか、索引へ `O-nn` で出す。**

**■ 根拠つき defer（着手前にこの理由が今も有効か再判定する）**

- **`WXDi-P05-006` choice① ＝着手禁止**（ピースカットイン割込み基盤）。**母集団 実測1効果/1カード**
  （`npm run census:population -- "カットインして使用できる"`・2026-09-06 再測）。
- **`WX20-Re20` ＝一体で要る**（選択数依存コスト・能力なし filter・任意複数配置UI・同一 instance 群の
  ターン終了時 trash）＝**部分実装しない**。**母集団 実測1効果/1カード**
  （`npm run census:population -- "選んだ数が[２2]つの場合"`・2026-09-06 再測）。
- **F. 保留**（core 改変が過大リスク）＝`WXDi-P00-026`（さんばかルリグ付与＝ルリグ再アタック未実装がブロッカー）／
  47枚の【使用条件】【チーム】（正規デッキ常時成立で機能等価＝保留妥当）。

**■ 既知の潜在結合（実害なし・触るときに読む）**

- ⚠**`executeSigniOnPlay` の `beatZones`（`Set<number>`）は `beat_signi` の「場ゾーン index」と
  `beat_signi_from_trash` の「トラッシュ index」で同じ集合を共有している。** 実データで両コストを併せ持つ効果は
  **0件**（全数走査）なので現状は実害なしだが、併記カードが出ると支払いが `ok:false` で**無言 abort** する。
- ⚠**付与ストア走査は `activeCondition` を評価しない**（`effectEngine.ts:3137-3142`）。今日の唯一のエントリ
  `WX24-P3-069-E1-G` は無条件なので実害なしだが、条件付き CONTINUOUS を付与する効果を足すときは
  effectsMap 側と同じ `checkActiveCondition` を通すこと。
- ⚠**UNKNOWN を実装するときは fresh parser の出力を先に測る**（H4 の教訓）＝fresh が主語を見ない
  `STUB DEPLOY_RESTRICT` を既に吐いており、live の `UNKNOWN` だけが「対戦相手は1体まで」への意味反転を
  止めていた（**採用した瞬間に過剰実行へ反転する型のドリフト**）。

**■ 監視だけしている項目（着手不要・壊れたら気付く）**

🔑**[LESSONS.md](./LESSONS.md) §4.8 へ移した**（2026-09-07）＝挙動が正しいと確認済みの近似・表示だけの不足・
恒久的な偽陽性の一覧で、**worklist ではなく「触るときに読む知識」**だから。⚠**ここへ書き戻さない**
（`cardProgressCensus.mjs` は §5.3 の本文からカード番号を拾うので、正しく動いているカードが `mech` に化ける）。
### 5.4 原文照合テール（構造混線）★🏁**残0＝新規に取る節ではない**

🏁**閉じた**（2026-09-06 第191バッチ）＝(a) `UNKNOWN` を含む効果／(b) `parseStatus:'PARTIAL'` 20効果／(c) 縦切り2件を全部処理し、機構が要る2件は §5.3 へ `O-266`／`O-267` として移した。**経緯の全文は [BUGFIXES.md](./BUGFIXES.md) の第187〜第191バッチ。**

⇒ **新しく「原文と JSON の構造がズレた効果」を見つけたときだけここへ足す。**
🔑**足すときの既定＝着手前に必ず現在の逆翻訳を読み直す**（登録時点の逆翻訳で立てた項目は腐る＝再照合した4件が4件とも実装不要、残2件が2件とも実装済みだった実測）。
**測り直し方**＝`parseCardEffects` → `mergeManualEffects` を回して `parseStatus==='PARTIAL'` と `"type":"UNKNOWN"` を数える使い捨て（`tmp_*`）で十数秒。live 側は `public/data/effects_*.json` を直接読む＝fresh と live の差が「配送が止まっている件数」。
⚠**完了判定について**＝**census が0になっても「すべてのカードが完璧」にはならない**（死角4つ＝参照解決の誤り／効果単位の粒度／JSON は正しいが engine 実装が違う／文間の実行順序）。**最終的な判定器は BEHAVIOR_AUDIT と §5.1 実機検証。**

### 5.5 低優先・保留（①〜③が尽きたときだけ取る）

- [ ] **CPU AI (g) 選択の精緻化**（旧 §8・盤面評価 v2 候補）＝(a)〜(f) は消化済み。⚠**§5.1 の実機返済を終えてから着手する**。⚠**CPU の対話自動応答は元から完備**（`BattleScreen.tsx:526`〜）＝効果を起動できれば対象選択まで自動で回るので、要るのは「起動する経路」と「何を撃つかの判断」だけ。
- [ ] **逆翻訳機の出力品質の残テール**（旧 §5b）＝混線テール実測823カードの再parse。§5.4 (iii) と同じ母集団。**英語 ID 漏れは `census:stubs` C群がゲート化済み**（0でゲート・増えたら exit 1）。
- [ ] **残る単発テール（原文とJSON構造がズレた混線／未構造化STUB）**＝**2026-07-02時点で「1 effect=1クリーンSTUB」で原文抽出できるものは全消化済み（444→367→その後の作業でカード母集団が変動し2026-07-12実測823・続き87で機械分類済み）**。effect構造そのものが原文とズレた混線で、1つのSTUBを原文化しても同 effect 内の他のズレが残り原文一致にならない＝decompilerの原文抽出では対応不能なものが大半。**effects JSON の再parse（機構実装・データ層修正）が本筋＝Opusタスク13**。系統別内訳（16テーマ・カード数の多い順＝デッキ操作系184／パワー修正系165／手札系102／トラッシュ系75／対戦相手コスト系63／エナ系50／ライフ系48／シグニ配置系48／ルリグ系36／能力付与系31／ガード・アタック制限系26／ソウル・アーツ系15／ウィルス系10／色・クラス系4／ゲーム除外系3／チャーム系1／その他54）は`docs/_stub_leak_classification.txt`参照（続き87・Sonnet）。進め方＝1カードずつ effects JSON を原文どおりの構造に手修正→逆翻訳が原文一致するか確認→smoke/golden/fuzz→push（**原文コピーでの一括潰しは禁止**＝実装未完成を隠蔽し検証目的に反する）。
- [ ] **🆕生JSON漏れ（`【シャドウ:{"levelGte":3}】` 形）72件**（2026-08-07 続き366 で観測）＝`シャドウ:{…}` は**JSON データ側の符号化**（engine もこの文字列を読む）なので、表示だけ日本語化するには decompiler に整形器を足す。`levelGte/levelLte/powerLte/cardType/color` の素直な形のほか `selfPowerHalfLte` `lrigTrashArtsColor` `downerLrigLevel` `declaredNumberPowerEq` `declaredColor` `artsCostLte` `selfColor` `selfPowerLte` など**約20種**あり、**1種ずつ原文照合が要る**（【シャドウ（レベル3以上）】等の原文表記に合わせる）。⚠英語**条件**ID漏れは続き366 で残0にした。
- [ ] **`doPhaseAdvance` の pure 抽出は「やらない」を既定**（費用対効果逓減と結論済み）。CPU 統一で必要になった部分だけ最小限切り出す。
- [ ] **BEHAVIOR_AUDIT キューの消化（⛔休眠）**＝`node scripts/_bqTriage.mjs` で高シグナル選別 → `npm run audit -- --id <CardNum>` で目視 → 「真no-op／シナリオ空振り／STUB未実装」に仕分け。**続き133 で高シグナル22件を精査して真 no-op バグ0件＝枯渇**。再開するなら**監査ツールの構造的盲点フィルタの実装が先**（低収量見込み）。
- [ ] **リリース判定**：fuzz 重め（`npm run fuzz -- --games 2000 --moves 80`）＋実機 PvP/CPU 通し対戦スモークをリリースゲートに。DESIGN §5 の手順（version bump→CI→push→`npx vercel --prod`）で本番反映。

---

## 6. 恒久指標（最新1ブロックのみ）

> **運用**＝この節は**「いまの数字」だけ**を置く。新しく作業したら ①上のブロックを [PLAN_DETAIL.md](./PLAN_DETAIL.md) の恒久指標アーカイブへ移す ②今回の値へ書き換える。⚠**溜め始めたら破綻する**（続き550 の整理時点で計測行15本＋ポインタ37本まで膨れ、cold start が最初に読む節が一番古い状態だった）。
> 🆕🔴**2026-09-01 改定＝3計器だけでは進捗が表示できなくなったので「在庫2本」を併記する**（理由は §3 の同日改定）。**3計器は底を打った＝これ以上は下がらないので、動かないことを「停滞」と読まない。**

- **2026-09-11（第260バッチ・Opus 5 単独＝索引 A を残1へ・本ブロックが直近の正）**
  📊**進捗3計器**＝**Sheet1 要対応 1 / 863**（据置・実測）｜**台帳 残 OPEN 0**（据置・実測）｜**census 高シグナル 1 / BASELINE 1**（据置・実測）。
  ⚠**3計器がどれも動かないのは想定どおり**＝直したのは**支払いの提示順**と**離場置換の読み手**で、
  どちらも「原文の語彙は live に出ている」層＝census も台帳も見ていない。`O-299` の3軸は **live JSON を1バイトも変えない**（engine のみ）。
  📦**在庫**＝🔥**実装キュー 123効果**（据置）｜**候補プール 7**｜**機構 worklist 26項目**（**A 1**／B 0／G 25。
  `O-298` をクローズし `O-326` を新設したので差し引き0）｜🏁**実機 残0**｜除外リスト 125効果。
  🔧**ゲート（全緑 ✅）**＝**golden 3935 PASS**（3929 → 3935）／smoke 10744 OK ／ fuzz 0 ／ census 1 / BASELINE 1 ／
  census:stubs A群 0 ／ census:enginetext A🔴 0行 ／ census:costtext A🔴 0規則 ／ manual field loss 0 ／ lint 0 errors / 254 warnings。
  **ratchet の較正なし。反転確認は6通り実行**（`handReveal` 抜き／parser 規則の停止／funnel の3軸をそれぞれ外す）。
  📈**live の per-effect 差分＝4 effectId**（`WXK10-080-E1`／`WXDi-P16-048-E1`／`WX16-029-TRAP`／`WX17-044-TRAP`）。

## 付録B. 偽陽性パターン（脱落疑いに出るが**直さない**）— 毎回まず除外

1. **使用条件＋本体**（「このカードは〜の場合にしか使用できない」が前置き）＝条件として正しく表現済み。【真の偽陽性】
2. **CHOOSE/チェインの1文圧縮**＝「以下の[N]つから[M]つ（まで）を選ぶ」で改善済み（BUGFIXES③）。択肢が全部出ていれば機能的には正しい。
3. **REVEAL_AND_PICK / LOOK_AND_REORDER の文法崩れ**＝主要系統は是正済（BUGFIXES上部）。残りは§5.5の低優先テール。
4. **ルール注記**（「（コストのない【出】能力は発動しないことを選べない）」等）＝効果ではない。
5. **アンコール/ベット注記のみ**訳に出ない＝本体が合っていれば正しい。
6. **BET_MECHANIC STUB**＝§5.5（機構待ち）。
7. **owner:any の一括変換は禁止**：POWER_MODIFY/BANISH の `owner:'any'` は大半が正当（「シグニ1体を対象とし±N」＝自他選択／「すべてをバニッシュ」）。原文に明示主語があるものだけ個別是正。
8. **`[STUB:id]` を含むからとスキップしない**：実装済みハンドラのタグ表示。ただしハンドラがカード全体を覆うか（◎）／断片だけで残りを落としたか（実バグ）はタグでは区別不可＝各外れは個別検証。[[stub-means-implemented]]
9. **LIFE_BURST 内 `CONDITIONAL{IS_MY_TURN}`** は実害なし（常時true＋「そうした場合」特別処理）。修正不要。

## 付録C. 触らなくてよい/枯れた系統（調査済み）

- 強制アタック＝実装済み（未配線は WX12-010 複雑レゾナのみ）。BURST丸ごと欠落＝残0。保護系キーワードのowner誤り＝残0。
- 同型★（`grouped_all.txt`）＝**枯れた・常に0維持**。残1件 `WX04-056` は無害な表現差（任意）。
- 「あなたのアタックフェイズ開始時」系（self約407件）は**全再生成禁止**（約90枚退化）。個別にtiming/triggerScopeを直す。

### 付録C-2. 残・大型機構オーナー表（ほぼ完了の台帳）

着手前に**この表の「状態」を `着手中(担当名)` に更新してコミット**（重複防止）。実装の型は §3「機構実装の型」に従う。

| 機構 | 影響 | リスク | 状態 |
|---|---|---|---|
| 引用AUTO付与（`GRANT_QUOTED_AUTO_ABILITY`） | 中 | 中 | **表現完了＋engine精緻化(B4)着手済**＝引用【自】/【常】能力を実発火（自場シグニ・ターン限定・parse成功時のみ）。残＝permanent/相手付与対応・誤パース是正（約30枚は原文に引用無しparser案件）。⚠要実機検証 |
| ~~SET_TRAP／動的閾値フィルタ／遅延条件トリガー／《相手ターン》《自分ターン》AUTO基盤／ビート機構Phase1-7／傀儡場出し汎用化・levelLteLastProcessed~~ | — | — | **✅完了**（詳細 [PLAN_DETAIL.md](./PLAN_DETAIL.md) 付録C） |
| engine未配線 timing 群の実機配線 | 大（~15 timing・R33-R58） | 高 | **✅C1全配線完了**。残るは実機検証のみ（§5.1参照）。 |

実装済み機構の履歴：コスト増加・ライフクラッシュ履歴・LOOK_PICK_CHAIN field宛先・リコレクト系統・改造素材機構・引用能力付与型・保護/制限系STUB・アーツコスト軽減句 は `BUGFIXES.md` 参照。

---
**関連**：[DESIGN.md](./DESIGN.md)（設計方針）／[PLAN_DETAIL.md](./PLAN_DETAIL.md)（消化済み履歴）／[PLAN_PROGRESS.md](./PLAN_PROGRESS.md)（過去セッション要約）／[BUGFIXES.md](./BUGFIXES.md)（修正記録）／[BEHAVIOR_AUDIT.md](./BEHAVIOR_AUDIT.md)／[SEMANTIC_AUDIT.md](./SEMANTIC_AUDIT.md)／[CODEX_GUIDE.md](./CODEX_GUIDE.md)／[BATTLE_CONTROLLER.md](./BATTLE_CONTROLLER.md)／[effects-json-guide.md](./effects-json-guide.md)／[STUBS.md](./STUBS.md)
