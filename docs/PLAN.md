# PLAN — 開発計画（唯一の正）

> **このファイルの読み方**＝新セッション（cold start）は **§1 現在地 → §2 作業の流れ → §5 作業キュー** の順に読む。設計方針は [DESIGN.md](./DESIGN.md)、修正記録は [BUGFIXES.md](./BUGFIXES.md)（新しいものを上）。
> **ここには「現在地・流れ・ルール・教訓・生きている worklist」だけを置く。** 消化済みの経緯・完了項目の全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md)、過去のセッション要約は [PLAN_PROGRESS.md](./PLAN_PROGRESS.md)、一次記録は [BUGFIXES.md](./BUGFIXES.md) にある。**クローズした項目はこのファイルから消す**（ID も残さない＝退避先と git 履歴が正）。
> **2026-08-23 に全面再編**（167,481字 → 約半分）。①フェーズ軸だった旧 §5(P1)/§6(P2)/§7(P3) を廃止して **§5 の単一キュー**へ統合（入口が3つあると作業順がばらつくため）②**Opus/Sonnet のモデル分担を廃止＝Opus 単独運用**③クローズ済み記述 179行・67,632字を削除し、そこに埋まっていた教訓を **§4 教訓集**へ集約（🆕2026-09-06 に [LESSONS.md](./LESSONS.md) と [DRIVE_TRAPS.md](./DRIVE_TRAPS.md) へ分離）④**§2「作業の流れ」を新設し、実機検証を1巡の中に含めた**（再編前の PLAN 全文は git 履歴＝再編直前のコミットにある）。
> 🆕**2026-09-01 に再編（第2回）**＝**3つの進捗計器が同時に底を打った**ことを受けて、**計器主導 → 機構主導**へ構造を寄せた。①**§5.3 を「登録順の巨大テーブル」から「母集団順の索引 A〜F」へ**作り替え、登録票の全文（82項目・68,401字）を PLAN_DETAIL へ**無改変で退避** ②**§5.2（意味照合）を本線から降ろし**、§5.4 の (i) 配線ギャップ・(ii) 機構ギャップを **§5.3 へ統合**（機構の置き場を1つにした） ③**§5.1 のクローズ済みエントリを教訓7本へ圧縮** ④**進捗指標に「在庫2本」（機構残／実機残）を追加**（§3・§6）。**PLAN 本文 195,282字 → 100,385字（-49%）。**

---

## 1. 現在地（直近1セッション）

> **運用**＝この節には**直近1件の要約だけ**を残す（入れ替え式）。新しく作業したら ①いまの要約を [PLAN_PROGRESS.md](./PLAN_PROGRESS.md) の先頭へ移す ②この節を今回の要約へ書き換える。**溜めない**（溜めると cold start が最初に読む節が一番古くなる）。
- **セッション（2026-09-09・第233バッチ・Sonnet 5＝Codex 委譲）＝機構不要の一点物16効果を `.codex-work` へ委譲して消化**
  **作業単位**＝ユーザー指示「PLANをよみ。機構のいらないバグ修正をcodex-workとcodexに投げる」。

  ✅**実装キュー（§5.0・triage 済み BUG 433件の在庫）から、新しい engine 機構が不要と事前確認した2系統16効果を選定**
  （A群＝「宣言した数字と同じレベル」条件欠落8効果／B群＝「トラッシュに置いてもよい」の任意性欠落・対象誤り8効果。
  受け皿は両群とも実在確認済み＝`TargetFilter.levelEqDeclaredNumber`／`LOOK_AND_REORDER.reorder`／
  `TrashAction.optional`／`MILLAction.optional`）。
  ✅`docs/CODEX_GUIDE.md` の指示書テンプレに沿って作成し、`CODEX_HOME=.codex-work codex exec` で投入（バックグラウンド）。
  ✅**Codex の申告＝16/16採用・見送り0・差し戻し対象0**。golden 新規19本（fresh/live 両読み＋任意skip時の反転含む）。
  ✅**Claude 側で独立検証**＝`git diff` の effectId 単位差分が申告どおりちょうど16件（`PR-K022-E1-G` は親
  `PR-K022-E1` として計上）／`typecheck`／`npm run gates` を独立実行して全緑（census 高シグナル 0/0・lint 0 errors/254
  warnings＝投入前と同値）／`golden -- --only "第233"` 19/19 PASS。**差し戻し0で採用。**
  ✅**副産物＝engineの配線漏れを1件発見・修復**（`levelEqDeclaredNumber` が `LAST_PROCESSED_MATCHES` と手札
  `TRASH` の2経路で未解決だった＝`nameEqDeclaredName` と同じ fail-closed パターンで解決を追加。新しい
  action/condition/state 型は0＝「機構不要」の見立てどおり着地）。
  ✅`src/engine/`・`src/data/effectParser.ts` を触ったが `src/screens/` は不触＝§2.2 により実機検証は不要。

  🔑**この巡の要点＝「機構不要」の絞り込みが Codex 委譲の成否を決めた。**
  委譲前に Claude が①受け皿（型・フィールド）の実在をコード上で確認②原文・現行JSON・triage出典を効果ごとに
  表で用意③既存機構の名前を指示書に明示、まで済ませたことで、Codex 側の探索コストが下がり差し戻し0で着地した。
  **「機構がいらない」の判定自体が事前調査の主要コスト**（今回は `grep` と型定義の確認で15分程度）。

  **次の一手**＝**§5.0 実装キュー残318効果から同様に「機構不要」の系統を追加選定して次バッチを投げる**
  （`node scripts/archive/semanticAuditBugList.mjs --list --grep "<語>"` で系統を探す）。
  一点物が尽きたら §5.3 索引 A（`O-299` 置換効果21効果 → `O-300` `GRANT_LRIG_ABILITY` の対象20効果 →
  `O-298` 自分の任意コスト先取り15効果 → `O-301` 遅延10効果）。**母集団はすべて上限値**なので §2.1 ② で割り直す。

## 2. 作業の流れ（1巡の定義）★このプロジェクトの唯一の作業単位

> **2026-08-23 制定。** 従来は「実装（旧 §6）」と「実機検証（旧 §7）」を別セクション・別モデルに割っていたため、**実装だけが進んで実機検証が溜まった**（実測＝V エントリ13件＋21バッチが未検証のまま）。§5.1 自身が「⚠寝かせるほど『バグか、シナリオの腐りか』の切り分けが高くつく」と警告していた失敗モードに、運用そのものが乗っていた。**以後は実機検証を1巡の中に含める。**
>
> 🆕🔴**2026-08-29 改定（ユーザー決定）＝「1効果に1時間かかっている。ゲートが大きくなり莫大な時間がかかっている。難しく考えすぎではないか」への回答。**
> 実測すると **③実装 56行 : 検証 282行 : 簿記 245行**（`O-123` の1コミット＝**1 : 5 : 4.4**）で、
> **`gates` 116秒のうち golden が102秒（88%）**、しかも1巡で `gates` を4回回して golden だけで7分溶かしていた。
> さらに決定的なのは **`census:clusters` の実測＝1テンプレあたり1.14効果**（2026-08-29 時点＝高シグナル571効果 / テンプレ501種）。
> 🆕**2026-08-30 に測り直しても結論は同じ**＝**340効果 / テンプレ330種＝1.03**（母数が 571→340 に減っても比は改善しない）。
> ⇒ **parser に regex を1本足しても平均1効果しか直らない。「同型が一括で直る」という parser 優先の前提が尽きている。**
> **以後は §2.0 の2レーン制を既定とし、`manualEffects.ts` への手書きを「速いレーン」として正面から使う。**

### 2.0 実装レーンの選び方 ★①の直後・③に入る前に決める（2026-08-29 新設）

**先に「どちらのレーンで書くか」を決める。** これを決めずに parser へ手を伸ばすのが、1効果1時間の主因だった。

| レーン | 使う条件 | 書く先 | 検証 | 目安 |
|---|---|---|---|---|
| **速い（既定）** | **同型が2枚以下** | **`manualEffects.ts` に手書き** | `build:effects` → **逆翻訳を目視** → `golden -- --only` ＋ `smoke`（計 約5秒） | **1件 5〜10分** |
| **遅い（例外）** | **同型が3枚以上** ／ 新しいアクション型・条件型を足す ／ `src/engine/` を触る ／ `src/screens/` を触る | `effectParser.ts` ほか | 現行フル（§2.1 ③〜⑤） | 1件 30分〜数時間 |

#### なぜ既定を反転したか（2026-08-29 実測・**次に読む人はここを消さないこと**）

- `npm run census:clusters` ＝ **高シグナル 571効果 → 文型テンプレ 501種**。上位クラスタでも 7件・5件・5件・3件…で、**残りはほぼ全部が一点物**（1テンプレあたり **1.14効果**）。
  🆕**2026-08-30 再実測＝340効果 / 330テンプレ＝1.03**。**census を 460→245 まで落としても比は 1.14→1.03 と悪化した**＝
  **減ったのは「まとまっていた分」で、残っているのは筋金入りの一点物**。⇒ **この判断は今後も覆らない見込み。**
- ⇒ **21,417行の `effectParser.ts` に regex を1本足して、golden 2,960本（102秒）で他の1万効果への波及を確かめて、直るのが平均1.14効果。** 割に合わない。
- 「parser 優先」（旧方針）は**同型が10件20件あった時期には正しかった**。その在庫が尽きただけで、判断自体は間違っていなかった。**在庫が戻れば（新カード追加で同型が増えれば）遅いレーンへ戻す。**
- 参考＝live 10,700効果のうち MANUAL/PARTIAL は **675件（6.3%）**。20%まで増えても構造上の問題は無い。

#### 🔴 やってはいけないこと＝「移設だけ」の manual 化

**parser が出した JSON を中身そのままコピーして `manualEffects.ts` へ移すのは禁止。**

- **中身が1バイトも変わらないので何も直らない。**
- `vocabCensus.ts` は **STUB/MANUAL を含む効果を高シグナルから免除する**（同ファイル冒頭に明記）＝**間違いが計器から消えるだけ**。
- 収穫マージは MANUAL/PARTIAL を**効果単位で不可侵**にする（CLAUDE.md）＝**その後の parser 改善が永久に届かない**。

⇒ 速いレーンで書くのは **「原文を読み直して、正しい JSON を手で書く」** ことであって、移設ではない。
⚠ **`manualEffects.ts` のトップレベル効果は `parseStatus:'MANUAL'` か `'PARTIAL'` のみ**（`manual-fields` ゲートが強制）。

#### 母集団はどこから取るか（🆕**2026-09-01 に全面改訂＝計器から取る時代は終わった**）

🔴**旧版はここに「census 高シグナル／`behaviorAudit --queue`／意味照合台帳」の3計器を並べて「確定しているものから取れ」と書いていた。その3本が同時に底を打った。**

| 計器 | いまの実測 | 母集団として使えるか |
|---|---|---|
| census 高シグナル | **11 / BASELINE 12**（1872 → 460 → 245 → 91 → 11） | ❌**使えない**。⚠**残り11は「新しい機構の縦切り」だけ**と続き749 が全数確定済み。🔴**「census に出ている＝直すべき」でもない**（先行セッションが理由つきで見送った効果が混ざっており、**golden の「見送り契約」が唯一の歯止め**） |
| `census:cards -- --sheet 1` | 要対応 **18 / 863**（うち `mech` **18**） | ❌**使えない**＝**即着手可能 0**。18枚すべてが §5.3 待ち |
| 意味照合 段2 台帳の残 OPEN | **13**（影響カード12／効果12） | ❌**使えない**＝残りは全件が `src/screens/` か新 engine 機構待ち。🔴**ただし「全数 triage 済みだから掘り尽くし」を信じないこと**＝第200バッチで 24 → 13（9件が閉じ忘れ／1件 FP／1件は engine 0行で直った） |
| `behaviorAudit --queue` 要review | 566 | ⚠**疑いのまま**（ヘッダ自身が「対象不在の空振りも含む」と明記）。⚠census との重なりは **28件だけ**＝**ほぼ別のものを見ている**＝どちらも選別に時間がかかる |

🆕🔑**⇒ 2026-09-01 以降の母集団は「§5.3 の索引」そのもの。** 計器に「次に何を直すか」を訊くのではなく、
**§5.3 索引 A（母集団2桁）→ B（3〜8効果）→ C（1〜2効果）の順に取り、着手前にその項目の母集団だけを実測する**。

🔴🔑**在庫の総量を測る手段は、いま無い。** 「原文にフレーズがあるのに live にキーが無い」型の計器（`censusMechPopulation` / `census:wiring`）は
**受け皿の"別名"を全部知らないかぎり必ず過大に出る**（2026-08-30 に9回連続で過大に外した）。**知らない穴を教えてくれた唯一の計器だった意味照合台帳は消化済み。**
⇒ **「あと何割か」に答えようとしない。** §5.3 の索引を上から潰し、**残項目数（§6 の在庫2本）だけを進捗として報告する。**

⚠**それでも段2 のバッチを始めるときは `node scripts/archive/semanticAuditRecheck.mjs` を先に回す**（→ §5.2）＝
台帳は手で追記しないと減らないので**「直っているのに OPEN のまま」が溜まる**（実測＝434 のうち **48件が既に済み**）。**実装より圧倒的に安い在庫なので先に回収する。**

### 2.1 1巡＝1バッチの手順

| # | 工程 | やること | 抜けたときに起きること |
|---|---|---|---|
| ① | **取る** | §5 の**上から1件**。並行して別の節から取らない。🆕⚠**`§5.3` だけは例外**＝あの節は**登録順**なので、取る順は **§5.3 冒頭の「取る順」表**に従う（上から取ると1枚の項目を30件消化するまで、288効果の `O-51` のような大物に着かない） | 作業順がばらつき、どれも終わらない／**規模を見ずに登録順で取ると、影響1枚の項目に時間を溶かす** |
| ② | **数える** | 着手前に**母集団を実測**する（[LESSONS.md](./LESSONS.md) §4.1）。worklist の件数・登録票の見立ては標本であって母数ではない | 見立てが外れて手戻り（実測で**9回連続**外れた実績） |
| ②' | **レーンを決める** | 🆕**§2.0 の表で「速い／遅い」を決める**。同型2枚以下なら `manualEffects.ts` へ手書き | 一点物のために parser を触り、golden 全件で波及を確かめる（＝1件1時間） |
| ③ | **実装** | 決めたレーンで書く。**型を足すなら「型＋両評価器＋golden」の3点セット**（[LESSONS.md](./LESSONS.md) §4.2） | 型だけ足すと**無条件成立**に落ちて全ゲート緑のまま意味が壊れる |
| ④ | **ゲート** | 🆕**速いレーンは1件ごとに `golden -- --only` ＋ `smoke`（約5秒）だけ。`gates` は10件たまってから1回。** 遅いレーンは従来どおり `npm run gates`。parser を触ったら `npm run regen` ＋ live の A/B 差分で「意図した件数だけ変わった」を機械確認 | 退化が素通りする（無検証置換で約90枚退化の前例）／逆に**1件ごとに `gates` を回すと golden 102秒を毎回払う** |
| ⑤ | **実機** | 🆕**`src/screens/` を触った回だけ**（判定は §2.2）。`node scripts/verifyBattleDrive.mjs <シナリオID>` で**新規1本＋関連する回帰**。⚠**着手前に [DRIVE_TRAPS.md](./DRIVE_TRAPS.md)（§4.4）を読む** | UI/タイミング層の退化が素通りする（`O-123` の実例）／逆に**JSON を書き直すだけの回に130行のドライバを書くと固定費が本体を超える** |
| ⑥ | **簿記** | 🆕**速いレーンは10件まとめて1回**（BUGFIXES は**真因1行／影響枚数／検証コマンド／反転確認の有無**の約10行でよい）。遅いレーン・機構変更は `/baton` フル | 次セッションが現在地を誤読する／逆に**1件ごとに BUGFIXES 100行＋PLAN 3ファイル入れ替えを書くと簿記が本体を超える**（実測＝docs 245〜722行/コミット） |
| ⑦ | **通知** | **リーダーへ停止通知メールを送る**（§2.5）＝**処理が終わって次の指示待ちで止まったことを知らせる**。⚠**push が要る作業をしたなら push を終えてから**送る。⚠**1巡が完了しなくても（測定だけ・質問待ち・ブロック）手が止まったら送る** | 離席中のリーダーが**PC に戻る合図を受け取れず、待ち時間が丸ごと無駄になる**・未 push の状態を「完了」と誤報する |

🆕**④の補足（2026-08-25）＝開発ループでは `npm run golden -- --only "<テスト名の部分文字列>"` で絞ってよい**（全2,770本で約89秒 → 絞ると約1.5秒。名前一覧は `npm run golden -- --list`）。🔴**ただし1巡を閉じる前に必ずフィルタなしで全件を回す**＝`goldenTest.ts:155` の POOL カーソル `cursor` が**テスト間で共有される可変状態**で、**スキップすると後続テストが引くカードが変わる**ため、フィルタ実行の PASS/FAIL は全件実行と等価ではない（**両方向に化けうる**）。⚠`npm run gates` と CI は常に全件なので、そちらは何も変わらない。

🆕**④の補足その2（2026-08-26 続き674・`O-73` で実測）＝`BASELINE_HIGH` を更新する回は `npm run gates` を2回回さない。**
census の高シグナル数が減ったかどうかは **`npm run census` 単独（約5秒）**で分かる。**先に census だけ回して
`BASELINE_HIGH` を実数へ直し、そのうえで `gates` を1回**にする。⚠**gates の所要は golden（約155〜170秒）が支配的**なので、
「gates で census が落ちる → baseline を直す → gates を回し直す」をやると **golden をまるごと1回余計に払う**
（実測＝O-73／O-89／O-91 の3巡ともこれで各4分ほど溶かした）。

### 2.2 完了の定義

🆕**2026-08-29 改定＝「⑤実機まで毎回」をやめ、`src/screens/` を触ったかで機械的に決める。**

| 触ったもの | 完了に要るもの |
|---|---|
| **`src/screens/` を触った**（UI・支払い・提示ゲート・モーダル・タイミング） | 🔴**⑤実機まで。** golden で反転確認を取っていても**実機を省かない** |
| **`src/data/` `src/engine/` `public/data/` だけ**（parser・engine・JSON） | **④まで**（`gates` 緑＋ live の A/B 差分）。実機は不要 |
| **新しいアクション型・条件型・機構を足した** | 🔴**⑤実機まで**（型は足したが engine のどこも消費していない、を実機で炙る） |

- **golden/smoke/fuzz が守れない層が実在する**＝`persist.commit` と React 反映のタイミング差、UI の選択リセット、モーダルの候補絞り込み。**この層の唯一の防御が実機シナリオ**（回帰トリップワイヤとして残す）。
- 🔑**2026-08-28 `O-123` が実例**＝壊れていたのは `artsUseGate.ts`（`src/screens/`）の1行で、**JSON にも逆翻訳にも1バイトも現れず、3つの計器がどれも動かなかった**。この層に触ったら実機は必須。
- ⚠ 逆に **`O-112`（`triggerCollect.ts` を `at(-1)` に絞るだけ）のような engine 内部の修正に実機は要らない**。golden 1本で十分に固定できる。
- 1巡のスコープは**⑤まで1セッションで回せる大きさ**に切る。効果数で言えば **速いレーンは10件／バッチ**、**遅いレーンは 15〜20効果／バッチ**が目安（4効果のバッチは固定費を満額払って割に合わない・実測）。
- **④まで（実機不要と判定した回）は、それが「完了」**。「要実機検証」と書いて次セッションへ送ることは**しない**（判定した理由を BUGFIXES に1行書く）。

### 2.3 実機検証の要否

🆕**2026-08-29 改定＝要否は §2.2 の表（触ったディレクトリ）で決まる。** ここはその補足。

- **`src/screens/` を触ったのに省略してよい唯一の条件**＝**実機から原理的に観測できないと分かっている**とき
  （例＝`optional`「〜してもよい」の【自】は CPU 側が自動応答するため画面に出ない）。
  このときは **golden で両方向を固定し、理由を §5.1 に `V-<次番号>` として登録**する。**「時間がないから次回」は該当しない。**
- **`src/data/` `src/engine/` だけの回は、そもそも省略ではない**（§2.2 の表で最初から不要）＝`V-nn` の登録も要らない。

### 2.4 作業中にバグを見つけたとき

**その場で直すのが既定。**（Opus 単独運用になったので、旧「Sonnet が観測して積む → Opus が直す」の交互サイクルは廃止。常設受け口だった「Opusタスク12」も廃止＝在庫0で閉じた。）

**登録して先送りしてよいのは次の2つだけ**：
1. **新しい機構が要る**（型・評価器・engine の新設を伴う）＝§5.3 に `O-<次番号>` で登録。
2. **母集団が別**（いま回しているバッチと違う語彙・別の入口）＝§5 の該当節へ1行で登録。

⚠**登録するときは「見立て」ではなく実測値を書く**（[LESSONS.md](./LESSONS.md) §4.1）。

### 2.5 停止通知メール（工程⑦）★どのアカウントの Claude でも共通

**2026-08-24 続き645 制定・2026-08-25 続き656 に目的を訂正。**
この VSCode では **3つの Claude アカウントが並行して動く**
（既定の `~/.claude` ／ `CLAUDE_CONFIG_DIR=C:\Users\zerom\.claude-alt` ／ `…\.claude-karka`）。

🔴**このメールの目的は「1巡の完了報告」ではない。**
**リーダーは離席している。Claude の処理が終わって次の指示待ちで止まったことを知り、PC に戻ってくるためのトリガーである。**
⇒ **判断基準は「1巡が完了したか」ではなく「Claude の手が止まって、リーダーの入力を待っている状態になったか」。**

**送る（＝手が止まったら毎回）**
- 1巡が⑥まで終わった
- 実装まで至らず**測定・調査だけで終わった**
- **質問・判断待ち**で止まった（どの案で進めるか等）
- **エラー／ブロッカー**で進めなくなった
- ユーザーの指示が**そこで終わっている**（続きの指示が要る）

**送らない**
- まだ作業を続けるとき（途中経過では送らない＝戻ってきても続きが動いていて意味がない）

```bash
node C:/Users/zerom/.claude-shared/notify-mail.mjs --subject "[WixossReact] <状態> — <一行主題>" --body "<本文>"
node C:/Users/zerom/.claude-shared/notify-mail.mjs --subject "件名" --body-file <path>   # 長文はファイルで
node C:/Users/zerom/.claude-shared/notify-mail.mjs --check                                # 送信せず設定だけ確認
```

- **件名の先頭で状態が分かるようにする**＝`完了` ／ `指示待ち` ／ `要判断` ／ `ブロック`。
  リーダーは**件名だけ見て「今すぐ戻るか」を決める**ので、ここを曖昧にしない。
- **置き場所**＝`C:\Users\zerom\.claude-shared\`（`notify-mail.mjs` ＋ 認証情報 `notify.env`）。
  ⚠**3つの config dir とも、このリポジトリとも独立**させてある。**`notify.env` をリポジトリ内へコピーしない**（`git push` で漏れる）。
- **実行許可**は3アカウントの `settings.json` に登録済み（`Bash(node C:/Users/zerom/.claude-shared/notify-mail.mjs *)`）。

**本文に必ず入れる**（メールだけ読んで**次の指示が出せる**粒度にする）：
①**いま何が終わっていて、何が終わっていないか** ②**次の選択肢**（複数あるなら並べる。推奨があれば書く）
③**リーダーの判断が要る点**（あれば。無ければ「なし」と書く） ④**commit SHA と push 済みか**（コード変更があった場合）

**実装まで進んだ回は、上に加えて**：
⑤直したバグの**真因**（症状ではなく） ⑥**ゲート数値**（golden / census / smoke / fuzz / lint・前回からの増減）
⑥′🆕**進捗は3つの計器を併記する**（§3・2026-08-27 ユーザー決定）＝**Sheet1 要対応枚数／台帳 残 OPEN／census 高シグナル数**。
　**動かなかった計器があるなら理由を1行で書く**（「11効果直したが Sheet1 は 0 減。直した中で Sheet1 は1枚だけで、そのカードは別の finding が残る」等）
⑦**実機シナリオ名と PASS 数・反転確認の有無** ⑧**follow-up として登録した `O-nn`**

⚠**踏んだ落とし穴（再発させない）**：
- 🔴**Resend は独自ドメインを verify するまで「登録アドレスと文字列一致する宛先」にしか送れない**＝`zeromainiaia@` では **403**。
  **`zeromain.iaia@gmail.com`（ドット入り）が正**（Gmail 側はドットを無視するので受信箱は同じ）。**`MAIL_TO` を勝手に書き換えない。**
- 🔴**`fetch` の後に `process.exit()` を呼ぶと libuv が `UV_HANDLE_CLOSING` で assert し、exit code が 1 ではなく 127 に化ける**
  ＝送信失敗を自動化から検知できなくなる。`notify-mail.mjs` は `process.exitCode` を立てる形で回避済み（**この形を崩さない**）。
- 🔴**`--body-file` のパスに `$TMPDIR` を使わない**（2026-08-25 続き655 で実測）＝Git Bash では未設定のことがあり、
  `/o66_mail.txt` のような**ルート直下のパスに化けて Permission denied** になる。スクラッチパッドの絶対パスを使う。
- **push が要る作業をしたなら、送信は push が終わってから**。未 push で「完了」と送ると、リーダーが見に行っても差分が無い。
  （調査だけで push するものが無い回は、その旨を本文に書けばよい。）

---

### 2.6 監査ラウンドの運用（型発見方式）★2026-09-06 ユーザー決定・§5.2 round4 以降の既定

> **決めた経緯**＝第197バッチ（30枚の監査＋2系統の修正＋簿記）で**月額プランのトークンを 40% 消費し、約1時間かかった**。
> このペースでは残 2,658枚を掃引できない（89倍）。原価を分解して分かったのは次の2点：

🔴**(a) `claude -p` は同じ月額プランの枠を食う。「別プロセスに流せばタダ」ではない。**
実測＝**1バッチ（10枚）＝入力25k（cache）＋出力10〜15k ≒ 35〜40kトークン（sonnet）／$0.29／2.3分**。
全2,658枚の掃引だけで **266バッチ ≒ 940万トークン**。⇒ **枚数を目標にしてはいけない。**

🔴**(b) 支配的なのは検出ではなく「opus セッションのターン数 × コンテキスト量」**＝40%の大半は
③triage（engine の受け皿と 26,000行超の `effectParser.ts` を読む）・⑤full gates 3回・⑥簿記一式だった。

#### 決定1＝監査の目的は「カードの網羅」ではなく**「型の発見」**

第197バッチで実際に効いたのは枚数ではなく、**1件の finding が10枚の系統に化けた**ことだった
（`WX11-043` の1件 → クロス宣言つき**10枚**）。**その10枚を見つけたのは LLM ではなく grep（無料）。**
⇒ **LLM は「まだ知らない型」を見つけるためだけに使い、その型の全数は grep / census で取る。**

- **監査プロンプトは finding ごとに `grep`（同型を探す原文の言い回し）を書かせる**（`semanticAuditExtract.mjs`）。
- **真バグを引いたら、直す前に必ず `grep` で母集団を数える**（PLAN §2.1 ②と同じ＝1枚だけ直して満足しない）。
- 🔴**止め時は「残枚数」ではなく「1バッチあたりの新型数」**＝**連続3バッチで新型0 なら監査を止める**。
  台帳＝`scripts/archive/scratchpad/semantic_audit_sheet1_round4/TYPE_LEDGER.md`（1バッチ1行）。
  ⚠**枚数カウンタは捨てる**＝「あと何割」に答えようとしない（CLAUDE.md の既存の戒めと同じ）。

#### 決定2＝偽陽性は必ずプロンプトへ還元する

triage で偽陽性と判定したら、**その場で `semanticAuditExtract.mjs` の「追加の読み方ルール」へ1行足す**
（現在 規則1〜16。13〜16 が第197バッチの3件から）。**還元しないと次の50枚で同じものを読まされる**＝
偽陽性の triage コストは**還元するまで毎回**かかる。⚠**偽陽性は全部「engine が JSON の見た目を裏で
読み替えている」型**だった（did-it ゲート／`OPPONENT_PAY_OPTIONAL` の既定の極性／STUB の id 名）。

#### 決定3＝1セッションの固定費を「バッチごと」から「セッションごと」へ

| 工程 | 旧（バッチごと） | 🆕**新（監査ラウンド中）** |
|---|---|---|
| ゲート | 1巡ごとに `gates` | **作業中は `golden -- --only` ＋ `smoke`。full `gates` はセッション末1回** |
| 簿記 | 1巡ごとに BUGFIXES＋PLAN | **バッチ中は TYPE_LEDGER に1行。BUGFIXES・PLAN §1/§6 はセッション末1回** |
| メール | 毎回フル（§2.5） | **監査ラウンド中は5行以内**（真因・ゲート増減・次の一手・commit SHA だけ） |

#### 決定4＝**モデルは工程で割る（🟢Sonnet＝定型／🔵Opus＝判定）**＝レーン別ワークリストは **§5.0** が正

単価差は約5倍だが、**品質が落ちるのは「判定を外すと再作業になる工程」だけ**＝
**triage（engine の受け皿を読んで真偽を決める）と parser/engine の修正だけを Opus に残す。**
監査の実行・grep 展開・manualEffects.ts の手書き・ゲート・簿記は Sonnet でよい。**残量の実測は §5.0 の表のコマンド。**

⚠**この軽量運用は「監査ラウンド中」限定**＝`src/engine/` や `src/screens/` を触る回は §2.1〜§2.5 のフル手順に戻る。

---

## 3. 不変の運用ルール

- **`effects_*.json` の curated 値は「上書きされない」ことが保証されている。**`build:effects` は ①`parseStatus` が `MANUAL`/`PARTIAL` を含むカードを**カード単位で温存**（`PRESERVE_STATUSES`）②それ以外も fresh が curated の純粋な上位集合でなければ**温存して held へ送る**（`buildEffectsJson.ts:158-162`）ため、**再生成しても勝手に値が消えることはない**。`heldReview --adopt` で明示採用したものだけが変わる。
- **逆翻訳を直したらエンジン実装までセット**（乖離＝偽陰性を作らない）。語彙が無ければ §5.3 の機構として実装するか、`engineUnwiredTimings` に登録し逆翻訳へ `【※engine未配線】` を付けて明示する。[[decompile-engine-parity]]
- **日本語を含むスクリプトは `scratchpad` に `.mjs` を書いて `node <path>` 実行**（Git Bash 経由の `node -e` は文字化けする）。papaparse 等が要るカード参照スクリプトは project root に一時 `.ts/.mjs` を置いて `npx tsx`/`node` 実行・終わったら削除。
- 🆕🔴**進捗は「3つの計器の併記」で報告する**（2026-08-27 続き684・**ユーザー決定**）＝**①Sheet1 要対応カード数**（`npm run census:cards -- --sheet 1`）**②意味照合 段2 台帳の残 OPEN**（`node scripts/archive/semanticAuditLedger.mjs`）**③census 高シグナル数**（`npm run census`）。**どれか1本を「進捗指標」に固定しない。**
  - 🆕🔴🔑**2026-09-01 改定＝3計器は底を打った。併記は続けるが、これだけでは進捗が表示できない。**
    **実測**＝**①18 / 863（うち `mech` 18・即着手可能 0）／②44（全件が UI か新機構待ち・続き766 の全数 triage）／③11 / BASELINE 12**。
    ⇒ **3本とも「これ以上は下がらない」ところまで来ている**＝**動かないことを「停滞」と読んではいけない**（続き768 は22効果を直して3計器とも据置だった）。
    🆕**代わりに「在庫2本」を併記する**＝**④機構 worklist の残項目数**（§5.3 の索引 A〜C・E）**⑤実機の残 `V-nn` 数**（§5.1）。**これが 2026-09-01 以降の進捗指標**（§6）。
    ⚠**在庫2本も「減れば前進」とは限らない**＝母集団を実測すると**項目が割れて増える**ことがある（§5.3 の登録ルール）。**減った理由／増えた理由を必ず1行書く。**
  - **理由（実測）**＝続き684（Sheet1 B6）は **11効果を直したのに Sheet1 の枚数は 0 減**だった。直した中で Sheet1 は1枚だけで、そのカードは**別の finding が残る**ため計器から落ちない。逆に続き679 は**計器のバグを直しただけ**で clean 率が 83.2%→85.7% に動いた（前進ではない）。**単独の計器は「進んだのに動かない」と「動いていないのに動く」の両方を起こす。**
  - **3本が別のものを測っている**＝①は**カード単位の網羅**（1枚に findings が複数あると1つ直しても落ちない）②は**指摘単位の消化**（標本なので、同じ parser 規則で直った標本外の実バグは数に出ない）③は**語彙の欠落**（STUB/MANUAL を免除するので、STUB を解体すると**増えて見える**）。
  - ⚠**バッチの着手時に「今回どれが動く見込みか」を宣言する**（続き594 の教訓＝計器主導でバッチを選ぶと台帳が動かない／PLAN_PROGRESS の第32バッチ＝母集団が `findings.jsonl` 外だと OPEN は構造的に 0 のまま）。**動かない計器があること自体は失敗ではない。動かない理由を書けないことが失敗。**
- **件数メトリクスを信じない**：「脱落疑いNN枚」は「。区切り文数」比較で粗く、逆翻訳器は複数効果を1行（、／そして）に圧縮するため内容を直しても件数は減らない。`_dropTriage` の分類（「文法崩れ」等）も構造ベースで文法品質は測れない。判断は必ず **同型★0＋該当カードの逆翻訳が原文一致（目視/grep）** で行う。
- **ゲートは `npm run typecheck`（＝`tsc -b --noEmit`）**。plain `tsc --noEmit` は project references を見ず CI が拾うエラーを見逃すので不可。
- **全再生成系の一括置換は禁止**（無検証置換で約90枚退化の前例）。系統ごとに機構を1回確立→同パターン適用→各カード verify。
- 🆕🔴**この禁止は「ソースの文字列置換」にもそのまま当てはまる**（2026-09-04 第127バッチで実証）＝
  `manualEffects.ts` に `s.replace('"usageLimit":"once_per_turn"', '')` をかけ、
  **無関係な21効果から《ターン1回》を消した**（`manual-fields` ゲートと「2回目が撃てる」golden が同時に落ちて発覚）。
  ⇒ **行番号を指定した1行だけの置換にする。**
  🔑**`git diff --stat` が「1行のはずが22行」と言ったら、そこで止まる。**
  🔑**ゲートが2本同時に落ちたら、engine を読み始める前に自分の直前の置換範囲を疑う。**
- **CSV の順番を必ず維持する**（スクリプト内の `sorted` ロジックで対応済み）。

### 横展開（系統バグ）のときの追加手順

①**抽出**：全シート走査で「同じ壊れ方」を機械抽出（`scratchpad` の `scan*.mjs` が雛形）。②**分類**：偽陽性(付録B)・既知複雑札を除外し、クリーンな系統を確定。③**パッチ**：`effectId` をアンカーにした一括スクリプトで安全に置換（他カードを巻き込まない）。MANUAL化する場合は `parseStatus:'MANUAL'`。④**検証ゲート**：上記ワークフローの④〜⑦と同じ。⑤**記録＆バトン**：`BUGFIXES.md` に追記（新しいものを上）→本ファイル §1 を上書き→コミット（末尾に「要実機検証」）→push。

### 機構実装の「型」
1. `src/types/effects.ts`（アクション/条件/timing の型）→ 2. `src/types/index.ts`（`PlayerState` 状態フィールド）→ 3. `src/engine/effectExecutor.ts`（実行）/`execUtils.ts`（`evalCondition`/`matchesFilter`）/`effectEngine.ts`（CONTINUOUS収集）→ 4. `src/screens/BattleScreen.tsx`（状態読み取り＋**ターン境界リセット3箇所**：PvP通常終了・PvP確認後・CPU）→ 5. `scripts/decompileEffects.ts`（表示）→ 6. JSON 配線 → 検証。

### 主要ファイル
- 語彙: `src/types/effects.ts` / `src/types/index.ts`（PlayerState）
- エンジン: `src/engine/effectExecutor.ts`（`execLookPickChain`/`payBeatSigniCost` 等）・`execUtils.ts`（`evalCondition`/`matchesFilter`/`addToBeatZone`）・`effectEngine.ts`（CONTINUOUS収集・`checkActiveCondition`）
- UI/ルール: `src/screens/BattleScreen.tsx`（コスト計算・バトル・ターン境界リセット・`crashOneLife`）
- 逆翻訳器: `scripts/decompileEffects.ts`、グルーピング: `scripts/group{Similar,BySentence}.mjs`（`--all` で全10シート統合）
- 監査: `scripts/behaviorAudit.ts`（`npm run audit`/`audit:html`/`audit:queue`）

---

## 4. 教訓集 ★**本体は別ファイル**（2026-09-06 に分離）

> 🔴**この節に教訓を書かない。** 2026-09-06 時点で PLAN.md は **1,313行中 634行（52%）が §4**で、
> 「計画表」として読めなくなっていた（うち §4.4 だけで 368行＝**全体の30%**）。⇒ 2ファイルへ分離した。
> 🔴**節番号 `§4.1`〜`§4.6` は住所として維持している**＝BUGFIXES・PLAN・`scripts/`・`src/` から**数百箇所**が
> 「§4.2」「§4.4-2c」の形で参照している（実測＝`§4.4` だけで `scripts/` から **216箇所**）。**詰め直さない。**

| ファイル | 中身 | 読むタイミング |
|---|---|---|
| **[LESSONS.md](./LESSONS.md)** | **§4.1** 着手前（見立てと母集団）／**§4.2** 実装（型・parser・engine）／**§4.3** 計器の読み方／**§4.5** 修正を live へ届ける経路／**§4.6** Codex へ委譲するときの作法／🆕**§4.7** 意味照合ラウンドと triage の読み方／🆕**§4.8** 監視だけしている項目 | 🔑**§5 のどの項目に着手するときも先に読む** |
| **[DRIVE_TRAPS.md](./DRIVE_TRAPS.md)** | **§4.4** 実機シナリオの罠（番号つき92項） | 🔑**実機シナリオを書く／直す回だけ**（`scripts/verifyBattleDrive.mjs`） |

⚠**新しい教訓はこの2ファイルへ足す**（PLAN へ書き戻さない）。
⚠**踏んだ罠は必ず残す**＝この2ファイルが生まれた理由は、教訓が**クローズ済みタスクの本文に埋まっていて**
**完了行を消すと巻き添えで消えていた**こと（2026-08-23 の §4 新設時の経緯）。

---

## 5. 作業キュー（単一・上から取る）

> **取り方**＝**上から1件**。並行して別の節から取らない（これが再編前に作業順がばらついた原因）。
> 1巡の手順は §2、着手前に読むのは [LESSONS.md](./LESSONS.md)。
> **番号 `V-nn`／`O-nn` は参照用の固定ID**＝消化しても番号を詰めない・再利用しない。
> 🔴**消化したら行ごと消す**（全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md) と [BUGFIXES.md](./BUGFIXES.md) が正）。
> ⚠**クローズの経緯をこの節に書き溜めない**＝2026-09-06 の整理時点で、この表の「残」列が
> **バッチごとのクローズ履歴 2,251字**に膨らみ、しかも**3節とも数字が stale**（§5.2「残36」／§5.3「2項目」／§5.4「5件」
> ＝実際はすべて 0）だった。**表に書くのは「いまの残数」だけ。**

| 節 | 役割 | 残（2026-09-09 実測） | 測り直すコマンド |
|---|---|---|---|
| §5.2 round4 | 🏁**掃引完了**＝意味照合を1度も通していないカードは無くなった | 🏁**残0枚**（全11シート 100%）。⚠**止め時の判定（新型0×連続3バッチ）は未実施**＝新規ラウンドを始めるならそこから | `node scripts/archive/semanticAuditGap.mjs` |
| §5.0 O-A | 🏁**完了**＝findings の triage | 🏁**未 triage 0**（572件を全数確定＝**BUG 433 / FP 70**・FP は全件 engine の行を人手で裏取り済み） | `node scripts/archive/semanticAuditPool.mjs` |
| **§5.0 実装キュー** | 🔥🔥**本線キュー**＝triage で真バグと確定した未修正バグ（O-D / S-3） | 🔥**残309効果**（確定433 − 消化済み125）。🆕**2026-09-09 第233＝機構不要の一点物16効果（宣言レベル一致8＋任意トラッシュ8）を `.codex-work` へ委譲して消化**（267→約251・厳密な系統別残数は未再クラスタリング）。🆕**第234（前半）＝`.codex-work` が30効果中6効果を実装した時点で利用上限に達し停止＝6効果を検証済み採用。残り24効果は Claude が引き継ぎ中。**
**2026-09-08 第232の内訳**＝**engine・受け皿に言及するもの86効果**（うち系統が立つ40効果は `O-287`〜`O-305` へ登録済み・残り46効果は engine 行を引用しているが**一点物**）／
**機構不要の一点物 267効果**（今回はここから選定）。⇒ **§5.3 から取れば系統がまとめて閉じ、ここから取れば1件ずつ閉じる。**🔴**このリストの precision は高くない**＝第230 の実測で**見た62効果のうち真バグ12・機構待ち8・偽陽性42**（特に `MISSING` は 17件中 真バグ0）。**実装前に必ず live と突き合わせる。**🔴`semanticAuditPool.mjs` は残0になったのでもう在庫を映さない | `node scripts/archive/semanticAuditBugList.mjs` |
| §5.3 | 機構 worklist（`O-nn`）＝新しい型・評価器・engine が要るもの | 🔥**10項目**（索引 B に🆕`O-294`/`O-295`／索引 G に `O-287`/`O-288`/`O-290`〜`O-293`＋🆕`O-296`/`O-297`）。🆕**2026-09-08 第230＝§5.0 の検証で機構が要ると確定した4件を登録した** | §5.3 の索引 G |
| §5.1 | 実機で確かめる（`V-nn`） | 🏁**0件**（`V-182` / `V-183` を 2026-09-08 に同日返済＝さらに5シナリオを `order` に常設） | §5.1 の本文 |
| §5.2 段2台帳 | 旧・本線。🏁掘り尽くした＝**もう在庫ではない** | 残 OPEN **0** | `node scripts/archive/semanticAuditLedger.mjs` |
| §5.4 | 🏁閉じた。**新しい構造混線を見つけたときだけ足す** | 0件 | — |
| §5.5 | 低優先・保留（母集団が測れていない） | — | — |

⚠**残数は毎回コマンドで数え直して書く**（この表の数字を足し引きしない）＝過去に
**triage 済み32件を二重に足して「77件」**と書いた実績がある。

🔴🔑**2026-09-06（第197バッチ）＝計器由来のキューは全部0のままだが、「完成した」ではない。**
`census:cards` の出力も毎回「**フラグ0 = 正しい ではない（計器が見ていないだけ）**」と言っている。
⇒ **本線は §5.2 round4＝「まだ誰も読んでいないカードを読む」**（30枚のパイロットで真バグ3件・影響11枚が出た）。

⚠**「§5 の上から1件」を §5.3 の索引にそのまま当ててよい**（索引が母集団順に並んでいる）。**旧「取る順」表は廃止した。**

### 5.0 レーン別ワークリスト（**Sonnet＝定型／Opus＝判定**）★2026-09-06 ユーザー決定

> 🔴**2026-08-23 の「Opus/Sonnet のモデル分担は廃止」を、別の軸で復活させたもの。**
> 旧分担は**機能で割っていた**（この機構は Opus、この機構は Sonnet）ので、難易度と噛み合わず破綻した。
> 🆕**新しい軸は「判定が要るか／定型か」**＝**外したときに live へ壊れた修正が入るか**で割る。
> **この軸は §2.0 の「速い／遅いレーン」と一致する**（`manualEffects.ts` 手書き＝定型／`parser`・`engine`＝判定）。
>
> **なぜ割るか**＝原価の支配項は**私（opus）のターン数 × コンテキスト量**（PLAN §2.6）。
> 定型工程を Sonnet に落とすと単価が約5分の1になり、**品質が落ちるのは「判定を外すと再作業になる工程」だけ**。
> 🔴**判定を外すコストが高い理由**＝このリポジトリの支配的な失敗モードは
> **「engine が JSON の見た目を裏で読み替える」「計器が嘘をつく」**型で、外すと
> **3計器も golden も smoke も緑のまま live が壊れる**（第197バッチの偽陽性3件が全部この型だった）。

#### 🟢 Sonnet レーン（定型＝判定が要らない・外しても live が壊れない）

| ID | 作業 | 残（2026-09-07 実測） | 測り直すコマンド |
|---|---|---|---|
| **S-1** | **意味照合バッチを回す**（10枚/バッチ）🆕**codex なら Claude 月額枠を消費しない**（2026-09-08 実測＝213バッチ / 失敗0・1バッチ 12〜81秒） | 🏁**残0バッチ**（全11シート監査完了） | `node scripts/archive/semanticAuditGap.mjs` |
| **S-2** | **真バグの母集団を grep で数える**（原文の言い回し × live JSON の受け皿の有無） | 🏁**残0**（2026-09-08＝系統7件＋機構4件を全数実測。登録23効果 → 実測48効果） | 使い捨て `tmp_*` で `docs/_effect_srctext.json` × `public/data/effects_*.json` を突き合わせる（道具＝`scripts/archive/scratchpad/s2_population_20260908/s2_lib.mjs`） |
| **S-3** | **`manualEffects.ts` への手書き修正**（§2.0 速いレーン＝同型2枚以下） | 🔥**40効果**（2026-09-08 実測＝残353効果のうち live の `parseStatus` が MANUAL＝**定義が `manualEffects.ts` に在るので手書きで直せる**）。⚠**在庫の見立てであって判定ではない**＝着手時に §2.1 ② で同型枚数を測る（AUTO 306 はそこで速い／遅いに割れる） | `node scripts/archive/semanticAuditBugList.mjs` ＋ live の `parseStatus` で引き算 |
| **S-4** | **golden の定型追加・ゲート実行・簿記**（セッション末1回） | 1回/セッション | `npm run gates` |

⚠**S-1 の残バッチ数は「止め時」ではない**＝止め時は **TYPE_LEDGER.md の「新型ゼロの連続3バッチ」**（§2.6 決定1）。
この表の枚数は**予算見積り用**（残 35バッチ ≒ 130万トークン・sonnet）であって、**消化目標ではない**。
🔑**1周期は 5〜8バッチで打ち切って Opus の triage へ渡す**（下記「**回し方＝周期**」）＝**全部掃引してから一括 triage しない。**

#### 🔵 Opus レーン（判定＝外すと live が壊れる／どの計器にも映らない）

| ID | 作業 | 残（2026-09-07 実測） | 測り直すコマンド |
|---|---|---|---|
| **O-A** | 🏁**findings の triage＝完了** | 🏁**0件**（572件を全数確定＝BUG 433 / FP 70）。🔑**委譲の形は残した**＝`scripts/semanticAuditTriageExtract.mjs` → `semanticAuditRunCodex.mjs`（**engine を読ませるのが委譲の条件**）。⚠**codex の判定は BUG 側 4/5・FP 側で2件の取り違え**＝確定は人間が engine を読んでから | `node scripts/archive/semanticAuditPool.mjs` |
| **O-B** | **意味照合 段2 台帳の残 OPEN** | 🏁**0件**（掘り尽くした＝もう在庫ではない） | `node scripts/archive/semanticAuditLedger.mjs` |
| **O-C** | **偽陽性のプロンプト還元**（`semanticAuditExtract.mjs` の読み方ルール）＝**`O-A` の副産物**（単独では着手しない） | **残0**（`O-A` が残0なので動かない）。**累計 34本**を還元済み | `grep -c "^[0-9]*\. " scripts/semanticAuditExtract.mjs` |
| **O-D** | **`effectParser.ts` / `src/engine/` を触る修正**（§2.0 遅いレーン＝同型3枚以上・新しい型） | 🏁**残0**（2026-09-08 第225〜228） | 下の実装キュー |

🔑**O-A を Sonnet に落とさない理由**＝監査員（sonnet・JSON のみ）の precision は**実測 50%**で、
**偽陽性は全部 engine の受け皿を読まないと判定できない型**だった。⇒ **triage は「engine を読む」工程**であり、
ここを外すと**壊れた修正が live に入り、しかもどの計器にも映らない**。

#### 🔥 実装キュー（O-D / S-3）＝**433効果**（2026-09-08 の O-A 全数 triage の結果）

> 🔴**「見立て」ではなく triage 済みの確定リスト**＝engine の受け皿まで読んで真バグと判定してある。
> 🔴🔑**この表が唯一の追跡先**＝**finding を「BUG」と triage した瞬間、`semanticAuditPool.mjs` からも
> `census:cards` の「意味照合の未消化 findings」からも消える**（どちらも数えているのは*未 triage*であって*未修正*ではない）。
> ⇒ **行を消してよいのは (a) 直して golden を張ったとき (b) 偽陽性と確定したとき (c) §5.3 へ `O-nn` で登録し直したときだけ。**
> 直したら BUGFIXES に真因を1行書く。
>
> **取る順**＝表の上から（**系統 → ①別の効果に化けている → ②丸ごと欠落 → ③限定の欠落**＝壊れ方の重い順）。
> **系統を先に取る**＝1 finding が `grep` で複数効果に化けたもので、1件ずつ取るより桁で安い。
> ⚠**「レーン」列は登録時の見立て**＝**着手時に §2.1 ② で母集団を測り直して決める**（実測で「速い」が「遅い」に化けた前例が複数ある）。
> 🔑**triage の履歴（precision・FP の内訳・どの規則へ還元したか）は [PLAN_DETAIL.md](./PLAN_DETAIL.md)、
> 再利用できる教訓は [LESSONS.md](./LESSONS.md) §4.7。この節に書き溜めない。**

🔴🔑**per-effect の正本は各ラウンド dir の `triaged.txt` の `:: BUG ::` 行**（433効果）。
**この表はそこから「系統」だけを引き出した取り掛かり表**＝1行が複数効果を畳んでいる。
**数え直すコマンド**＝`grep -h ':: BUG ::' scripts/archive/scratchpad/semantic_audit_*/triaged.txt | wc -l`
⚠**系統の効果数は上限値**＝着手時に §2.1 ② で割り直す（[LESSONS.md](./LESSONS.md) §4.7）。

| 分類 | 効果 | 壊れ方 | 受け皿 | レーン |
|---|---|---|---|---|
| 🏁系統 | ~~`ADD_TO_FIELD` の `asDown` 欠落~~＝**7効果を修正済み**（2026-09-08・`normalizeAddToFieldAsDown`）。**残9は別の穴**＝MANUAL 3件＋該当 `ADD_TO_FIELD` が JSON に無い6件（個別行へ） | — | — | 🏁完了 |
| 系統 | `OPPONENT_PAY_OPTIONAL` が対象を事前選択しない（**grep 実測 32効果**／findings 由来12） | 相手が支払いを判断する時点で対象が未確定＝原文と情報量が違う | ❌**要新設**（`freezeStoredTargets` は `targetsStored` があるときだけ働く）→ §5.3 `O-288` | 遅い |
| 系統 | 遅延誘発が即時実行に化けている（🆕**S-2 実測 A 8効果＋M 2**／登録6） | 「次の〜時に」が解決時に走る | ✅実在（`INSTALL_DELAYED_TRIGGER` ほか6経路） | 遅い |
| 系統 | 期限が「次のあなたのエナフェイズ終了時まで」でなく現ターン終了時（🆕**S-2 実測 10効果**／登録5＝**全候補が壊れている**） | 効果が2フェイズ早く切れる。🔴**受け皿の名前が嘘**＝`LIMIT_CHANGE_UNTIL_ENERGY_PHASE_END` が書く `lrig_limit_mod` は**ターン終了時に消える**（`BattleScreen.tsx:4284` と `:4735`） | ❌**要新設**（「次のエナフェイズ終了まで」生きるストアが engine に無い）→ §5.3 `O-293` | 遅い |
| 系統 | 「宣言した数字と同じレベル」条件欠落＋`reorder:false`（**grep 実測 12効果**／findings 由来4） | 無条件バウンス＋並べ替え不可 | ✅実在 | 遅い |
| 系統 | 「置いてもよい」が素の `TRASH`／`MILL`／`ENERGY_CHARGE` で強制（🆕**S-2 実測 A 9効果＋M 2**／登録4） | 任意が強制になる | ✅実在（`optional`／`mandatory:false`／`CHOOSE`／`OPTIONAL_ACTIVATE`／任意 STUB の5経路） | 遅い（3実装単位にまたがる） |
| 系統 | 「1枚をデッキ上・残りを下」／「残りをシャッフルして下」（🆕**S-2 実測 A 5効果＋M 3**／登録2） | 積み込みが効かない／無作為化されない | ✅実在（`first_top_rest_bottom`／`split_top_bottom`／`remainder.shuffle`／`deckBottomShuffled`） | 遅い |
| 系統 | 【ライド】が2つの起動能力に重複（🆕**S-2 実測 2効果＝登録どおり・両方 MANUAL**） | 同じ能力が2回提示され使用回数も別管理 | ✅本線は `RIDE_ON`（`effectParser.ts:27030`／UI `battleUtils.ts:126`） | 速い（`manualEffects.ts:10109`） |
| 系統 | 色付きルリグ／シグニ対象の色フィルタ欠落（🆕**S-2 実測 A 2＋B 2＋M 1＝5効果**／登録2） | 色条件を無視して対象にできる＝**不発すべき効果が通る** | ✅実在（`filter.color`／`anyOf[].color`／`colorMatchesLrig`） | 遅い（A2）＋速い（M1） |
| 系統 | グロウ時「公開した場合」が「手札にある」判定（🆕**S-2 実測 2効果＝登録どおり・両方 MANUAL**） | 公開せずに軽減を受けられる | △通常の公開受け皿はあるがグロウ支払いフローへの配線が無い | 速い（`manualEffects.ts:4566`/`:4578`）＋engine |
| 系統 | 🆕「この【起】能力でまだ選ばれていない」「ゲーム中1回だけ」が無条件（**S-2 実測 2効果**・旧 `O-289`） | 同じ選択肢を何度でも選べる | ✅実在（`CHOOSE.noRepeat` ＋ 永続 `taken_choice_keys`＝`execStubPart1.ts:922-930`） | 遅い（parser・engine 新機構は不要） |
| ①別の効果に化けている（`WRONG`） | **217効果** | 対象・所有者・領域・順序の取り違え | 個別 | 個別 |
| ②丸ごと欠落（`MISSING`） | **188効果** | 主要処理・条件が JSON に無い | 個別 | 個別 |
| ③STUB の疑い（`SUSPECT_STUB`） | **19効果** | STUB の payload が原文の主要処理に対応していない | 個別 | 個別 |
| ④過剰（`EXTRA`） | **8効果** | 原文に無い処理・制約が足されている | 個別 | 個別 |

#### 🔴 433効果の内訳（2026-09-08 実測・**この表の「一点物」が本体である**）

**系統で畳めているのは 99効果だけで、残る 334効果（77%）は上の①〜④に入る**。
**「一点物」と書いてあっても件数は 3桁**＝**PLAN のこの表を見て「少ない」と思ってはいけない。**
🏁**2026-09-08 時点＝確定433 のうち 7効果を消化済み（系統①`asDown`）＝残 426。**
**消化は `scripts/archive/scratchpad/semantic_bug_fixed.txt` に1行ずつ記録する**
（`triaged.txt` の行は履歴なので消さない＝**消化側を引き算しないとカウンタが永久に減らない**）。

| 軸 | 内訳 |
|---|---|
| **壊れ方** | `WRONG` **217** ／ `MISSING` **188** ／ `SUSPECT_STUB` **19** ／ `EXTRA` **8** |
| **深刻度** | HIGH **266** ／ MED **154** ／ LOW **12** |
| **シート** | Sheet3 **77** ／ Sheet9 **75** ／ Sheet8 **69** ／ Sheet2 **53** ／ Sheet7 **48** ／ Sheet4 **39** ／ Sheet1 **29** ／ Sheet5 15 ／ Sheet6 14 ／ Sheet10 10 ／ TK 3 |

**バッチの切り方（推奨）**＝**シート単位ではなく「系統 → HIGH の一点物」**。
系統を先に取るのは1件あたりのコストが桁で安いから（§5.0 冒頭の「取る順」）。
**一点物に入ったら HIGH 266件から取る**（`triaged.txt` の行に severity は無いので
`findings.jsonl` と突き合わせる＝下のコマンド）。

**列挙するコマンド**＝🆕**`node scripts/archive/semanticAuditBugList.mjs`**（2026-09-08新設）:
```
node scripts/archive/semanticAuditBugList.mjs                      # サマリ（壊れ方・深刻度・シート）
node scripts/archive/semanticAuditBugList.mjs --list --sev HIGH    # HIGH から取る
node scripts/archive/semanticAuditBugList.mjs --list --grep asDown # 系統を抽出する
```
⚠**`triaged.txt` には severity も type も入っていない**＝**`findings.jsonl` と突き合わせないと HIGH から取れない**
（2026-09-08 に実測。この突き合わせを毎回書かずに済ませるために上のツールを作った）。
⚠**直しても `triaged.txt` の行は消さない**（消すと「何を直したか」の履歴ごと消える）＝
**消化の記録は BUGFIXES.md と golden が正**。PLAN §5.0 の表からは行を消す。

🔴🔑**2026-09-08 実測＝S-2（grep 展開）をやると系統の母集団が 2.7〜3.7倍になる**
（`usageLimit` 3→**11**／`OPPONENT_PAY_OPTIONAL` 12→**32**／`reorder:false` 4→**12**／`commonClass` 9→**27**）。
⇒ **監査員が気づくのは同型の約1/3**＝**着手前に必ず S-2 を回す**（やらないと同じ型を何度も踏む）。
⚠**grep 実測も上限値**＝別軸で表現済みのものを「欠落」と数える（「次のエナフェイズ終了時まで」は
findings 由来5に対し grep 実測3＝**判定式が甘くて逆に取りこぼした**例）。**着手時に1件ずつ割る。**

🔴**機構が要ると判明したものは §5.3 索引 G へ `O-287`〜`O-292` で登録した**（この表からは外れる）。
⚠**`src/screens/` を指す BUG が5件ある**＝着手する回は §2.2 により**実機まで必須**（観測点は §5.1 へ `V-nn` で足す）。

⚠**新しく triage で真バグと確定したものが出たらこの表を復活させる**（書式＝`| 分類 | 効果 | 壊れ方 | 受け皿 | レーン |`）。

🏁**消化・却下した分の全文**（第217の系統①③④＝トラップアイコン未分離4カード／`hasIcon:"トラップ"` 欠落3効果／
無意味な `LOOK_AND_REORDER` 17効果、第218の `PLAY_FREE` 3効果、第219の一点物9効果と偽陽性 `WX16-031-BURST`、
第220の `WX07-014-E1`＝残2件は §5.3 `O-283` へ分離、第221の4効果（`WX18-038-BURST`／`WX16-074-E1`／
`WX21-052-E1-G`／`WX12-Re22-E1`）＝派生発見2件は §5.3 `O-284`／`O-285` へ分離、第222の4効果
（`WX22-005-E1`／`WX20-001-E2`／`WX19-064-E1`／`WX22-022-BURST`）、第223の2行3効果
（`WX17-004-E1`＋母集団実測で発見した同型 `WXK03-TK-01B-E1`／`WX15-061-E1`）は
[PLAN_DETAIL.md](./PLAN_DETAIL.md) の「2026-09-07 整理：§5.0 実装キューの統合」節と [BUGFIXES.md](./BUGFIXES.md) にある。

#### 回し方＝**周期**（Sonnet 5〜8バッチ : Opus triage 1回）★2026-09-06 決定

> 🔴**「Sonnet で全261バッチ（2,608枚）を掃引してから Opus で一括 triage」も、「finding が出るたび Opus」も採らない。**
> **周期で交互に回す。**

```
[Sonnet セッション] S-1 を 5〜8バッチ（50〜80枚）→ TYPE_LEDGER.md に行を足す → S-4（簿記・ゲート）→ commit
  ↓ 簿記 → commit → /clear → /model opus
[Opus セッション]   O-A triage（pool 3〜6件）→ S-2 で grep 母集団 → O-D / S-3 で修正 → O-C 還元 → gates → 簿記 → commit
  ↓ 簿記 → commit → /clear → /model sonnet
繰り返す。**TYPE_LEDGER が「新型0 × 連続3バッチ」になったら監査を止める**（§2.6 決定1）。
```

**周期を 5〜8バッチにする根拠**＝実測の歩留まり **50枚 → findings 3件**。
1周期で **findings 3〜6件**＝**Opus の cold start（PLAN + [LESSONS.md](./LESSONS.md) + engine の受け皿）を償却できる最小単位**であり、
同時に **O-C のプロンプト還元が2周期以内に後続バッチへ効く**粒度。

🔴**上の「どちらも採らない」の根拠4つ**（止め時が triage からしか出ない／偽陽性の還元ループが切れる／
真バグの grep 展開が遅れる／backlog 自体が Opus のコンテキスト問題になる）は
**[LESSONS.md](./LESSONS.md) §4.7 にある。周期を変えたくなったら先にそこを読む。**

🔴⚠**モデルの切り替えはセッション内でやらない**（2026-09-06 ユーザー指摘）＝**プロンプトキャッシュはモデル別**なので、
途中で切り替えると**切り替え後の最初のターンで全コンテキストを full price で読み直す**。
⇒ **必ず「簿記 → commit → `/clear` → `/model`」の順**にする。つまり **1セッション1モデル**が既定で、
上の周期は**必ず別セッションに割る**（だから §2.6 決定3 の「続きがディスク上だけで再開できる」簿記が要る）。

### 5.1 【最優先】実機未検証の返済（`V-nn`）

> **2026-08-23 ユーザー決定＝新規実装を止めて、溜まった実機検証を先にゼロにする。**
> 以後は §2.2 のとおり1巡に実機検証を内包するので、ここが再び溜まることはない。
> ⚠**着手前に [DRIVE_TRAPS.md](./DRIVE_TRAPS.md) を読む**（§4.4＝番号つきの罠 94項）。
> ⚠**`verifyBattleDrive.mjs` は必ず明示シナリオIDで実行する**（引数なしのフルバッチはフリーズ報告あり）。
> **FAIL を見たときの切り分け3分類**＝(a)**シナリオの腐り**（[DRIVE_TRAPS.md](./DRIVE_TRAPS.md) の 26）は**その場で直す**
> (b)**engine/parser のバグ**も**その場で直す**（§2.4） (c)**未実装**は §5.3 へ登録。

🏁**残0**（2026-09-08 実測・2回目）＝**索引 G の巡で登録した `V-182` / `V-183` も同日に返済した**。
**返済した2件は5シナリオを `order` に常設した**（`v182*` 2本＝トラッシュ候補を選ばせる／候補ちょうどなら出さない、
`v183*` 3本＝範囲なし無料／同一タイプ無料／重なるだけは無料にならない）。
🔴🔑**`V-182` は「engine を直したせいで新しくモーダルが出るようになった」型の返済**（[DRIVE_TRAPS.md](./DRIVE_TRAPS.md) の 62）＝
`O-274` は **live 28効果**が新たに `self_trash` の選択を挟むので、`EffectInteractionModal` がそのスコープを
描けないと**28効果まるごとソフトロック**する。**golden は `resumeSelectTarget` を直接呼ぶので必ず緑**。
🔑**`V-183` は `src/screens/` を触った回の本来の返済**（`GrowModal` の候補ごとの `isFreeGrow` 計算）＝
**エナ0枚で押せるか**を観測点にして「無料だから通った」以外の説明を消した。
⚠**この巡で踏んだ運用ミス**＝反転確認の後始末に `git checkout <file>` を使い、**同じファイルの未コミットの本体を
巻き込んで消した**（[DRIVE_TRAPS.md](./DRIVE_TRAPS.md) の 61 へ採番済み）。

🏁**残0**（2026-09-08 実測）＝**同日に登録した `V-180` / `V-181` を同日に返済した**（§2.2 の「実機を溜めない」）。
**返済した2件は5シナリオを `order` に常設した**（`v180*` 3本＝対照つきの3方向／`v181*` 2本＝正面の入れ替え対）。
🔑**`V-181` は `V-176` と同型の穴を狙って、実際に「配線が抜けていれば必ず赤くなる」ことを反転で確かめた**＝
`ExecCtx.sourceLeftZoneIdx` のフォールバックを殺すと**対象選択が1度も立たない**（`abortIfNoCandidate` で降りる）。
🔑**反転確認は「対照の本数ぶん」用意した**（[DRIVE_TRAPS.md](./DRIVE_TRAPS.md) の 60）＝
`hasIgnoreLrigRestriction` は **scope 判定**と **`levelEq` 判定**の2段なので、
**scope だけ殺すと `v180…Lv4Blocked` は緑のまま**だった。軸ごとに1本ずつ反転して対応づけた。
⚠**採番は `V-180` から**＝`V-179` は第220バッチで返済済みの既存 ID（`verifyBattleDrive.mjs` の
`o283CounterSpellPlayFreeCarriesCardNum` のコメントが参照している）。**使用済み番号を再利用しない**
（[DRIVE_TRAPS.md](./DRIVE_TRAPS.md) の 59）。

🏁（2026-09-07 実測）＝`V-176`〜`V-178` を同日に返済した。
**返済した3件は6シナリオを `order` に常設した**（`v176*` / `v177*` / `v178*`＝**3組とも対照つきの対**）。
🔑**`V-176` は実機だけが出せるバグを1件釣った**＝`execSearch` の `TREAT_AS_LEVEL1_IN_DECK_TRASH` 差し替えが
**instanceId をキーに書いていたのに、読み手は CardNum で引いていた**＝実機では候補が常に0件の恒久 no-op。
**golden は素の CardNum で state を組むので緑のままだった**（§2.2 の「実機が要る回」の実例）。
⚠**新しい観測点はここへ `V-<次番号>` で足す**（機構項目は §5.3 へ）。**返済したら行ごと消す。**
⚠🔑**置き場を2つにしない**＝この節は**生きている観測点だけ**を持つ
（実機の教訓は [DRIVE_TRAPS.md](./DRIVE_TRAPS.md) へ一本化した＝2026-09-06）。
⚠**残数は毎回「数え直して」書く**＝この行は過去に2度数え違いを起こしている
（返済済みを残に数える／登録した観測点を数え落とす）。
🔑**返済済み `V-04`〜`V-175` の全文（何を踏んだか・何が壊れていたか）は**
[PLAN_DETAIL.md](./PLAN_DETAIL.md) の「§5.1 実機返済の完了報告」と [BUGFIXES.md](./BUGFIXES.md) にある。
🔑**`order` に入れたシナリオは「壊れたら気づく」ための番人**＝返済後も外さない
（例＝`o267CutinResonaResolvesBeforeSpell` は「`effect_stack` を空にしてからスペルを解決する」ガードの唯一の番人）。

### 5.2 意味照合監査（semantic audit）★🔥**2026-09-06 から本線キュー**（round4＝未監査カードの新規監査）

> 🔥🔴**2026-09-06 第197バッチで本線に戻した。** §5.3（機構 worklist）が全項目クローズし、
> **いまある計器が指すものが尽きた**ため。⚠**「段2 の台帳」と「round4」は別物**＝
> **台帳（残 OPEN 13）は残り全件が機構待ち**（`src/screens/` か新 engine 機構＝ここから新規に取らない）。
> **新規に取るのは round4＝「意味照合を1度も通していないカード」への監査。**

#### ■ round4（未監査カードの新規監査）＝🏁**Sheet1・Sheet2 完了（2026-09-07・残0）。Sheet3 は2/37バッチ消化・残349枚**

🏁**Sheet1 は2026-09-07（第205バッチ・S-1）に全252枚を監査完了**（`scripts/archive/scratchpad/semantic_audit_sheet1_round4/`）。
🏁**Sheet2 は2026-09-07（第210バッチ18＋続きの回で s2-19〜30 の12＋さらに続きで s2-31〜36 の6＝計36バッチ・356枚）を完了**
（`scripts/archive/scratchpad/semantic_audit_sheet2_round4/`）。🏁**findings 73件は全数 triage 済み**（s2-01〜18 の32件＝第211／s2-19〜36 の41件＝**第216**・`triaged.txt`）。
🆕**Sheet3 は2026-09-07（続きの回・S-1）に s3-01〜02 の2バッチ（20/369枚）を消化**
（`scripts/archive/scratchpad/semantic_audit_sheet3_round4/`）。🏁**findings 4件も第216で triage 済み**。**残 35バッチ / 349枚**は S-1 で続ける（README に再開コマンド）。

**母集団の実測**＝`node scripts/archive/semanticAuditGap.mjs` で毎回測り直す（**シート別の未監査枚数**。
`--sheet 3 --list 2>/dev/null` でそのシートの未監査カード番号を列挙＝残りの `pending` を作り直せる。⚠**`[gap] …` 行は stderr へ出る**）。
📊**2026-09-07 s3-02 消化後＝効果あり 6,032枚 / 監査済 3,972 / 未監査 2,060（34.1%）**・**Sheet3 残は 349枚**。
⚠**監査済みの集合は `scratchpad/semantic_audit_*/` の `*cumulative.txt`・`sampled_cards.txt` の和集合を自動走査する**
（ラウンドを増やしても勝手に入る＝ディレクトリを固定列挙した初版は round4 を数え落とした）。
🔑**意味照合は「受け皿の名前を知らない穴」も拾える唯一の発見器**（逆翻訳・census・golden は**知っているキーしか見ない**）。

**取り方**（作業ディレクトリ＝`scripts/archive/scratchpad/semantic_audit_sheet3_round4/`・README が正）：

```
node scripts/semanticAuditRun.mjs --out scripts/archive/scratchpad/semantic_audit_sheet3_round4 \
  --model sonnet --batches 3,4,5,6,7,8
```

- ⚠**シャッフルは seed 42 の mulberry32**（初回抽出時に固定済み。並びを変えると監査済みと重なる）。**1バッチ 10枚・所要 87〜443秒。**
- 📉**歩留まりの実測＝Sheet2 s2-01〜18＝180枚 → findings 32件（1.8件/バッチ）**。**まだ triage していないので precision は不明**
  （Sheet1 は r4-01〜08 が1.1件/バッチ→r4-09〜26 が2.4件/バッチで、頻度と偽陽性率は無相関だった＝**予測しない**）。
- 🔴🔑**findings をそのまま直さない**＝偽陽性は**すべて「engine が JSON の見た目を裏で読み替えている」型**だった
  （did-it ゲート＝`DID_IT_GATED_TYPES`／`OPPONENT_PAY_OPTIONAL` の既定の極性）。**JSON だけを読む監査員には
  原理的に判定できない**＝**引き当てたら engine の受け皿を必ず読んでから triage する。**
- 🔑**1件が系統になることがある**（Sheet1＝`WX11-043` の1 finding が**クロス宣言つき10枚**に展開した）＝
  **真バグを引いたら「同じ言い回しのカード全数」を grep で数えてから直す。**
- **消化したら** `audited_cards_cumulative.txt` に追記する（README の手順。⚠**キー名は `num`**＝
  `batches/batch_NN.json` の各要素は `{num, group}`。`cardNum` と読むと空行を書く事故になる＝第210バッチで実測）。
  **残数はこの行に書き直す**（Sheet2 が閉じたら次のシートへ移る）。

#### ■ 段2 台帳（旧・本線）＝🏁**残 OPEN 0（2026-09-07 に掘り尽くした）**

📊**残 OPEN ＝0**（1,444 findings＝段0 200 / 段1 偽陽性 111 / 段2 消化 1,133）。
**毎回 `node scripts/archive/semanticAuditLedger.mjs` で測り直す**（内訳を PLAN に書き写さない＝必ず腐る）。
⇒ **この台帳はもう在庫ではない**。新しい穴は §5.2 round4（未監査カードの新規監査）から出す。

🔑**閉じるまでに踏んだ罠（「掘り尽くした」を2度誤判定した経緯・`semanticAuditRecheck.mjs` の読み方）は
[LESSONS.md](./LESSONS.md) §4.7 へ移した。台帳を再び開けるときは先にそこを読む**
（コスト系の消費地点・受け皿の疑い方も同ファイル）。

### 5.3 機構 worklist（新しい型・評価器・engine が要るもの）★**登録先**（2026-09-08＝索引 A/B は残0・索引 G に11項目。本線は §5.2 round4）

> 🔴**この節は「索引（ここ）＋登録票の全文（[PLAN_DETAIL.md](./PLAN_DETAIL.md)）」の二層構造。**
> 旧構造は登録票82行の第4列に経緯・実測・罠が全部入って **68,401字**あり、読めないので
> 別に「取る順」表が要る二重管理になっていた（しかもすぐ腐った）。⇒ **索引は残数だけを持つ。**

> 🔴🔑**この節に「開いていない項目のカード番号」を書いてはいけない。**
> `census:cards` の `mech` フラグは **§5.3 の節テキスト全体を haystack にしてカード番号を拾う**
> （`cardProgressCensus.mjs:196`）＝**クローズ注記の中の引用カードまで「機構待ち」に化ける**
> （実測＝クローズ注記に書いた5枚のせいで、項目を1つ閉じたのに `mech` が 38 → 40 に増えた）。
> 🔴🔑**同じ理由で、クローズ注記を `| \`O-nn\`` で始まる表の行で書いてもいけない**＝
> `cardProgressCensus.mjs:176` はその形の行だけを「開いている項目」と読む（2026-09-06 に実際に踏んだ）。
> ⇒ **クローズ注記は散文で短く。カード番号は BUGFIXES.md に置く。この節を編集したら `npm run census:cards` で差分を見る。**

**この節の使い方**
1. 下の索引 **A → B → G → E** の順に、**母集団の大きいものから**取る（旧「取る順」表は廃止＝索引の並び順がそのまま取る順）。
2. ID を決めたら、**着手前に [PLAN_DETAIL.md](./PLAN_DETAIL.md) の同じ ID の登録票を読む**（経緯・実測・受け皿の在処・罠・「取り方」がそこにある）。
3. **②母集団を実測し直す**（§2.1 ②）。索引の数字は**登録・再計測時点の値**であって、いま正しいとは限らない。
4. **②' レーンを決める**（§2.0）＝**母集団1〜2効果（索引 G）は速いレーン**が既定、A/B は遅いレーン。

**登録のルール（2026-08-30 に決めた既定・2026-09-01 に再確認）**
- **新規登録は「索引に1行」＋「PLAN_DETAIL に登録票1項目」**。索引に長い経緯を書かない（それが旧構造の破綻原因）。
- 🔑**登録時に母集団を実測して書く。** 実測していないなら **D（母集団未計測）**へ入れる＝**`S`/`M`/`L` は実装量の見立てであって母集団ではない**（この混同で「S・1効果」の項目に34枚・19枚・15枚の塊が隠れていた前例）。
- **消化したら索引の行ごと消す**（ID は再利用しない。全文は PLAN_DETAIL と BUGFIXES が正）。
  🔑**クローズは「実装が終わった」ではなく「索引から行を消し、3箇所の残数（§1／§6／この節）を直す」まで**＝
  第178バッチはこれを落としていた簿記漏れの回収で、**§1 の「次の一手」が閉じた項目を5セッション指し続けていた**。
- **実機で確かめるだけの項目はここに書かない**＝§5.1 へ `V-<次番号>` で足す（守らなかった結果、旧 `O-13` が8185字・30ブロックに肥大した前例がある）。

**⚠索引の「母集団」列の読み方（3回とも同じ罠で外したので必ず読む）**
> 1. **`*` 印＝`liveRe` 未確定**＝「受け皿が既にあるカード」を引けていない＝**過大に出る**。
> 2. **`*` が無くても `liveRe` が不完全なら過大**＝`O-159` が実例（個別実測は1効果、この表は20枚）。
> 3. **原文 regex は用法を区別しない**＝この改訂でも `O-92` 58→13／`O-104` 40→0／`O-158` 19→0 と縮んだ。
> 4. ⇒ 🔑**着手時は必ず「原文を1枚ずつ読んで用法で割り、live と engine の消費地点まで見る」**。
> 5. 🔴🔴**2026-08-30 の結論＝この測り方には構造的な限界がある（同日 9回連続で過大に外した）。**
>    「原文にフレーズがあるのに live にキーが無い」は、**受け皿の"別名"を全部知らないかぎり必ず過大に出る**。
>    実例＝`census:wiring` の最大セル（24件）は**実際は1件**で、36件は `colorNotMatchesLrig` が
>    「エナ対象では対象オーナー基準で解決する」ため**既に正しかった**。`O-106`（25枚）も大半が `hasNoAbilities` で実装済み。
>    🔑**含意＝「受け皿の名前を知っている必要がある」計器は、知らない穴を測れない。**
>    **在庫の見積もりに使えるのは §5.2 の意味照合台帳**（LLM が意味で比較するので受け皿名を知らなくてよい）。
>    ⇒ **この表と `census:wiring` は「着手前のふるい」として使い、"どれだけ残っているか"の答えには使わない。**
>    2026-08-30 の1巡では**投入前実測が6回中6回とも登録票の見立てを訂正した**（母集団は毎回縮んだ）。

**測り直し方**＝`node scripts/archive/censusMechPopulation.mjs`（全項目の regex を保持。`--id O-nn` で1件）。**原文 regex で拾う → live の受け皿 regex で絞る**の2段。原文 grep が意味を持たない項目（engine / 計器 / UI 経路）は同スクリプトが別枠で列挙する。

> 🆕🔴**2026-09-01 の補足＝上の 5. の「在庫の見積もりには §5.2 の意味照合台帳を使え」はもう使えない。**
> 台帳は**残 OPEN 44 まで消化されており、その44も続き766 の全数 triage で「全件が `src/screens/` か新 engine 機構待ち」**と確定した＝
> **もう "知らない穴" を教えてくれる計器は無い。** ⇒ **在庫の総量を測る手段は現在ない**ことを前提に、**索引の A から順に潰す**のが唯一の進め方。

#### 🆕 1〜3枚の項目の取り方（型を足す前に読む）

1. 🔑**まず受け皿を疑う。** 2026-08-29 の実測で **2件とも既に在った**＝`O-123`（支払いUI `useTimeCost.ts` が丸ごと実装済み）／`WX14-049`（`forResonaCondition`/`resonaClass` ＋ `targetsTriggerSource`）。**探し方**＝①原文の言い回しで `src/` を grep ②同じ言い回しで `scripts/goldenTest.ts` を grep（**実装済みなら golden が張ってあることが多い**）。
2. **アクション側は「型」ではなく `STUB` ハンドラ1本で書く。** 実測＝`execStubPart*.ts` に **923本**の実績・**1本あたり平均17行**。`census:stubs` の A群（engine に消費が無い STUB）からも消えるので計器も追随する。
3. 🔴**条件側には STUB の道が無い。** `COND_STUB` は `execUtils.ts:2413` が **`return true`＝無条件成立**（live で2件しか使われていないのはそのため）。条件は **型＋`CONDITION_TYPES`＋`evalCondition`＋`checkActiveCondition`＋golden＋parser の6箇所**を必ず揃える（[LESSONS.md](./LESSONS.md) §4.2）。
4. **1枚のために機構を作らないと決めてよい。** その場合は**無言 no-op にせず** id を `DEFERRED_*` へ改名して理由をこの節に書く（`census:stubs` A群🔴 の運用どおり）。

#### 索引 A'. 実機が出した配線ギャップ

🏁**残0**。⚠**golden が緑のまま実機だけが出した穴**をここへ足す（2件とも真因は収集器やゲートではなく**その手前の配線**だった）。

#### 索引 A. 母集団2桁（**ここから取る**・遅いレーン）

🏁**残4項目**（2026-09-08 第232＝§5.0 実装キューの残353効果を系統化して登録した）。⚠**母集団は上限値**＝着手時に §2.1 ② で割り直す（`O-269` は登録時の見立て16 → 実測1だった）。

| ID | 母集団 | 何が無いか |
|---|---|---|
| `O-298` | **15効果**（`STUB{OPTIONAL_COST}`→`CONDITIONAL` かつ `STORE_LAST_PROCESSED_TARGETS` 無しの187効果のうち、**原文で対象が任意コストより前に来る**もの） | **自分の任意コストで「対象を先に確定する」軸が無い**＝engine は `STUB{任意コスト}` の直後の `CONDITIONAL` を組にして先取りする（`effectExecutor.ts:5530-5535` ほか）ので、原文「〜1体を対象とし、…を支払ってもよい」が**支払ってから対象を選ぶ**順に化ける。🔴**`O-288` は「相手が払う」側**＝こちらは自分側で分岐が別（`5530` 系と `5871` 系） |
| `O-299` | **21効果**（原文「場を離れる場合／バニッシュされる場合、代わりに」40効果のうち受け皿が無いもの） | **置換効果（「代わりに」）の受け皿が足りない**＝`BANISH_SUBSTITUTE` 系は在るが、`WX06-019-E1` のように「**対戦相手の効果によって**」「**場を離れる**（バニッシュより広い）」という発生原因・範囲の限定を持てない |
| `O-300` | **20効果（上限）**（`GRANT_LRIG_ABILITY` 132効果のうち原文に「ルリグ1体を対象とし」があるもの） | **`GRANT_LRIG_ABILITY` が対象を選ばない**＝プレイヤー共通の付与ストアへ入れる（`effectExecutor.ts:9779-9818`）ので、原文「あなたの**レベル3の**ルリグ1体を対象とし」の対象指定とフィルタが落ちる。⚠**20件すべてが穴とは限らない**（センタールリグ1体しか居ない盤面では差が出ない） |
| `O-301` | **10効果**（原文「次の（あなたの／対戦相手の）アタックフェイズ・ルリグアタックステップ」23効果のうち `INSTALL_DELAYED_TRIGGER` が無いもの） | **「次の〜フェイズ開始時に」の遅延が無く解決時に即実行**する。⚠受け皿（`INSTALL_DELAYED_TRIGGER`＋`ON_ATTACK_PHASE_START`）は実在＝**JSON の書き方の問題**が大半のはずだが、`ON_LRIG_ATTACK_STEP_START` 相当の timing が無いものは機構が要る |

⚠**新しく母集団2桁の項目が出たらここへ足す。**
🔑**2026-09-07 に `O-269` をクローズ**（スペル使用の色記録）。**登録時の見立て「母集団16」は過大**で、
着手して実測したら**14件は別名の受け皿で配線済み**（`triggerFilter.color`／`ARTS_USED_THIS_TURN.color`／
`COST_REDUCTION.color`）＝**真の穴は1効果**だった。全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md) と BUGFIXES にある。

#### 索引 B. 母集団 3〜8効果

⚠**新しく母集団 3〜8効果の項目が出たらここへ足す。**
🏁**2026-09-07（第215の投入前実測）＝`O-271` をクローズ**（ルリグ【起】の `fieldTrash` 未払い）。
🔴**実装は第204バッチ（`4712b91a7`）で既に終わっていたのに、索引から消し忘れて11日ぶん残っていた**＝
`lrigActivateGate.ts:144` に提示ゲート、`BattleScreen.tsx:15044` に `payFieldTrashCost` の呼び出し、
`fieldTrashCost.ts` に funnel、golden も `goldenTest.ts:70316` に在る。**登録票の「`upToCount` が無い」も失効**
（live の `SPDi44-16-E2` / `WX25-P1-030-E2` は既に `upToCount:true` を持つ）。
🔑**CODEX_GUIDE §3-1「簿記を信用せず実測する」は、自分が数日前に書いた索引にも効く。**

🆕🔥**残4項目**（第230 で2件・第232 で2件を登録）。

| ID | 母集団 | 何が無いか |
|---|---|---|
| `O-294` | **live 7効果**（`WX16-Re20-E1` / `WXDi-P03-034-E1` / `WXDi-P07-005-E1` / `-sub-E1` / `WXDi-P13-042-E1` / `-E2` / `WXDi-P15-046-E2`。原文「能力を持たないシグニとして場に出す」6効果とほぼ一致＝取りこぼし0） | **`ADD_TO_FIELD.abilitiesRemoved` に engine の消費地点が無い**＝`effectParser.ts:8112` が書くだけの真 no-op（`O-287` の `commonClass` と同型）。⚠**`abilitiesRemoved` という名前は engine にもう1つある**（`effectEngine.ts:2428` の `collectContinuousAbilitiesRemovedSigni`＝**CONTINUOUS の能力喪失**で別物・`BoardComponents.tsx:408` は `string[]`）＝**grep だけで「在る」と読むと外す** |
| `O-302` | **6効果**（原文「〜につき1つまで選ぶ」） | **可変選択数が表せない**＝`effectExecutor.ts:6449` は `action.choose_count` をそのまま使うので、原文「あなたのセンタールリグの**レベル1につき**1つまで選ぶ」が固定1に潰れる。⚠`CHOOSE.upTo` は「N個まで」の N が定数のとき用 |
| `O-303` | **3効果**（`WXK03-026-E4` / `WXK06-029-E2` / `-E3`） | **`zone_moved_just` が場外→場の入れ替えでも記録される**（`effectExecutor.ts:11804`）＝原文「**場にある**このシグニが他のシグニゾーンに移動したとき」に絞れず過剰発火する。🔴**live JSON にキーが無い**（engine 内部の state）＝**どの計器にも映らない** |
| `O-295` | **live 6効果**（`SPDi44-04-E2`＋`-GRANT` / `WX19-046-E3` / `WX25-P1-026-E2`＋`-GRANT` / `WXK03-011-E1`）＋**原文にあって JSON に無い1件**（`SPK01-13-E1`） | **「対戦相手の効果によってダメージを受けない」の期間が表せない**＝受け皿 `PREVENT_DAMAGE_FROM_OPP_EFFECTS` が立てる `prevent_lrig_damage` は **1回消費型**（`BattleScreen.tsx:13289` が消費時に `undefined` に戻す）。原文「**このターン**」「【常】」は回数無制限なので**2回目以降が素通りする**。⚠`PREVENT_DAMAGE`（期間型・回数無制限）は既に在るが `scope` が `'ALL' | 'LRIG'` だけ＝**「効果による」を表せない**（`ALL` にするとアタックのダメージまで防ぐ過剰実行） |

#### 索引 G. 新規分離（母集団 1〜2効果）

🔥**残8項目**（`O-287`/`O-288`/`O-290`〜`O-293`／🆕`O-296`/`O-297`）。
🆕🔴**2026-09-08 の S-2（母集団 grep 実測）で `O-289` は「機構不要」と確定して降ろした**＝受け皿 `taken_choice_keys`
（`src/types/index.ts:885`・読み `effectExecutor.ts:6432`・**ターン境界で消さずに**書く `execStubPart1.ts:922-930`）が実在し、
**parser が `CHOOSE.noRepeat` を出せば閉じる**（§5.0 の系統行へ移した）。
🔑**登録票の「受け皿が無い」も stale になる**＝着手前に必ず `grep` で消費地点を読む。
⚠**新しく母集団 1〜2効果の項目が出たらここへ足す**（速いレーンが既定＝§2.0）。

| ID | 母集団 | 何が無いか |
|---|---|---|
| `O-287` | **live 27効果**（grep 実測・findings 由来は9） | **`filter.commonClass` に engine の消費地点が無い**＝`effectParser.ts` が生成するだけの真 no-op（`census:deadstate` と同型）。「共通するクラスを持つ／持たない」がすべて素通り |
| `O-288` | **live 32効果**（grep 実測・findings 由来は12） | **`OPPONENT_PAY_OPTIONAL` の対象事前選択**＝相手が支払いを判断する前に対象を確定・保存する軸が無い（`freezeStoredTargets` は `targetsStored` 前提） |
| `O-290` | 🆕**S-2 実測 3カード**（登録4＝stale。`WXK03-014` は `coinReduction` で処理済み） | **キーを場に出すときのコスト条件・軽減**＝「場に出すためのコストは《コイン×0》になる」2枚（`WXK10-015`/`WXK11-012`）と「エナの色が3種類以上ある場合にしか出せない」1枚（`PR-K060`）。⚠**この3文は effectId が切り出されていない**（`_effect_srctext.json` に無い＝CSV 全文が正本）。専用 `PLACE_KEY_FROM_LRIG_DECK.coinReduction` はあるが**通常キープレイ経路（`BattleScreen.tsx:9074`／`KeyUseModal.tsx:35-56`）は印刷コインを直読み**する |
| `O-291` | 🆕**S-2 実測 1効果＝登録どおり**（`WXK11-020-E1`・MANUAL） | **`STRIP_OPP_ENA_MULTI_ENA` の後半**＝「対戦相手のエナゾーンのカードは対戦相手の効果を受けない」。消費地点は `costs.ts:1233` と `artsUseGate.ts:71` の2箇所だけで【マルチエナ】剥奪しか実装していない |
| `O-292` | 🆕**S-2 実測 3効果＝登録どおり**（`WXDi-CP01-006-E2`/`-007-E2`/`-008-E2`） | **通常効果からのコラボ**＝起動コスト「コラボライバー1人とのコラボ」。実行機構は `STUB{INTERNAL_DO_COLLAB}` にしかなく、`execStubPart3.ts:1311-1314` が「通常効果からの生成元が無い」と明記している |
| `O-296` | 🆕 **live 4効果中1件**（`WXDi-D09-H15-E1`）＋**原文にあって JSON に無い2件**（`WX11-051-BURST` / `WXDi-P01-039-E1`） | **`SET_BASE_LEVEL.until` が `'END_OF_TURN'` しか取れない**＝原文「次の対戦相手のターン終了時まで、基本レベルは3になり基本パワーは12000になる」の前半だけが**恒久化**する（隣の `POWER_SET` は `UNTIL_OPP_TURN_END` を持てるので**同じ文の2つのアクションで寿命が食い違う**）。🔑`O-293`（「次のエナフェイズ終了まで」）と**同じ「寿命の語彙が足りない」族**＝まとめて取ると安い |
| `O-297` | 🆕 **1効果**（`WXDi-P16-074-E2`） | **ON_BANISH のトリガー元シグニのゾーンを参照する軸が無い**＝原文「**同じシグニゾーンに【ゲート】がある**あなたのシグニ1体がバニッシュされたとき」。⚠**受け皿は2つ在るがどちらも軸が違う**＝`SAME_ZONE_HAS_GATE`（`execUtils.ts:2858`）は **`sourceCardNum`＝効果元**のゾーンしか見ず、`filter.inGateZone`（`effectEngine.ts:1207`）は**いま場に在るシグニ**の状態フィルタ＝**バニッシュ後のトリガー元**には当たらない（`prevOwnerState` が要る）。🔑**`FIELD_HAS_GATE` を使う live 10効果のうち 9件は原文が「あなたの場に【ゲート】がある場合」＝正しい**（誤用はこの1件だけ） |
| `O-304` | **2効果**（`WX18-020-E1` / `WXDi-P06-032-E1`。原文「〜の数以下の」8効果のうち動的上限キーが無いもの） | **動的な数値上限が表せない**＝原文「レベルが**あなたの場にいる白のルリグの数以下**の」。⚠`levelLteFieldVirusCount`（ウィルス数）は実在するので、**軸ごとに専用キーを増やす形が限界に来ている**＝汎用の「参照値で上限を決める」型が要る |
| `O-305` | **1効果**（`PR-469-E3`） | **起動をまたいだ「まだ選ばれていない」の管理が無い**＝`CHOOSE.noRepeat` と `taken_choice_keys`（旧 `O-289`）は**同一解決内**の重複を止めるもので、「この【起】能力で**過去に選んだ**選択肢」を跨いで覚える軸が無い |
| `O-293` | 🆕 **S-2 実測 10効果**（軸① リミット期限 10・軸② 付与能力期限 5・重複あり） | **「次のあなたのエナフェイズ終了時まで」の寿命ストアが無い**＝`LIMIT_CHANGE_UNTIL_ENERGY_PHASE_END`（`execStubPart1.ts:4208`）は `lrig_limit_mod` を足すだけで、それは**ターン終了時に消える**（`BattleScreen.tsx:4284` / `:4735`）。🔴**STUB 名・ログ文言「（エナフェイズ終了まで）」・型コメント（`types/index.ts:799`）の3つとも嘘**＝**受け皿が在るように見えて 7効果が2フェイズ早く切れている**。付与能力側（`GRANT_LRIG_ABILITY.duration`）も `UNTIL_OPP_TURN_END` が最長で原文に届かない |

🔴**全文の登録票は [PLAN_DETAIL.md](./PLAN_DETAIL.md) の「§5.3 機構 worklist 登録票の全文」にある**（着手前に同じ ID を読む）。

⚠**必ず表の行で足す**＝`cardProgressCensus.mjs:176` は `| \`O-nn\` |` の行しか「開いている項目」と読まない
（箇条書きで足すと**その項目が計器から丸ごと消える**＝過去に実際に消えていた）。
🔴**ID セルの先頭に絵文字を置かない**（`| 🆕\`O-281\`` と書くと同じ正規表現から外れる）＝
**2026-09-07 に `O-281`／`O-282` が実際にこれで計器から消えていた**（登録した当日から `mech` に数えられていなかった）。

🔑**この11項目で分かったこと（次に索引を作る人へ）**＝**登録票の「新機構が要る」は 11件中 3件が誤りだった**。
・`O-276`（`WX06-019-E1`）＝「まるごと恒久 no-op」は **stale**。実装は別軸（`collectLeaveSubstituteOptions` の
　`powerReduction` 軸）に**最初から在り**、真の穴は `cardMap.get(victimNum)` が **instance id を base 化していない**
　1行だけだった（同名2体目が victim のとき無言で守れない）。⇒ **登録票が名指しした関数だけを読んで結論しない。**
・`O-274`（`WX06-014-E2`）＝「1効果」は**過小**で、実体は **live 28効果**の engine バグ
　（トラッシュから固定N枚をデッキへ戻す経路が `cands.slice(0, N)` で先頭N枚を無言確定）。
・`O-286`（`WX21-Re18-E1`）＝「記録側が丸ごと無い」は半分 stale（`last_cost_trashed_cards` は在った）。
　足りなかったのは**色の記録**と**読み手の条件型**で、`PAID_COLORS_INCLUDE_ALL` は**基本コスト用の別軸**だった。

#### 索引 E. 計器の較正・掃除（**機構ではない**＝カードの挙動は変わらない）

🏁**残0**。⚠**これらを消化しても実機の挙動は1件も変わらない**＝効くのは「**計器が嘘をつかなくなる**」ことだけ。
**計器主導でバッチを選ぶ前**に取ると後続が楽になる（逆に、挙動を直したい回にここから取ってはいけない）。

> 🔑**索引をすべて消化したら各見出しの下に `🏁**残0**` と書く**＝`cardProgressCensus.mjs` は
> 「`#### 索引 ` の数だけ残0宣言があるか」で**「書式が壊れた」と「本当に空」を区別する**。
> 宣言を書き忘れると ⚠ 警告が出る（2026-09-06 に較正した）。

🔑**クローズした項目の経緯は [PLAN_DETAIL.md](./PLAN_DETAIL.md) の登録票と [BUGFIXES.md](./BUGFIXES.md) が正**＝
**この節には書き戻さない**（上の 🔴 のとおり計器が誤読する）。

#### 個別カードの機構待ち・監視項目（**worklist ではない**＝着手前の参照用）

> 🔴🔑**この節の使い方**（2026-09-06 に再整理）＝**ここに実作業を置かない。**
> 「worklist ではない」と書いた節に作業を置くと**索引にもキューにも計器にも出ない**＝
> 第182・第184 の2回とも、ここに埋もれた記述が**実測すると30効果／10効果の実バグ**だった。
> ⇒ **残ってよいのは (a) 母集団を実測したうえでの根拠つき defer と (b) 監視だけの既知の近似**。
> **stale が判明した記述はその場で消すか、索引へ `O-nn` で出す。**

**■ 根拠つき defer（着手前にこの理由が今も有効か再判定する）**

- **`WXDi-P05-006` choice① ＝着手禁止**（ピースカットイン割込み基盤）。**母集団 実測1効果/1カード**
  （`npm run census:population -- "カットインして使用できる"`・2026-09-06 再測）。
- **`WX20-Re20` ＝一体で要る**（選択数依存コスト・能力なし filter・任意複数配置UI・同一 instance 群の
  ターン終了時 trash）＝**部分実装しない**。**母集団 実測1効果/1カード**
  （`npm run census:population -- "選んだ数が[２2]つの場合"`・2026-09-06 再測）。
- **F. 保留**（core 改変が過大リスク）＝`WXDi-P00-026`（さんばかルリグ付与＝ルリグ再アタック未実装がブロッカー）／
  47枚の【使用条件】【チーム】（正規デッキ常時成立で機能等価＝保留妥当）。

**■ 既知の潜在結合（実害なし・触るときに読む）**

- ⚠**`executeSigniOnPlay` の `beatZones`（`Set<number>`）は `beat_signi` の「場ゾーン index」と
  `beat_signi_from_trash` の「トラッシュ index」で同じ集合を共有している。** 実データで両コストを併せ持つ効果は
  **0件**（全数走査）なので現状は実害なしだが、併記カードが出ると支払いが `ok:false` で**無言 abort** する。
- ⚠**付与ストア走査は `activeCondition` を評価しない**（`effectEngine.ts:3137-3142`）。今日の唯一のエントリ
  `WX24-P3-069-E1-G` は無条件なので実害なしだが、条件付き CONTINUOUS を付与する効果を足すときは
  effectsMap 側と同じ `checkActiveCondition` を通すこと。
- ⚠**UNKNOWN を実装するときは fresh parser の出力を先に測る**（H4 の教訓）＝fresh が主語を見ない
  `STUB DEPLOY_RESTRICT` を既に吐いており、live の `UNKNOWN` だけが「対戦相手は1体まで」への意味反転を
  止めていた（**採用した瞬間に過剰実行へ反転する型のドリフト**）。

**■ 監視だけしている項目（着手不要・壊れたら気付く）**

🔑**[LESSONS.md](./LESSONS.md) §4.8 へ移した**（2026-09-07）＝挙動が正しいと確認済みの近似・表示だけの不足・
恒久的な偽陽性の一覧で、**worklist ではなく「触るときに読む知識」**だから。⚠**ここへ書き戻さない**
（`cardProgressCensus.mjs` は §5.3 の本文からカード番号を拾うので、正しく動いているカードが `mech` に化ける）。

### 5.4 原文照合テール（構造混線）★🏁**2026-09-06 第191バッチで残0＝新規に取る節ではない**

> 🏁**閉じた。** (a) `UNKNOWN` を含む効果／(b) `parseStatus:'PARTIAL'` 20効果／(c) 続き749 の縦切り2件は全部処理し、
> **機構が要る2件は §5.3 索引 G へ `O-266`／`O-267` として採番して移した**（同日クローズ）。
> **経緯と内訳の全文は [BUGFIXES.md](./BUGFIXES.md) の第187〜第191バッチ**（PLAN には残さない）。

⇒ **新しく「原文と JSON の構造がズレた効果」を見つけたときだけここへ足す。**
🔑**足すときの既定＝登録時点の逆翻訳で立てた項目は腐るので、着手前に必ず現在の逆翻訳を読み直す**
（実測＝第160バッチは再照合した4件が**4件とも実装不要**、第191バッチは残2件が**2件とも実装済み**だった）。

**■ 測り直し方**＝`parseCardEffects` → `mergeManualEffects` を回して `parseStatus==='PARTIAL'` と
`"type":"UNKNOWN"` を数える使い捨てスクリプト（`tmp_*`）でよい（十数秒）。
🔑**live 側は `public/data/effects_*.json` を直接読む**＝fresh と live の差が「配送が止まっている件数」。
🔑**「なぜ PARTIAL なのか」は `parseStatus = 'PARTIAL'` の代入 23箇所を一時計装して特定する**
（`'PARTIAL'` を `(process.env.X ? (console.error(...), 'PARTIAL') : 'PARTIAL')` へ機械置換し、**すぐ元に戻す**）。
⚠**`markSilentFallback` の理由ログだけでは足りない**（実測 24件中0件がそこ由来）。

⚠**完了判定について**＝**census が0になっても「すべてのカードが完璧」にはならない**。census の死角は4つ
（参照解決の誤り／効果単位の粒度／JSON は正しいが engine 実装が違う／文間の実行順序）。
**最終的な判定器は BEHAVIOR_AUDIT と §5.1 実機検証。**

### 5.5 低優先・保留

- [ ] **CPU AI (g) 選択の精緻化**（旧 §8・盤面評価 v2 候補）＝(a)〜(f) は消化済み。⚠**§5.1 の実機返済を終えてから着手する**。⚠**CPU の対話自動応答は元から完備**（`BattleScreen.tsx:526`〜）＝効果を起動できれば対象選択まで自動で回るので、要るのは「起動する経路」と「何を撃つかの判断」だけ。
- [ ] **逆翻訳機の出力品質の残テール**（旧 §5b）＝混線テール実測823カードの再parse。§5.4 (iii) と同じ母集団。**英語 ID 漏れは `census:stubs` C群がゲート化済み**（0でゲート・増えたら exit 1）。
- [ ] **残る単発テール（原文とJSON構造がズレた混線／未構造化STUB）**＝**2026-07-02時点で「1 effect=1クリーンSTUB」で原文抽出できるものは全消化済み（444→367→その後の作業でカード母集団が変動し2026-07-12実測823・続き87で機械分類済み）**。effect構造そのものが原文とズレた混線で、1つのSTUBを原文化しても同 effect 内の他のズレが残り原文一致にならない＝decompilerの原文抽出では対応不能なものが大半。**effects JSON の再parse（機構実装・データ層修正）が本筋＝Opusタスク13**。系統別内訳（16テーマ・カード数の多い順＝デッキ操作系184／パワー修正系165／手札系102／トラッシュ系75／対戦相手コスト系63／エナ系50／ライフ系48／シグニ配置系48／ルリグ系36／能力付与系31／ガード・アタック制限系26／ソウル・アーツ系15／ウィルス系10／色・クラス系4／ゲーム除外系3／チャーム系1／その他54）は`docs/_stub_leak_classification.txt`参照（続き87・Sonnet）。進め方＝1カードずつ effects JSON を原文どおりの構造に手修正→逆翻訳が原文一致するか確認→smoke/golden/fuzz→push（**原文コピーでの一括潰しは禁止**＝実装未完成を隠蔽し検証目的に反する）。
- [ ] **🆕生JSON漏れ（`【シャドウ:{"levelGte":3}】` 形）72件**（2026-08-07 続き366 で観測）＝`シャドウ:{…}` は**JSON データ側の符号化**（engine もこの文字列を読む）なので、表示だけ日本語化するには decompiler に整形器を足す。`levelGte/levelLte/powerLte/cardType/color` の素直な形のほか `selfPowerHalfLte` `lrigTrashArtsColor` `downerLrigLevel` `declaredNumberPowerEq` `declaredColor` `artsCostLte` `selfColor` `selfPowerLte` など**約20種**あり、**1種ずつ原文照合が要る**（【シャドウ（レベル3以上）】等の原文表記に合わせる）。⚠英語**条件**ID漏れは続き366 で残0にした。
- [ ] **`doPhaseAdvance` の pure 抽出は「やらない」を既定**（費用対効果逓減と結論済み）。CPU 統一で必要になった部分だけ最小限切り出す。
- [ ] **BEHAVIOR_AUDIT キューの消化（⛔休眠）**＝`node scripts/_bqTriage.mjs` で高シグナル選別 → `npm run audit -- --id <CardNum>` で目視 → 「真no-op／シナリオ空振り／STUB未実装」に仕分け。**続き133 で高シグナル22件を精査して真 no-op バグ0件＝枯渇**。再開するなら**監査ツールの構造的盲点フィルタの実装が先**（低収量見込み）。
- [ ] **リリース判定**：fuzz 重め（`npm run fuzz -- --games 2000 --moves 80`）＋実機 PvP/CPU 通し対戦スモークをリリースゲートに。DESIGN §5 の手順（version bump→CI→push→`npx vercel --prod`）で本番反映。

---

## 6. 恒久指標（最新1ブロックのみ）

> **運用**＝この節は**「いまの数字」だけ**を置く。新しく作業したら ①上のブロックを [PLAN_DETAIL.md](./PLAN_DETAIL.md) の恒久指標アーカイブへ移す ②今回の値へ書き換える。⚠**溜め始めたら破綻する**（続き550 の整理時点で計測行15本＋ポインタ37本まで膨れ、cold start が最初に読む節が一番古い状態だった）。
> 🆕🔴**2026-09-01 改定＝3計器だけでは進捗が表示できなくなったので「在庫2本」を併記する**（理由は §3 の同日改定）。**3計器は底を打った＝これ以上は下がらないので、動かないことを「停滞」と読まない。**

- **2026-09-09（第233バッチ・Sonnet 5＝Codex 委譲・本ブロックが直近の正）**
  📊**進捗3計器**＝**Sheet1 要対応 3 / 863**（据置）｜**台帳 残 OPEN 0**（据置）｜**census 高シグナル 0 / BASELINE 0**（据置）。
  ⚠**3計器が動かないのは想定どおり**＝実装キューの triage 済みバグを直しても、Sheet1／台帳／census のどの母数にも入らない。
  📦**在庫**＝🔥**実装キュー 318効果**（353 → 318・`node scripts/archive/semanticAuditBugList.mjs`／確定433 − 消化済み116）｜
  🔥**機構 worklist 18項目**（据置）｜**⑤実機 残 0件**（据置）｜🏁意味照合 未監査 0枚・未 triage 0件（据置）。
  🔧**ゲート（全緑 ✅）**＝**golden 3709 PASS**（3690 → +19＝第233バッチ新規）／ smoke 10744 OK ／ fuzz 200ゲーム 0 ／
  census 0 / BASELINE 0 ／ census:stubs A群 0・C群 0 ／ census:enginetext A🔴 0行 ／ census:costtext A🔴 0規則 ／
  lint 0 errors / 254 warnings（投入前と同値）。**Claude 独立実行で全数検算済み**（`git diff` effectId 単位差分16件一致）。
  🖥**実機＝不要**（`src/engine/`・`src/data/effectParser.ts` のみ＝`src/screens/` 不触＝PLAN §2.2 の機械判定）。

## 付録B. 偽陽性パターン（脱落疑いに出るが**直さない**）— 毎回まず除外

1. **使用条件＋本体**（「このカードは〜の場合にしか使用できない」が前置き）＝条件として正しく表現済み。【真の偽陽性】
2. **CHOOSE/チェインの1文圧縮**＝「以下の[N]つから[M]つ（まで）を選ぶ」で改善済み（BUGFIXES③）。択肢が全部出ていれば機能的には正しい。
3. **REVEAL_AND_PICK / LOOK_AND_REORDER の文法崩れ**＝主要系統は是正済（BUGFIXES上部）。残りは§5.5の低優先テール。
4. **ルール注記**（「（コストのない【出】能力は発動しないことを選べない）」等）＝効果ではない。
5. **アンコール/ベット注記のみ**訳に出ない＝本体が合っていれば正しい。
6. **BET_MECHANIC STUB**＝§5.5（機構待ち）。
7. **owner:any の一括変換は禁止**：POWER_MODIFY/BANISH の `owner:'any'` は大半が正当（「シグニ1体を対象とし±N」＝自他選択／「すべてをバニッシュ」）。原文に明示主語があるものだけ個別是正。
8. **`[STUB:id]` を含むからとスキップしない**：実装済みハンドラのタグ表示。ただしハンドラがカード全体を覆うか（◎）／断片だけで残りを落としたか（実バグ）はタグでは区別不可＝各外れは個別検証。[[stub-means-implemented]]
9. **LIFE_BURST 内 `CONDITIONAL{IS_MY_TURN}`** は実害なし（常時true＋「そうした場合」特別処理）。修正不要。

## 付録C. 触らなくてよい/枯れた系統（調査済み）

- 強制アタック＝実装済み（未配線は WX12-010 複雑レゾナのみ）。BURST丸ごと欠落＝残0。保護系キーワードのowner誤り＝残0。
- 同型★（`grouped_all.txt`）＝**枯れた・常に0維持**。残1件 `WX04-056` は無害な表現差（任意）。
- 「あなたのアタックフェイズ開始時」系（self約407件）は**全再生成禁止**（約90枚退化）。個別にtiming/triggerScopeを直す。

### 付録C-2. 残・大型機構オーナー表（ほぼ完了の台帳）

着手前に**この表の「状態」を `着手中(担当名)` に更新してコミット**（重複防止）。実装の型は §3「機構実装の型」に従う。

| 機構 | 影響 | リスク | 状態 |
|---|---|---|---|
| 引用AUTO付与（`GRANT_QUOTED_AUTO_ABILITY`） | 中 | 中 | **表現完了＋engine精緻化(B4)着手済**＝引用【自】/【常】能力を実発火（自場シグニ・ターン限定・parse成功時のみ）。残＝permanent/相手付与対応・誤パース是正（約30枚は原文に引用無しparser案件）。⚠要実機検証 |
| ~~SET_TRAP／動的閾値フィルタ／遅延条件トリガー／《相手ターン》《自分ターン》AUTO基盤／ビート機構Phase1-7／傀儡場出し汎用化・levelLteLastProcessed~~ | — | — | **✅完了**（詳細 [PLAN_DETAIL.md](./PLAN_DETAIL.md) 付録C） |
| engine未配線 timing 群の実機配線 | 大（~15 timing・R33-R58） | 高 | **✅C1全配線完了**。残るは実機検証のみ（§5.1参照）。 |

実装済み機構の履歴：コスト増加・ライフクラッシュ履歴・LOOK_PICK_CHAIN field宛先・リコレクト系統・改造素材機構・引用能力付与型・保護/制限系STUB・アーツコスト軽減句 は `BUGFIXES.md` 参照。

---
**関連**：[DESIGN.md](./DESIGN.md)（設計方針）／[PLAN_DETAIL.md](./PLAN_DETAIL.md)（消化済み履歴）／[PLAN_PROGRESS.md](./PLAN_PROGRESS.md)（過去セッション要約）／[BUGFIXES.md](./BUGFIXES.md)（修正記録）／[BEHAVIOR_AUDIT.md](./BEHAVIOR_AUDIT.md)／[SEMANTIC_AUDIT.md](./SEMANTIC_AUDIT.md)／[CODEX_GUIDE.md](./CODEX_GUIDE.md)／[BATTLE_CONTROLLER.md](./BATTLE_CONTROLLER.md)／[effects-json-guide.md](./effects-json-guide.md)／[STUBS.md](./STUBS.md)
