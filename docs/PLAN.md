# PLAN — 開発計画（唯一の正）

> **このファイルの読み方**＝新セッション（cold start）は **§1 現在地 → §2 作業の流れ → §5 作業キュー** の順に読む。設計方針は [DESIGN.md](./DESIGN.md)、修正記録は [BUGFIXES.md](./BUGFIXES.md)（新しいものを上）。
> **ここには「現在地・流れ・ルール・教訓・生きている worklist」だけを置く。** 消化済みの経緯・完了項目の全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md)、過去のセッション要約は [PLAN_PROGRESS.md](./PLAN_PROGRESS.md)、一次記録は [BUGFIXES.md](./BUGFIXES.md) にある。**クローズした項目はこのファイルから消す**（ID も残さない＝退避先と git 履歴が正）。
> **2026-08-23 に全面再編**（167,481字 → 約半分）。①フェーズ軸だった旧 §5(P1)/§6(P2)/§7(P3) を廃止して **§5 の単一キュー**へ統合（入口が3つあると作業順がばらつくため）②**Opus/Sonnet のモデル分担を廃止＝Opus 単独運用**③クローズ済み記述 179行・67,632字を削除し、そこに埋まっていた教訓を **§4 教訓集**へ集約④**§2「作業の流れ」を新設し、実機検証を1巡の中に含めた**（再編前の PLAN 全文は git 履歴＝再編直前のコミットにある）。
> 🆕**2026-09-01 に再編（第2回）**＝**3つの進捗計器が同時に底を打った**ことを受けて、**計器主導 → 機構主導**へ構造を寄せた。①**§5.3 を「登録順の巨大テーブル」から「母集団順の索引 A〜F」へ**作り替え、登録票の全文（82項目・68,401字）を PLAN_DETAIL へ**無改変で退避** ②**§5.2（意味照合）を本線から降ろし**、§5.4 の (i) 配線ギャップ・(ii) 機構ギャップを **§5.3 へ統合**（機構の置き場を1つにした） ③**§5.1 のクローズ済みエントリを教訓7本へ圧縮** ④**進捗指標に「在庫2本」（機構残／実機残）を追加**（§3・§6）。**PLAN 本文 195,282字 → 100,385字（-49%）。**

---

## 1. 現在地（直近1セッション）

> **運用**＝この節には**直近1件の要約だけ**を残す（入れ替え式）。新しく作業したら ①いまの要約を [PLAN_PROGRESS.md](./PLAN_PROGRESS.md) の先頭へ移す ②この節を今回の要約へ書き換える。**溜めない**（溜めると cold start が最初に読む節が一番古くなる）。

- **セッション（2026-09-04・`O-60` 第64〜68バッチ・索引 A 第24〜28巡・Opus 5 単独）＝**🏁**A群最大 catch-all が live 0 に到達**
  📊**進捗3計器＝Sheet1 要対応 17 / 863（据置・全部 `mech`）｜台帳 残 OPEN 44（据置）｜
  census 高シグナル 3 / BASELINE 3（据置）**
  **この項目の計器 `census:enginetext` は A🔴 13行 / 12ハンドラ（据置）だが、`live` は 27 → 0。**
  🏁**主産物＝引用付与 catch-all の3綴り（`GRANT_ABILITY_INNER_TEXT` /
  `GRANT_QUOTED_ABILITY` / `GRANT_QUOTED_AUTO_ABILITY`）を live 0 にした**＝
  第63の較正時点で 27効果あった母集団を、5バッチ（第64〜68）で**全部**受け皿か
  **名前のある穴**へ振り分けた（`O-235` クローズ）。内訳＝**実装で解いた 11効果**／
  **明示 defer にした 8効果**（＝`O-236`〜`O-242` の7項目に登録）／第63で消化済み 12効果。
  📦**在庫2本＝④機構 worklist 39項目**（`O-235` クローズ −1／🆕`O-236`〜`O-242` 登録 +7＝33→39）｜
  **⑤実機 残 15件**（🆕`V-145`＝14→15）。
  gates 全緑（golden **3418 / 3418**＝+14本・smoke 10725 全異常0・fuzz 全0・census 3 / BASELINE 3・
  census-stubs A🔴0・C0・manual-fields 0・census-enginetext **A🔴13**・census-costtext A🔴0 据置・lint 0 errors）。
  **ブラスト半径＝効果 変更14・追加0・削除0、予定外0**（14カード×各1効果ちょうど）。
  🖥**実機＝機械判定では不要**（`src/screens/` 0行）だが `src/engine/` を2箇所触り、
  **14効果の挙動が変わった（うち10効果は恒久 no-op から実際の効果／明示 defer へ）**ので `V-145` を登録（未実施）。

  🔴**この巡でいちばん重い発見＝PLAN の記述が間違っていた。**
  旧 §1／`O-60` 登録票の「**A群の2行が減るのは 3 id すべてが live 0 になったとき**」は**誤り**。
  `census:enginetext` が数えているのは **engine のコード行**なので、**live が 0 になっても
  ハンドラを消すまで行は 1 行も落ちない**（実測＝live 27→0 で A🔴 13行のまま）。
  ⇒ **`O-60` の次の一手は「parser 側の catch-all 生成地点を畳んでから、ハンドラごと撤去する」**。
  ⚠**いま消してはいけない**＝parser には安全網の生成地点が5箇所残っており、PLAN §5.3 の
  「**parser に生成元がある安全網は触らない・消さない**」に当たる。生成元を先に畳む。

  🔑**この5巡の一般則**＝
  ①🔴**「engine では1本のハンドラ」なのに parser 側の復元規則が綴りを1つしか見ていないことがある**
    （第65＝`restoreQuotedTargetGrant` が `GRANT_ABILITY_INNER_TEXT` だけを見ており、
    **どの綴りに落ちたかだけで構造化の有無が変わっていた**）。**engine 側の分岐条件を parser 側の
    ガードにも同じ広さで写す。**
  ②🔴**宣言型 STUB は CONTINUOUS として読まれて初めて意味を持つ**＝引用の中身が宣言型なら
    `GRANT_EFFECT` で包んで `granted_effects` へ積む（裸で即時実行すると**誰も読まない**）。
    `GRANT_PROTECTION` は `execGrantProtection` が自前で包み直すのでこの問題が出ない
    ＝**「アクション型」と「宣言型」で扱いが違う**（第64）。
  ③🔴**live が `PARTIAL` の効果は parser をどう直しても届かない**（収穫マージが効果単位で不可侵・
    `heldReview --adopt` も held バケツにしか効かない）＝`manualEffects.ts` ＋ `syncManualLive` だけが道（第64）。
  ④🔴**「見出しだけ取れた」は宣言ではない**＝`gameGrants` が `abilityBlockHeader` だけのときに
    兄弟の catch-all を落とすと**穴が計器から消える**。名前のある穴へ置き換える（第68）。
  ⑤🔴**近似で既存の受け皿へ寄せない**＝寄せた4件はどれも過大実行になる形だった
    （期間つきプレイヤー付与→ゲーム中ずっと／クラッシュ順つきバースト付与→ライフ全部／
    ルリグのアタック上限→無制限／「最初のグロウ」条件落ち→グロウのたびにエナチャージ）。

**▶ 次の一手**＝**`O-60` のハンドラ撤去（A🔴 13→11行）**＝①parser の catch-all 生成地点5箇所
（`parseSentencePart2` の `」を得る` 末尾／`parseSentencePart4` 2箇所／`effectParser` の引用漏出安全網／
`normalizeGrantKeywordSpelling`）を**名前のある穴か構造化**へ畳む →②`execStubPart1.ts:1459` の
GRANT_QUOTED_* ハンドラと `effectEngine.collectGrantedFromLayer` の STUB 分岐を撤去 →
③`BASELINE_SELF_TEXT` を 11 へ下げる。
次点＝**`O-234`**（`choiceTextParser` 最後の呼び出し元・⚠`src/screens/` を触る）。

---

## 3. 不変の運用ルール

- **`effects_*.json` の curated 値は「上書きされない」ことが保証されている。**`build:effects` は ①`parseStatus` が `MANUAL`/`PARTIAL` を含むカードを**カード単位で温存**（`PRESERVE_STATUSES`）②それ以外も fresh が curated の純粋な上位集合でなければ**温存して held へ送る**（`buildEffectsJson.ts:158-162`）ため、**再生成しても勝手に値が消えることはない**。`heldReview --adopt` で明示採用したものだけが変わる。
- **逆翻訳を直したらエンジン実装までセット**（乖離＝偽陰性を作らない）。語彙が無ければ §5.3 の機構として実装するか、`engineUnwiredTimings` に登録し逆翻訳へ `【※engine未配線】` を付けて明示する。[[decompile-engine-parity]]
- **日本語を含むスクリプトは `scratchpad` に `.mjs` を書いて `node <path>` 実行**（Git Bash 経由の `node -e` は文字化けする）。papaparse 等が要るカード参照スクリプトは project root に一時 `.ts/.mjs` を置いて `npx tsx`/`node` 実行・終わったら削除。
- 🆕🔴**進捗は「3つの計器の併記」で報告する**（2026-08-27 続き684・**ユーザー決定**）＝**①Sheet1 要対応カード数**（`npm run census:cards -- --sheet 1`）**②意味照合 段2 台帳の残 OPEN**（`node scripts/archive/semanticAuditLedger.mjs`）**③census 高シグナル数**（`npm run census`）。**どれか1本を「進捗指標」に固定しない。**
  - 🆕🔴🔑**2026-09-01 改定＝3計器は底を打った。併記は続けるが、これだけでは進捗が表示できない。**
    **実測**＝**①18 / 863（うち `mech` 18・即着手可能 0）／②44（全件が UI か新機構待ち・続き766 の全数 triage）／③11 / BASELINE 12**。
    ⇒ **3本とも「これ以上は下がらない」ところまで来ている**＝**動かないことを「停滞」と読んではいけない**（続き768 は22効果を直して3計器とも据置だった）。
    🆕**代わりに「在庫2本」を併記する**＝**④機構 worklist の残項目数**（§5.3 の索引 A〜C・E）**⑤実機の残 `V-nn` 数**（§5.1）。**これが 2026-09-01 以降の進捗指標**（§6）。
    ⚠**在庫2本も「減れば前進」とは限らない**＝母集団を実測すると**項目が割れて増える**ことがある（§5.3 の登録ルール）。**減った理由／増えた理由を必ず1行書く。**
  - **理由（実測）**＝続き684（Sheet1 B6）は **11効果を直したのに Sheet1 の枚数は 0 減**だった。直した中で Sheet1 は1枚だけで、そのカードは**別の finding が残る**ため計器から落ちない。逆に続き679 は**計器のバグを直しただけ**で clean 率が 83.2%→85.7% に動いた（前進ではない）。**単独の計器は「進んだのに動かない」と「動いていないのに動く」の両方を起こす。**
  - **3本が別のものを測っている**＝①は**カード単位の網羅**（1枚に findings が複数あると1つ直しても落ちない）②は**指摘単位の消化**（標本なので、同じ parser 規則で直った標本外の実バグは数に出ない）③は**語彙の欠落**（STUB/MANUAL を免除するので、STUB を解体すると**増えて見える**）。
  - ⚠**バッチの着手時に「今回どれが動く見込みか」を宣言する**（続き594 の教訓＝計器主導でバッチを選ぶと台帳が動かない／PLAN_PROGRESS の第32バッチ＝母集団が `findings.jsonl` 外だと OPEN は構造的に 0 のまま）。**動かない計器があること自体は失敗ではない。動かない理由を書けないことが失敗。**
- **件数メトリクスを信じない**：「脱落疑いNN枚」は「。区切り文数」比較で粗く、逆翻訳器は複数効果を1行（、／そして）に圧縮するため内容を直しても件数は減らない。`_dropTriage` の分類（「文法崩れ」等）も構造ベースで文法品質は測れない。判断は必ず **同型★0＋該当カードの逆翻訳が原文一致（目視/grep）** で行う。
- **ゲートは `npm run typecheck`（＝`tsc -b --noEmit`）**。plain `tsc --noEmit` は project references を見ず CI が拾うエラーを見逃すので不可。
- **全再生成系の一括置換は禁止**（無検証置換で約90枚退化の前例）。系統ごとに機構を1回確立→同パターン適用→各カード verify。
- **CSV の順番を必ず維持する**（スクリプト内の `sorted` ロジックで対応済み）。

### 横展開（系統バグ）のときの追加手順

①**抽出**：全シート走査で「同じ壊れ方」を機械抽出（`scratchpad` の `scan*.mjs` が雛形）。②**分類**：偽陽性(付録B)・既知複雑札を除外し、クリーンな系統を確定。③**パッチ**：`effectId` をアンカーにした一括スクリプトで安全に置換（他カードを巻き込まない）。MANUAL化する場合は `parseStatus:'MANUAL'`。④**検証ゲート**：上記ワークフローの④〜⑦と同じ。⑤**記録＆バトン**：`BUGFIXES.md` に追記（新しいものを上）→本ファイル §4 を上書き→コミット（末尾に「要実機検証」）→push。

### 機構実装の「型」
1. `src/types/effects.ts`（アクション/条件/timing の型）→ 2. `src/types/index.ts`（`PlayerState` 状態フィールド）→ 3. `src/engine/effectExecutor.ts`（実行）/`execUtils.ts`（`evalCondition`/`matchesFilter`）/`effectEngine.ts`（CONTINUOUS収集）→ 4. `src/screens/BattleScreen.tsx`（状態読み取り＋**ターン境界リセット3箇所**：PvP通常終了・PvP確認後・CPU）→ 5. `scripts/decompileEffects.ts`（表示）→ 6. JSON 配線 → 検証。

### 主要ファイル
- 語彙: `src/types/effects.ts` / `src/types/index.ts`（PlayerState）
- エンジン: `src/engine/effectExecutor.ts`（`execLookPickChain`/`payBeatSigniCost` 等）・`execUtils.ts`（`evalCondition`/`matchesFilter`/`addToBeatZone`）・`effectEngine.ts`（CONTINUOUS収集・`checkActiveCondition`）
- UI/ルール: `src/screens/BattleScreen.tsx`（コスト計算・バトル・ターン境界リセット・`crashOneLife`）
- 逆翻訳器: `scripts/decompileEffects.ts`、グルーピング: `scripts/group{Similar,BySentence}.mjs`（`--all` で全10シート統合）
- 監査: `scripts/behaviorAudit.ts`（`npm run audit`/`audit:html`/`audit:queue`）

---

## 4. 教訓集（同じ穴に落ちないための知識）

> **2026-08-23 新設。** 再編前はこれらが**クローズ済みタスクの本文に埋まっていた**ため、完了行を消すと巻き添えで消える状態だった。ここへ集約したので、**§5 のどの項目に着手するときも先にこの節を読む**。各行の末尾は根拠（実測した回）。

### 4.1 着手前 — 見立てと母集団

- 🔴**worklist の「見立て」は実測と食い違う。着手前に必ず数え直す。**（続き629〜636 で**9回連続**で当たった。実例＝「少なくとも3枚」が実測75効果／「caller 0件」が実は既に2箇所配線済みで正常／「新しい仕組みが要る」が実は既存フィールドに消費実績あり／「hasCharm 68効果」が実測17効果／「998効果」が実測29効果／miss「541」が実測242）
- 🔴**母集団はカード全文ではなく `docs/_effect_srctext.json`（効果単位の原文・`build:effects` が生成）で数える。** カード全文で数えると同じカードの別効果まで巻き込む。
- **母集団の切り方＝10原則**（続き603〜608 で確立・詳細は `CODEX_GUIDE.md` §5）＝①消費地点まで見る ②srctext でなく CSV ③完全一致で数えない ④同義語彙を全部列挙 ⑤「変な形＝バグ」と決めつけない ⑥原文の括弧内ルール説明を能力として数えない ⑦再帰探索で入れ子まで拾わない ⑧既存の受け皿型を先に一覧化して引き算する ⑨カード単位の「個数差」で数えない ⑩`CardData_Sheet8.csv` の先頭 BOM を必ず剥がす
- ⚠**原文の `《…》` はカード名だけでなくコスト記号にも使う**（`《黒》`／`《ダウンズ》`/`《ターン１回》` 等）＝素朴な正規表現でカード名を数えると過大計上になる。
- ⚠**簿記の件数を完了指標にしない。**「脱落疑いNN枚」「census NN」は粗い網で、内容を直しても件数が減らないことがある。判断は**同型★0＋該当カードの逆翻訳が原文一致**で行う。
- ⚠**「クローズ済み系統だから全部偽陽性」でも「全部バグ」でもない**（実例＝「そうした場合」94件のうち真に要調査は9件）。**クラスタ単位で機械照合してから triage する。**
- 🆕🔴**実測ハーネスが本番と同じ型・同じ生成物を使っているか先に確かめる**（2026-08-27 Sheet1 B13 で実測）。
  「見立ては実測と食い違う」の**次の段**＝**実測そのものが嘘をつく**。`computeArtsEffectiveCost` を直接叩いて
  「場の＜クラス＞シグニ比例のコスト軽減が10効果とも不発」と結論したが、原因は**テストが素の `Map` を
  `cardMap` に渡していた**ことだった。本番は `InstanceMap`（`get` が `getCardNum` へフォールバックする派生クラス）で、
  測り直すと **9件中8件は HEAD で既に動いていた**（本当に壊れていたのは別原因の1件だけ）。
  ⇒ **派生クラスで挙動が変わる引数（`cardMap` / `effectsMap`）を素の型で渡さない。**
  ⇒ **「直した」と思ったら、直す前のコードで同じハーネスを回して FAIL することを確かめる**（反転確認）。
- 🆕🔴**②「母集団を実測する」は、母数だけでなく「受け皿が既にあるか」も測る**（2026-08-28 続き709・`O-123`）。登録票は「`costScaling` に `countSource:'thisPayment'` 相当が要る／🔴**UI が先**＝支払いパネルの再計算が成立するか確かめる」と**新機構の設計まで書いていた**が、**そのパネルは既に在って動いていた**（`useTimeCost.ts`＝タスク12(lxxxv)/(lxxxix)。golden も33枚ぶん張ってあった）。⇒ **既存関数に母集団を流すだけ**（`parseUseTimeCostReduction` に15枚）で **15/15 が読めた**ことが分かり、作業が「新機構の実装（M）」から「提示ゲート1行の修正（S）」に変わった。⚠**受け皿の探し方**＝①型名ではなく**原文の言い回し**で `src/` を grep する ②`scripts/goldenTest.ts` を同じ言い回しで grep する（**実装済みなら golden が張ってあることが多い**＝今回はここで一発で出た）。
- 🆕🔴**新しい export を足した回は「ファイル単位の `git stash`」で反転確認できない**（2026-08-29・`O-140`）。
  `src/engine/effectExecutor.ts` だけ戻すと `BattleScreen` の import が解決できず**ビルドが落ちて実機が動かない**
  （`verifyBattleDrive` は静かに終わるので、**FAIL ではなく「何も出ない」**という紛らわしい形になる）。
  ⇒ **新機構の反転確認は「分岐だけを一時的に殺す」**（`if (false && a.splitTotal)` のように条件へ `false &&` を足す）。
  ⚠**戻し忘れが致命的**なので、殺す→実行→戻すを**1つのコマンドにまとめて** `git diff --stat` で確認する。
- 🆕⚠**「割り振る／分ける」機構の実機観測は、1体に寄せた結果でも PASS してしまう**（同・`O-140`）。
  UI の `＋` を `.first()` で押し続けると全部が先頭の対象に乗り、「割り振れた」ことの証明にならない。
  ⇒ **`nth(k % n)` で交互に押して「2体に分かれた」ことまで assert する。**
- 🆕🔴**「据置」と記録する前に fresh を見る**（2026-09-01 続き768・`O-188` 第5）。
  収穫マージは **fresh が純粋上位集合でない変更（キーが減る形）を live へ届けない**。
  その状態で**負方向 golden を live だけに張ると、parser が既に直っていても緑のまま**＝
  **「直っていない」という記録だけが残り、`heldReview --adopt` の1コマンドで済む在庫が眠る**
  （実例＝`WXDi-P09-043-E2` は第2バッチで「上流規則に食われて未修正」と記録され負方向 golden まで張られたが、
  その時点で fresh は正しく、live が古いだけだった）。
  ⇒ **据置を記録する golden は live と fresh の両方を assert する。** §5-29（live だけの assert は
  parser の退行を見逃す）の**逆向きが同じ穴**だと気付くこと。
  ⇒ **バッチの前に `docs/_held_fresh.json` を見る**（`_partial_fresh` / `_idset_fresh` も同様）。
- 🆕🔴**STUB の日本語ラベルは「実装の要約」であって「原文の要約」ではない**（2026-09-01 続き768・`O-188` 第6）。
  `BANISH_MULTI_COLOR_SIGNI` は原文「白、赤、青、緑、黒のシグニを**それぞれ１体**対象とし、それらを**トラッシュに置く**」に対し、
  engine が **「2色以上を持つ相手シグニを、選択させずに全部バニッシュ」**を実装していた。
  🔴**どの計器にも映らない**＝engine に消費地点があるので `census:stubs` A群🔴 に出ず、カード全文 regex を
  読まないので `census:enginetext` にも出ず、**逆翻訳はその（間違った）ラベルを日本語で表示するだけ**なので
  C群ゲートも通る。⇒ **STUB を見たら、ラベルではなく原文とハンドラ本体を突き合わせる。**
- 🆕⚠**逆翻訳が「群」や「フィルタ」を描けないと、直したこと自体が読めない**（2026-09-01 続き768・`O-188` 第4/第6）。
  `transferGroups` の逆翻訳は `cardType` と `color` しか描いておらず、クラス・レベル・アイコン・宣言クラスが
  **逆翻訳から丸ごと消えて**いた（＝原文照合という主軸の検査が効かない）。群に `cardType` を持たせないと
  「カード1体」と描かれるのも同じ穴。⇒ **typed 化の PR では必ず `npm run regen` して逆翻訳を1行読む。**
- 🆕🔴**同じ意味を2通りの構造で表す実装があるとき、片方を一般化すると もう片方に二重適用される**
  （2026-09-01 続き768・`O-188` 第7）。「〈A〉1枚と〈B〉1枚」は**群1つ**でも**2ステップ**でも表せる。
  群の生成規則を一般化した瞬間、**既に2ステップへ割れている効果の各ステップにも群が乗って合計枚数が倍**になった
  （`WXEX1-30-BURST` が 2枚 → 4枚）。⇒ §5-3-4″（別構造で正しい群は触るな）は**据置するだけでは足りない**＝
  **「対象ノードが1つのときだけ畳む」ガードと対照テスト**まで書く。
- 🆕🔴**反転確認が PASS したら、まず「観測点」を疑う**（2026-08-29・`O-144`）。
  これまでは「反転確認で FAIL する＝本物の修正」を確かめる向きに使ってきたが、
  **反転確認が PASS したときの読み方**が要る＝(a) 観測点に判別力が無い (b) **そもそも直した箇所が効いていない**。
  `O-144` は 41効果へ `remainder.reorder` を届けたのに**実機の挙動が前後で同じ**で、
  シナリオを2回組み直しても（`REVEAL_AND_PICK` → `LOOK_PICK_CHAIN`）2回とも PASS した。
  真相は **(b)**＝別経路が並べ替え対話を無条件に立てており、フラグは効いていなかった。
  ⇒ **「配線するだけ」の項目こそ、着手前に「フラグを外すと消える形」を1つ実機で作る**。
  それが作れないうちは、**登録票の『届いていないN効果』という数字は挙動の話ではない**（JSON の話でしかない）。
- 🆕🔴**「engine がカード全文を読む」形の実害は "当たる/外れる" ではなく **実行経路で読めたり読めなかったりする** こと**（2026-08-29・`O-60` 第9バッチ）。
  `SEED_BLOOM` を実機で `effect_stack` 注入（＝**効果元が場に居ない**経路）から撃つと、
  `ctx.sourceCardNum` からカード全文が引けず、**同じ効果が「全開花」ではなく「1枚だけ」に化けた**。
  ⇒ **`census:enginetext` の `miss` 列も「壊れている効果数」も、経路が変われば別の数字になる**
  ＝A群は**静的な計器では原理的に測り切れない**（登録票の「`miss=0` を安全の根拠にしない」の一段先）。
  ⇒ **実機で旧挙動を観測してから判定文を書く**（このときも「2枚とも開花する」と書いた初稿を、
  実機が出した「1枚だけ」へ書き直した）。
- 🆕🔴**登録票の「設計判断」は見立てであって設計ではない＝着手時に必ず読み直す**（2026-08-29・`O-77`／`O-76`／`O-135` の**3巡連続**）。
  上の「①母数を数え直す」「②受け皿があるか測る」の**さらに次の段**＝**登録票が書いた「こう直すべき」自体が古い**。
  実例＝`O-135` の登録票は「一括で中央 diff を呼ぶのが正しいが**`ON_PLAY` と二重 collection になりうる**ので
  `suppressOnPlay` 等の meta 設計が要る／**族ごとにインライン収集を1つずつ足せ**」と3段の手順まで指定していたが、
  実装を読むと**中央 diff の `ON_PLAY` ブロックはスペル側の手書きと同一コード**で、
  `fieldPlacementOnPlayOpts()` を spread するだけで二重にならなかった（＝**②と③を飛ばして1呼び出しに置換**・−128/+27行）。
  ⇒ **登録票は「症状の記録」として読み、「直し方」は毎回コードから決め直す。**
  ⇒ 逆に**登録票の「規模」欄が過大なら、それ自体が worklist の並べ替え理由になる**（`O-135` は「大」で1位に置かれていた）。
- 🔴**母集団は「その語彙が live にあるか」ではなく「その意味を担うノードが在るか」で数える**（2026-08-25 続き649 で実測）。⚠**既存型の名前は命名規約に従っていないことがある**＝正規表現で型名を推測すると静かに取りこぼす。 実例＝「このターンに〜していた場合」を「live に `*_this_turn` 語彙があるか」で数えると**74件**に膨れたが、「**条件ノードが在るか**」で数えた真の母集団は**19効果**だった（`TURN_HAND_DISCARD_GTE`／`THIS_CARD_PLACED_BY_CLASS` など**命名規約に従っていない既存型**を正規表現が取りこぼす）。**測り方を3回作り直して初めて正しい母集団に着いた。**

### 4.2 実装 — 型・parser・engine

- 🔴**型を足すときは「型 ＋ 両評価器 ＋ golden」の3点セットで初めて1件完了。**`checkActiveCondition` は case の無い型を **`return true`＝無条件成立**に落とすので、**型だけ足すのは条件が無いのと同じか、それ以下**（条件つき常在能力が常時発動する）。しかも **smoke/census/fuzz は全部緑**のまま通る。
- 🔴**評価器も `matchesFilter` も2つある**＝`Condition`＝`execUtils.ts` の `evalCondition`／`ActiveCondition`＝`effectEngine.ts` の `checkActiveCondition`。**両方に入れないと片側だけ穴が空く。**
- ⚠**条件節は「場合」と「かぎり」で別の表に入る**（`STATE_CONDITION_CLAUSES_V2` ／ 先頭条件節テーブル）＝片方だけ直すと**同じ文型なのに一部だけ直らない**。「規則が無い」と「共通表に無い」は別物。
- ⚠**新しい語彙を足す前に「同義の別キー」を必ず探す**（`noGuard`/`hasGuard`、`eachDistinctLevel`/`selectionConstraint.distinct`、`IS_SELF_CHARMED`/`THIS_CARD_IS_CHARMED` ＝**同型・同実装の別型**）。3例以上の前例がある。
- ⚠**「機構が無い」と決めつける前に timing 別の類似フィールドを全部探す**（発生源クラス限定は `trashSourceStory`／`banishedSourceStory`／`powerDecreaseSourceStory`／`milledSourceStory` の**4本が既にあった**）。
- 🔴**1つの根から過剰実行と過小実行が同時に出る。**条件が消えるだけでなく、条件節の中のクラス修飾が**直前の対象へ誤付着**する。**条件だけ足すと過小実行が残り、誤付着だけ消すと過剰実行になる＝片方だけ直してはいけない。**
- ⚠**部分 filter だけを採用しない**＝同一の対象に2系統が掛かる形（`WXEX2-06`）では、片方だけ載せると原文と逆の過小実行になる。ただし**別ステップの別対象**なら片方だけ直してよい。
- ⚠**同語の別用法を filter にしない。** 状態語（チャーム／アクセ／凍結等）は **①対象フィルタ ②効果元の自己条件 ③装着ホスト参照**の3用法に分岐する。付与文・条件節・リマインダー文を filter にすると**原文と逆の過小実行**になる。⚠**「アップ状態のルリグN体をダウンする：」は使用コスト**であって対象フィルタではない（9件中8件がコスト側だった）。
- ⚠**UNKNOWN を実装するときは fresh parser の出力を先に測る**＝live の `UNKNOWN` だけが意味反転を止めていることがある（採用した瞬間に過剰実行へ反転する）。
- 🆕🔴**引用「…」の中のトリガー句を外側の regex で拾わない**（2026-08-24・`O-55`）＝【常】ブロックの分岐が「このシグニがバニッシュされたとき」を**引用の内外を区別せず**拾い、`effectType` を AUTO へ倒して本文を引用の中身へ差し替えていた＝**外側の付与宣言が丸ごと消えて内側だけが自分の能力になる**（`WXDi-D09-P18-E1` は `POWER_MODIFY` だけになっていた）。判定は**開き括弧の数**（`「『` と `」』` の出現差）で内外を見る。⚠同じ理由で**引用付与の解析は条件抽出ループより前に置く**（`parseActiveCondition` が引用内の「〜されたとき、」を先に食うと `^` アンカーが外れて丸ごと取り逃す）。
- 🆕🔴**「規則が無い」ではなく「活用形が違う」だけで語彙は丸ごと落ちる**（2026-08-25・`O-46`）＝原文は複数のコスト要素を「、」で並べるとき**手前を連用形**（「捨て、」「置き、」「公開し、」）で書くが、`parseCost` の規則は**終止形しか見ていなかった**＝**最後の1要素以外がまるごと live から消えて踏み倒せた**（`WXK08-027-E2` は3要素中2要素が無料）。⚠**同じ穴を過去に3回スポット修正していた**（`trash_self` の `置[くき]`／`trash_key`／`discardSelfFromHand` の `捨て[る、]`）＝**3回目のスポット修正は一般化の合図**。⚠一般化するときは**正規化する語を実データから全数採取して絞る**（推測で足すと非コスト用法を巻き込む）。
- 🆕🔴**部分採用は踏み倒しになる**（2026-08-25・`O-46`）＝受け皿の無いコスト要素が1つでもあるなら、**解釈できた分も捨てて `costUnparsed` に倒す**（提示そのものが止まる＝安全側）。`WXDi-P03-019-E1` は `energyTrashAll`＋`discardAll` だけ採ると**自分の場のシグニを1体も失わずに相手の場を全滅**できた。
- 🆕🔑**コスト語彙を1つでも足したら「提示ラベル・可否ゲート・支払い実行」の3地点を grep で数える**（`O-67`／`O-46` と**2回連続**で支払い経路が空だった）＝`O-67` はルリグ【起】、`O-46` は**シグニ【起】モーダル**と**キー【起】実行**の2つ。**ラベルに出ないコストはプレイヤーにも見えない**（§4.4 罠8m の撃ち分けにも効く）。
- ⚠**「読めた」は「正しい」ではない**＝文型 regex は巻き込む。規則を足したら `build:effects` で held を見て、意図した効果だけが変わったかを確認する。
- ⚠**JSON 手パッチ単独は禁止**＝parser 同修正か MANUAL 化とセット（さもないと held が増える）。**全再生成系の一括置換も禁止**（約90枚退化の前例）。
- 🆕🔴**ソース編集は「入ったこと」を必ず別コマンドで確かめる**（2026-08-26 続き674・`O-73`）＝
  スクリプト置換は**成功を表示しながら1文字も書き換えていない**ことがある（改行コードの差・エスケープの差）。
  実測＝probe の挿入が「成功」と出たのに `grep` すると存在せず、**入っている前提で1往復デバッグを溶かした**。
  ⇒ **編集の直後に `grep -n "<挿入した目印>"` で件数を確認する**。0件なら置換は失敗している。
  ⚠**アンカー文字列が長いほど改行コードで外れやすい**＝外れたら**行番号ベースの置換**へ切り替え、
  置換前に `repr()` で当該行を出して形を確かめる。

### 4.3 計器の読み方

- 🆕🔴🔑**census を減らすときの手順（2026-08-30 続き739〜741 で確立・3巡で 460→245／較正 -173・実装 -42）**
  🆕**手順1〜2は道具になった**＝**`node scripts/archive/censusKeyScan.mjs`**
  （引数なし＝カテゴリ別の型名・キー名の頻度表／`<カテゴリ名の一部>` ＝そのカテゴリを原文 × live JSON で全数 dump）。
  ⚠**事前に `npm run census` を回して `docs/_vocab_census.txt` を最新にする。**
  1. **カテゴリを1つ選び、その高シグナル効果を全数 dump する**（原文ブロック × live JSON）。
  2. 🔑**JSON の「型名」と「キー名」を縦に並べて集計する**（2件以上に出る名前を数える）。
     **同じ名前が縦に並んだら実装ではなく較正**＝計器が受け皿の綴りを知らないだけ。
     実測でこれが **40／25／16／12／9／9／7／7／6／5…** の塊を出した（合計173）。
  3. **1件ずつ原文 × live JSON を目視**する（**閾値を下げて件数を稼がない**）。
  4. **較正の前後で高シグナル id 集合を機械差分**し、「**消えた分＝目視した分／新規流入0**」を確認する。
     これをやらないと**別の効果が静かに隠れる**。
- 🆕🔴**較正は必ず narrow ＋ 残渣チェック**＝「その節を除いた**残り**に語が残るならフラグを維持する」。
  **キー表に足すだけで済ませない**（例＝`*SourceStory` は `extraOk` にした＝「＜X＞の効果で発火」と
  「＜Y＞を対象とする」を併せ持つ札で**対象側の脱落まで隠さない**ため）。
  ⚠**JSON 全体を `zen2han` しない**（`rawText` の中の数まで拾って本物の脱落を隠す）＝`condText` の値だけを見る。
- 🆕🔴**「綴り違い／基準違いで当たらない」が計器の最大の穴**（実測10種類以上）＝
  `maxCost` / `Under` / `deltaFromZone` / `*SourceStory` / `_SUM`（大文字）/ `Exile`（大文字 E）/
  `LastProcessed`（大文字 L）/ `LRIG_TO_DECK` / `NAME_CONTAINS` / `whenPowerZero` …。
  🔑**受け皿（型・キー）を新設したら `vocabCensus.ts` の対応表にも足す**のを恒久の運用にする。
  さもないと**正しい実装がカテゴリ単位でまとめて「バグ」に見える**。
- 🆕⚠**`re`（原文正規表現）側の誤検出もある**＝`機構:ゲート` の `/ゲート/` が《イリスア**ゲート**》という
  **カード名の一部**に当たっていた。**キー名だけでなく原文正規表現も疑う。**
- 🆕🔴**「兄弟効果（`-E1b` / `-E1c`）」は census を二重に汚す**＝あれは**1つの原文ブロックを2レコードに割った断片**で、
  `_effect_srctext.json` に自前のブロックを持たない。従来は子を**カード全文**と突き合わせていたので
  **親（語彙が半分）と子（全文と比較）の両方**が高シグナルになっていた。
  ⇒ `buildUnits` が**親の原文ブロック × 親＋子の JSON** を1件として判定する（2026-08-30 に修正済み）。
- 🆕🔑**census が減らない＝バグが無い、ではない**（逆も真）＝2026-08-30 の3巡で
  **census には1件も出ない engine バグを4件**見つけた（`execPowerModify` の動的 filter 未解決／
  `nameEqLastProcessed` の部分一致／`fieldExileSelf` コストの欠落／`PowerSetAction.duration` の未消費）。
  **どれも「反転確認つき golden」が赤で残ったことから見つかった。**

- 🆕🔴🔑**「census に残っている＝直すべき」でもない**（2026-08-31 続き744 で実測）＝**先行セッションが理由つきで
  見送った効果が混ざっている**。census だけを見て手を出した5効果が、既存の golden「**見送り契約**」で赤になった：
  ①「同じレベル」の**ペア付け機構**が無いまま `count` を増やすと過剰実行（`WDA-F02-07-E1`／`WX24-P2-036-E1`）
  ②**N体強制の中間動作**は `EffectInteractionModal.canConfirm` の「選択数 ≧ count」で**実機がソフトロック**（`O-104`）
  ③「**次に**〜される場合」の**1回消費**耐性の語彙が無い（`WX15-010-E1`）。
  🔑**歯止めは golden の見送り契約1つだけ**（census にも `census:wiring` にも「見送り済み」の印は無い）。
  ⇒ **census の1件に着手する前に、その effectId で `scripts/goldenTest.ts` を grep する。**
  ⇒ ⚠**ただし契約は「永久に触るな」ではない**＝同じ回に `WXK09-029-BURST` は**解除できた**（契約自身が
  「表せるようになったら採用する」と書いており、`colorMatchesLastProcessed` が後から入って前提が変わっていた）。
  **契約は毎回「理由がまだ生きているか」を読み直す。**
- ⚠**`census` に残っている＝バグ、ではない。** 実測で残りの約3割が**計器側の対応語彙漏れ**（部分一致なので大文字小文字で穴が空く）。**較正は実装より安い**ので先に取る。⚠ただし**較正は「実バグが減った」ではない**＝母集団から偽陽性が抜けただけで、実バグの絶対数は変わらない。
- ⚠**`census:wiring` の miss 数は「1本直せば N 件直る」ではない。** 効果は持っている入口ラベル全部のセルに計上される（**クロス計上**）＝「原文にフレーズはあるが同じ効果の**別の入口**に掛かっている」ものが関係ないセルにも出る。**`--cell` で原文を全部読んで、真の脱落／クロス計上／条件節用法／コスト側フィルタに仕分けてから配線する。miss 数は「掘る価値の指標」であって「見込み件数」ではない。**
- ⚠**`census:wiring` は `has===0` の語彙を表から捨てる**＝**正規表現を絞りすぎると穴ごと不可視になる**（`hasAcce` の除外を広く書いたら has が落ちて穴が消えた実測あり）。**「census:wiring が0件だから穴はない」と読んではいけない。**
- ⚠**`census:stubs` の「STUB＝未実装」ではない**＝実装済みハンドラの表示名でもある。**新しい綴りを足す前に、同義の STUB が engine に無いかを必ず見る。**
- ⚠**`docs/_partial_report.txt` を「直すべきリスト」として読まない**＝刻印は *fresh parse* の記録で、live が `MANUAL` なら既に解消済み（40件中38件がそれだった）。
- 🔴**既存の計器に映らない穴が1種類ある**＝**「受け皿（型・engine）はあるのに parser が配線していない」**。`census` にも `census:wiring` にも出ない。🆕**2026-08-24 続き640 の `O-53` はさらに悪い型**＝**逆翻訳が原文どおり正しいのに engine だけが別のゾーンを読む**（`LOOK_AND_REORDER` の `source.location:'hand'` が `state.deck` へ暗黙フォールバック）―**逆翻訳・census・golden・smoke・fuzz の全部が緑のまま16効果が壊れていた**。⚠**型の union にある値を executor が全部分岐しているか**はどの計器も見ていない（三項演算子の else に落ちる形）。**7回連続で当たった型**なので、「型はあるが live 利用が極端に少ない語彙」を数える計器は最優先の在庫（未着手）。
- 🆕🔴**逆翻訳がカード全文 regex から描いている STUB は、計器として嘘をつく**（2026-08-24・`O-55`）＝`PLACE_TRAP_OPTIONAL` の表示は原文をそのまま切り出していたので、**JSON が出所（どこから設置するか）を1つも持っていなくても逆翻訳シートは緑**だった。**逆翻訳は必ず JSON のフィールドから描く**（§6.4 O-20 と同じ落とし穴）。engine 側が `ctx.cardMap` の `EffectText` を読んで分岐している STUB も同類で、**JSON を見ても何が起きるか分からない**（`TRAP_OPERATION`／`TRAP_OP`＝`O-56`）。
- 🆕🔴**「N セッション連続で同じ FAIL が再現する」は「本物のバグ」の証拠ではない**（2026-08-29・`V-89`）。
  `lookReorderCanTrash` は**4セッション連続で同じ数字（`hDeck=2 hTrash=3`）で FAIL** し、そのたびに
  「`git stash` しても HEAD で FAIL＝本セッション起因ではない」と正しく確認されて次へ送られていた。
  **真相は「シナリオが assert している UI が live にもう存在しない」＋「spec の deck に候補カードが1枚も無い」**で、
  **あの数字こそ仕様どおりの挙動だった**。⇒ **FAIL を寝かせる前に、まず「live JSON を読み直して、
  シナリオが assert している形が今も出るか」を確かめる**（§4.1「実測そのものが嘘をつく」の実機版）。
  ⚠**「HEAD でも再現する」は「engine のバグである」を意味しない**＝計器が古いときも同じ見え方をする。
- ⚠**golden は「修正前データで実際に落ちるか」を確かめてから採る**＝フィルタ無しでも候補の先頭がたまたま正解で **vacuous に PASS** することがある。**対象を候補の先頭以外へ置く。**
- ⚠**枝の数を固定した golden トリップワイヤは、正しい是正でも落ちる**＝`join(',')` の完全一致ではなく「全エントリが opponent かつ1つ以上」のような性質で書く。

### 4.4 実機シナリオの罠（着手前に必読）

1. 🔴**`spec.hostSet`／`guestSet` の両方に `'field.check': null` を必ず入れる**＝`injectScenario` の `CORE_FIELD_KEYS` に `check` が含まれ**リセットされない**。前シナリオのライフクラッシュ確認モーダルが全画面を覆い**クリックが1つも通らない**（3回踏んだ）。⚠🆕**2026-08-24（続き637）＝引き継がれるのは `check` だけではない。`CORE_FIELD_KEYS`（`lrig` `signi` `assist_lrig_*` `check` `key_piece*` `free_zone` `beat_zone`）は全部そう**で、**使うシナリオが自分で書き切らない限り前シナリオの盤面が残る**。実例＝`guardExtraColorlessSufficient` が `field.signi` を固定しておらず、直前シナリオが置いた `WX18-039`（【常】レベル２と３でガード不可）が残って**手札の Lv2 サーバントが「正しく」候補から消え**、engine バグに見える FAIL になった。**盤面依存の判定をするシナリオは `field.*` を自分で固定する**。⚠**後始末も自分でする**＝ライフクラッシュを起こして抜けるシナリオは `field.check` を消化（「エナに送る」まで）してから返す。
2. 🔴**`getByRole('button', { name: <正規表現>, exact: true })` は `count()` が常に 0**（`exact` は文字列名にしか効かない）＝**モーダルもボタンも出ているのに30反復空振り**する。ラベル照合は `data-action-label` の前方一致か `H.clickBtn`/`clickTextOrBtn`/`clickTestId`。
2c. 🆕🔴**「同じ index を押し続けるとトグルで選択が外れる」＝2026-08-29 に1セッションで3回踏んだ**（スペルのエナ選択・複数 pick・`O-135`）。
   複数枚を選ばせる UI（`spellcost-energy-N` / `pick-N` / `optcost-energy-N`）は**押すたびにトグル**なので、
   `.first()` や固定 index を毎ティック押すと**選んで外してを繰り返して永久に揃わない**（実測＝26ティック空振り）。
   ⇒ **押した index を `Set` で覚えて、未選択のものだけを1つずつ押す。** 揃ってから確定ボタンを押す。
   ⚠`H.stdStep` は**「決定 (1/」しか見ない**＝2枚以上選ばせる画面では pick を1枚も押さずに素通りするので、自前で積む。
2d. 🆕🔴**ゾーン選択ボタンを `.first()` で取らない**（2026-08-29・`V-89`）＝マッチする最初のボタンが
   **disabled**（自分が居るゾーン／ライズ条件不一致）だと、そこで打ち止めになって `SELECT_SIGNI_ZONE` のまま空振りする。
   ⇒ **全マッチを `nth(i)` で走査して、visible かつ enabled な最初の1つを押す。**
2b. 🆕🔴**`H.clickTextOrBtn` を CHOOSE のボタンラベルに使わない**（2026-08-24・`O-55`）＝**盤面ログのテキスト**（「ハトハットを【トラップ】として設置しますか？」）に当たって**「押せた」と報告しながら何も進まず 34反復まるごと空振り**する。ボタン限定の `H.clickBtn` を使い、**先に進む側（2段目のゾーン選択など）から**試す。⚠罠2（`exact` が効かない）と合わせて、**ラベル照合は「ボタン要素に限定」＋「前方一致」**が既定。
3. 🔑**負方向テスト（「出ない」「起きない」）は必ず対照とセット**＝**単独では何も検証していなくても緑になる**。対照は**盤面を1文字も変えず原因だけを外す**（実例＝`damageSource` の1語／guest ルリグ1枚／相手資源の枚数）。
3b. 🔴🆕**反転確認の判定は「旧挙動で観測できる痕跡」から書く**（2026-08-26 続き674・`O-73`）＝
   新実装を前提にした witness を必須条件にすると、**旧形へ戻した反転確認が「前提崩れ」で落ちて
   旧挙動の再現が判定文から読めない**。実例＝`o73DelayedMillTrigger` は「設置を観測した時点の `powerMods`」で
   即時実行を判定していたが、**旧実装には「設置」自体が存在しない**ので値が永久に null になり、本題の判定に
   到達しなかった（反転確認を2回走らせる羽目になった）。⇒ **「設置を1度も観測しないまま相手のパワーが下がった」**の
   ように、**旧実装でも必ず残る痕跡**を本題判定の先頭に置く。⚠これは 📌3 の「対照とセット」とは別軸＝
   **対照の作り方ではなく、FAIL 文言が原因を指すかどうか**の話。
4. 🔑**「盤面だけを見る判定」も偽陽性**＝**機構を1度も通らなくても同じ盤面になる**ことがある（続き466 の実例＝対話が出なくても身代わり成立と同じ絵）。**機構が動いた証拠（問いログ・`asks` 件数など）を必須条件に入れる**。
5. 🔑**クリック前から成立している条件で判定しない**＝`pendingEffect==null && stackLen===0` は**効果の開始前と完了後の両方で true**。**効果が走り出したことを1度でも観測してから判定する**（さもないと単体で PASS・バッチで FAIL の位置依存 flakiness になる）。
6. 🔑**候補が複数ある選択で `pick-0` を盲目に押さない**。⚠🔴**ただし `pendingCandidates` の index を `pick-<idx>` にそのまま使うのも誤り**（続き469 で判明）＝**相手の場が対象のときモーダルは候補を reverse して描画する**（`EffectInteractionModal.tsx:189-192`＝`targetScope==='opp_field'` なら `[...candidates].reverse()`）。`data-testid` は**表示順の index**（`:282`）。⇒ **集合として assert してから `[data-testid^="pick-"][data-card-num="<カード番号>"]` で狙う**（`clickPendingInstance`）。⚠**`data-card-num` はカード番号でインスタンスIDではない**＝**同名カードが複数並ぶ盤面では一意にならない**ので、そのときは表示順を自分で反転して index を出す。⚠先例 `cheatingSameLevelDownFilter` が index で動いていたのは**有効候補が1件しかなかったから**。
7. 🔑**DOM の描画待ちを必ず入れる**（続き469 で2回踏んだ）＝`H.queryState()` は **Supabase を直接照会するので DOM より先に真になる**。`pendingCandidates` が立った瞬間に `pick-*` や `決定 (N/M)` を掴みにいくと**0件で即 null**。**待機予算は 3秒程度で全ヘルパに揃える**（`clickPendingInstance`／`clickExactVisibleText`）。⚠**500ms だと1回目 PASS・2回目 FAIL の位置依存フレークになる**。
8. ⚠**`a?.x === a?.y` は `a` が null のとき `undefined===undefined` で true**＝1周目に誤検出して即 FAIL する。
8z. 🆕⚠**「盤面を作るためだけに撃つカード（銀行役）」の副作用を期待値に織り込む**（2026-08-24・`O-55`）＝`PR-378` の選択肢①は**バニッシュの前後に1枚引く**ので、後続が見る「デッキの一番上」は**注入時の先頭ではなく2枚目**。1枚目を期待値にすると engine は正しいのに FAIL する。⚠**観測対象のゾーンを汚さない枝**を選ぶこと（`PR-378` の②はデッキ上をエナへ置くのでエナの候補集合を観測するシナリオでは使えない）。
8b. 🆕🔴**`H.clickTextOrBtn` / `H.clickBtn` は `disabled` を見ない**（2026-08-24・V-85／V-04 で実測）＝
   未選択で灰色の「発動する」を**毎ティック「押せた」と報告し続け**、本来先に行うべきエナ選択に永久に到達しない
   （26ティック空振り）。**支払いモーダルは「候補を選ぶ →（enabled を確かめて）確定ボタン」の順に書く**。
   確定ボタンは `getByRole('button', { name, exact: true })` ＋ `isEnabled()` で掴む
   （⚠ `exact` が効かないのは**正規表現名のとき**＝上の 2 とは別の話）。
8c. 🆕⚠**確認ダイアログを先に処理する**（2026-08-24・V-87）＝ルリグ未アタックで END へ進めると
   「まだルリグが攻撃していません／このまま進みますか？」が挟まる。**その裏のヘッダーボタンは押せてしまう**ので、
   フェイズ進行ボタンを押し続けても永久に進まない（30ティック空振り）。**先に「このまま進む」を押す。**
8d. 🆕⚠**盤面差分の「起きた」判定は sticky にする**（2026-08-24・V-83）＝「トラッシュが3枚増えた」で判定すると、
   その後の回収（トラッシュ→エナ）で**判定が false へ戻り**返却条件に到達しない。一度立てたら下げない。
8e. 🆕🔴**host のゾーン index と guest のゾーン index は「正面」で一致しない**（2026-08-24・V-86）＝盤面が180度
   回っているので、**中央（zone1）同士だけが対応する**。1ゾーンだけ壁を置いてアタックすると**正面素通りで
   ライフクラッシュ**になりバトルが起きない（実測＝`シヴァがライフをクラッシュ`）。**バトルを作るなら中央を使うか
   3ゾーンとも埋める。**
8f. 🆕🔴**バニッシュの行き先はエナゾーンでトラッシュではない**（2026-08-24・V-86）＝`trashCards` だけを見る
   「場を離れたか」判定は**偽陰性**になる（実測で2本が誤判定）。**場に無い＋トラッシュかエナに居る**で見る。
8g. 🆕⚠**`turn_phase:'END'` を直接注入しない**（2026-08-24・V-19）＝room 正規化に負けて MAIN へ戻され、
   「ターン終了」ボタンが一度も出ないまま空振りする（§5.1 が V-30 で「設計限界」と書いていた現象）。
   **フェイズボタンを実際に歩く**ほうが速くて確実（43秒 FAIL → 11秒 PASS）。
8h. 🆕⚠**手札上限超過モーダルを「ターン終了を押したか」でゲートしない**（2026-08-24・V-19）＝この
   モーダルは「ターン終了」より**前**に出ることがあり、覆われてボタンも押せず捨て札にも進めない
   （263秒空振り）。**モーダルが出ていたら最優先で処理する。**
8i. 🆕⚠**「同じゾーンへの上書き召喚」では場を離れさせられない**（2026-08-24・V-86）＝
   `SigniSummonZoneModal` は**占有ゾーンのボタンを disabled** にする。自分のシグニを離場させたいときは
   「あなたの**他の**シグニ1体をトラッシュに置く」【出】等を使う。
8k. 🆕⚠**限定（＜ルリグ名＞限定）とリミットを満たさないと「召喚」「使用」ボタンがそもそも出ない**
   （2026-08-24・V-44/V-63）＝`WX19-028`＝緑子限定Lv4／`WX17-003`＝ドーナ限定。**盤面を作る前に
   カードの限定・レベル・リミットを見る**（実測でそれぞれ24・40ティック空振りした）。
   🆕⚠**限定はシグニの召喚だけでなくスペル／アーツの使用も止める**（2026-08-24・`O-54` で**3度目**）＝`WD08-018`（グレイブ・ペイン）は
   `Team` 列が `-` なのに `Restriction` が「ウムル限定」で、CardModal に**「発動」ボタンが出ないまま34反復まるごと空振り**した。
   ⇒ **銀行役（対象を作るためだけに撃つカード）は `Restriction === '-'` で選ぶ**のが安全（本件は `PR-378`《無》×0 へ差し替えて解決）。
   🆕⚠**限定を調べる列は CSV の `Restriction`**（2026-08-24・`O-53` で再び踏んだ）＝`LrigLimit` や `Limitation` という列は**存在しない**ので、
   自作の調べものスクリプトでそれを読むと**静かに `undefined`＝「限定なし」に見える**。実例＝`WXK02-089` はリメンバ限定で、
   タマヨリヒメのルリグで盤面を作ったため**「召喚」が一度も出ず26ティック空振り**した（切り分けの決め手は `scratchpad-verify/<id>-N.png`＝
   カード画像に「リメンバ限定」と写っていた）。
8l. 🆕⚠**候補がちょうど必要数なら選択モーダルは出ない**（`cands.length === pickCount && !upTo`）＝
   **絞り込みを実機で見たいなら「条件に合う札」を2枚以上入れる**（1枚だと自動配置されて
   `pendingCandidates` が一度も立たない）。
8m. 🆕⚠**同名の【起】を複数持つカードはコストまで含めたラベルで指定する**（2026-08-24・V-44(b)）＝
   `WX13-007` はエクシード**1**（自分のシグニを手札へ）とエクシード**2**（レゾナ召喚）を持ち、
   前方一致 `【起】` だと**別の能力を誤射**して盤面まで変わる。
8n. 🆕🔴**押せなかったからといって「決定」へ進まない**（2026-08-24・V-44(b)）＝`pendingCandidates` は
   DB 由来で **DOM より先に真になる**（8 の7と同根）。1ティック目に `pick-*` が未描画のまま決定を押すと
   **0枚で確定**する（実測＝`決定 (0/2)` で完了して FAIL）。**ゾーン選択も `stdStep` より先に試す。**
8o. 🆕🔴**`data-card-num` の意味はモーダルごとに違う**（2026-08-24・段2第38）＝`EffectInteractionModal` の
   `pick-*` は **カード番号**（`getCardNum(rawId)`＝§4.4 の6 に既出）、本セッションで追加した
   `guard-card-*` / `enddiscard-hand-*` は **インスタンスID**。**同名2枚を撃ち分けるときは `nth` を使う。**
8p. 🆕🔴**コスト支払いモーダルの候補セルは常に可視**（2026-08-24・段2第34/38）＝毎ティック押すと
   **トグルで外れて「発動」に一度も到達しない**（V-19/V-35/V-44 と同じ罠を4回踏んだ）。
   **「1回だけ押す」フラグ／集合を必ず置く**（`onplaycost-energy-*`／`artscost-energy-*`／`spellcost-energy-*` 共通）。
8j. 🆕🔴**engine のルールを直す前に、影響範囲のシナリオを走らせて「既に赤いもの」を確定させる**
   （2026-08-24・O-47）＝さもないと修正後の赤が「自分が壊した」のか「元から赤い」のか切り分けられない。
   実測＝アタック系30本のうち**修正前から4本が赤**で、修正後は**2本が緑に戻り**（進行不能バグが同時に解けた）、
   **3本が「腐り」で赤くなった**（盤面が旧挙動前提だった）。⚠**「腐り」は engine のバグではない**＝
   ルールが正しくなったぶん盤面を作り直す（例＝アタッカーを勝たせる／エナ集計から自分を除く）。
9. ⚠**`ATTACK_ARTS_OP` は非ターンプレイヤーが「アーツ終了」で進める**＝押さないと**CPU のターンが永久に終わらない**。
10. ⚠**切り分けの決め手は `scratchpad-verify/<id>-final.png`**＝ログだけ見ていると原因を engine 側に誤診する。
11. ⚠**Codex は実行できない**（サンドボックスのネットワーク遮断）＝**起案だけさせて実行・判定は検証側**が引き取る。指示書の冒頭でそれを明言すると時間を溶かさない（CODEX_GUIDE §5.5 続き460〜468）。
12. 🔴🆕**赤のまま在庫化した「engine バグ」は、罠が新しく見つかるたびに遡って洗い直す**（続き475 で実証）＝§3 (cxxv) は**在庫バグではなくシナリオ偽陽性**だった（罠6＝`pick-<idx>` の reverse は、その4シナリオを書いた**後**の回で判明した）。**在庫の症状記述を信じて engine を読み始める前に、まずシナリオを現在のヘルパで書き直して再実行する**ほうが安い（実測＝4本中3本が調査ゼロで緑へ反転）。
13. 🔑🆕**判定は「settled になった最初の1回」で確定させない**（続き475 で実証）＝盤面（`battle_states`）は先に真になるが**ログ行（`game_logs`）は数百ms遅れて届く**ので、`normalLog=false` の形で**単体PASS・バッチFAIL の位置依存フレーク**になる。⇒ **PASS しない間は数秒ぶん反復してから FAIL を確定する**（罠7 の「DOM 待ち」と同じ理由・別の観測面）。
14. ⚠🆕**「今は緑」のシナリオが、写り込んでいるバグを見ていないだけのことがある**（続き475 で実証）＝`leaveSubAllTargetsAskedPerVictim` は問いの回数だけを見ていて、**最終盤面に同一 instance の複製（§3 (cxxvi)）が写っているのに緑**だった。**final の盤面はダンプするだけでなく、不変条件（重複なし・枚数保存）を assert する。**
15. 🔴🆕**観測面を間違えると「engine バグそっくりの偽陰性」になる**（続き478 で実証・2件）＝(a)**`H.queryState().powerMods` は `temp_power_mods` しか写していない**（`verifyBattleDrive.mjs:16847`）。`duration:'UNTIL_OPP_TURN_END'` の POWER_MODIFY は **`power_mods_until_opp_turn` へ書かれる**（`effectExecutor.ts:1456`）ので **`powerMods` を見ると永久に false**＝「発火したのにバフが乗らない」絵になる（`v13TrashActCoinChain` が実際に踏んだ。⇒ 長期ストアを直接照会する `v13PowerModsUntilOpp` を使う）(b)**カードアクションのラベルに効果の限定は現れない**（`WXDi-P12-053` は `【起】トラッシュから出す（手札2枚を捨てる）`＝**ディソナ限定はラベルに出ない**）＝ラベル照合で絞ると永久不一致になり、**FAIL 文言が「対象【起】なし（actions=[…【起】…]）」という自己矛盾**になる。**限定の検査は DOM 属性（`data-selectable`）で行う。**
16. 🔑🆕**対照は「盤面」だけでなく「判定の待ち方」まで揃える**（続き478 で実証）＝§3 (cxxxiii) の誤診は、**本命シナリオが即 FAIL 型・対照だけがポーリング型**だったことで生まれた（本命は engine が正しくても必ず赤／対照は正しく緑）。**非対称な観測方式は「片方だけ壊れている」というもっともらしい結論を作る。**
17. 🔴🆕**Playwright の accessible name は空白を正規化する**（続き479 で実証）＝**カード名の全角スペース（U+3000）が ASCII スペースへ畳まれる**ため、原文どおり全角で書いた `getByRole('button', { name: /コードイート　マヨ【アクセ】/ })` は**永久に一致しない**。⚠**データ側と regex はコードポイントまで一致していた**のに `found=false` になる＝**静的照合では気付けない**。⇒ **`\s*` で吸収する**。既存の 📌2（`exact:true` は正規表現に効かない）と同族＝**「画面には出ているのに掴めない」**型。**切り分けの決め手は `scratchpad-verify/<id>-final.png`**（📌10 の実例がまた1つ）。
18. 🔴🆕**場のシグニをタップして開くのは `CardModal` ではなく `StackModal`**（ライズ用の複数枚ビュー・続き479 で実証）＝`card-detail-modal`（`BoardComponents.tsx:73`）は **CardModal 専用**なので、場のシグニでは**永久に不可視**になり `labels=[]` で落ちる。⚠**アクションボタン側（`card-action-{i}` / `data-action-label`）は両モーダル共通**（`:98` と `:210`）なので、**スコープにした親だけが取れない**という分かりにくい形で出る。⇒ StackModal 側に **`stack-detail-modal`** を追加済み（両方を許す locator を使う）。
19. 🔴🆕**Playwright の `click()` に timeout を渡さないと既定30秒待つ**（続き480 で実証）＝「DOM には在るが操作不能（他要素に覆われている）」ボタンを**毎ティック押しにいくドライバ**では **1ティック30秒 × ループ回数**で実質ハングする（**実測＝120ティックで57分停止**）。⇒ **`click({ timeout: 1200 })` を必須にする**。⚠**ハーネスの10分 kill はラッパーしか止めない＝タスク通知も来ない**ので、気付く手段は**`scratchpad-verify/<id>-*.png` の mtime が進んでいるか**と **`Get-CimInstance Win32_Process` でドライバの生存確認**。
20. 🔑🆕**実機ドライバの出力はパイプ（`| tail`）に通さず、必ずファイルへリダイレクトする**（続き480 で実証）＝パイプ越しだと**完走するまで1行も読めない**ため、上の 📌19 のハングを30分以上検知できなかった。ファイルなら `v15.phaseEnd[NN]` 等の**ティックログで進捗が即分かる**。
21. 🔑🆕**「決定 (N/M)」は完全一致リストに掛からない**（📌2 と同型・続き480）＝**前方一致で掴む**（共通ヘルパー `clickDecideNofM`）。さらに🔴**必要枚数が既に選択済みなら、候補クリックより先に決定を押すこと**＝**候補を押し直すと選択がトグルで外れ**、永久に決定へ到達しない。
22. 🔑🆕**spec の `deck: []` は効果解決中に「リフレッシュ（デッキ再構築）」を誘発する**（続き480 で実証）＝観測対象のカードが**トラッシュからデッキへ戻って盤面追跡が壊れる**。⚠**同じ盤面で手札・ライフ・埋め札を全部同名カードにするのも禁物**（📌6 の「同名カードが複数並ぶと `data-card-num` で一意にならない」の実例）＝**デッキに数枚積み、埋め札は別カードにする**。
23. 🔴🆕**レゾナの配置先は `SELECT_SIGNI_ZONE`＝`EffectInteractionModal.tsx:911` の「ゾーンN」ボタン**であって、**通常召喚の `summon-zone-{zi}`（`SigniSummonZoneModal`）ではない**（続き481 で実証）。取り違えると `zonePicked` が永久に立たず**配置そのものに到達しない**（実測＝574秒／606秒を溶かした）。⇒ **既存 `H.clickZone()`（`^ゾーンN` 走査）を使う**。**この修正だけで「配置未完了」→「ターン終了返却未完了」へ前進し、所要も 109秒→73秒になった。**
24. ⚠🆕**ラベルを読んだ時点では開いていたモーダルが、クリック時には閉じていることがある**（続き481 で実証）＝`did=なし` が延々続く形で出る。⇒ **掴めなければ開き直す自己回復**を入れる（実測＝`v18Wxk10049*` が **264秒 FAIL → 6秒 PASS** に反転）。
25. 🔴🆕**「シナリオが軒並み落ちる」ときは自分の作業ではなく基盤の故障を疑う**（2026-08-18 続き554 で実証）＝
    実際に `BattleScreen` の Rules of Hooks 違反（早期 return の後ろの hook）で **React #310 が出て画面が真っ黒**になり、
    **全シナリオが無条件 FAIL** していた（`verifyBattleDrive.mjs` は盤面注入後に `page.reload()` するので必ず踏む）。
    **切り分けは3手**＝①**既存の別シナリオも同じ error で落ちるか** →②**`git checkout HEAD~1 -- src/` で前コミットでも再現するか**
    （＝自分の変更由来かの一発判定）→③`vite.config.ts` に一時 `build:{minify:false}` を入れて `pageerror` の **stack** を読む
    （`e.message` だけでは「Minified React error #NNN」しか出ない。stack 出力は driver 側に恒久化済み）。
    ⚠**engine/UI の構造バグはここまで切り分けたうえで、§2.4 のとおりその場で直すか §5.3 へ登録する。**
26. 🔴🆕**「昔書いたシナリオが FAIL」＝バグとは限らない（仕様変更でシナリオが腐る）**（2026-08-18 続き554 で実証）＝
    `wxk04003Label` は続き552c で入った**《コインアイコン》所持チェック**により `coins` 未設定の spec が古くなって落ちていた
    （`coins: 1` を足して PASS）。**FAIL を見たらまず「原文どおりの是正が入って spec が追いつかなくなっただけ」を疑う**。
    ⚠**寝かせるほどこの切り分けが高くつく**＝実機 worklist を溜めないこと自体が計器の維持になる。
27. ⚠🆕**CPU ターンの観測は「開始時の `activeUser`」を基準にしない**（2026-08-18 続き555 で実証）＝
    注入 →`page.reload()`→ 待ち の間に **CPU が END まで走り切る**ことがあり、最初のサンプル時点でもう人間のターンになる。
    ターン終了の判定は **`activeUser !== '00000000-0000-0000-0000-000000000001'`（`CPU_PLAYER_ID`）の絶対条件**で見る。
    （📌9 と併せて、**押さないと止まる**／**基準がずれる**の両方が「無限ループ」に見えることに注意。）
28. 🔑🆕**`optional`（「〜してもよい」）の【自】は実機から観測できないことがある**（2026-08-18 続き555 で実証）＝
    **CPU の自動応答が「しない」側に倒れる**ので、収集されていても盤面差分が出ない（`WX24-P2-075` の例）。
    **「収集されるか」を見る層は golden**（`collectTurnTriggers` を直接呼ぶ）＝実機は「フェイズ進行が壊れないこと」に絞る。
29. 🔴**実機 FAIL は「engine の穴」と決めつけない**（2026-08-25 続き650・`V-88`）＝**ドライバが押せていないだけ**のことがある。**押したはずのボタンがログに出ていないか**をまず見て、必要なら probe（ボタン数・全ラベル）を1回仕込んで実測する。 実例＝`v12GrantedEnergyCharge*` は `【起】コストなし` を **`nth: 1` 固定**で押していたが、spec が `field.lrig_down: true` で始まるため**《ダウン》コストの E1 が提示されずボタンは1個だけ**になり、毎回空振りしていた。⚠**`H.clickBtn` は対象が無いと `null` を返すだけで例外にならない**＝空振りは静かに腐る。**同一ラベルのボタンが複数出るカードは `nth = count - 1` の相対指定にする。**
30. 🔴🆕**差分検知型の watcher は「初回観測ではスナップショットしか取らない」**（2026-08-25 続き653・`O-64`）＝
    `ON_POWER_THRESHOLD`／`ON_ENERGY_CHARGE` は前回スナップショット（`prevPowersRef`／`prevEnergyRef`）との
    差分で発火するので、**ページの初回レンダリングが「条件を満たす注入」の後ろにずれ込むと `wasBelow=false` に
    なって永久に発火しない**。実測では **dist 再ビルドを挟んだ初回だけ FAIL・以後 PASS** という位置依存フレークで出た。
    ⇒ **「条件を満たさない盤面を必ず一度注入して待つ」**＋**「その状態（例＝`powerMods` が空）であることまで前提に含める」**。
31. 🔑🆕**CPU ターンの負方向テストは `witness`（狙った状況を本当に通ったか）を必須条件にする**（2026-08-25 続き653・`O-64`）＝
    CPU は勝手にフェイズを進めるので、条件を載せた時点でもう別のフェイズに居ることがある。そのまま緑にすると
    **「ターン主で落とした」のか「フェイズで落とした」のか**が分からない＝**別のゲートを再テストしているだけ**になる。
    実測＝`b64PowerThresholdOppMainBlocked` は最終 phase が `ATTACK_ARTS_OP` で、`witness`（ブースト中に
    `phase==='MAIN' && active===OPP` を1度でも観測したか）を入れて初めて主張が成立した（📌3／📌4 の実装形）。
32. 🔴🆕**複合属性セレクタ（`[data-testid^=…][data-action-label^=…]`）は当たらない**（2026-08-25 続き654・`O-65`）＝
    画面にボタンが出ていて `[data-action-label]` 単体なら拾えるのに、**testid と併記した瞬間に0件**になる
    （実測26ティック空振り）。⇒ **`data-action-label` を全部読んでから index で `card-action-{i}` を押す**。
    📌2（`exact` が正規表現に効かない）・📌17（accessible name の空白正規化）と同じ**「画面には出ているのに掴めない」**型。
33. 🔴🆕**キーの【起】の確定ボタンは「発動」**（`KeyActivatedModal`）で、`KeyUseModal` の「セット」／「使用」とは**別のモーダル**
    （2026-08-25 続き654・`O-65`）。⚠**「スロットを開き直す」フォールバックを確定ボタンより先に置くと、
    全画面オーバーレイ（`onClick={onClose}`）を叩き続けてモーダルを閉じ、永久に確定へ到達しない**。
    ⇒ **確定ボタンを先に試し、スロットの開き直しは「モーダルが1つも出ていないとき」だけにする**（📌2b の同型）。
34. 🔑🆕**負方向シナリオの「準備失敗」は前シナリオの盤面残り**（2026-08-25 続き654・`O-65`）＝
    `b65WallOppMainProtected` が直前の `b65KeyTrash*` の置いたシグニを見て準備失敗した。
    **`O-63` の「盤面注入は1発で決まらない」は単発実行でも起きる**＝**載るまで再注入するループを既定にする**
    （📌1 の `CORE_FIELD_KEYS` 引き継ぎと同根で、こちらは**注入そのものが競合で流れる**側）。
35. 🔴🆕**新規シナリオは「推測で直す」前に probe を1回入れる**（2026-08-26 続き674・`O-73` で実測）＝
    `o73DelayedMillTrigger` は **8回実行してようやく PASS**したが、**PASS に必要だったのは最後の1回**で、
    途中の失敗3つは**すべてこの節に既出の罠**だった（📌8z 銀行役の副作用／📌8p トグルで閉じる／📌2b 先に進む側から試す）。
    🔑**しかも3つとも probe 1回で同時に見えていた**＝`actionLabels=[]`（モーダルが開いていない）と
    `handCards`（積んだ札は正しい）が出た時点で「押せていない」ではなく「**開いていない／閉じている**」と確定できた。
    ⇒ **新規シナリオには最初から次の probe を1回だけ仕込む**（`probed` フラグで1回に限る＝毎ティック出すと
    ログが埋まる）。**推測で直して再実行するより速い**（実測＝推測2回で約5分、probe 1回で即断）：
    ```js
    if (!probed) { probed = true;
      const labels = await page.locator('[data-action-label]').evaluateAll(
        els => els.map(e => e.getAttribute('data-action-label'))).catch(() => []);
      const btns = await page.getByRole('button').evaluateAll(
        els => els.map(e => (e.textContent ?? '').trim()).filter(Boolean).slice(0, 40)).catch(() => []);
      const tids = await page.locator('[data-testid]').evaluateAll(
        els => els.map(e => e.getAttribute('data-testid')).slice(0, 60)).catch(() => []);
      H.log('  PROBE hand=', JSON.stringify(st?.host?.handCards), ' labels=', JSON.stringify(labels),
        ' buttons=', JSON.stringify(btns), ' testids=', JSON.stringify(tids));
    }
    ```
    ⚠**probe は調査用**＝1巡を閉じる前に撤去し、**撤去後にもう一度 PASS を確認する**（撤去でループ条件が変わりうる）。
    ⚠**`H.log` に出した文字列を後で `grep` するときは `grep -F`**＝`[probe]` のような角括弧は文字クラスとして解釈され、
    **1件もヒットしないのに「probe が動いていない」と誤診する**（実測で1往復溶かした）。
35b. 🔑🆕**「銀行役」はカードを選ぶ時点で CSV を引く**（同・`O-73`）＝📌8z の**事前チェック版**。
    ミル役に選んだ `WX02-070` は**クラスが「精像：悪魔」**で、観測対象カード自身の【自】
    「あなたの**＜悪魔＞の**シグニの効果１つによって…」に**同時発火**し、その STUB が解決できずに
    **stack=2 で停止**した（＝engine バグに見える FAIL）。⇒ **銀行役を決めたら `Restriction` だけでなく
    `CardClass` / `Color` / `Level` を引き、観測対象カードの「発生源限定」と衝突しないことを確かめる**。
    ⚠**副作用の無い札を選ぶ**のが基本（`WXK01-108`＝「【出】：デッキの上から2枚トラッシュ」だけ）。
36. 🔑🆕**待ちの粒度を実所要に合わせる**（同・`O-73`）＝シナリオ本体は **12〜77秒**で終わるのに
    固定 `sleep 105`〜`115` で待つと**終わっているのに待つ**時間が積み上がる（8回で数分）。
    ⇒ **`until grep -q "結果サマリ" <log>; do sleep 5; done` の条件待ち**にする。
    ⚠**baseline を取る比較（`stat -c %Y` 等）は「いまの値」を先に実測してから書く**＝
    古い値を決め打ちすると**条件が最初から真**で即座に抜け、「完了した」と誤読する（実測で1回踏んだ）。

### 4.5 修正を live へ届ける経路（届かないと恒久 no-op になる）

- 🔴**`build:effects` の収穫マージは `parseStatus:MANUAL`／`PARTIAL` の効果を不可侵にする**＝`manualEffects.ts` を後から直しても **live には永久に届かない**（新しい id の追加だけは通る）。届けるには **`npx tsx scripts/syncManualLive.ts <CardNum>`**。⚠実行後は必ず `npm run gates`（live を直接書くので、ゲートだけが安全網）。
- 🔴**parser を直したのに live が変わらないときは、まず3ファイルを見る**＝`docs/_held_fresh.json`／`_partial_fresh.json`／`_idset_fresh.json`。**id 集合が live と fresh でズレたカードは `_idset_fresh` に出る**（実測46カード）。
- 🆕🔴**`parseStatus:MANUAL` なのに `manualEffects.ts` に実体が無いカードがある**（2026-08-24・`O-54`＝`WXDi-P08-038`）＝過去の外科パッチが刻印だけ残した **live 限りの遺物**。この状態だと **`syncManualLive` は「同期元が無い」ので効かず**、`build:effects` はカード単位 PRESERVE で永久に凍る。**直し方＝`grep <CardNum> src/data/manualEffects.ts` が空なら、live の `parseStatus` を `AUTO` へ戻してから `build:effects`→`heldReview --adopt`**（以後 parser 改善が届くようになる）。⚠戻す前に **fresh が live の全情報を保っているか**を effectId のリーフパス単位で必ず確認する（本件は fresh が上位＝BURST の `isUp` 脱落まで同時に直った）。
- ⚠**`manualEffects.ts` のトップレベル効果は `MANUAL` か `PARTIAL` のみ**（`AUTO` と書くと `manual-fields` ゲートが exit 1）＝`mergeManualEffects` は `parseStatus` を見ず effectId 一致で常に manual 側を勝たせるので、`AUTO` の手書きコピーは **parser の最新出力を永久に上書きし続ける**。
- ⚠**`build:effects` は破壊的ではない**（curated は温存され、`heldReview --adopt` で明示採用したものだけが変わる）。正しい規律は「実行禁止」ではなく**「実行後に effectId 単位で全数比較して、意図した件数だけが変わったことを確認する」**（出力は minified 1行なのでテキスト diff は役に立たない）。
- ⚠**カード単位 PRESERVE で held に載らない効果**（同居効果が MANUAL 等）は `build` では直せない＝`effectId` をアンカーにした**外科パッチ**で JSON を直接訂正する。共有関数にカード固有テーブルを埋めるのは禁止。

### 4.6 Codex へ委譲するときの作法

- ⚠**Codex は実行できない**（サンドボックスのネットワーク遮断）＝**起案だけさせて、実行・判定は必ず引き取る。** 指示書の冒頭でそれを明言する。
- ⚠**Codex の修正提案は分類の根拠であって実装指示ではない。** 条件を JSON へ足す前に、**その timing の collector が self スコープでそのフィールドを読むか**を必ず確認する（読まないと条件が効かず過剰実行のまま）。
- ⚠**判定の誤りは「クラスタの共通根拠を、構造の違う1件へそのまま適用する」形で出る**＝クラスタ単位の結論は**構造が同じことを1件ずつ確かめてから**採る。
- ⚠**1バッチ 20クラスタ／40 findings 前後を厳守**（107件を投げたバッチは根拠がテンプレート化して証拠にならなかった）。**件数を減らして1件ごとの証拠を厚くするほうが総合的な収量は高い。**
- ⚠**投入前に必ず `git status --porcelain` を空にする**（計器の生成時刻1行の差分で Codex が起動を拒否した実績）。
- **投入コマンド**＝`codex exec -C "C:/Users/zerom/source/WixossReact" -c model_reasoning_effort="high" -o <report> - < <指示書> > <log> 2>&1`。⚠**既定は `model_reasoning_effort = "low"` なので `-c` の指定は必須**。⚠**`-c sandbox_mode=…` は "danger" 文字列で分類器に弾かれるので付けない**。詳細は [CODEX_GUIDE.md](./CODEX_GUIDE.md)。

---

## 5. 作業キュー（単一・上から取る）

> **取り方**＝**上から1件**。並行して別の節から取らない（これが再編前に作業順がばらついた原因）。1巡の手順は §2、着手前に読むのは §4。
> **番号 `V-nn`／`O-nn` は参照用の固定ID**＝消化しても番号を詰めない・再利用しない。**消化したら行ごと消す**（全文は PLAN_DETAIL と BUGFIXES にある）。
>
> 🆕🔴**2026-09-01 整理＝局面が変わったので、節の役割を並べ替えた。**（🏁**続き769 で §5.1 は残0**＝本線は §5.3 だけ）
> **何が起きたか**＝**3つの進捗計器が同時に底を打った**＝`census` 高シグナル **11 / BASELINE 12**（旧ベースライン 1872）／
> `census:cards --sheet 1` の要対応 **18枚は全部 `mech`＝即着手可能 0**／意味照合台帳の残 OPEN **44 は全件が UI か新機構待ち**。
> ⇒ **「計器が指した在庫を消化する」時代は終わった。** 残っているのは **機構（§5.3）** と **実機・UI（§5.1）** だけ。
>
> | 節 | 役割（2026-09-01 以降） | 残 |
> |---|---|---|
> | **§5.1** | **実機で確かめる**（`V-nn`）＝`src/screens/`・`src/engine/` に触れた回／挙動が変わった回の返済 | **15件**（`V-131`〜`V-145`。🆕2026-09-04 第64〜68で `V-145` を登録） |
> | **§5.3** | 🔥**唯一の本線キュー**＝機構 worklist（`O-nn`）。索引 A→B→C の順に取る | **39項目**<br>🆕2026-09-04（`O-60` 第64〜68）＝`O-235` クローズ −1／`O-236`〜`O-242` 登録 +7＝33→39。⚠**増えたのは退化ではない**＝catch-all に隠れていた穴を1件ずつ名前つきで出した結果 |
> | §5.2 | 🏁掘り尽くし。**ここから新規に取らない**（残44は §5.3／§5.1 の結果として減る） | 44 |
> | §5.4 | 構造混線だけ（1件あたりのコストが桁で高い＝**後回しでよい**） | 5件 |
> | §5.5 | 低優先・保留 | — |
>
> ⚠**「§5 の上から1件」を §5.3 の索引にそのまま当ててよい**（索引が母集団順に並んでいる）。**旧「取る順」表は廃止した。**

### 5.1 【最優先】実機未検証の返済

> **2026-08-23 ユーザー決定＝新規実装を止めて、溜まった実機検証を先にゼロにする。** 以後は §2.2 のとおり1巡に実機検証を内包するので、ここが再び溜まることはない。
> 🏁**2026-08-24＝この節は残0クローズ**。`V-nn` エントリ14件（V-04/16/19/30/35/44/45/58/63/83/84/85/86/87）に加え、**続き616〜634 の21バッチのうち「実機経路へ直接触った4本」**（第34/43「0枚選択可の UI」・第37「クラッシュ主体の限定」・第38「群割当の決定可否」・第40「`END_OF_ATTACK`」）も実機で返済した。📋**残り17バッチは parser 側の構造是正が主で golden と census が母集団を押さえている**ぶん＝実機で踏むべきものは残っていない。
> ⚠**着手前に §4.4 の29項を読む。** ⚠**`verifyBattleDrive.mjs` は必ず明示シナリオIDで実行する**（引数なしのフルバッチはフリーズ報告あり）。
> **FAIL を見たときの切り分け3分類**＝(a)**シナリオの腐り**〔仕様変更に spec が追いついていない＝§4.4 の26〕は**その場で直す** (b)**engine/parser のバグ**も**その場で直す**（§2.4） (c)**未実装**は §5.3 へ登録。

> 🏁🆕**2026-09-01 続き769＝この節は再び残0クローズ。** `V-107`（`WX22-018-E2` の `both_trash`）・
> `V-105`（`WXK03-070` の `cost.energyTrashGroups` の支払い2経路）・`V-104`（`WXK11-013` の `LRIG_LIMIT_MODIFY{owner:'any'}`）・
> `V-103`①【ライド】の【起】／②`split_top_bottom` の振り分けUI を**新規シナリオ13本**で全部踏んだ（**単体でも13本一括でも全 PASS**）。
> 全文は [BUGFIXES.md](./BUGFIXES.md) の「2026-09-01（続き769）」。**engine/parser は不変更**＝触ったのは
> `scripts/verifyBattleDrive.mjs` と `SigniOnPlayCostModal.tsx` の `data-testid` 1行だけ。

> 🏁🆕**2026-09-01 続き771＝索引 C バッチの分をその巡のうちに返済した（この節に残さない）。**
> **`V-108`**（`O-146`＝エナ→デッキ上の任意選択）・**`V-109`**（`O-145`＝ライフ→デッキの二択）・
> **`V-110`**（`O-212`＝パワー合計ちょうど）を**新規シナリオ5本**で踏み、**単体でも5本一括でも全 PASS**
> （`o146EnergyTopTake/Skip`・`o145LifeTopTake/Skip`・`o212PowerSumExact`）。全文は BUGFIXES.md の同日。
> 🔑**5本とも `effect_stack` 注入で解決させた**＝`SPDi43-26-E1` はルリグアタック時、`WXDi-P12-034-E2` は
> アシストルリグの【出】、`WXK09-023-E1` はアーツで、**カードを実際に出す経路は観測に無関係な固定費が高い**。
> 🔴**この巡で踏み直した罠2つ**＝(a)**注入したスタックは最初のクエリ時点で既に `pending_effect` へ移っている**
> ＝`stackLen > 0` を前提条件にすると「積まれていない」で即 FAIL する（b22 の「基準値は最初のクエリではなく
> スペックの固定値」と同じ形。`LOOK_AND_REORDER` が1枚抱えている間は **life が2枚に見える**のも同根）
> (b)**確定直後の1回読みはコミット前の盤面を掴む**＝単体では PASS するのに**一括実行だけ FAIL** する。
> ⇒ **`settled` ストリーク（3ティック連続で pending も stack も無い）を進行条件にする。**

> 🏁🆕**2026-09-01 続き772＝索引 B バッチの分もその巡のうちに返済した（この節に残さない）。**
> **`V-111`**（`O-191`＝付与 `ON_SPELL_USE` の《ディソナ》限定・**負方向**）・**`V-112`**（`O-180`＝グロウ候補のルリグタイプ無視）・
> **`V-113`**（`O-148`＝【ウィルス】の枚数選択と倍率の運搬）を**新規3本**で踏み、
> **既存 `v12GrantedSpellUseMinus4000` を正方向の回帰として同時に再実行**して**4本とも PASS**
> （`o191SpellUseNonDisona`・`o180GrowIgnoreLrigType`・`o148VirusAnyCount`）。
> 🔑**`BattleScreen` の共通化リファクタは「正方向の既存シナリオ」＋「負方向の新規シナリオ」の2本で挟む**＝
> 片方だけだと「全部発火」も「全部不発」も緑に見える（§5-3′ の実機版）。
> 🔑**ルリグ【起】も手札スペルも UI は2段**（「【起】…」→「発動」／「発動」→「発動する」）＝
> 1段目だけ押して止まると**前提崩れの FAIL** になる。**押せなかったら開き直して自己回復する**ループにする。

> 🏁🆕**2026-09-02 続き774＝索引 C バッチの分もその巡のうちに返済した（この節に残さない）。**
> **`V-114`**（`O-200`＝ルリグデッキからキーを場に出す＋枠2の積み上げ）・**`V-115`**（`O-200` の `BattleScreen` 側ゲート）・
> **`V-116`**（`O-201`＝`trashToDeckBottom` の支払いUI）・**`V-117`**（`O-199`＝テキスト形アンコール）・
> **`V-118`**（`O-202`＝ダメージ置換の宣言）・**`V-119`**（`O-162`＝プレイヤー選択と `maxCount`）を
> **新規シナリオ9本**で踏み、**単体でも9本一括でも全 PASS**。
> 🔴**この巡で見つけた実バグ＝「キーが1枚も場に出せない」**（`V-115` が最初 FAIL したので調べて発覚）＝
> `getMyLrigDeckCardActions` が **CSV の空欄 `'-'` を `!timing`（＝タイミング指定なし）と判定**していた。
> ⇒ 🔑**実機シナリオの FAIL は「シナリオの腐り」より先に「本当に壊れている」を疑う**（§5.1 の3分類の (b)）。
> 🔴**踏み直した罠2つ**＝(a)**「召喚」→ゾーン選択は別ティック**（同じ tick で両方押すループを書くと永久に置けない）
> (b)**`UNTIL_OPP_TURN_END` のパワー修整は `power_mods_until_opp_turn`**（`temp_power_mods` を見て「走っていない」と誤読）。
> 🔑**同じ testid を押し続けない**＝`keycost-energy-0` はトグルなので、複数枚コストは index を進めて1枚ずつ選ぶ。

> 🏁🆕**2026-09-02（索引 C 第9巡）＝この巡の分もその巡のうちに返済した（この節に残さない）。**
> **`V-118`**（`O-114`＝エナゾーン自己起動【起】の新入口）・**`V-119`**（`O-114`＝トラッシュ自己回収【起】）・
> **`V-120`**（`O-180`＝次のアシストグロウ修整の一過性）・**`V-121`**（`O-206`＝`trashExile` の集合制約）・
> **`V-122`**（`O-186`＝次の自ターン終了までの寿命）・**`V-123`**（`O-84`＝条件つき追加使用タイミング）を
> **新規シナリオ10本**（うち**反転確認5本**）で踏み、**単体でも10本一括でも全 PASS**。
> 🔴**実機が机上では出ない嘘を2件捕まえた**＝
> (a)**`TrashActivatedModal` の実行ボタンが「発動する（トラッシュから場に出す）」固定**＝
>   アクション出し側（`getMyTrashCardActions`）のラベルだけ直しても**モーダルは嘘のまま**だった。
>   ⇒ 🔑**同じ文言を2箇所で組み立てているものは必ず両方直す**（`trashActivateVerbLabel` へ集約）。
> (b)**`WXDi-P13-043` はシグニではなくアシストルリグ**＝【出】は手札召喚ではなく**アシストグロウ**で発火する。
>   ⇒ 🔑**シナリオを書く前に `CardData` の `Type` 列を読む**（「【出】＝召喚」と決めつけない）。
> 🔑**支払いモーダルの選択セルは testid で狙う**＝`page.locator('img').nth(i)` は常設の手札ストリップを掴んで
>   背景キャンセルを誘発する（今回 `signiact-trashexile-*` / `assistgrow-ena-*` / `assistgrow-cand-*` /
>   `assistgrow-submit` / `signiact-fire` を追加）。
> 🔑**トグルするセルを毎ループ押さない**＝`signiactcost-energy-0` を毎ティック押すと選択が付いたり外れたりして
>   実行ボタンが永久に有効にならない（「1回だけ押す」フラグを持つ）。
> 🔑**「スペル」と書いてあるカードを CSV で確かめる**＝`WX05-016`／`WX04-008` は**アーツ**なので
>   `filter:{cardType:'スペル'}` に当たらず、`O-206` の成立側が前提崩れで FAIL した。

> 🏁🆕**2026-09-02（索引 A `O-86` 第2〜6バッチ）＝この巡の分もその巡のうちに返済した（この節に残さない）。**
> **`V-124`**（`O-86`＝【ブースト】の追加エナコストが payload から請求される）・
> **`V-125`**（`O-86`＝ベット宣言で使用コストが《緑×0》へ置換される）を**新規シナリオ2本**で踏み、
> **既存4本（`o199EncoreTextCostPay`／`craftArtsBetK07105`／`o123usetimepay`／`o123usetimenopay`）を
> 回帰として同時に再実行して 6/6 PASS**。
> 🔑**payload 化の実機観測点は「請求額そのもの」で書く**＝「OFF なら0枚で使える／ON なら N枚要る」のように
> **払える／払えないの境界**へ盤面を置くと、payload が読めていないとき必ず落ちる（表示だけ見ると緑に化ける）。
> 🔴**踏んだ罠2つ**＝(a)**`ATTACK_SIGNI` ではアーツ窓が開かない**（`BattleScreen.tsx:8645`＝`ATTACK_ARTS` /
> `ATTACK_ARTS_OP` だけが timing `ATTACK` へ写像される）＝ルリグデッキのカードを押しても画像拡大になるだけ。
> (b)**エナの色を確かめずに置くと「payload は読めているのに成立しない」に見える**（《緑》要求に黒3枚を置いた）。

> 🏁🆕**2026-09-02（索引 A `O-86` 第7・8バッチ）＝この巡の分もその巡のうちに返済した（この節に残さない）。**
> **`V-126`**（`O-86`＝比例軽減が payload だけで効く＝`WX12-Re04` のルリグトラッシュのアーツ2枚で
> 印刷7枚→3枚）・**`V-127`**（`O-86`＝センタールリグ＜花代＞で《赤×1》軽減＝`WX11-015` が
> エナ1枚で使える）を**新規シナリオ4本**（うち**反転確認2本**）で踏み、
> **`O-86` の実機8本を一括再実行して 8/8 PASS**。
> 🔑**payload 化の観測点は「請求額そのもの」＋「条件を外した対照」で書く**＝
> 条件を満たさない側で**提示すらされない**ことまで見ると、「条件を見ずに一律で安くしている」型の
> 壊れ方も同時に捕まる。

> 🏁🆕**2026-09-02（索引 A `O-86` 第9バッチ＝項目クローズ）＝この巡の分もその巡のうちに返済した（この節に残さない）。**
> **`V-128`**（`O-86`＝**場のシグニのクラス条件**による軽減が payload だけで効く＝`WX20-005` は
> 場に＜精生＞が居れば《青×1》減り、**青を1枚も持たないエナ2枚**で使える／居なければ提示すらされない）を
> **新規シナリオ2本**（反転確認つき）で踏み、**`O-86` の実機8本を一括再実行して 8/8 PASS**。
> 🔑**「青を持たないエナ」で払わせるのが観測点**＝枚数だけを見ると「軽減が効いていないのに
> たまたま払えた」を緑と誤読する。**色の要求ごと消える**ことまで見る。

> 🆕🔴**2026-09-03（`O-60` 第37〜48バッチ＝索引 A 第8巡）＝`src/screens/BattleScreen.tsx` を触った**ので
> PLAN §2.2 の機械判定で**実機必須**。新しい型・機構も6つ足したので観測点を3本登録する（**未実施**）。
> **`V-133`**（第39バッチ＝**`ON_RISE` の収集元と「そのシグニ」を反転した**）＝
> ①`WX16-039`（鎌振の十勇 ユリカマ）を場に出し、その上に**【ライズ】シグニを重ねる**と、
>   **重ねた側（上のシグニ）**が【ダブルクラッシュ】を得る（下敷きの ユリカマ ではない）。
> ②`WX20-056`（オイチ）の上に**カード名に《オダノブ》を含む**【ライズ】シグニを重ねたときだけ
>   E2 が発火し、**上のシグニ**が「手札に戻らず／ダウンせず／新たに能力を得ない」を得る。
> ⚠**反転確認**＝《オダノブ》を含まない【ライズ】シグニを重ねると E2 は発火しない（E1 のパワー＋3000だけ）。
> 🔴**旧実装ではこの11枚が1度も発火しなかった**＝「発火すること」自体が観測点。
> **`V-134`**（第41バッチ＝**相手エナの色制限が「宣言された色」で効く**）＝`WXK09-037` を場に出し、
> 【出】で相手に色を宣言させたあと、**その色を持たないカード**を相手がエナチャージすると
> トラッシュへ行き、**その色を持つカード**と**無色のカード**は普通にエナへ入る（原文「無色ではない」）。
> ⚠**反転確認**＝宣言前は制限が張られない（全部エナへ入る）。
> **`V-135`**（第43・45バッチ＝**新設した選択UIが実機で出る**）＝
> ①`WXDi-P01-013`（【出】）で相手シグニ1体を選んだあと**「デッキの一番上／一番下」の2択**が出る。
> ②`WXK02-035`（【出】《青》）でデッキの**一番下**の札がチェックゾーンへ行き、シグニなら
>   「場に出す／出さない」を選べる。**出したあとチェックゾーンに残っていない**こと（＝ターン終了時に
>   場のカードが消えないこと）まで見る。

> 🆕🔴**2026-09-03（`O-60` 第59バッチ＝索引 A 第19巡）＝この巡も `src/screens/` を触っておらず、
> 新しい interaction 型も0**なので PLAN §2.2 の機械判定では**実機不要**。
> ⚠だが **3効果が「原文と違う数値／落ちていたゾーン／並び順」で動いていた**ので観測点を1本登録する（**未実施**）。
> **`V-140`**（`O-60` 第59バッチ＝**比例パワー修正・全体トラッシュ・選んだ能力の付与**）＝
> ①`WX07-017`（アーツ）を使うと**両プレイヤーの手札とエナも空になる**
>   （🔴旧は**シグニとキーしか流れず**、手札とエナは1枚も動かなかった。しかも**原文に無いキー**を流していた）。
>   **キーが場に残っていること**まで見る。
> ②`WXK10-084` の【自】で、**レベル1のトリック**がアタックしたら相手シグニのパワーが**－1000**、
>   **レベル4**なら**－4000**（🔴旧は効果元 Lv3 に引きずられうる形だった）。
>   対象候補が**レベルが偶数（E1）／奇数（E2）の相手シグニだけ**に絞られていることまで見る。
> ③`WXK04-002` の【起】《ゲーム１回》で、選択肢が**原文の①②③の並び順**
>   （アサシン → ダブルクラッシュ → バニッシュ不可 → ダウン不可 → バウンス不可）で出て、**2つ**選べる
>   （🔴旧は engine 側のパターン順で出ていた）。
> 🔴**反転確認**＝`WXDi-D06-016` の【出】《黒》で、パワー減少量が
>   **自分の場のシグニが持つ色の種類 × －3000**になっていること（🔴旧は regex が外れると
>   **原文に無い既定 －3000（1種ぶん）** へ落ちる形だった＝色が3種でも－3000のままになりうる）。

> 🆕🔴**2026-09-04（`O-60` 第64〜68バッチ＝索引 A 第24〜28巡）＝`src/screens/` は0行だが `src/engine/` を2箇所触った**
> （①パワー／バウンス保護の `excludeSelf` ②`LAYER_ABILITY_COPY{source:'last_processed'}`）。
> **14効果の挙動が変わった（うち10効果は恒久 no-op から実際の効果／明示 defer へ）**ので観測点を1本登録する（**未実施**）。
> **`V-145`**（`O-60` 第64〜68バッチ＝**引用付与 catch-all の解体**）＝
> ①🔴**3択の消えていた2枝**＝`WX22-Re04`（験英の応援 ＃ウカロー＃）を場に出し、【出】の3択で
>   ①**他の＜英知＞シグニのパワーが相手効果で下がらない** ②**他の＜英知＞シグニが相手効果で手札に戻らない**
>   が実際に効くこと（🔴旧は①②とも**無言 no-op**＝選んでも何も起きなかった。③のバニッシュ耐性だけが動いていた）。
>   ⚠**反転確認**＝**効果元の ウカロー 自身**は守られない（原文「あなたの**他の**＜英知＞のシグニ」）。
> ②**引用【自】の付与**＝`PR-K076`（大装 ネメシス）の【起】《黒》＋自身トラッシュで自分のシグニ1体に
>   ＋3000し、**そのシグニがアタックしたとき**に相手シグニのパワーが**そのシグニのパワーぶん**下がること
>   （＝もう一度対象を選ばされないこと＝`targetsLastProcessed`）。
> ③**クラフトの下から上へ**＝`WXDi-CP02-TK03A`（虎丸）の上に《棗イロハ》を置き、
>   **イロハのパワーが＋5000**され、**アタックフェイズ開始時に相手シグニ2体まで−3000**が飛ぶこと
>   （🔴旧は E1 が「場のシグニ1体」に潰れて**クラフト自身**にバフし、E2 は恒久 no-op だった）。
> ④**レイヤー能力のコピー**＝`WXEX1-32`（幻怪姫 ヌエ）の【出】でデッキから＜怪異＞2枚をトラッシュへ落とし、
>   **そのターンの間ヌエがその2枚の《レイヤーアイコン》能力を持っている**こと（🔴旧は落とすだけで何も付かなかった）。
> ⑤**穴が見えること**＝`WXEX2-66`／`WX25-P2-004`／`WXDi-P03-002`／`WXDi-P05-068`／`WXDi-P12-036`／
>   `WXDi-D04-011`／`WXK03-042` の逆翻訳に**【未実装】**が出ること（旧は「能力を付与」と嘘をついていた）。

> 🆕🔴**2026-09-04（`O-60` 第63バッチ＝索引 A 第23巡）＝この巡は `src/engine/` も `src/screens/` も触っておらず、
> 新しいアクション型・条件型も0**（足したのは parser の**条件節2形**＝どちらも既存 `ActiveCondition` の組み合わせ）
> なので PLAN §2.2 の機械判定では**実機不要**。
> ⚠だが **12効果の挙動が変わった（うち2効果は恒久 no-op から実際の効果へ）**ので観測点を1本登録する（**未実施**）。
> **`V-144`**（`O-60` 第63バッチ＝**引用【常】の条件つきキーワード付与**）＝
> ①🔴**期間の撃ち分け**＝`WXDi-P06-032`（または `WXDi-P13-044`）の【起】を**自分のターンに**使い、
>   そのまま**相手ターンへ渡して**、そのシグニが**【シャドウ】を持っている**こと（＝相手の対象に選べない）。
>   **旧は恒久 no-op**＝付与が `granted_effects`（ターン内）へ入り**自分のターン終了で消えていた**ので、
>   「対戦相手のターンの間」という発動条件が真になる頃には付与そのものが無かった。
> ②**引用が2つ並ぶ形**＝`WXDi-P15-069` の【起】（コイン2）で、**【ランサー】が付く**こと
>   （🔴旧は `「A」と「B」を得る` の**前half が丸ごと落ちて**いた＝【ランサー】が一度も付かなかった）。
> ③**ゲートが効く**＝`WXDi-P12-078`（正面がレベル1のときだけ【ランサー】）／`WXDi-P13-069`
>   （正面が**凍結かつパワー5000以下**のときだけ【アサシン】）で、**条件を満たさない正面では発動しない**こと。
> 🔴**反転確認の観測点**＝③は「正面を non-レベル1／非凍結にすると【ランサー】【アサシン】が消える」ことまで見る
>   （engine のパターン表から parser の `activeCondition` へ移した箇所なので、**常時発動へ倒れていないこと**が本質）。

> 🆕🔴**2026-09-03（`O-60` 第62バッチ＝索引 A 第22巡）＝この巡も `src/screens/` を触っておらず、
> 新しいアクション型・条件型・interaction 型も0**（足したのは `StubAction` の payload キー2本と
> `triggerCondition` の宣言キー1本）なので PLAN §2.2 の機械判定では**実機不要**。
> ⚠だが **8効果の挙動が変わった（うち3効果は真 no-op から実際の盤面変化へ）**ので観測点を1本登録する（**未実施**）。
> **`V-143`**（`O-60` 第62バッチ＝**引用能力の付与**）＝
> ①`WD17-001`（ルリグ）の【起】で、**《ライズアイコン》を持つ自分のシグニ**を選び、
>   そのシグニが**バトルで正面の相手シグニをバニッシュしたときにアップする**（🔴旧は**真 no-op**＝何も付かなかった。
>   さらに一度は `ON_PLAY`＝**場に出た瞬間にアップ**へ落ちる形だった）。
> ②`WXDi-P07-085` の【起】3択で、**基本パワーが 5000／10000／12000 に変わる**
>   （🔴旧は**3択とも基本パワーの変更が丸ごと落ちていた**）。③を選んだら**相手の効果でダウンしない**こと。
> ③`WX25-CP1-003` の《リコレクト》条件を満たして使うと、**相手のすべてのシグニが凍結する**
>   （🔴旧は連用形の前半が落ちて凍結しなかった）。
> ④`WX25-P2-004`（アーツ）を使うと、**相手のすべてのシグニ**が「アタック時に《無×3》を払わないと自壊」を得る
>   （🔴旧は**真 no-op**）。
> 🔴**反転確認**＝`WXDi-P03-002`（「このゲームの間、あなたは以下の能力を得る」）は**据置のまま**＝
>   グロウしても**エナチャージが起きない**こと（載せると「最初のグロウ」条件が無く**毎回発動**する過剰実行になるため
>   差し戻した。機構は `O-235`）。

> 🆕🔴**2026-09-03（`O-60` 第61バッチ＝索引 A 第21巡）＝この巡も `src/screens/` を触っておらず、
> 新しいアクション型・条件型・interaction 型・payload キーも0**（既存の `energyTrash` / `IS_BETTING` /
> `additionalCostChoose` を使っただけ）なので PLAN §2.2 の機械判定では**実機不要**。
> ⚠だが **3効果の提示形が変わった**ので観測点を1本登録する（**未実施**）。
> **`V-142`**（`O-60` 第61バッチ＝**モーダル選択 family の残り3件**）＝
> ①`WDK01-010`（アーツ）を**ベットせずに**使うとトラッシュから**3枚まで**、
>   **ベット宣言すると4枚まで**選べ、**どちらの場合も**「センタールリグと共通する色」かつ
>   「それぞれレベルが異なる」候補だけが出る（🔴旧は**ベット時の追加1枚だけ**がその絞り込みを持たなかった）。
> ②`WX13-003`（アーツ）で**①②③④の4つが表示され4つまで**選べる（🔴旧は選択肢が live JSON に無かった）。
> ③`WX26-CP1-024`（アーツ）で「エナから＜プリオケ＞3枚をトラッシュしますか？」が**1回だけ**出て、
>   支払うと**2つ**選べる（🔴旧は engine が**カード全文**から＜クラス＞と枚数を読み直していた）。
> 🔴**反転確認**＝`WX26-CP1-024` で**支払わなかった**ときは**1つしか**選べないこと。

> 🆕🔴**2026-09-03（`O-60` 第60バッチ＝索引 A 第20巡）＝この巡も `src/screens/` を触っておらず、
> 新しいアクション型・条件型・interaction 型も0**（足したのは `StubAction` の payload キー1本）なので
> PLAN §2.2 の機械判定では**実機不要**。
> ⚠だが **18効果の提示形（pending）が受け皿 STUB 由来から parser 生成の `CHOOSE` へ変わった**ので
> 観測点を1本登録する（**未実施**）。
> **`V-141`**（`O-60` 第60バッチ＝**モーダル選択 family の提示**）＝
> ①`PR-K072`（ベット）を**ベットせずに**使うと**①②が2つ表示されて1つだけ**選べ、
>   **ベット宣言すると2つとも**選べる（🔴旧は選択肢が live JSON に無く engine が実行時に組んでいた）。
> ②`WX17-004` を**センタールリグが＜リル＞**のときに使うと**2つまで**選べ、
>   **＜リル＞でも＜メル＞でもない**ときは**1つだけ**（🔴旧は**カード名**照合だったので
>   `紅蓮乙女 リル` のような通常名では**永久に1つのまま**だった）。
> ③`SP26-005`（アーツ）を使うと「追加コストを支払いますか？」が**1回だけ**出る
>   （🔴旧は `OPTIONAL_COST` と受け皿 STUB が**同じ支払いを二重に提示**していた）。支払うと**2つ**選べる。
> ④`WD23-044-EA`（スペル）で、①が**《ライフバースト》を持つカードだけ**に絞られている
>   （🔴旧は `cardName:'ライフバースト'` で**どのカードにも一致しない**か、絞り込みが丸ごと落ちていた）。
> 🔴**反転確認**＝`WDK06-R08`① で、**自分の《ライズアイコン》持ちより低いパワーの相手シグニだけ**が
>   候補になること（🔴旧の parser 経路は比較句が落ちて**どの相手シグニでも選べた**）。

> 🆕🔴**2026-09-03（`O-60` 第58バッチ＝索引 A 第18巡）＝この巡も `src/screens/` を触っておらず、
> 新しい interaction 型も0**なので PLAN §2.2 の機械判定では**実機不要**。
> ⚠だが **4効果が「無言 no-op／原文と逆／過小実行」から実際の盤面変化へ変わった**ので観測点を1本登録する（**未実施**）。
> **`V-139`**（`O-60` 第58バッチ＝**リミット修正の向きと、下に置く枚数・絞り込み**）＝
> ①`WXDi-P16-002`（ピース）を使うと**あなたの**センタールリグのリミットが**＋2**になる
>   （🔴旧は**対戦相手の**リミットが＋2になっていた＝原文と逆）。**そのターンのうちに**効くこと
>   （🔴旧は `NEXT_TURN` 扱いで**払ったターンには1も効かなかった**）まで見る。
> ②`WX25-P2-014`（レリーズ）を使うと**自分＋1／相手−2**の**両方**が起きる
>   （🔴旧は自分の＋1が消え、相手の−2が**二重**に掛かって−4になっていた）。
> ③`WDK15-001`（【起】《ゲーム１回》ブラインド）で**トラッシュのシグニを2枚**選べる
>   （🔴旧は**常に1枚**しか選べず、しかも**トラッシュの＜ウェポン＞しか候補に出なかった**）。
>   配置先が**あなたの＜ウェポン＞のシグニ**に絞られていることまで見る。
> ④`WXEX2-61`（【自】アタック時）で配置先が**《ライズアイコン》を持つシグニ**に絞られる
>   （🔴旧は＜武勇＞のシグニが配置先候補になっていた）。
> 🔴**反転確認**＝`WXDi-CP01-005` の【常】（【ガード】時の代替コスト）で
>   **アシストルリグを場に出す対話が出ないこと**（🔴旧は原文と無関係にコラボ対話が開いた。
>   いまは `DEFERRED_GUARD_ALT_COST_COLLAB`＝機構待ち＝§5.3 `O-230`）。

> 🆕🔴**2026-09-03（`O-60` 第57バッチ＝`O-228`＝索引 A 第17巡）＝この巡も `src/screens/` を触っておらず、
> 新しい interaction 型も0**なので PLAN §2.2 の機械判定では**実機不要**。
> ⚠だが **8効果が「恒久 no-op／原文と無関係な UI」から実際の対話へ変わった**ので観測点を2本登録する（**未実施**）。
> **`V-137`**（`SOUL_OP` のコスト先取り撤去＝**ルリグの下を払う対話が初めて出る**）＝
> ①`WXDi-P04-009`（ノンストップ Dr.タマゴ）でアタックすると
>   **「ルリグ下のカード1枚を使用して発動しますか？」が出る**（旧は「ソウルを使用して発動しますか？」で
>   **支払い肢が常にグレー＝スキップしか選べなかった**＝効果元がルリグでシグニゾーンに居ないため）。
>   払うと**ルリグの下の1枚がルリグトラッシュへ移り、相手の手札が1枚減る**。
> ②`WXDi-CP02-002`（セミナー・ピース）で**「ルリグ下のカード4枚を使用して発動しますか？」**が出て、
>   **センターとアシスト両方の下を合わせて4枚**払える（`fromAllLrigs`）。
> 🔴**反転確認**＝ルリグの下の合計が4枚未満なら**支払い肢がグレー**（払えない＝絆も獲得しない）。
> **`V-138`**（`SOUL_OP` payload 化＝**ルリグトラッシュの選択UIとルリグ下の強制消費**）＝
> ①`WX22-Re20`（月欠けの戦場）の③を選ぶと**ルリグトラッシュから「レベル2以下のルリグ」だけが候補の
>   SELECT_TARGET が出て、2枚まで選べる**（旧は**どの regex にも当たらず既定値へ落ちていた**＝miss）。
>   選んだルリグが**センタールリグの下に入る**こと（ルリグトラッシュから消えること）まで見る。
> ②`WXDi-P13-003B`（未知の巫女 マユ）を出すと**ルリグの下から1枚が自動でルリグトラッシュへ行く**
>   （旧は無言で何も起きなかった）。続けて相手の全シグニが能力を失う。
> ③`WD22-016-UG`（バッド・エクシード）＝**ルリグの下が4枚あるときだけ**、4枚を払って
>   **トラッシュのシグニを2枚まで選んで場に出す**（旧は**デッキの一番上**が出る JSON で、
>   さらに支払い肢がグレーで**恒久 no-op** だった）。
> 🔴**反転確認**＝ルリグの下が3枚以下なら`WD22-016-UG`は**1枚も払わず場にも出さない**。

> 🆕🔴**2026-09-03（`O-60` 第51バッチ＝索引 A 第11巡）＝この巡は `src/screens/` を触っておらず、
> 新しいアクション型／interaction 型も0**なので PLAN §2.2 の機械判定では**実機不要**。
> ⚠だが **1枚だけ「無から対話が生えた」**ので観測点を1本登録する（**未実施**）。
> **`V-136`**（`WXDi-P15-067`＝INSPIRATION・スペル）＝
> ①**【起】を撃つと「どのシグニの下に置きますか？」の CHOOSE が出る**（候補は**自分の場の
>   ＜解放派＞のシグニだけ**）→ 続けて**手札の＜解放派＞のシグニ**を1枚選ぶと、
>   選んだ**場のシグニのスタックが2枚になる**（手札からは消える）。
> ②**「置かない」を選べる**（原文「置いても**よい**」＝任意）。
> 🔴**旧実装ではこの2文目が1度も動かなかった**＝効果元が**スペル**なので `PLACE_UNDER_SOURCE_SIGNI` の
> 「効果元シグニのゾーン」検索が `-1` を返し、**無言で終了**していた。⇒ **「対話が出ること」自体が観測点。**
> ⚠**反転確認**＝場に＜解放派＞のシグニが1体も無ければ対話が出ない（`置き先のシグニなし`）。
> 🔑**併せて見るとよい**＝置いた後、`WXDi-P15-060`（幻竜 遊月）の【常】「場にあるシグニの下に
> カードがあるかぎり＋4000」が**実効パワーに乗る**（下に入ったことの独立した観測）。

> 🆕🔴**2026-09-02（🏁`O-220` クローズ回）＝この巡は `src/screens/` を触っていない**ので
> PLAN §2.2 の機械判定では**実機不要**だが、**新設した型・語彙は選択UIの見え方を変える**ので
> 観測点を2本登録する（次に実機を回す巡で踏む）。
> **`V-131`**（`O-220` 第6・7バッチ＝**新設した宣言スコープが実機で選べる**）＝
> ①`WX17-029-TRAP`＝トラッシュから**【トラップ】能力を持つカードだけ**が候補に出て、
>   **支払いより前**に1枚選べる（`TargetFilter.hasTrapAbility`）。**二重プロンプトが出ないこと**まで見る。
> ②`WX25-P1-061-E2`＝デッキから落とした3枚のうち**＜古代兵器＞のシグニだけ**が候補に出て、
>   **支払いより前**に1枚選べる（`trashedPick.dest:'declare'`）。⚠**反転確認**＝落ちた3枚に
>   ＜古代兵器＞が無ければ支払いを提示しない／黒0枚なら何も出ない。
> **`V-132`**（`O-220` 第1・2・8バッチ＝**即適用分岐でモーダルが挟まらない**）＝
> ①`WX16-029-E1`（【トラップ】窓）＝アタック中のシグニ1体しか候補が無いので**問いが出ずに**アタックが止まる。
> ②`SPDi43-20-E1`＝支払いのあと**もう一度選ばされずに**入れ替わる。
> ③`WX21-025-TRAP`＝OR コストのどちらを払っても**同じ1体**が【トラップ】化する。
> 🔴**②③は前回2度踏んだ「絞り込みだけ足して即適用分岐を書かない」形の再発確認**＝
> **golden は全緑のままなので実機でしか出ない。**

> 🏁🆕**2026-09-02（🏁`O-96` 第7〜13バッチ＝クローズ回）＝この巡の分もその巡のうちに返済した。**
> **`V-130`**（`O-96` 第8バッチ＝**トラッシュから「場に出す」形でも、支払いの前に選んだ1枚だけが出る**＝
> `WXDi-P05-040-E3` でトラッシュの＜天使＞2枚のうち選んだ1枚だけが場に出る／エナ0枚なら何も出ない）を
> **新規シナリオ2本**（`o96AddToFieldPay`/`None`・反転確認つき）で踏み、engine を触ったので
> `V-129` 2本＋`O-86` 5本＋`O-71` 1本を回帰として同時に再実行して**全 PASS**。
> 🔴🔑**この巡も実機だけが捕まえたバグが1件あった（前回とまったく同じ形＝2度目）**＝
> `ADD_TO_FIELD` に `fixedCardNums` の絞り込みだけを足して**「選択UIを開かずに即適用する」分岐を書かなかった**ため、
> 実機で「払ったのに対象をもう一度選ばされ、確定できずに止まる」になっていた（**golden 3282 は全緑のまま**）。
> ⇒ 🔴**3点契約の③は「絞り込み」と「即適用分岐」の2つで1つ**（`O-220` の登録票に明記した）。
> ⚠**その即適用分岐で `leaveSubstituteAskQueue`（離場置換の問いかけ）を飛ばさない**
> （`execTransferToDeck` で踏んだ＝golden「task12(lxxxiii) 第15波」が落ちて発覚）。
>
> 🏁**2026-09-02（索引 A `O-96` 第5・6バッチ）＝この巡の分もその巡のうちに返済した（この節に残さない）。**
> **`V-129`**（`O-96`＝**支払いの前に選んだ対象**が支払いプロンプトを跨いで生き残り、**その1体だけ**に帰結が当たる＝
> `WX25-P3-059` で場の3体のうち選んだ1体だけが【アサシン】を得る／エナ0枚なら何も付かない）を
> **新規シナリオ2本**（反転確認つき）で踏み、engine を触ったので `O-86` の実機5本も回帰として同時に再実行して **7/7 PASS**。
> 🔴🔑**この巡は実機だけが捕まえたバグが1件あった**＝`freezeStoredTargets` が `targetsStored` を
> `fixedCardNums` へ焼いた後、`execGrantKeyword` の**「選択UIを開かずに即付与する」分岐が `targetsStored` しか見ておらず**、
> 実機で「払ったのに対象をもう一度選ばされ、確定できずに止まる」になっていた。
> **golden は 3282 本すべて緑のまま通っていた**（構造だけ見ていて UI の再プロンプトを観測できない）。
> ⇒ **engine の対象解決を触った回は実機まで必ず行く**（§2.2 の判定どおり）。

**■ この節の運用で効いた教訓（クローズ済みエントリの全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md) と BUGFIXES.md へ退避）**
- 🔴**盤面 spec は「CORE フィールド」を明示クリアする**（2026-09-01 続き769 実測）＝`field.key_piece` / `key_piece_extra` /
  `hand` / `deck` / `energy` などは `injectScenario` の**除外方式が消さない**ので、**前の巡の1枚が残ったまま**次のシナリオが回る。
  症状は「**【召喚】ボタンが出ない**」のような**別の場所の空振り**になるので、カード・ルリグ限定・待ち時間を疑って時間を溶かす。
  ⇒ **「盤面に1枚あるかどうか」で結果が変わる観測をするときは、その枠を両サイド明示的に null / [] にする。**
- 🔴**反転確認の後始末を `mv`（mtime 保存）で戻すと `distIsFresh()` が build をスキップし、次の実行が反転版の dist で回る**
  （続き769 で「一括実行だけ2本 FAIL」を踏み、シナリオ間汚染を疑って調査した＝真因は stale dist）。
  ⇒ **復元は `git checkout`、または復元後に `touch`。疑わしいときは `SKIP_BUILD=0`。**
- 🔴**`manualEffects.ts` は実行時にも勝つ**＝`BattleScreen` が `buildEffectsMap` → `mergeManualEffects` を毎回呼ぶので、
  **`public/data/effects_*.json` を削っても manual 側が復活させる**。⇒ **MANUAL 効果の反転確認は `manualEffects.ts` を触る。**
- 🔴**「ブラウザ実行環境のブロッカー」はもう無い**（2026-08-31 続き747 に実証）＝`node scripts/verifyBattleDrive.mjs censusHasTrapInField` は普通に PASS する。**残っているのは「シナリオを書く」作業だけ**なので、§5.1 を開いたら**まず1本回してみる**こと。
- 🔑**「ドライバは書いてあるが未実走」の在庫は実装より圧倒的に安い**＝この節に来たら**まず全部回す**（続き753 は回すだけで1件クローズできた）。
- 🔴🔑**実機ドライバでは「押した」ではなく「盤面/DOM が変わった」を進行条件にする。** 連続クリックを `.catch(() => {})` で握り潰すと、React の再描画で detach 済みを掴んで**半端な状態のまま先へ進む**（続き753 のフレークの真因）。`.catch(() => {})` を置いた行は**必ず次の行で「効いたか」を測る**。
- 🔑**候補モーダルは「このモーダルで選んだ札」を覚える**＝毎ティック同じ札を押すと選択がトグルして `決定` に永久に進まない（続き769 で2回踏んだ）。
- 🔑**「出ないこと」を主張する観測は、出る側と同じ時間だけ待ってから結論する**（`getMyHandCardActions` は `loading` 中 `[]` を返す）。
  出なかったときは**そのとき何が出ていたか**（`data-action-label` の一覧）を必ずログに残す。
- 🔑**観測だけの巡では押し切らない**＝`V-104` はモーダルを開いて読んでキャンセルし、「**盤面は不変**」を判定に含めた。
- 🔑**回数制・置換の対照は「同じ札・同じ操作で1ビットだけ反転」して測る**（別カードで反転すると別経路を踏んで比較にならない）。⚠**patch のあとを固定 sleep で済ませない**＝`queryState` で反映を観測してから次へ進む。
- 🔴**この節が要る理由の実例**＝ソフトロックのバグは **smoke / fuzz / golden / CPU がすべて「クランプする側」の経路しか通らない**ため、**全ゲート緑のまま人間だけが操作不能**だった。同様に `O-123` は `artsUseGate.ts` の1行が壊れていて **JSON にも逆翻訳にも1バイトも現れず、3つの計器がどれも動かなかった**。⇒ **`src/screens/` に関わる疑いは、実機を書くまで「直った」と言わない。**
- 🔑**engine の真バグも実機からしか出ないことがある**＝「このターン手札から捨てた」台帳の書き漏れ（10箇所が3種類に割れていた）／`ON_ATTACK_SIGNI` の遅延トリガー二重積み／`TRANSFER_TO_DECK.position` の `second`/`third` が実経路に無い、はすべて実機由来。🔑**真因は3件とも「同じ式を2箇所以上に書いた」こと**＝片方だけ機能を足すと**JSON も型も golden も緑のまま、実際に使われる経路にだけ実装が無い**。🔑**二重発動は肯定側テストでは緑になる＝回数まで assert する。**
- 🔑**盤面注入で「召喚ボタンが出ない」ときはまずルリグ限定を疑う**（`meetsRestriction(Restriction, lrigClass)`）＝盤面注入は成功しているのに操作が1手も始まらない、が症状。⚠**ただし続き769 では真因が「リミット」だった**＝限定・リミット・配置数・パワー制限の**4つを順に潰す**。

**■ 未実施の観測点＝🏁 なし（残0）**


### 5.2 意味照合監査（semantic audit）の段2 消化 ★🏁**掘り尽くし＝新規に取る節ではなくなった**

> 🆕🏁🔴**2026-09-01 整理＝この節は「本線キュー」から降りた。**
> **実測（`node scripts/archive/semanticAuditLedger.mjs`＝唯一の正）**＝findings 総数 1444／段0 機械除去 200／段1 偽陽性 111／
> **✅段2 で消化 1089**／🔥**残 OPEN 44**（影響カード 41・効果 32／HIGH 37・MED 7・LOW 0）。
> 🔴**その残44は、続き766 が1件ずつ全数 triage して「何が塞いでいるか」を確定させた＝engine/JSON だけで閉じられるのは0件。**
> **全件が `src/screens/`（支払いUI・召喚UI・ガードUI）か、新しい engine 機構を要する。**
> ⇒ **この節から「次の1件」を取ることはもう無い。** 実際の作業は **§5.3（機構）と §5.1（実機・UI）**で行い、
> **その結果としてここの OPEN が減る**、という向きになる。**「-30」のような件数目標はこの節に関して成立しない**
> （1件あたり機構1つなので、**件数ではなく「機構1つ＝1バッチ」で見積もる**）。
>
> ⚠**それでも段2 のバッチを始めるときは、実装より先に `node scripts/archive/semanticAuditRecheck.mjs` を回す**＝
> 台帳は `stage2_closed.txt` へ手で追記しないと減らないので**「直っているのに OPEN のまま」が恒常的に溜まる**（実測＝434 のうち48件が既に済み）。
> 🔴**ただし候補出しであって判定ではない**（LCS≥7 の108件のうち真の stale は48件＝約44%）＝**`--full` で原文 × 逆翻訳 を1件ずつ目視してから書く。閾値を下げて件数を稼がない。**

**■ 残44の内訳（2026-09-01 続き766 の全数 triage・次セッションは再導出しないこと）**

> **結論＝engine/JSON だけで閉じられるのは 1件だけだった**（それは消化して 46 → 45）。
> **残 45 は全部が `src/screens/`（支払いUI・召喚UI・ガードUI）か新 engine 機構を要する。**
> 🔴**「-30」のような件数目標は、この節に関してはもう成立しない**＝1件あたり機構1つなので、
> **件数ではなく「機構1つ＝1バッチ」で見積もること。**
>
> **A. 支払いUI（`src/screens/`）が要る … 約17件**
> `WDK05-T12-E1`（場→デッキトップ）／`WX21-031-CB-E2`（自身を場から手札へ）／`WXDi-CP02-100-E1`（トラッシュ→デッキ下）／
> `WXDi-P03-019-E1`・`WXDi-P12-031-E2`（場/手札/エナを全部）／`WXDi-P13-089-E3`（3領域から同名各1枚除外）／
> `WXK03-070-E1`／`WXDi-CP02-001-E1`（ルリグ下から4枚）／`PR-K056-E1`（選択数×《青》1）／
> `SPDi44-16-E2`・`WX25-P1-030-E2`（「N体**まで**」）／`null`（白か赤か青でしか払えない）／`WXDi-P07-046-E3`／
> `WX25-P2-075-E1`／`WXDi-D03-004-E3`（ガードアイコンでダメージ回避）ほか。
> 🔴**実測でわかった3つの落とし穴（型があっても消費が無い）**＝
>   ①**`cost.selfPowerDown` は parser が書いて decompiler が表示するだけで、engine にも支払いUIにも消費が無い**
>     （＝そのコストは実際には課金されない）。`WXDi-P07-046-E3` をこれで書いても無言 no-op。
>   ②**`cost.energyTrashGroups` の消費は `screens/battle/resonaSummon.ts` の1箇所だけ**＝
>     レゾナ召喚経路にしか無く、シグニの【出】コストとしては払われない（`WXK03-070-E1`）。
>   ③**`cost.fieldTrash` に `upToCount` が無い**＝「N体まで」が「N体固定」に化ける（`SPDi44-16-E2`／`WX25-P1-030-E2`）。
>   ⇒ **コスト系に着手するときは「型の有無」ではなく `screens/battle/costs.ts` ＋各コスト専用ファイルに
>     支払い経路があるか」を先に見る。**
>
> **B. 召喚UI（複数体ライズ）… 7件**＝`O-147`。`getRiseFilter`（`execUtils.ts:3478`）は
> **複数体ライズを明示的に `null` で落としている**（1体ぶんだけ通すと「半分だけ実装した嘘」になるため）。
>
> **C. ハーモニー … 2件**＝機構そのものが無い。
>
> **D. 新 engine 機構 … 残り**
> `WX18-002-E3`・`WXEX2-27-E3`（「相手がダメージを受けたとき」の遅延誘発＝`O-160`）／
> `WXDi-P11-TK02-E2`（シグニ**合計**1回のアタック制限＝既存の `ATTACK_COUNT_BY_POWER` は per-signi で別物）／
> `WX25-CP1-016-E1`（`ON_HAND_DISCARDED` の原因限定）／
> `WXK10-004-E1`（🔴`ZoneMoveImmunityAction.zones` も `activeOppMoveImmunityZones` も
>   **型が `'hand'|'energy'` 固定**＝「場以外の全領域」を表せない。`PREVENT_NON_FIELD_MOVE_BY_OPP` STUB も同じ近似）／
> `WX25-P2-052-E2`（レゾナとしても扱う）／`WX24-P2-043-E1`（ルリグタイプ無視）／`WXK03-059-E1`（【ライド】の使用時機拡張）／
> `WX25-P1-022-E2`（「このターン使用できる」権利）／`WXDi-P10-007-E3`／`WX13-005B-E1`（処理順序＋`REQUIRES_NEW_FLOW`）／
> `WXDi-P01-039-E1`×2（デッキ/トラッシュのシグニの基本レベル変更）／`WX24-P3-043-E1`×2／
> `WXDi-CP01-002-E1`・`WXDi-CP02-001-E1`（ピース使用済み条件）／`WXK08-040-E1`（**このスペル自身**を手札に戻す）／
> `WXDi-P09-036-E1`（`NEGATE_ATTACK` の対象がシグニ限定＝ルリグのアタックを無効にできない）／
> `WDK06-C14-E1`（対象宣言のタイミング＝**`SELECT_TARGET_ONLY` がトラッシュを候補にできない**＝`O-188` と同根）。
> ⚠**`WXDi-CP02-034-E1` は偽陽性の疑い**＝原文の括弧は「**コストのある**【出】は払わない選択ができる」という
> ルール説明で、当該 E1 にはコストが無い。**再判定してから閉じる/残すを決めること。**
>
> ✅**この巡で消化した1件**＝`WXDi-P03-004-E1`。🔑**登録コメントの「選択肢ごとのコストは `ChoiceOption` に
> 受け皿が無い（支払いUIが要る）」が誤りだった**＝受け皿は**枝の action の中**にあり、
> `STUB{OPTIONAL_COST}` → `CONDITIONAL{PAID_ADDITIONAL_COST}` で engine が支払いの `CHOOSE` を出す
> （`O-96` 第1〜3バッチで挙動まで golden 済み）。**「受け皿が無い」という過去の判断こそ最初に疑う。**

**■ この節が残した再利用可能な教訓（本体は [PLAN_DETAIL.md](./PLAN_DETAIL.md) へ退避）**
- 🔑**「受け皿が無い」という過去の判断こそ最初に疑う。** 続き766 の1件は登録コメントが「`ChoiceOption` にコストの受け皿が無い」と書いていたが、**受け皿は枝の action の中にあった**（`STUB{OPTIONAL_COST}` → `CONDITIONAL{PAID_ADDITIONAL_COST}`）。
- 🔑**実装を書く前に、その STUB ハンドラを開く。** 「STUB があるのに JSON が指していない」型は、中身を読むと**較正だけで終わる**ことがある（`WXEX2-84-E1` は既に全ルリグを下へ置いていた）。
- 🔑**「同じ札を指す参照」は1種類ではない**＝**`lastProcessedCards` / `storedTargetCards` / ゾーンの位置** の3つを候補に持ってから枝を組む（`WX19-061-E1` は3つ目で解けた）。
- 🔑**新機構が多段対話を含むときは resume チェーンを golden に固定する**（`repeatWhilePossible` / `orderChosenBy` のような「1つ処理するたびに同じ問いへ戻る」形）。**UI 層は golden で守れないが、engine 側のチェーンだけでも固定すれば組み立ての破壊は必ず落ちる。**
- 🔑**着手前に「その語彙を誰が読むか」を先に確かめると選別が一気に速くなる**＝型に在っても**消費地点が `src/screens/` なら engine だけでは閉じられない**（触ると §2.2 で実機必須になる）。

### 5.3 機構 worklist（新しい型・評価器・engine が要るもの）★**2026-09-01 以降ここが唯一の本線キュー**

> 🆕🔴**2026-09-01 整理＝この節の形を「登録順の巨大テーブル」から「母集団順の索引」へ作り替えた。**
> **理由（実測）**＝3つの進捗計器が**同時に底を打った**＝**census 高シグナル 11 / BASELINE 12**（旧ベースライン 1872 → 460 → 245 → 91 → 11）／
> **`census:cards --sheet 1` の要対応 18枚は全部 `mech`＝即着手可能 0**／**意味照合台帳の残 OPEN 44 は続き766 の全数 triage で「engine/JSON だけで閉じられるのは0件」と確定**。
> ⇒ **「計器が指した在庫を消化する」時代は終わり、残っているのは機構（この節）と実機・UI（§5.1）だけ**になった。
> **旧構造の問題**＝登録票82行の第4列に経緯・実測・罠が全部入って **68,401字**あり、読めないので**別に「取る順」表**が要る、という二重管理になっていた
> （しかもその取る順表は 1位 `O-106` が続き768 に「実測1効果」へ降格・2位 `O-128` が事実上クローズ済み、と**すぐ腐った**）。
> ⇒ **索引（下の A〜F）と登録票を分離**し、登録票の全文は [PLAN_DETAIL.md](./PLAN_DETAIL.md) の「§5.3 機構 worklist 登録票の全文」へ**無改変で退避**した。

**この節の使い方**
1. 下の索引 **A → B → C** の順に、**母集団の大きいものから**取る（旧「取る順」表は廃止＝索引の並び順がそのまま取る順）。
2. ID を決めたら、**着手前に [PLAN_DETAIL.md](./PLAN_DETAIL.md) の同じ ID の登録票を読む**（経緯・実測・受け皿の在処・罠・「取り方」がそこにある）。
3. **②母集団を実測し直す**（§2.1 ②）。索引の数字は**登録・再計測時点の値**であって、いま正しいとは限らない。
4. **②' レーンを決める**（§2.0）＝索引 C（1〜2効果）は**速いレーン**が既定、A/B は遅いレーン。

**登録のルール（2026-08-30 に決めた既定・2026-09-01 に再確認）**
- **新規登録は「索引に1行」＋「PLAN_DETAIL に登録票1項目」**。索引に長い経緯を書かない（それが旧構造の破綻原因）。
- 🔑**登録時に母集団を実測して書く。** 実測していないなら **D（母集団未計測）**へ入れる＝**`S`/`M`/`L` は実装量の見立てであって母集団ではない**（この混同で「S・1効果」の項目に34枚・19枚・15枚の塊が隠れていた前例）。
- **消化したら索引の行ごと消す**（ID は再利用しない。全文は PLAN_DETAIL と BUGFIXES が正）。
- **実機で確かめるだけの項目はここに書かない**＝§5.1 へ `V-<次番号>` で足す（守らなかった結果、旧 `O-13` が8185字・30ブロックに肥大した前例がある）。

**⚠索引の「母集団」列の読み方（3回とも同じ罠で外したので必ず読む）**
> 1. **`*` 印＝`liveRe` 未確定**＝「受け皿が既にあるカード」を引けていない＝**過大に出る**。
> 2. **`*` が無くても `liveRe` が不完全なら過大**＝`O-159` が実例（個別実測は1効果、この表は20枚）。
> 3. **原文 regex は用法を区別しない**＝この改訂でも `O-92` 58→13／`O-104` 40→0／`O-158` 19→0 と縮んだ。
> 4. ⇒ 🔑**着手時は必ず「原文を1枚ずつ読んで用法で割り、live と engine の消費地点まで見る」**。
> 5. 🔴🔴**2026-08-30 の結論＝この測り方には構造的な限界がある（同日 9回連続で過大に外した）。**
>    「原文にフレーズがあるのに live にキーが無い」は、**受け皿の"別名"を全部知らないかぎり必ず過大に出る**。
>    実例＝`census:wiring` の最大セル（24件）は**実際は1件**で、36件は `colorNotMatchesLrig` が
>    「エナ対象では対象オーナー基準で解決する」ため**既に正しかった**。`O-106`（25枚）も大半が `hasNoAbilities` で実装済み。
>    🔑**含意＝「受け皿の名前を知っている必要がある」計器は、知らない穴を測れない。**
>    **在庫の見積もりに使えるのは §5.2 の意味照合台帳**（LLM が意味で比較するので受け皿名を知らなくてよい）。
>    ⇒ **この表と `census:wiring` は「着手前のふるい」として使い、"どれだけ残っているか"の答えには使わない。**
>    2026-08-30 の1巡では**投入前実測が6回中6回とも登録票の見立てを訂正した**（母集団は毎回縮んだ）。

**測り直し方**＝`node scripts/archive/censusMechPopulation.mjs`（全項目の regex を保持。`--id O-nn` で1件）。**原文 regex で拾う → live の受け皿 regex で絞る**の2段。原文 grep が意味を持たない項目（engine / 計器 / UI 経路）は同スクリプトが別枠で列挙する。

> 🆕🔴**2026-09-01 の補足＝上の 5. の「在庫の見積もりには §5.2 の意味照合台帳を使え」はもう使えない。**
> 台帳は**残 OPEN 44 まで消化されており、その44も続き766 の全数 triage で「全件が `src/screens/` か新 engine 機構待ち」**と確定した＝
> **もう "知らない穴" を教えてくれる計器は無い。** ⇒ **在庫の総量を測る手段は現在ない**ことを前提に、**索引の A から順に潰す**のが唯一の進め方。

#### 🆕 1〜3枚の項目の取り方（型を足す前に読む）

1. 🔑**まず受け皿を疑う。** 2026-08-29 の実測で **2件とも既に在った**＝`O-123`（支払いUI `useTimeCost.ts` が丸ごと実装済み）／`WX14-049`（`forResonaCondition`/`resonaClass` ＋ `targetsTriggerSource`）。**探し方**＝①原文の言い回しで `src/` を grep ②同じ言い回しで `scripts/goldenTest.ts` を grep（**実装済みなら golden が張ってあることが多い**）。
2. **アクション側は「型」ではなく `STUB` ハンドラ1本で書く。** 実測＝`execStubPart*.ts` に **923本**の実績・**1本あたり平均17行**。`census:stubs` の A群（engine に消費が無い STUB）からも消えるので計器も追随する。
3. 🔴**条件側には STUB の道が無い。** `COND_STUB` は `execUtils.ts:2413` が **`return true`＝無条件成立**（live で2件しか使われていないのはそのため）。条件は **型＋`CONDITION_TYPES`＋`evalCondition`＋`checkActiveCondition`＋golden＋parser の6箇所**を必ず揃える（§4.2）。
4. **1枚のために機構を作らないと決めてよい。** その場合は**無言 no-op にせず** id を `DEFERRED_*` へ改名して理由をこの節に書く（`census:stubs` A群🔴 の運用どおり）。

> 🆕**2026-09-01 続き768 に更新（続き766 で登録した6件のうち4件を消化）。**
> **全文と根拠は [BUGFIXES.md](./BUGFIXES.md) の 2026-09-01 の7件**（`O-190` 第1／`O-188` 第1〜第7）。
>
> | 効果 | 原文にあるのに live に無いもの | 実害の向き | 状態 |
> |---|---|---|---|
> | ~~`SPK01-12-E1`~~ | 「このターンに手札から捨てた」 | 過剰実行 | 🏁**消化（第5バッチ）**＝`parseDiscardedFromHandThisTurnFilter` を名詞句側へ新設 |
> | ~~`WXK02-028-E1`~~ | 「あなたのセンタールリグと同じレベルの」 | 過剰実行 | 🏁**消化（第5バッチ）**。🔴**この行の旧記述「受け皿は無い・新キーが要る・母集団6カード」は誤りだった**＝`TargetFilter.levelEqLrig` は型・`resolveDynamicFilter`・golden まで**実装済み**で、無かったのは**一般規則**（カード固有 `addFilter` 2箇所だけが使っていた）。**登録票の「受け皿が無い」は実コードで確かめるまで信じない。** |
> | ~~`WXDi-P04-022-E1`~~ | 「赤と黒をそれぞれ1枚まで」 | 過小実行 | 🏁**消化（第4バッチ）**＝`transferGroups` の一般規則。同型7効果を同時に修正 |
> | ~~`WXDi-P09-043-E2`~~ | 「このシグニを」の自己回収 | 過剰実行 | 🏁**消化（第5バッチ）**。🔴**実は parser は既に直っており `heldReview --adopt` 漏れだった**（負方向 golden が live しか見ていなかったので緑のままだった） |
> | ~~`WX24-P4-051-E2`~~ | コスト「エナから**それと同じレベルの**シグニ1枚」 | コストが安い＝過剰実行 | 🏁**消化（2026-09-01）**＝`energyTrashSameLevelAsTarget` が受け皿だった。`MANUAL` で手書き＋golden 2本（同レベルだけを払う／同レベルが無ければ pay 不可）。⚠**逆翻訳にレベル一致が出ない**（下の「監視だけしている項目」） |
> | ~~`WX24-P2-054-E2`~~ | 「相手シグニ3体まで／レベル合計1につき《緑》／それらをエナゾーンへ」 | **動作そのものが別物**（live は `ENERGY_CHARGE{DECK_CARD}`） | 🏁**消化（2026-09-01）**＝**新キー `costColorsPerTargetLevelSum`**（型＋`sumCardLevels`＋`resolveOptionalCostSpec`＋逆翻訳の4箇所）。負方向 golden を正方向3本へ差し替え |
>
> 🏁**この表は残0**（2026-09-01 に最後の2件を消化）。
> 🔑**その負方向 golden は live と fresh の両方を見ること**（続き768 で `WXDi-P09-043-E2` が
> 「live だけの負方向 assert」のせいで**直っているのに据置扱いのまま3セッション眠っていた**）。
> 🔴🔑**2026-09-01 に同じ穴の別の形を踏んだ**＝`manualEffects.ts` に書いただけで **`build:effects` を回さず live が旧出力のまま**だった
> （新しい golden が `manualEffect()` ヘルパで **MANUAL_EFFECTS を直接読む**ので緑になり、気づけなかった）。
> **捕まえたのは §6.3 K のトリップワイヤ**（「manualEffects.ts の定義が live JSON に届いている」）だけ。
> ⇒ **既存 id を書き直したら `npx tsx scripts/syncManualLive.ts <CardNum>` まで回して初めて1巡が閉じる**（収穫マージが温存するので `build:effects` では届かない）。

---

#### 索引 A. 母集団2桁（**ここから取る**・遅いレーン）
| ID | 母集団 | 何が無いか |
|---|---|---|
| `O-60` | 🔴**12ハンドラ / A🔴 13行**<br>（第68バッチ後も据置＝理由は右）<br>🆕2026-09-04 第68バッチ後 |  engine が原文（カード全文／アビリティブロック）を regex で読んで意味を決めている箇所<br>🏁🔑**2026-09-04（第64〜68）＝引用付与 catch-all の3綴り**（`GRANT_ABILITY_INNER_TEXT` / `GRANT_QUOTED_ABILITY` / `GRANT_QUOTED_AUTO_ABILITY`）**が live 0 になった**（第63の較正時点で27効果）。**残る11ハンドラも全部 live 0**。<br>🔴🔑**A群の行数は「engine のコード行」で数える**＝**live が 0 になっても行は1行も減らない**（実測＝live 27→0 で A🔴 13行のまま）。⚠**旧 `O-60` 登録票と PLAN §1 の「live 0 になれば2行減る」は誤りだった。**<br>🔑**次の一手＝ハンドラ撤去**＝①parser の catch-all 生成地点**5箇所**（`parseSentencePart2` の `」を得る` 末尾／`parseSentencePart4` 2箇所／`effectParser` の引用漏出安全網／`normalizeGrantKeywordSpelling`）を**名前のある穴か構造化**へ畳む →②`execStubPart1.ts:1459` の GRANT_QUOTED_* ハンドラと `effectEngine.collectGrantedFromLayer` の STUB 分岐を撤去 →③`BASELINE_SELF_TEXT` を 11 へ下げる。<br>⚠**先に生成元を畳む**＝いま消すと「parser に生成元がある安全網を消す」ことになり、新カードが無言 no-op へ落ちる（PLAN のルール違反）。<br>🔴🔑**計器の `miss` は「カード全文」に当てて数える**＝着手前に `abilityBlockTextOf` で当て直す（第58）<br>🔴🔑**計器に映らない第2の原文解析器がある**＝`choiceTextParser.ts`（残る呼び出し元は `O-234` の1本）<br>**第68バッチまで消化済み**（第50 -17行／第51 -8／第52 -12／第53 -10／第54 -7／第55 -3／第56 -2＋較正+16／第57 -1／第58 -4／第59 -6／第60 -5／第61 -4／第62 ±0＝live 15→8／第63 ±0＋較正 live 8→27＝27→15／第64 live 15→11／第65 11→8／第66 8→6／第67 6→4／**第68 4→0**）<br>🔑**直し方は4択**＝(a) payload へ移す (b)**同じ意味を決めている別の場所**があるなら撤去 (c)**構造化された等価物で判定し直す** (d)**既にある受け皿へ振り分ける**<br>🔴🆕**(e) 受け皿が無いなら「名前のある穴」へ**（第64〜68で8効果）＝`DEFERRED_*` にすると逆翻訳へ【未実装】が出る。**catch-all に残すのは「穴を宣言した」ことにならない**（engine の既知パターン表に当たらず無言 no-op）。<br>🔴**既定値のある regex は「当たらないこと」が可視化されない**（第58／第59）<br>🔑**payload 化したら逆翻訳の固定文言も撤去する**（第59・第60・第62・第64）<br>🔴🆕**engine では1本のハンドラでも parser 側の復元規則が綴りを1つしか見ていないことがある**（第65＝`restoreQuotedTargetGrant`）＝**どの綴りに落ちたかだけで構造化の有無が変わる**<br>🔴🆕**宣言型 STUB は CONTINUOUS として読まれて初めて意味を持つ**＝引用の中身が宣言型なら `GRANT_EFFECT` で包む（第64） |
| `O-197` | **約15効果** | 「それぞれ〜異なる」の対象節が `DISTINCT_BATCH5C` のオプトイン表に載っていないだけ（**機構は揃っている**）<br>⚠表へ足す前に `visit` が正しいノードへ付くか1件ずつ確かめる |
| `O-92` | **13枚** | 「このターン、〜したとき」の遅延設置（`INSTALL_DELAYED_TRIGGER` は既存）<br>2026-08-30 続き736 実測。⚠**2026-08-28 の仕分けで実害7効果は消化済み**＝残りは受け皿待ち |
| `O-147` | **13カード** | 複数体ライズ（2体以上／トラッシュ・エナから重ねる）が配置条件として表せない |
| `O-70` | **12枚** | 「下に置く」の複合対象＝2種の対象をそれぞれの上限で選ぶ形が表せない<br>⚠`liveRe` 未確定＝過大の可能性 |
| `O-188` | **11効果**<br>🔴要再計測 | `TRANSFER_TO_HAND` が `targetsStored` を持たない＝「対象を先に固定してから任意コストを払う」形に載せられない<br>⚠**登録時の値**＝続き766〜768 で**第1〜第7バッチ（計19効果）を消化済み**なので**残は未再計測**。着手前に必ず数え直す |
| `O-185` | **11効果**<br>🆕2026-09-01 再計測 | census 高シグナルの残り＝「絞り込み／条件／機構が丸ごと落ちている」テール<br>🔴**登録票の「144効果」は失効**＝`npm run census` 実測で **高シグナル欠落 11効果**（ベースライン12）。`O-217` はこの11効果と全域重複 |
| `O-152` | **34効果**<br>🆕2026-09-01 実測 | 🔴 `ON_HAND_DISCARDED` の watcher が「効果による手札捨て」で発火しない（配送のどこかで落ちている）<br>🔴**登録票の「1カード」は標本1枚を数えていただけ**＝live 実測で **34効果／33カード**。受け皿（`execTrash` の `hand_discarded_just`／`collectHandDiscardTriggers`）は在り、**壊れているのは配送**。⚠**再現は実機のみ**＝`node scripts/verifyBattleDrive.mjs o143CheckPlace`（`queryState` で `hand_discarded_just` が永続しているかを先に見る）
| `O-69` | **10枚** | 「この方法でカードが N枚以上トラッシュに置かれた場合」＝コスト支払いで実際に動いた枚数を読む条件型が無い<br>⚠`liveRe` 未確定＝過大の可能性 |

#### 索引 B. 母集団 3〜8効果

| ID | 母集団 | 何が無いか |
|---|---|---|
| `O-232` | **1効果** | 🆕**2026-09-03（`O-60` 第59バッチで発見）**＝「デッキを**シャッフル**し一番上のカードを**公開**し手札に加える」の**シャッフルと公開が live JSON に無い**（`STUB{DRAW, count:1}` に潰れている）。<br>`WXDi-P10-006-E3`「あなたのデッキをシャッフルし一番上のカードを公開し手札に加える。その後、あなたのシグニ１体を対象とし、**この方法で公開されたカードが【ライフバースト】を持つ場合**、…【アサシン】を得る。公開されたカードが【ライフバースト】を持たない場合…」<br>🔴後段の分岐が**公開したカードを参照する**のに、公開の記録が無いので**どちらの枝も判定できない**。<br>■**受け皿の見立て**＝`SHUFFLE_DECK` ＋ `REVEAL_AND_PICK`（`lastProcessedCards` に公開カードを残す）を並べ、後段を `LAST_PROCESSED_MATCHES{hasBurst}` 系の条件へ。⚠**条件型が要る＝遅いレーン**。 |
| `O-233` | **1効果** | 🆕**2026-09-03（`O-60` 第60バッチで分離）**＝「このターンに**対戦相手の効果によって**あなたのシグニが**場を離れていた**場合」の条件語彙が無い。<br>`SPK16-13E-E1`①「対戦相手のシグニ１体を対象とし、**このターンに対戦相手の効果によってあなたのシグニが場を離れていた場合**、それをバニッシュする。」<br>🔑**`ENERGY_TRASHED_BY_OPP` / `HAND_TRASHED_BY_OPP` の**シグニ版が欠けているだけ**（同じ「相手の効果で失った」系の3本目）。<br>■**要るもの**＝ターン内カウンタ（`turn_signi_left_by_opp_effect_count` 相当）＋条件型1本（型・`CONDITION_TYPES`・`evalCondition`・`checkActiveCondition`・golden・parser の6箇所）。⚠**条件側に STUB の道は無い**＝`COND_STUB` は無条件成立なので流用禁止。<br>■現状は `STUB{DEFERRED_BANISH_IF_SIGNI_LEFT_BY_OPP_EFFECT}`（明示 defer・逆翻訳に【未実装】と出る）。条件を落とすと**無条件バニッシュ**の過剰実行になるのでこの向きに倒してある。 |
| `O-234` | **2効果** | 🆕**2026-09-03（`O-60` 第61バッチで分離）**＝**`src/engine/choiceTextParser.ts`（492行・約30分岐）の最後の呼び出し元**が残っている。<br>🔑**この巡でこのファイルは「1本の継続ハンドラからしか呼ばれない」状態になった**＝`INTERNAL_ECRV_APPLY`（`EXTRA_COST_REMOVE_VIRUS` の継続・live 2効果＝`WX16-023`／`WX16-048`）。ここを移せば**engine の第2の原文解析器を丸ごと削除できる**。<br>原文＝「以下の3つ（4つ）から、**追加で取り除いた【ウィルス】の数に1を加えた数だけ**選ぶ」。<br>■**受け皿の見立て**＝`CHOOSE{countChoose:{count:{$ref:…}}}` は**在る**。足りないのは①`$ref`1本（取り除いた数＋1）②その数を**効果解決中に**運ぶ口。<br>🔴**`pre_use_virus_removed` は `src/screens/BattleScreen.tsx` が使用宣言時に書く**（＝スペル/アーツの使用コスト経路）ので、**`src/screens/` を触る＝遅いレーン＋実機必須**。<br>⚠**着手前に「使用時コストとして取り除く」経路と「効果の中で取り除く」経路のどちらなのかを1件ずつ確かめる**（`EXTRA_COST_REMOVE_VIRUS` は後者として実装されている）。 |
| `O-236` | **1効果** | 🆕**2026-09-04（`O-60` 第64バッチで分離）**＝**ルリグへの引用能力付与のうち3機構が無い**（`WXDi-D04-011-E1`＝ENDLESS-PUNCHLINE）。原文「あなたのレベル３のルリグ１体を対象とし、ターン終了時まで、それは『【常】：このルリグは**ダウン状態でもアタックでき**、１ターンにこのルリグが**アタックできる上限は３になる**。【自】《ターン１回》：…**アタックできる上限を減らす**。』を得る」。<br>■**要るもの**＝①ルリグの「ダウン状態でもアタックできる」（既存 `ATTACK_WHILE_DOWN` は**シグニ限定**）②**1ターンのアタック回数上限**そのもの ③その上限をターン中に増減する口。<br>🔴**旧 live は `STUB{GRANT_QUOTED_ABILITY}`**＝付与先が自場シグニに絞られるのにこのカードはピース＝毎回「能力付与：…（ログのみ）」の**無言 no-op** だった。いまは `DEFERRED_GRANT_QUOTED_LRIG_ATTACK_ABILITY`。<br>⚠**live が `PARTIAL` なので `manualEffects.ts` ＋ `syncManualLive` でしか届かない**（parser を直しても収穫マージが不可侵）。 |
| `O-237` | **1効果** | 🆕**2026-09-04（`O-60` 第65バッチで分離）**＝**空きシグニゾーンへの自己移動**（`WXK03-042-E1`「あなたがアーツを使用したとき、このシグニをあなたの他のシグニゾーン１つに配置してもよい」）。<br>⚠`REARRANGE_SIGNI{swap}` は候補を「**シグニが居るゾーン**」に限る（`execRearrangeSigni` の `candidates`）ので**空きゾーンへは動かせない**。次文「そのシグニゾーンにシグニがある場合、…場所を入れ替える」＝占有時だけは既存で表せている（過小のまま）。<br>🔴旧 id は `GRANT_QUOTED_AUTO_ABILITY`＝**引用付与ではない**（id の名前が嘘）。いまは `DEFERRED_MOVE_SELF_TO_OTHER_SIGNI_ZONE`。 |
| `O-238` | **1効果** | 🆕**2026-09-04（`O-60` 第66バッチで分離）**＝**アタックの置換**（`WXDi-P05-069-E2`「あなたの《翠将姫　ロビンフッド》は「【常】：このシグニがアタックする場合、代わりにあなたのシグニを２体まで裏向きにしてアタックし、このターン終了時、これによって裏向きにしたシグニを、同じ場所にシグニがない場合、表向きにする。」を得る」）。<br>■**要るもの**＝①アタック宣言の置換フック ②シグニを**裏向きにしたままアタックする**状態 ③ターン終了時の復帰判定。<br>🔑**付与そのものは解けるようになった**＝主語が《カード名》の場全体付与を `GRANT_FIELD_SIGNI_ABILITY{filter:{cardName}}` で出す規則を第66で足した（引用が表せる日にはそこへ載る）。いまは `DEFERRED_GRANT_QUOTED_ATTACK_REPLACEMENT`。 |
| `O-239` | **1効果** | 🆕**2026-09-04（`O-60` 第67バッチで分離）**＝**そのターンのクラッシュ順**を軸にした【ライフバースト】付与（`WXDi-P12-036-E1`「このターン、１枚目と２枚目にあなたのチェックゾーンに置かれたライフクロスは【ライフバースト】「…」を得る」）。<br>⚠**近い受け皿は在る**（`GRANT_ALL_ZONE_LIFEBURST` / `SET_DISPAIR_BURST_GRANT`）が、あちらは「全領域の**【ライフバースト】を持たない**カード」が対象で、この軸を持てない＝流用すると**ライフ全部が別バーストを持つ**過大実行。<br>⚠消費側は `src/screens/battle/allZoneBurst.ts`＝**UI 層＝遅いレーン＋実機必須**。 |
| `O-240` | **1効果** | 🆕**2026-09-04（`O-60` 第68バッチで分離）**＝**全領域への《トラップアイコン》付与**（`WXEX2-66-E1`「あなたのすべての領域にある《トラップアイコン》を持たない＜トリック＞のカードは《トラップアイコン》「カードを１枚引くか【エナチャージ１】をする。」を得る」）。<br>⚠`GRANT_ALL_ZONE_LIFEBURST` の**トラップ版が無い**（【ライフバースト】側だけ実装されている）。消費側は `src/screens/battle/allZoneBurst.ts` と同じ層＝**実機必須**。 |
| `O-241` | **1効果** | 🆕**2026-09-04（`O-60` 第68バッチで分離）**＝**自分自身の効果だけ**を除外するアタック無効化耐性（`WXDi-P05-068-E1`「【常】：このシグニのアタックは**このシグニの効果によって**無効にされない」）。<br>⚠耐性語彙（`GrantProtectionAction.from`）に「**アタック無効化**」軸も「**発生源＝効果元自身**」も無い。<br>🔴旧は `normalizeGrantKeywordSpelling` が「文が丸ごと `keyword` に入った」形を catch-all へ落としていた（＝`hasKeyword` に一度も当たらない無言 no-op）。いまは `DEFERRED_ATTACK_NOT_NEGATED_BY_SELF_EFFECT`。 |
| `O-242` | **1効果** | 🆕**2026-09-04（`O-60` 第68バッチで分離）**＝「このゲームの間、あなたは以下の能力を得る」の**引用の中身が表せない**形（`WXDi-P03-002-E1`＝「あなたのルリグがグロウしたとき、それが**そのターンであなたの最初のグロウである場合**、【エナチャージ１】をする」）。<br>⚠**条件語彙が無い**＝載せると**グロウのたびにエナチャージ**する過大実行（第62で実測して差し戻した件）。<br>🔑**`gameGrants` が `abilityBlockHeader` だけ＝見出ししか取れていない**ので、宣言側ごと`DEFERRED_GAIN_ABILITY_THIS_GAME_QUOTED` にした（残すと逆翻訳が「このゲームの間、あなたは以下の能力を得る。」で終わり**実装済みに見える**）。 |
| `O-230` | **1効果** | 🆕**2026-09-03（`O-60` 第58バッチで分離）**＝**【ガード】の代替コストとしての「コラボする」**の受け皿が無い。<br>`WXDi-CP01-005-E1`「【常】：あなたが【ガード】する際、《ガードアイコン》を持つカードを１枚捨てる代わりに《無》を支払い**コラボライバー１人とコラボしてもよい**。」<br>🔴旧実装は `STUB{COLLAB}` の「コラボしてもよい」枝へ落ちて**原文と無関係にアシストルリグを場へ出す対話**が開いていた（いまは `DEFERRED_GUARD_ALT_COST_COLLAB`）。<br>■**受け皿の見立て**＝実行部 `INTERNAL_DO_COLLAB`（アシスト1人配置）は**在る**。無いのは**ガード宣言時に代替コストを提示する経路**＝`src/screens/` のガード UI を触る＝**遅いレーン＋実機必須**。 |
| `O-231` | **1効果** | 🆕**2026-09-03（`O-60` 第58バッチで発見）**＝「〈条件〉のシグニ1枚を**場に出し**、〈…〉を**そのシグニの下に置く**」の**前半（場に出す）が live JSON に無い**。<br>`WDK15-007-E1`「あなたのトラッシュから対象の＜ウェポン＞のシグニ１枚を**場に出し**、あなたのトラッシュから対象のシグニを２枚まで**そのシグニの下に置く**。」<br>🔴live は `SEQUENCE[ARTS_COST_REDUCTION_BY_EFFECT, TRASH_SIGNI_UNDER_FIELD_SIGNI]` ＝**召喚が丸ごと落ちている**（過小実行）。<br>⚠**後半の `destLastPlayed` も engine が絞れない**＝「直前に場に出したシグニ」を engine が保持していないので、いまは**全ゾーンを候補**にしている（過小に倒さない選択）。<br>■**取り方**＝①召喚を `ADD_TO_FIELD{source:TRASH_CARD, filter}` として出す ②`lastPlacedCards` 相当を ctx に足して `destLastPlayed` を効かせる。 |
| `O-223` | **1効果** | 【シード】を対象に取る宣言の受け皿が無い（🏁`O-220` 第8バッチで分割・2026-09-02）<br>`WXK05-050-E2`「あなたの【シード】１枚を**対象とし**、《白》を支払って**もよい**。**そうした場合、それを**開花し〜」。<br>🔴`STUB{SEED_FLOWER_OP}` は**支払いのあとで**開花するシードを CHOOSE させる＝(a) シードが0枚でも先に支払いを提示する (b) 宣言と実行の順序が原文と逆。<br>⇒ `SELECT_TARGET_ONLY` に**シードの `TargetScope`** が無いのが根（シードは `field.signi_seeds[zi]`）。**UI スコープを1つ足す＝遅いレーン**。 |

> 🏁**2026-09-02（索引 B 第2巡）に旧 9件を全消化して索引 B をいったん空にした**＝
> `O-71` / `O-68` / `O-137` / `O-138` / `O-163` / `O-78` / `O-104` / `O-118` / `O-160`。
> 全文は [BUGFIXES.md](./BUGFIXES.md) の 2026-09-02（索引B 第2巡）。
> 🔑🔴**この巡の教訓（他の索引でもそのまま効く）**
> ① **索引の母集団列は登録当時の標本**＝9件を数え直したら **29効果 → 実測20効果**
>    （`O-137` は 4→**0**＝実測だけでクローズ／`O-78` 3→1／`O-71` 5→2／`O-138` 4→2／`O-104` 3→2）。**3巡連続で当たった。**
> ② **受け皿を疑う**が4件で当たった（`O-71` の3点組／`O-78` の `SIGNI_DEPLOY_BAN{namesFromTargets}`／
>    `O-138` の `CHECK_ZONE_COUNT`／`O-68`② の「クラフト除外は `Type==='アーツ'` の完全一致が既に弾いている」）。
>    ⚠`O-68`② は **`excludeCraft` キーを足すのが「存在しない仕事」**だった＝実データの `Type` を見るまで分からない。
> 🔴③ **「条件を足すだけ」は逆側（無言 no-op）の事故になる**（`O-138`）＝
>    条件は足せても**その条件が真になる盤面が実装上どこにも無かった**（解決待ちのスペルは `pending_spell` が
>    持っていて `field` のどこにも属さない）。⇒ **条件型を足す前に「真になる盤面が実在するか」を確かめる。**
> 🔴④ **対象が確定しているのに選択UIを開くと、遅延トリガーでターン終了時に盤面ごと止まる**（`O-71`・実機で発見）。
>    **golden の autopilot は勝手に選ぶので出ない型**＝`ok(result.done)` を書くまで見えない。
> 🔴⑤ **偽ゲート**（`O-104`）＝「そうした場合」が `CONDITIONAL{IS_MY_TURN}`（常に真）で、
>    中間動作を1つも払えなくても本体が通っていた。`LAST_PROCESSED_COUNT_GTE` が正しい受け皿。
> 🔴⑥ **JSON に分岐が「素のステップ列」として並ぶ過剰実行**（`O-163`・`WX16-Re17-E1`）＝
>    3分岐が毎回まとめて起きていた。engine のカード全文 regex（`census:enginetext` A群）の典型的な壊れ方。

#### 🏁 索引 C. 母集団 1〜2効果 ＝**2026-09-02 に全件クローズ（ここから取らない）**

> ⚠**「1枚だから型を足す」の前に、上の「1〜3枚の項目の取り方」4項を必ず読む**（受け皿は既に在ることが多い／アクション側は `STUB` ハンドラ1本／**条件側に STUB の道は無い**）。

> 🏁**2026-09-01（続き771）に12件クローズ**＝`O-145` / `O-146` / `O-153` / `O-154` / `O-170` / `O-189` / `O-208` / `O-212` / `O-214`（実装9件）
> ＋ `O-171` / `O-207` / `O-215`（**登録票が stale ＝先行セッションで既に消化済みだった3件**）。全文は
> [BUGFIXES.md](./BUGFIXES.md) の 2026-09-01（続き771）。
> 🔑**この巡で効いた教訓＝「1〜3枚の項目の取り方」の第1項（まず受け皿を疑う）が 12件中 7件で当たった。**
> `O-170`（`powerDiffersFromPrinted`）／`O-207`（`gateZoneOnly`）／`O-212`（`SelectionConstraint`）／
> `O-215`（`hasSoul` / `frontOfAllyWithSoul`）／`O-171`（`ZONE_SUM_COUNT`）は**受け皿が既にあり**、
> 欠けていたのは**配線か、そもそも既に消化済みという事実**だった。⇒ **着手前に必ず実コードで確かめる。**
> 🔴**逆に「規模＝1効果」の登録が母集団を外していた例も出た**＝`O-170` は登録票 1効果に対し**原文12カード**
> （うち未配線3効果）、`O-153` は 2効果に対し**多段 `LOOK_PICK_CHAIN` 16効果のうち実害3効果**。
> ⇒ **索引 C でも②「数える」を飛ばさない。**

> 🏁**2026-09-01（索引 C 第2巡）に6件クローズ＝`O-168` / `O-169` / `O-172` / `O-174` / `O-176` / `O-204`。**
> 🔴🔑**6件とも「1バイトも実装していない」＝受け皿も engine の消費地点も全部すでに在った。**
> 実装ではなく **golden を張って閉じた**（`索引C 2026-09-01: …` 2本＋`段2 Sheet3① 契約` の反転1本）。
> 🔴🔑**この巡の最大の発見＝golden の「見送り契約」が項目を眠らせていた。**
> `段2 Sheet3① 見送り契約` は **`sheet3b1Fresh`（＝parser 出力）だけ**を見て「まだ表せない」を assert していたが、
> `O-168`（`extraZones`）と `O-169`（`banishedHadCharm`）は **`manualEffects.ts` 側で先に実装され live には届いていた**
> ＝**契約が緑のまま、直っている項目が「据置」として索引に残り続けていた。**
> ⇒ 続き768 の `WXDi-P09-043-E2`（「live だけの負方向 assert」）の**鏡像**。**負方向契約は live と fresh の両方を見る。**

> 🏁**2026-09-01（索引 C 第3巡）にさらに6件クローズ＝`O-101` / `O-110` / `O-158` / `O-165` / `O-173` / `O-182`。**
> **6件とも実装済み**（第2巡と同じ＝1バイトも実装していない）。**うち5件は既存 golden が挙動まで固定済みだった**
> （`O-101`＝`B12 「そうしない場合」は else 側…`／`O-158`＝`続き760 ATTACH_ACCE.repeatWhilePossible`／
> `O-165`＝続き759／`O-173`＝`levelMultisetFromLastProcessed`／`O-182`＝`WX24-P4-040-E2`）＝**登録票を消し忘れていただけ。**
> 新しく張ったのは `O-110` の1本だけ（`索引C 2026-09-01(2巡目): O-110 …`）。
> 🔑**受け皿を実装したときに索引の行を消す運用が抜けている**＝2巡で12件が「直っているのに worklist に残っていた」。
> ⇒ **実装した巡で §5.3 の行を消す**（BUGFIXES だけ書いて索引を放置しない）。
> 🔴🔑**逆に母集団が桁で増えた項目も出た＝`O-152` を索引 A へ移した。**
> 登録票の「1カード」は**分離のきっかけになった標本1枚**で、live 実測は **34効果／33カード**。
> ⇒ **索引 C の「1カード」表記は母集団ではないことがある。**「1効果」と「1カード」を読み分ける。

> 🏁**2026-09-01（索引 C 第4巡）にさらに6件クローズ＝`O-95` / `O-102` / `O-136` / `O-139` / `O-159` / `O-178`。**
> **3巡で計18件**が「実装済みなのに索引行が残っていた」＝**索引 C の残りの3割強がこれだった。**
> `O-159` にいたっては **golden のテスト名が `O-159: …` そのもの**（2026-08-30 に実装＋両方向固定済み）で、行だけが残っていた。
> 🔑**受け皿の名前が登録票の提案と違う**のが見落としの主因＝`O-101`→`TRASH_HAS_CARD` ／ `O-139`→`THIS_CARD_PLACED_BY_CLASS` ／
> `O-178`→`SIGNI_LEFT_FIELD_THIS_ATTACK_PHASE` ／ `O-95`/`O-102`→`colorNotMatchesSource`。
> ⇒ **§4.2 のとおり「提案キー名で grep して無いと言わない」。原文の言い回しと golden のテスト名でも引く。**
> 🔴**部分完了を「未着手」と書いていた例も1件**＝`O-164` は**1回消費の器は完成済み**で、残るのは**効果バニッシュ経路**だけ。
> ⇒ **登録票の見出しが残作業を表していないことがある。着手前に golden のテスト名を grep する。**

> 🏁**2026-09-01（索引 C 第5巡）に3件クローズ＝`O-106` / `O-109`（stale）＋ `O-183`（**この巡で唯一の実装**）。**
> **stale の掃除はここで枯れた**＝4巡で **53 → 31項目**（21件クローズ／実装は `O-183` の1件だけ）。
> 🔑**`O-183` は「向き」を取り違えると受け皿があるように見える項目だった。**
> `allColorSigniNums` は `fieldCandidates`（＝**候補側**）には以前から渡っていたが、
> `resolveDynamicFilter` の**基準側（効果元）**は `cardMap.get(source).Color`＝**印字色しか読んでいなかった**。
> ⇒ `WXK05-029` は E1（`STUB{ALL_COLOR}`）で全色を得ても E2 の対象が広がらない過小実行だった。
> ⚠**対で直す**＝`colorMatchesSourceCard`（executor）と `colorNotMatchesSource`（executor＋`execUtils.evalCondition`）を
> 片方だけ直すと、同じ盤面で「共通色を持つ」と「持たない」が同時に成立する。
> 🔴**作業中に別のバグを1件見つけて直した**＝`colorExclude` に**文字列**を渡していたので `[「白赤青緑黒」]` の1要素配列になり、
> `card.Color.includes(...)` が常に false ＝**1色も除外されていなかった**（`execUtils` と `effectExecutor` の2箇所）。
> **複数色の効果元でも同じ穴**だったので、単色ソースで偶然当たっていただけ。

> 🏁**2026-09-02（索引 C 第6巡）に `O-105` をクローズ＝2効果を実働化した（実装2件目）。**
> **新設**＝`Condition.FIELD_ATTACHED_COUNT.filter`（**どのシグニの分を数えるか**＝ホスト側の絞り）。
> ⚠**数える対象（下のカード）ではなく、下に敷いている親シグニにかかる絞り**。
> 🔴**登録票の「部分採用は禁止」は正しかった**＝`WXDi-P16-056-E1` は**3つ同時に壊れていた**。
> ①対象が `owner:self`＋`targetsTriggerSource`（アタックフェイズ開始時にトリガー元は無い）
> ②「代わりに」が畳めておらず **-5000 と -8000 が両方走る** ③＜解放派＞の条件が丸ごと無い。
> ⇒ **1つでも残すと過小から過剰へ裏返る**ので、3つ同時に直して初めて原文になる。
> 🔴**2件目 `WXDi-P15-007-E2` は `COND_STUB` だった**＝`execUtils.ts` の `COND_STUB` は **`return true`（無条件成立）**なので、
> 印刷された【使用条件】が丸ごと消えて**いつでも撃てる**過剰実行だった。⚠**`COND_STUB` は「未実装」ではなく「無条件成立」**＝過剰側の穴。
> ⚠**`build:effects` では live に届かなかった**（収穫マージが既存 id を温存する）＝`npx tsx scripts/syncManualLive.ts` まで回して1巡が閉じた。

> 🏁**2026-09-02（索引 C 第7巡）に3件＝`O-211` / `O-148` を実装、`O-179` は stale でクローズ。**
> **`O-211`**＝遅延トリガーの発火源を**カード個体**で縛る（`trigger.attackerFixedFromStored` を新設し、
> `execInstallDelayedTrigger` が `storedTargetCards` を `attackerFixedCardNums` へ焼き込む）。
> 🔴旧は `attackerOwner` だけ＝**対象に取っていない相手シグニのアタックでも発火**し、`once:true` を食い潰していた。
> **`O-148`**＝【みこみこ親衛隊】を**専用のプレイヤーカウンタ**にした（3枚4効果・登録票の「1効果」は過小）。
> 🔴旧は2方向に破損＝①得る側が `GRANT_KEYWORD`＝engine に消費が無い真 no-op ②取り除く側が
> `STUB{REMOVE_VIRUS}` の**誤流用**＝【ウィルス】は `field.signi_virus`（シグニゾーン単位）なので
> **相手のウィルス state を壊しながら**動いていた。
> 🔑**着手前の実測が3件とも登録票を訂正した**＝`O-148` 1効果→**3枚4効果**／`O-186` 未計測→**2枚**／`O-179` 真→**stale**。
> 🔴**この巡で自分の編集ミスを1件出した**＝重複キー解消のスクリプトが範囲を取りすぎ、
> 無関係の `WXDi-P16-069` の manual 定義を巻き込んで削除していた（HEAD から復元）。
> ⇒ **`manualEffects.ts` を機械編集したら「HEAD とのキー集合差分」を必ず取る**（失われたキー／追加したキーの2方向）。
> ⚠**`O-74` は engine だけでは閉じられないと判明**＝`canSelfPlay` の呼び出しは `BattleScreen` の通常召喚1箇所だけで、
> `ctx.effectsMap` は一部経路でしか代入されない（型のコメントが「dead flag になる」と明記）。**実機必須の側へ回す。**





> 🔴**逆向きの外し方も1件出た**＝`O-183` は「受け皿がある」と読みかけたが、`allColorSigniNums` が繋がっているのは**候補側**で、
> 落ちているのは**基準側**（`colorMatchesSourceCard` が印字色しか読まない）＝**別物だった。着手前に「どちら側か」を必ず読む。**


> 🏁**2026-09-02（索引 C 第8巡）に5件クローズ＝`O-162` / `O-199` / `O-200` / `O-201` / `O-202`。**
> 🔑**この巡の主題＝「登録票の『受け皿が無い』が5件中4件で失効していた」。新設した型はゼロ。**
> ①`O-162`＝**既存 `CHOOSE{upTo}`** で足りた（選べるプレイヤーは2人しかないので「選ばれた側を運ぶ口」は不要）。
> ②`O-200`＝**`PLACE_KEY_FROM_LRIG_DECK` は既にあった**（`WDK03-001-E1` 用に続き760 で新設済み）。
> ③`O-201`＝**支払いUI（`OptionalCostSpec`）も窓も既存**。欠けていたのは `optionalOnPlayCostStub` の `SUPPORTED` 3語。
> ④`O-202`＝**窓は2本とも既存**（ダメージ置換＝`lifeCrashReplace.ts` の `pay_cost`／離場置換＝`selfAbilityPay` の軸列）。
> 欠けていたのは**支払い種別**（アシストルリグのダウン／宣言者自身のダウン）だけ。
> ⑤`O-199` だけが本当に新規（`parseEncoreCost` のテキスト形）。**しかも母集団は登録票の2効果ではなく5枚だった。**
> 🔴🔑**作業中に別のバグを1件見つけて直した＝「キーが1枚も場に出せない」**（`BattleScreen` のキー使用ゲートが
> **CSV の空欄 `'-'` を `!timing` で判定していた**＝`'-'` は truthy なので **全80枚のキーが永久に使用不可**だった）。
> ⇒ **CSV の空欄は空文字ではなく `'-'`**。`Story` 列も同じ（`TargetFilter.story` が読むのは `CardClass` の「：」より後ろ）。
> 🔴**census 高シグナル 12→6 のうち -4 は MANUAL 免除＝較正**（前進は `WXDi-CP02-100-E1` の1件だけ）。
> **`O-199` の2枚は高シグナルに残した**＝アンコールのコストは `screens/battle/costs.ts` が読むので JSON には載らない。
> 全文は [BUGFIXES.md](./BUGFIXES.md) の 2026-09-02。

> 🏁**2026-09-02（索引 C 第9巡）に15件クローズ＝`O-84` / `O-114` / `O-180` / `O-151` / `O-164` / `O-167` /
> `O-177` / `O-186` / `O-203` / `O-205` / `O-206` / `O-209` / `O-210` / `O-213` / `O-72`。**
> 🔑**この巡の主題＝「登録票の『受け皿が無い』は 15件中 5件で失効していた」**（`O-209` の `GAIN_BOND{declared}` ／
> `O-213` の `LRIG_TRASH_COUNT{filter}` ＋ parser 規則 ／ `O-164` の効果経路 ／ `O-205` は丸ごと stale ／
> `O-151` の `SELECT_TARGET_ONLY`＋`STORE_LAST_PROCESSED_TARGETS`＋`targetsStored`）。
> **新しく足した型は5組だけ**＝`EffectCost.bounceSelf`（`O-167`）／`StubAction.extraUseTiming`＋`ActiveCondition.LIFE_COUNT`（`O-84`）／
> `GrowCostReductionAction.nextAssistGrowOnly`＋`ignoreLrigType`（`O-180`）／`BanishRedirectAction.byEffectOnly`＋`consumeOnce`（`O-210`）／
> `EffectDuration.UNTIL_NEXT_OWN_TURN_END`（`O-186`）。ほかに `StubAction` を2つ（`EXTRA_USE_TIMING`／`TREAT_SELF_AS_RESONA`）。
> 🔴🔑**この巡の最大の発見＝「実装済み」と書かれた項目が2件とも経路単位で穴だった。**
> ①`O-210`＝`bySource` は **`banish_redirect_by_source_nums`（バトル経路だけが読む配列）**に載るので、
>   原文が「このシグニの**効果**によって」と書く `WX24-P4-050-E2` は**効果バニッシュで一度も置換されず、
>   逆にバトルでだけ置換されていた**（向きが真逆）。
> ②`O-164`＝効果バニッシュの盾は `applyDirectAction` 側にしか無く、**`execBanish` の選択経路（本線）は素通り**
>   していた＝「対象を選んで撃つと防げないのに、それ経由なら防げる」無言のズレ。
> ⇒ **完了報告は「型があるか」ではなく「入口が何本あるか」で確かめる。**（`applyBanishPreventShield` /
>   `consumeBanishRedirectOnce` / `applySplitTotalToTargets` / `trashExileCostSatisfied` の4本を funnel 化した。）
> 🔴**実機が机上では出ない嘘を2件捕まえた**＝①`TrashActivatedModal` の実行ボタンが
> 「発動する（トラッシュから場に出す）」**固定**で、自己回収／エナゾーン起動では嘘のラベルだった
> （アクション出し側だけ直しても片肺）②`WXDi-P13-043` は**シグニではなくアシストルリグ**＝
> 【出】はアシストグロウで発火する（机上のシナリオが前提から外れていた）。
> 🖥**実機10本すべて PASS**（反転確認5本＝`o114EnergyActivatedGated` / `o114TrashSelfNoCharm` /
> `o206TrashExileSameName` / `o84ExtraTimingHighLife` ＋ `o186` の寿命カウント3）。
> 全文は [BUGFIXES.md](./BUGFIXES.md) の 2026-09-02（第9巡）。

> 🏁**2026-09-02（索引 C 第10巡）に残り7件をすべてクローズ＝`O-74` / `O-79` / `O-83` / `O-94` / `O-103` / `O-130` / `O-150`。**
> **⇒ 索引 C は空になった。次に取るのは索引 A（母集団2桁）。**
> 🔑**この巡の主題＝「1効果の項目でも受け皿は engine の funnel 側に既にあった」**＝
> `O-74`/`O-79` は `deployLimit.ts` の `deployLimitBlockReason`（呼び出し元10箇所の既存 funnel）へ1本足しただけ、
> `O-130` の「そのシグニ」は**新機構ではなく既存の `triggeringCardNum` → `targetsTriggerSource`**、
> `O-83` の条件は既存の `LRIG_LEVEL_CMP_OPP{lt}`・グロウ予約は既存の `pending_flip_grow_card` と同じ形、
> `O-103` は**丸ごと stale**（`manualEffects.ts` が3択 CHOOSE で既に表していた）。
> 🔴🔑**この巡の最大の発見＝「実装済みの機構が pending／funnel の境界でフラグを落としていた」型が3件。**
> ①**`O-150`**＝`LookAndReorderAction.reorder` が **pending へ運ばれていなかった**。UI は ↑↓ を常時描き、
>   `resumeLookAndReorder` はクライアントの並びを**無条件で信じて**いた＝**live 151効果のうち 105効果（`reorder:false`）
>   が全部「並べ替え可能」だった**。`O-144` で 41効果へフラグを届けても実機が変わらなかったのはこれが理由
>   （分岐すべきは `remainder.reorder` ではなくこの1本）。**engine 側も権威にした**（UI だけ直すと片肺）。
> ②**`O-94`②**＝ゾーン配置制限の判定が funnel の**外**（`collectCenterZoneDeployRestrict`）にあり、
>   呼び出しは通常召喚UIの1箇所だけ＝**CPU 配置も効果配置も素通り**。しかも**レベル3とゾーン1がハードコード**だった。
> ③**`O-130`**＝`collectOppArtsUseTriggers` が「効果を受けたシグニ」を**特定はしていたのに entry へ載せていなかった**。
> 🔴**作業中に別のバグを2件見つけて直した**＝
> ①**`WXDi-P11-TK01` が STUB を誤流用**＝原文は「対戦相手はシグニを**2体まで**しか場に出せない」（体数制限）なのに
>   `STUB{OPP_ZONE_PLACEMENT_RESTRICT}`（中央ゾーンのレベル制限）で書かれており、**体数制限は1件も効かず
>   代わりに中央ゾーンだけ封じていた**。正しい形は parser が既に出していたので manual 定義ごと撤去した（§6.4 `O-42`）。
> ②**「効果によって得ている能力を失う」が印刷能力ごと消していた**（`WXK11-019-E2`／`SPK01-13`⑤ の2効果）＝
>   `abilities_removed` は全能力喪失なので**原文が触れていない印刷【常】【自】【起】まで消える過剰実行**だった。
>   `RemoveAbilitiesAction.grantedOnly` ＋ `PlayerState.granted_abilities_removed` を新設し、
>   **付与ストアの読みを `grantedStore.grantedEffectsOf` 1本へ funnel 化**した（散在していた読み口10箇所）。
> 🔴**「ルール注記」に見えて実効ルールだったものが1件**＝`SP38-001` の「この方法でグロウしたルリグの【出】能力は
>   発動しない」は `STUB{RULE_REMINDER_TEXT}`＝**完全な no-op** だった。
> 🖥**実機6シナリオ／28アサートすべて PASS**（反転確認5本）。全文は [BUGFIXES.md](./BUGFIXES.md) の 2026-09-02（第10巡）。

#### 索引 G. 新規分離（母集団 1〜2効果・**索引 A を消化したあとで取る**）

| ID | 母集団 | 何が無いか |
|---|---|---|
| `O-219` | **7効果**<br>🆕2026-09-02 実測 | 🔴**グロウ先カード自身が持つグロウコスト修正が1件も読まれない（恒久 no-op）**＝原文「**この**カードにグロウするためのコストは〜」。`collectGrowCostReductions` は**自分の場のシグニ＋センタールリグ**しか走査せず、**いまグロウしようとしている先のカード（ルリグデッキの中）**は候補に入らない。`GROW_COST_ZERO` / `GROW_FREE` も同じ（`freeGrowFilter` は別効果が立てる）。<br>**母集団＝7カード7効果**（`WX14-009` `WD13-002` `WD13-003` `WD14-001` ＝`GROW_COST_REDUCTION`／`WX21-017` `WX21-018` ＝`STUB{GROW_COST_ZERO}`／`WX13-001` ＝`GROW_FREE`）。<br>🔑**受け皿は在る**＝直すのは**収集の軸**（`collectGrowCostReductions` に「グロウ先カード」を渡す口を足す）。⚠**場に居るときに読んではいけない**（既にセンターなら原文の意味が消えており、そのまま拾うと**次のグロウが不当に安くなる**）。<br>⚠**4効果は条件つき**＝`WD13-002`/`WD13-003` は「グロウする際に手札から公開した場合」・`WX21-017`/`WX21-018` は「手札から捨ててもよい」＝**グロウ時の支払い／公開 UI** が要る（`WD13-002` はいま**白×1と黒×1を無条件で両方**持っており、収集軸だけ直すと**過小コストになる**＝条件を先に付ける）。<br>▶**2026-09-02（`O-195` 再計測）で分離**。全文は PLAN_DETAIL.md |
| `O-224` | **1効果** | 🆕**2026-09-03（`O-60` 第26バッチの実装中に発見）**＝「**レベルがこの方法でダウンしたシグニの数以下の**対戦相手のシグニ１体」（`SPDi43-23-E1`）の**レベル条件が丸ごと落ちている**。live は `BOUNCE{target:{SIGNI, opponent, count:1, filter:{cardType:'シグニ'}}}`＝**どのシグニでも手札に戻せる過剰実行**。🔑**前段は直った**＝`DOWN_UP_SIGNI_AND_CHOOSE` は第26バッチで payload 化し、ダウンした分を `lastProcessedCards` に残すようになった（＝**数を運ぶ足場はできている**）。■**要るもの**＝`TargetFilter` に「レベル ≤ 直前に処理した枚数」の動的しきい値（`levelLteLastProcessedCount` 等）＝`O-80` 族（動的カウントを filter へ運ぶ）と同じ設計問題。⚠**着手前に母集団を実測する**（「この方法で〜した数以下」を含む効果の全数）。 |
| `O-226` | **3効果 / 2カード** | 🆕**2026-09-03（`O-60` 第49バッチの実装中に発見）**＝**`GAIN_ABILITY_THIS_GAME` が書く state キーのうち3本に読み手が1人もいない**（真no-op）。🔑**payload 化で初めて見えた**＝旧実装は engine がカード全文 regex で state を立てていたので「ハンドラがある＝実装済み」に見えていた（`census:stubs` A群にも出ない）。■**内訳**＝(a)`game_declared_signi_level_zero` ＋ (b)`game_declared_signi_ignore_restriction`（`WXK09-001-E3`「無色ではないシグニのカード名１つを宣言する。このゲームの間、あなたのメインデッキと手札と場にある**宣言したシグニの基本レベルは０になり**、あなたは**宣言したシグニを限定条件を無視して場に出せる**」）＝**setter だけで getter が0**（`src/types/index.ts` の宣言 ＋ `execStubPart1.ts` の代入のみ）／(c)`lrig_activation_count`（`WXK03-003A-E2`「この【起】を使用したのが５回目である場合、**このルリグを裏返す**」）＝**カウンタは正しく進むが裏返しが未実装**（ログのみ）。🔑**受け皿の見立て**＝(a) は `execUtils.ts:1592` の `deck_signi_level_override` と**同じ読み出し関数の家系**（あちらは §6.4 `O-34(c)` で getter を足して生き返った前例がある）＝**カード名軸に一般化するだけ**。(b) は `deployLimit.ts` の限定条件チェックに例外を1本。(c) は `card_identity_overrides`（`WXDi-P11-010A` の B面反転で実績あり）を使えば `A→B` の裏返しは書ける。⚠**着手前に母集団を実測する**（「宣言したシグニ」「このルリグを裏返す」を含む効果の全数＝**この3効果だけとは限らない**）。 |
| `O-229` | **2効果 / 2カード** | 🆕**2026-09-03（`O-60` 第56バッチで分離）**＝**2つのゾーンの一番上を入れ替える**機構が無い。`WX13-073-E1`「**対戦相手の**ライフクロスの一番上とデッキの一番上を見る。あなたはそれらを入れ替えてもよい」／`WXDi-P10-047-E1`「あなたのデッキの一番上を見る。そのカードと**エナゾーンにあるこのシグニ**を入れ替えてもよい」。🔴**旧 id `SWAP_OPTIONAL` は「シグニの配置替え」ハンドラに落ちており、自分の場のシグニをゾーン移動する UI が開いていた**（原文と無関係な盤面操作）。⇒ 第56バッチで `DEFERRED_SWAP_OPP_LIFE_TOP_AND_DECK_TOP` / `DEFERRED_SWAP_DECK_TOP_WITH_SELF_IN_ENERGY` へ分離し、逆翻訳に `【未実装】` を出すようにした。■**要るもの**＝**任意の2ゾーンの一番上（または指定カード）を入れ替える**汎用アクション＋**相手のライフクロスを覗いて戻す**提示（`LOOK_OPP_LIFE_TOP` の受け皿が近い）。⚠`WXDi-P10-047` は「エナゾーンにある**このシグニ**」＝バニッシュ後の自分自身を指すので、**効果元の現在位置を運ぶ**必要がある。⚠**着手前に母集団を実測する**（「入れ替えてもよい」を含む効果の全数）。 |
| `O-227` | **1効果 / 1カード** | 🆕**2026-09-03（`O-60` 第55バッチで分離）**＝**期間つきでプレイヤーが得る【起】能力の受け皿が無い**（`WXDi-P09-066-E1`「次の対戦相手のターン終了時まで、あなたは『【起】《メインフェイズアイコン》《アタックフェイズアイコン》《青×0》：この方法で置いたカードをルリグゾーンから手札に加える。』を得る」）。🔴**旧 `STUB{GRANT_QUOTED_ACTIVATE_ABILITY}` は「effectEngine の CONTINUOUS 処理で対応」というコメント付きの真 no-op**（`npx tsx scripts/censusStubs.ts --id GRANT_QUOTED_ACTIVATE_ABILITY` で**消費地点0**を実測）＝実体はカード全文から引用文を切り出してログに出すだけだった。⇒ 第55バッチで `DEFERRED_GRANT_QUOTED_ACTIVATE_ABILITY` へ改名し、逆翻訳に `【未実装】` を出すようにした。■**要るもの**＝①**期間つき**のプレイヤー付与ストア（`GRANT_PLAYER_ABILITY` は `permanent:true` 固定で`game_granted_effects`＝**このゲームの間**の AUTO 用）②**起動型**の提示＝`src/screens/` 側で「プレイヤーが得ている【起】」をボタンに出す口。🔑**近い前例**＝`O-114`（スペル／アーツの【起】が UI から不可）と同じ層。⚠**着手前に母集団を実測する**（「あなたは「【起】…」を得る」の全数＝この1効果だけとは限らない）。**`src/screens/` を触るので遅いレーン＋実機必須。** |
| `O-218` | **1効果** | 🆕**2026-09-02（索引 B 第2巡・`O-68`④ から分離）**＝**【シード】として置いたカードの【起】が UI から一度も提示されない**（`WXK04-060-E2`「【シード】であるこのカードを公開し、エナゾーンから＜植物＞のシグニ２枚をトラッシュに置く：この【シード】を開花する」）。🔑**コストの問題ではない**＝`energyTrash{2,植物}` は既に載っており、「【シード】である」ゲートも `SEED_BLOOM{seedTargetSelf}` が実行時に見ている（シードゾーンに居なければ何もしない）。🔴欠けているのは**入口**＝`field.signi_seeds` を読むのは `BattleScreen.tsx:744`（cardMap のロード）**1箇所だけ**で、シードの能力を surface するコードが無い。⇒ `O-114`（スペル／アーツの【起】が UI から不可）と同じ層。**`src/screens/` を触るので遅いレーン＋実機必須。** |

#### 🏁 旧「索引 D. 母集団未計測」（34件）＝**2026-09-01 に全数実測して索引 A〜C・F へ解消した**

> **何をしたか**＝34件は「規模（`S`/`M`/`L`）の見立て」しか無く母集団が一度も測られていなかったので、
> **34件すべてを実測して索引 A〜C の該当ブロックへ移した**（この節はもう worklist ではない）。
> **行き先**＝**索引 A 2件**（`O-86` 178カード／`O-185` 11効果）／**索引 B 12件**（`O-97` `O-58` `O-52` `O-59` `O-71` `O-68` `O-137` `O-138` `O-78` `O-104` `O-118` `O-160`）／
> **索引 C 18件**（`O-84` `O-102` `O-105` `O-109` `O-110` `O-114` ＋ `O-72` `O-74` `O-79` `O-83` `O-94` `O-95` `O-101` `O-103` `O-130` `O-136` `O-139` `O-150`）／**索引 F 2件**（`O-85` `O-217`＝母集団0）。

**🔑 実測で分かったこと（次に同じ作業をする人へ）**

- 🔴**「母集団未計測」は「大きい」を意味しなかった**＝34件のうち**2桁だったのは2件だけ**（`O-86` 178／`O-185` 11）。**残り32件は8効果以下**、うち**18件が1〜2効果**。
  規模列が `L` の `O-58`／`O-59` も実測は**6効果／5効果**だった。⇒ **`S`/`M`/`L` は取る順の材料にならない**（§5.3 冒頭の警告の実証）。
- 🔴**`censusMechPopulation.mjs` の regex が0と言っても信じない**＝`O-74` は「場に出**せない**」しか見ておらず「場に出す**ことができない**」を落として0と誤報していた（実際は1件）。
  `O-71` も同じ理由で 0→**5件**。⇒ **0が出たら必ず登録票の seed カードを原文で引き直す。**
- 🔴**逆に regex が過大に出る形も踏んだ**＝`O-104` は緩い regex で21枚に当たるが、**18枚はコスト句「：」の前に書かれた同型**（＝正常に動く）で母集団は**3効果**。
  `O-72` も13枚に当たるが、12枚は「**【自】側**にフェイズ限定が付く」別形で母集団は**1効果**。⇒ **文型が同じでも構文上の位置が違えば別物。**
- 🆕**`O-150`（要調査）は実測の副産物として真因まで割れた**＝`EffectInteractionModal.tsx:826-834` が `pending.reorder` を見ずに ↑↓ を常時描画していた。
  ⇒ **「机上では決められない」と書かれた項目でも、フラグの消費地点を grep するだけで割れることがある。**
- 🆕**`O-217` は実測すると項目ごと消えた**＝旧クラスタ集計の7区分（合計165効果と書いてあった）は `census:clusters` を回し直すと**高シグナル欠落が全区分で0〜2**＝`O-185` の11効果に吸収済みだった。

#### 索引 E. 計器の較正・掃除（**機構ではない**＝カードの挙動は変わらない）

> ⚠**これらを消化しても実機の挙動は1件も変わらない。** 効くのは「**計器が嘘をつかなくなる**」ことなので、
> **計器主導でバッチを選ぶ前**に取ると後続が楽になる（逆に、挙動を直したい回にここから取ってはいけない）。

| ID | 母集団 | 何が無いか |
|---|---|---|
| `O-93` | M | `manualEffects.ts` の古い shadow が parser 改善を永久に凍らせる（計器から見えない） |
| `O-132` | S〜M／1バッチ＝1カテゴリ群 | `vocabCensus.ts` が「JSON に入っている語彙」を知らず、高シグナルの一部が偽陽性（第1バッチ消化・残＝未調査の 491） |
| `O-134` | S | 「代わりに」を帯分解で表した効果が、census では「置換語彙が落ちている」に見える |
| `O-187` | 計器の較正 | 🔴 `census:cards` の `mech` フラグが「§5.3 に出てくるカード番号」を全部拾う＝実装済みの"雛形として引用したカード"まで機構待ちに数える |

- [ ] **（旧 §5.4 (iv) 計器較正の残1件）filter による条件の等価表現を条件節クラスタの `extraOk` に入れる**＝「それが〈desc〉の**場合**」を `ADD_TO_FIELD{source:{filter}}`／`SEARCH{filter}` で表す形（`WX15-001-E2`）。

#### 索引 F. 事実上クローズ（**ここから取らない**・次の整理で行ごと消す）

| ID | 母集団 | 何が無いか |
|---|---|---|
| `O-128` | **0** | 【常】/【起】の中の引用能力付与が `STUB{GRANT_ABILITY_INNER_TEXT}` へ落ちる（残り27カード）<br>🏁**事実上クローズ**＝残14の内訳は engine 全文 regex で既に動作5／据置契約を golden 化3／新機構待ち6 |
| `O-85` | **0** | 「あなたの場にシグニがN体ある場合、代わりにカードを〜トラッシュに置く」＝置換の口が無い<br>🏁**2026-09-01 実測で原文0・live 0**＝「場にシグニがN体ある場合」は全 CSV に5枚あるが**どれも「代わりに」を伴わない**。`DEFERRED_FIELD_COUNT_ALT_TRASH` は死枝＝parser の枝ごと落としてよい |
| `O-217` | **0** | 旧クラスタ集計の「機構待ち」7区分（チーム34／アンコール19／合計制約25／同一性16／ゲームから除外26／正面19／数量比例26）<br>🏁**2026-09-01 に `npm run census:clusters` を回し直して解消**＝チーム／同一性／数量比例／正面／ゲームから除外の高シグナル欠落は**全て0**、残るのは 合計制約1／アンコール2／小さい数1＝**`O-185` の11効果と全域重複**。行ごと消してよい |

#### 🏁 旧「未採番の機構在庫」（旧 §5.4 (ii)・30件）＝**2026-09-01 に採番して索引 A〜D へ解消した**

> **何をしたか**＝旧 §5.4 (ii) は「機構ギャップは §5.3 へ送る」と書きながら送らずに溜め続けており、**機構の置き場が2つ**あった。
> 2026-09-01 に §5.3 へ移設したが `O-nn` が無く母集団順にも並べられなかったので、**30件すべてに採番して上の索引へ吸収した**。
> **全文（経緯・実測・罠・取り方）は [PLAN_DETAIL.md](./PLAN_DETAIL.md) の登録票**（`O-191`〜`O-217`）＝**着手前に必ず読む。**
> ⇒ **この節はもう worklist ではない。取るのは上の索引 A → B → C から。**

**30件の行き先**

| 行き先 | 件数 | 内訳 |
|---|---|---|
| **新規採番**（索引 A〜D へ1行ずつ） | **27件** | `O-191`〜`O-217`（下表） |
| **既出 `O-nn` へ統合**（採番せず吸収） | **5件** | `O-104`（N体・N枚の強制中間動作が UI でソフトロック）／`O-137`（デッキの一番上とライフクロスの入れ替え＝`WX19-061-E1`）／`O-158`（`ATTACH_ACCE` がエナから filter でアクセ札を選べない）／`O-164`（「次に〜される場合」の1回消費耐性・`WX15-010-E1` のバトルバニッシュ経路限定＝**2件が同じ器**）／`O-173`（「同じレベル」のペア付け） |
| **§5.4 (iii) 構造混線へ送り** | **2件** | live の `parseStatus:'PARTIAL'` 11件／続き749 の縦切り8（`WX21-028-E2`／`WXDi-P10-007-E3`）＝どちらも**木ごと作り直す**種類で、機構の追加では閉じない |

**新規採番の一覧（母集団順・詳細は上の索引と PLAN_DETAIL の登録票）**

| 索引 | ID | 母集団 | 何が無いか（1行） |
|---|---|---|---|
| A | `O-195` | 145効果🔴 | 「〜場合、」側の条件節テール（`STATE_CONDITION_CLAUSES_V2` 側） |
| A | `O-193` | 25効果 | `census:wiring` の has=0 語彙（`isDrive`／`powerLteSelf`／`levelLtSelf`） |
| A | `O-198` | 23効果 | `LOOK_PICK_CHAIN` が「ちょうどN枚」を表現できない |
| A | `O-192` | 19効果 | `HAS_CARD_IN_FIELD{cardType:'ルリグ'}` がアシストルリグを数え落とす |
| A | `O-197` | 約15効果 | 「それぞれ〜異なる」の対象節が `DISTINCT_BATCH5C` の表に載っていないだけ |
| B | `O-216` | 5効果 | `BANISH{ALL}` 側の `TargetFilter` 軸4種 |
| B | `O-191` | 3効果 | `isDisona` の別機構3件（置換・数量比例・引用付与の側） |
| B | `O-196` | 3効果 | 「それぞれ〜異なる」のコスト節／条件節の別機構3種 |
| C | `O-199` | 2効果 | アンコールの「テキスト形」コストが未実装 |
| C | `O-200` | 2効果 | ルリグデッキからキーを場に出す経路が engine に無い |
| C | `O-201` | 2効果 | 【出】任意コストの新しい支払い種別（支払いUIまでの縦切り） |
| C | `O-202` | 2効果 | コスト付きの置換＝発生時に支払いを問う窓が無い |
| C | `O-212` | 2効果 | 選択集合のパワー合計を「実行時の値」と比べられない |
| C | `O-215` | 2効果 | 【ソウル】が付いているシグニを指すフィルタが無い |
| C | `O-203` | 1効果 | 「レゾナとしても扱う」＝同一性の上書きを参照側で読む機構 |
| C | `O-204` | 1効果 | 発生源の否定限定「あなたの効果**以外**によって」 |
| C | `O-205` | 1効果 | `lrigAttackNoDamage` の発火地点が【ガード】経路だけ |
| C | `O-206` | 1効果 | `trashExile` の `selectionConstraint` を支払いモーダルが enforce しない |
| C | `O-207` | 1効果 | `LOOK_PICK_CHAIN` に「配置ゾーンの限定」が無い |
| C | `O-208` | 1効果 | `LRIG_TRASH_TO_UNDER_AND_RETURN_ARTS` がアーツを「全部」戻す |
| C | `O-209` | 1効果 | 絆を獲得するアクションが無い |
| C | `O-210` | 1効果 | `BANISH_REDIRECT` に「次に1回だけ」の消費フラグが無い |
| C | `O-211` | 1効果 | 遅延トリガーの発火源を「カード個体」で縛れない |
| C | `O-213` | 1効果 | ピース種別の使用歴（リレーピース／《連邦生徒会》）の受け皿が無い |
| C | `O-214` | 1効果 | `ZONE_SUM_COUNT` の `distinctBy` がゾーンごとに数えて足す |
| D | `O-217` | 🔴要再計測 | 旧クラスタ集計の「機構待ち」7区分＝**機構ではなく未整理の集計** |

⚠🔑**母集団はどれも登録時（続き376c〜749）の値**＝索引の「②母集団を実測し直す」を必ず回してから着手する。
⚠**`O-199` と `O-217` は重なっている**（旧集計の「アンコール 19」）＝`O-217` を再計測したら `O-199` と突き合わせて重複を落とす。

**■ 個別カードの機構待ち**
- **📋 個別カード機構待ちの defer 3件＝いずれも根拠つき（着手前にこの理由が今も有効か再判定すること）**：(a) **`WX17-044`＝⚠2026-08-10 再判定＝先行ブロッカーだった §5.3 は消化済（続き403）だが、このカードは**依然として別要因**で止まっている**＝①`WX17-044-E1` に `trashActivated` が立っておらず（parser が「トラッシュにあるこのカードを〜」の【起】をトラッシュ起動と認識していない）②本体アクションが `ADD_TO_FIELD` ではなく**【トラップ】を表向きにして発動させる**（現行の逆翻訳は別物になっている）③コストの `trashExile.self` は `trashActivateCost.ts` の対応キーに入れていない（使う効果が0件のため未実装のまま置いた）。**着手時は parser 側（①②）から。UI は③を足すだけで載る。**(b) **`WXDi-P05-006` choice①＝着手禁止**（ピースカットイン割込み基盤）。(c) **`WX20-Re20`＝一体で要る**（選択数依存コスト・能力なし filter・任意複数配置UI・同一 instance 群のターン終了時 trash）＝部分実装しない。
- **K. `manualEffects.ts` ↔ live JSON の乖離（2026-08-08 続き381 発見・続き382 で計器化＆消化）＝残 15 効果**＝**`build:effects` は live 側 `parseStatus:MANUAL` を不可侵にするので、`manualEffects.ts` を後から直しても live には永久に届かない**（`buildEffectsJson.ts:187`「手修正は不可侵」＝**JSON 直編集**の保護が意図だが、`mergeManualEffects` の出力も MANUAL なので新しい manual エントリも同じ網に掛かる）。`_held_review` にも `_partial_fresh` にも出ない**第3の死角**。**計器＝`npx tsx scripts/censusManualDrift.ts`**（明細 `docs/_manual_drift.txt`／`--date` で方向判定 `docs/_manual_drift_dates.txt`／`--card <ID>` で1カード完全diff／`--adopt <effectId,…>` で**効果単位**同期）。**ゲート＝goldenTest の「§5.3 K トリップワイヤ」**＝`MANUAL_DRIFT_KNOWN` に無い乖離が出たら即 FAIL・解消したのにリストに残っていても FAIL（＝リストは worklist であって許可リストではない）。⚠🔴**実測値は 52 効果／46 カード**（続き381 の速報 105/427 カードは**素朴な JSON 文字列比較＋キー順依存**の過大計上で、続き382 に計器を作って実測し直した値が正）。**続き382 で 37 効果を同期して残 15**。⚠**一括同期は不可（双方向）**＝(a)`LIVE_ONLY`/`LIVE_RICHER` は live のほうが新しい（`PR-426-E3` 等の後付け手修正）(b)**同じ効果でも項目ごとに新旧が違う**＝`WX16-023-E1`/`WXK10-008-E1` は action は manual が新しいのに **timing は manual が退化**していた（CSV の `Timing` 列より狭い `["ATTACK"]`）＝`--adopt` は timing が変わると既定で中止する（`--allow-timing-change` で明示解除）。(c)🔴**`parser由来` の効果に日付判定を使ってはいけない**＝「manual 側の日付」はそのカードのブロックが触られた時刻でしかない。実測で日付が MANUAL_NEWER と言った parser 由来5件は**全件 live のほうが正しかった**（`WX22-013-E2` は fresh が2択を DRAW 1本へ平坦化＝退化）＝計器はこれを `PARSER_REVIEW` として別枠に出す。**残 15 の内訳＝`PARSER_REVIEW`（通常の目視レビュー案件）＋ manual 定義の `UNDATED` 8／`SAME_TIME` 2**。
- **F. 保留**（core改変が過大リスク）＝WXDi-P00-026（さんばかルリグ付与・ルリグ再アタック未実装がブロッカー）／47枚の【使用条件】【チーム】（正規デッキ常時成立で機能等価＝保留妥当）。
- **G. 置換else系統の残（続き269・§3タスク5から正式送り）**＝**B 2件の置換else部分は✅続き289で消化**。SPK06-01-E1 は追加赤0/2/4の三択を同一対話で保持し、対象数1/2/3を排他的に実行（付随するレイラのコイン技回数・次回コスト軽減は既存基盤が無く別途defer）。WXK06-032-E1 は既存 `refresh_count_this_turn` を双方参照し、最初に選んだ同一対象へ－4000/－12000を排他的に適用する。**残はC 13件**（反復、引用能力／ルリグ能力付与、複雑CHOOSE、支払い系ルール等）。CのWXDi-P02-042-E1を再確認したが、相手側のターン中手札捨て枚数を読む条件軸が無いためhonest defer。effectId 全明細と不足機構は `docs/_replace_else_triage.txt`。
- **⚠H が残した横展開の教訓＝UNKNOWN を実装するときは fresh parser の出力を先に測る。** H4 は fresh が主語を見ない `STUB DEPLOY_RESTRICT` を既に吐いており、live の `UNKNOWN` だけが「対戦相手は1体まで」への意味反転を止めていた（採用した瞬間に過剰実行へ反転する型のドリフト）。
- **⚠I の潜在結合（実害なし・将来の落とし穴）**＝`executeSigniOnPlay` の `beatZones`（`Set<number>`）が **`beat_signi` の「場ゾーン index」と `beat_signi_from_trash` の「トラッシュ index」で同じ集合を共有**している。実データで両コストを併せ持つ効果は**0件**（全数走査）なので現状は実害なしだが、将来併記カードが出ると支払いが `ok:false` で**無言 abort** する。併記が現れたら選択集合を分離すること。
- **⚠I の付与ストア走査は `activeCondition` を評価しない**（`effectEngine.ts:3137-3142`）。今日の唯一のエントリ `WX24-P3-069-E1-G` は無条件なので実害なしだが、条件付き CONTINUOUS を付与する効果を足すときは effectsMap 側と同じ `checkActiveCondition` を通すこと。
- **⚠honest defer 継続＝`WX20-Re20`**（選択数依存コスト・能力なし filter・任意複数配置UI・同一instance群のターン終了時 trash が**一体で**要る＝部分実装しない）。

**■ 監視だけしている項目（着手不要・壊れたら気付く）**
- 🆕**`energyTrashSameLevelAsTarget` の「それと同じレベルの」が逆翻訳に出ない**（2026-09-01・`WX24-P4-051-E2`）＝逆翻訳は「あなたのエナゾーンからシグニを1枚トラッシュに置いてもよい」とだけ出るが、**挙動は正しい**（`resolveOptionalCostSpec` がレベル一致で絞り、golden が「同レベルのエナだけを払う」「同レベルが無ければ pay 不可」の両方向を固定済み）。⚠**続き768 の教訓(b)「逆翻訳が描けないと、直したこと自体が読めない」の再発**＝`actionJa` の `OPTIONAL_COST` 分岐にレベル一致の語彙を足せば解消する。
- 🆕**`TRANSFER_TO_DECK` の逆翻訳は「好きな順番で」を表示しない**（2026-08-24 続き640・`O-53`）＝`あなたのカード(手札)2枚をデッキの一番下に置く` とだけ出る。**挙動は正しい**（bottom は `resumeSelectTarget`→`applyDirectAction` が選択配列順に末尾へ append するので**選択順が保存される**）で、**表示だけが不足**。⚠**既に正しかった兄弟4効果（`WXK01-102-E1` 等）も同じ表記**なので、今回入った退化ではない。⚠**top は同じ経路だと逆順になる**（各札を先頭へ prepend）＝「好きな順番でデッキの一番**上**に」を `TRANSFER_TO_DECK` へ寄せるときは**ここを先に直す**。
- 🆕**`census:wiring` の `levelExact × BLOCK_ACTION{PLAYER}`（miss=3 / has=0）は恒久的な偽陽性**（2026-08-22 続き609・O-41 決着時）＝`WD15-010-E1`／`WDK05-T09-E1`／`WX18-039-E1` は**レベル限定を actionId の文字列に載せて**解決済み。⚠**`target.filter.level` を足して計器を黙らせないこと**＝この `target` は `PLAYER` であり、レベルが属するのは**ガードする側のカード**でプレイヤーではない（意味を壊す埋め合わせになる）。常在（【常】）側は `ContinuousBlockResult.forSelf`＝`Set<string>` で運ばれるので、そもそも型フィールドでは届かない。
- 🆕**`census:stubs` は C群（逆翻訳の生ID露出）も 0 でゲート**（2026-08-18 続き545・§5.3 O-12 完了時に追加）＝新しい STUB を足して**日本語の表示語彙を書き忘れる**と赤で止まる。直し方は①ハンドラ直前に `// <ID>: 日本語の説明` を書いて `node scripts/genStubsMd.mjs`（STUBS.md 経由で自動反映）②ハンドラを持たない宣言型は `scripts/decompileEffects.ts` の `miscStubMap` に足す。⚠**どちらも `npm run regen` まで回すこと**（計器は逆翻訳シートの実出力を読む）。
- 🆕**`genStubsMd.mjs` はハンドラ直前コメントの `<ID>:` ラベルを剥がす**（2026-08-18 続き545）＝`ID1 / ID2:` `ID AUTO:` `ID（カード番号）：` の3綴りに対応し、**その id のラベル行だけ**を説明に採る（他 id 宛ての行を混ぜない）。⚠**ラベル行が `ID（…）：` だけで本文が次行以降にある**綴りが実在するので、自己完結していないときだけ続きの行を足す。⚠`stub.id === '[A-Z0-9_]+'` で拾うので**日本語入りの id**（`ENERGY_COLOR_SUBSTITUTE_赤_OR_青_TO_白`）は原理的に拾えない＝そちらは `decompileEffects.ts` 側で id から色を読んで組む。
- 🆕**シグニ【自】の支払いゲートは【出】も巻き込む**（2026-08-18 続き544・§5.3 O-38 実装時）＝`CardEffect` に **【出】と【自】を分ける情報が無い**（どちらも `effectType:'AUTO'`／【出】は `timing:['ON_PLAY']` で、逆翻訳も両方「【自】」と出す）。原文「対戦相手のシグニの**【自】**能力」は本来【出】を含まないが、**旧ハードブロック（`BLOCK_OWN_SIGNI_AUTO`）も同じ母集団を止めていた**のでここは据置＝母集団は変えていない。分けるならアイコン情報を parser から `CardEffect` に載せる必要があり、`BLOCK_OPP_SIGNI_AUTO` 系3枚と同時に直す話になる。
- 🆕**`SPDi43-01-E2` の宣言は実行時のカード原文 regex のまま**（2026-08-18 続き544）＝`STUB{GRANT_ABILITY_INNER_TEXT}` のハンドラ内分岐で、live JSON には構造が載っていない。**engine 側は構造化済み**（`signiAutoPayGateMarkers` → `blocked_actions` → `findSigniAutoPayGate`）なので挙動は正しいが、`GRANT_ABILITY_INNER_TEXT` の受け皿を畳むときはこの分岐も一緒に構造へ移すこと。
- 🆕**ダメージ置換（`DAMAGE_REPLACE_BY_COST`）の「支払うかどうか」は自動適用の近似**（2026-08-18 続き543・§5.3 O-37(a) 実装時）＝原文は「代わりに〈コスト〉を支払って**もよい**」で本来は被害側が選ぶが、消費地点（`crashOneLife`／ルリグアタック応答）は**同期経路で対話窓が無い**。既存の `optional` なライフクラッシュ置換と**同じ枠の近似**（funnel 冒頭のコメント参照）。支払い方は**原文の並び順**で最初に払えるもの、捨てる手札／エナは**末尾から**（決定論）。⚠**払えない盤面では置換が成立しない**ので自滅は構造的に起きない。対話化は離場置換（§5.3 M2）と同じ枠組みで別バッチ。
- 🆕**「その効果によってトラッシュに置かれたカードの中から」の対象は「トラッシュ末尾」の近似**（2026-08-18 続き543）＝`TRASHED_CARD_TO_HAND_OR_ENERGY` は `lastProcessedCards[0]` → 無ければ `trash.at(-1)` を見る**既存ハンドラ**（姉妹 `WX24-P3-030-E1` と共用）。1回の解決で複数枚が置かれたときだけ「最後の1枚」に丸まる。**候補プールを StackEntry へ載せる**なら `leftFieldUnderCards` と同じ作法で複数形フィールドを足す。
- 🆕**`LIFE_TO_ENERGY` は engine に完全実装済み**（2026-08-17 続き529）＝`execStubPart1.ts:3733`。「〈誰か〉のライフクロス1枚をエナゾーンに置く」を新しい action 綴り（`SEND_TO_ENERGY{LIFE_CLOTH_CARD}`）で書きかけて撤回した。⚠**`census:stubs` は「STUB＝未実装」ではない**＝新しい綴りを足す前に、同義の STUB が engine に無いかを必ず見ること。
- 🆕**「エナゾーンから〈限定〉すべてのカード」の2件は1枚（過少）のまま据置**（2026-08-17 続き528）＝`WXEX1-07-E2`／`WXK09-037-E1` の「宣言した色ではない色を持つすべてのカード」。**色限定が未表現**なので `ALL` へ広げると**相手のエナを全部飛ばす過剰**に化ける（A/B で実測）。規則は「エナゾーンから」と「すべてのカード」の**間に修飾が挟まらない綴りだけ**に限定してある＝**このガードを外さないこと**。宣言色の filter が表せるようになったら同時に解ける。
- 🆕**枝の数を固定している golden トリップワイヤは、正しい是正でも落ちる**（2026-08-17 続き528）＝`(lxxv)` は相手ミルの owner を `join(',')` の完全一致で見ており、`WX08-020-E1` が `CONDITIONAL{then, else}` に解けて `DECK_CARD` が2つになっただけで赤になった。**「全エントリが opponent かつ1つ以上」へ書き換え済み**。同型のトリップワイヤを書くときは**性質（owner・型）を固定し、出現回数は固定しない**。
- 🆕**「そうでない場合、〈X〉」の else 節が公開札を指す照応の2件は据置**（2026-08-17 続き527）＝`WXDi-P09-068-E1`「**その**カードをデッキの一番下に置く」／`WXK03-050-E1`「**それ**を…置いてもよい」。単独 parse では参照先が束縛されず UNKNOWN／別物の `LOOK_AND_REORDER` に化け、**行き先は `remainder` の領分で枝ごとに変えられない**＝据置のほうが正しい。ビルダー側は文頭の照応語と UNKNOWN の両方で弾いている（規則を広げるときはこのガードを外さないこと）。
- 🆕**`parseColorFilter` は「AかBの」形で B しか拾わない**（2026-08-17 続き527 に発見）＝`text.includes('〈色〉の')` の直列走査なので「**白か黒の**＜天使＞のシグニ」は `{color:'黒'}` になり**白を落とす過少**。現状の該当は `WXEX1-57-E1` 1件だけで、**live は MANUAL で `color:["白","黒"]` と手当て済み＝実害なし**（＝壊れているのは parser のみ）。複色 OR を書くカードが増えたら `parseLastProcessedMatchesCondition` にある「(色)か(色)のシグニ」規則と同じ形へ寄せる。
- `POWER_THRESHOLD_TRASH`＝parser が生成しうるのに engine に消費地点が無い。**live 0件**で無害。golden に「live 0件」の契約テストがあり、parser 規則が生えた瞬間に赤くなる。
- **【マジックボックス】の複数枚設置**＝現行の分岐は**1枚目だけ設置する**。原文が「1枚まで」の3効果しか無いので実害なし。「2枚以上を同時に設置」するカードが出たら**トラップ分岐と同じ per-card 展開へ寄せる**。
- `STUB{REVEAL_PICK_HAND_SHUFFLE_BOTTOM}` 5効果＝2026-08-12 に実測したところ**全件 `revealPickParams` が完備**（枚数・行き先・filter・二段ピック）で、`REVEAL_PICK_PLAY` のような原文再parse依存ではない。**対象外。**

### 5.4 原文照合テール（1効果ずつ・**構造混線だけが残った**）

> 🆕🔴**2026-09-01 整理＝この節は (iii) 構造混線だけになった。**
> **(i) 配線ギャップ**（`census:wiring` のセル単位バッチ）は**一覧の数字が全部 stale**（続き376b／656 時点）で、そのまま取ると偽陽性を掘る＝
> **入口だけ残して一覧は [PLAN_DETAIL.md](./PLAN_DETAIL.md) へ退避した。取るなら `npm run census:wiring` を回し直してから**
> （`npx tsx scripts/censusWiring.ts --cell <キー>:<入口ラベル>` で原文を全部読む）。⚠**この計器は `has===0` の語彙を表から捨てる**＝「0件だから穴はない」と読んではいけない。
> **(ii) 機構ギャップ**（未採番の在庫30件）は **§5.3 へ移し、2026-09-01 に全件採番して索引 A〜D へ解消した**（`O-191`〜`O-217`＋既出5件へ統合）＝機構は1か所で管理する。
> **そのうち2件だけ「機構の追加では閉じない」と判定して下の (iii) へ送った**（live の `PARTIAL` 11件／続き749 の縦切り8）。
> **(iv) 計器較正**は残1件を §5.3 の計器ブロックへ移した。
> ⇒ **ここに残るのは「効果の木ごと作り直す」ものだけ**で、**1件あたりのコストが (i)(ii) より桁で高い。後回しでよい。**

**目標**＝効果単位の修飾脱落を全数是正する。⚠**作業の単位は「カード/効果」であって「文型」ではない。**

**進め方**
1. `npm run census:clusters` → `docs/_census_clusters.txt` の**単発テンプレ**から効果を取り、原文とJSONを並べて照合する。
2. 落ちている修飾を**欠落パターンで分類**する（下表）。**同じパターンが繰り返し出たら parser へ還元する**のがこの作業の主目的。
3. 単発の外科パッチは curated JSON へ入れてよいが、**parser が同じ出力を出すようにするか MANUAL 化するか**をセットにする。⚠**§2.0 の「移設だけの manual 化は禁止」を必ず読む。**
4. ゲートは `npm run gates`＋`npm run regen`＋同型★0。**parser を触ったら A/B 差分で「意図した効果だけ変わった」ことを機械確認**する。

**欠落パターンの分類（続き369 の無作為12件・以後ここを更新して母数を測る）**

| # | 欠落パターン | 例 | 件数(12件中) | parser 還元の見込み |
|---|---|---|---|---|
| A | **対象フィルタの脱落** | 「《X》**以外の**」「**能力を持たない**」「**このターンに捨てた**」「＜C＞の」「数**以下のレベル**」 | 6 | ◎（系統化しやすい・最優先） |
| B | **owner 取り違え・任意性喪失** | 「**対戦相手は**〜しても**よい**」→ 自分・mandatory | 2 | ○ |
| C | **条件節・機構ゲートの脱落** | 「手札が5枚以下の場合」「【チーム】ルリグ3体」 | 2 | ○（§5.4 の残規則と地続き） |
| D | **重度の構造混線** | `WXDi-P04-016-E3`＝別効果の内容混入・コスト消失・2体→1体 | 1 | ✕（1件ずつ再parse＝§5.4 (iii)） |

**⚠完了判定について**＝**census が0になっても「すべてのカードが完璧」にはならない**。census の死角は4つ
（参照解決の誤り／効果単位の粒度／JSONは正しいが engine 実装が違う／文間の実行順序）で、重度混線（D）も
たまたま census に掛かっただけの個体が居る。**最終的な判定器は BEHAVIOR_AUDIT（§5.4）と §5.1 実機検証**。

**続き369 で照合済み・未修正の12件（1効果ずつ取るときの入口）**＝`WXDi-P11-044-E2`(A+C)／`WX17-Re05-E1`(過小)／
`WXDi-P03-024-E2`(A)／`WXDi-P05-072-E2`(B)／`WXEX2-55-E1`(A)／`WXK05-027-E1`(A)／`WXK05-072-E1`(A+B)／
`WXDi-P16-092-E1`(C)／`WXDi-P04-016-E3`(D)／`WX17-032-E2`(トリガー主語)／`WXK05-016-E2`(A)／`WX25-P1-TK6-E2`(原文未取得・要確認)。


#### (iii) 構造混線 — 効果の木ごと作り直し（**1件ずつ再parse・バッチ化できない**）
- [ ] **②で新規発見した3件**：`WXK04-090-E1`（「公開しないかぎり**自分を**トラッシュ」→「自分の水獣シグニをトラッシュ」）／`WXK04-025-CB-E2`（多段閾値全脱落・`ALL`→1・能力付与が**直接ライフクラッシュ**に化ける・エナ全トラッシュのコスト欠落）／`WXDi-P16-034-E2`（引用付与の平坦化で《無》支払い回避が消え**無条件アタック禁止**・owner/duration も誤り）。
- [ ] **続き369 で照合済みの `WXDi-P04-016-E3`**（パターンD）。
- [ ] **§5.4 末尾の照合済み12件**のうち (D) 相当。
- [ ] 🆕**旧 §5.4 (ii) から送られた `parseStatus:'PARTIAL'` 11効果**（2026-08-30 実測。⚠PLAN の旧記述「union 1162 中 23件」「54件」はどちらも古い）＝中身は `LOOK_AND_REORDER`＋`UNKNOWN` 残渣・引用付与の内側 UNKNOWN 等の**構造混線**で、**(ii) の「安い在庫」ではない**＝1件ずつ木を作り直す。旧・census 高シグナルだった実例＝`WX14-046-E1`／`WX21-028-E1`／`WX24-P4-038-E1`／`WX25-P1-044-E1`／`WX25-P3-050-E1` ほか。
- [ ] 🆕**続き749 の「census 高シグナル残り12」の縦切り8＝構造混線2件**（2026-08-31）＝`WX21-028-E2`（相手の場/エナ/トラッシュの3ゾーンを同時に対象化して自分のエナからデッキへ戻す）／`WXDi-P10-007-E3`（デッキ10枚→コスト合計4以下のスペル2枚をチェックゾーン→好きな順で使用）。**他の7件は機構なので `O-199`〜`O-204`／`O-137` へ採番済み。**
- ⚠**「効果はだいたい parse できていて修飾が1つ落ちている」(i)(ii) と違い、(iii) は木ごと作り直し**＝1件あたりのコストが桁で違う。**後回しでよい。**

### 5.5 低優先・保留

- [ ] **CPU AI (g) 選択の精緻化**（旧 §8・盤面評価 v2 候補）＝(a)〜(f) は消化済み。⚠**§5.1 の実機返済を終えてから着手する**。⚠**CPU の対話自動応答は元から完備**（`BattleScreen.tsx:526`〜）＝効果を起動できれば対象選択まで自動で回るので、要るのは「起動する経路」と「何を撃つかの判断」だけ。
- [ ] **逆翻訳機の出力品質の残テール**（旧 §5b）＝混線テール実測823カードの再parse。§5.4 (iii) と同じ母集団。**英語 ID 漏れは `census:stubs` C群がゲート化済み**（0でゲート・増えたら exit 1）。
- [ ] **残る単発テール（原文とJSON構造がズレた混線／未構造化STUB）**＝**2026-07-02時点で「1 effect=1クリーンSTUB」で原文抽出できるものは全消化済み（444→367→その後の作業でカード母集団が変動し2026-07-12実測823・続き87で機械分類済み）**。effect構造そのものが原文とズレた混線で、1つのSTUBを原文化しても同 effect 内の他のズレが残り原文一致にならない＝decompilerの原文抽出では対応不能なものが大半。**effects JSON の再parse（機構実装・データ層修正）が本筋＝Opusタスク13**。系統別内訳（16テーマ・カード数の多い順＝デッキ操作系184／パワー修正系165／手札系102／トラッシュ系75／対戦相手コスト系63／エナ系50／ライフ系48／シグニ配置系48／ルリグ系36／能力付与系31／ガード・アタック制限系26／ソウル・アーツ系15／ウィルス系10／色・クラス系4／ゲーム除外系3／チャーム系1／その他54）は`docs/_stub_leak_classification.txt`参照（続き87・Sonnet）。進め方＝1カードずつ effects JSON を原文どおりの構造に手修正→逆翻訳が原文一致するか確認→smoke/golden/fuzz→push（**原文コピーでの一括潰しは禁止**＝実装未完成を隠蔽し検証目的に反する）。
- [ ] **🆕生JSON漏れ（`【シャドウ:{"levelGte":3}】` 形）72件**（2026-08-07 続き366 で観測）＝`シャドウ:{…}` は**JSON データ側の符号化**（engine もこの文字列を読む）なので、表示だけ日本語化するには decompiler に整形器を足す。`levelGte/levelLte/powerLte/cardType/color` の素直な形のほか `selfPowerHalfLte` `lrigTrashArtsColor` `downerLrigLevel` `declaredNumberPowerEq` `declaredColor` `artsCostLte` `selfColor` `selfPowerLte` など**約20種**あり、**1種ずつ原文照合が要る**（【シャドウ（レベル3以上）】等の原文表記に合わせる）。⚠英語**条件**ID漏れは続き366 で残0にした。
- [ ] **`doPhaseAdvance` の pure 抽出は「やらない」を既定**（費用対効果逓減と結論済み）。CPU 統一で必要になった部分だけ最小限切り出す。
- [ ] **BEHAVIOR_AUDIT キューの消化（⛔休眠）**＝`node scripts/_bqTriage.mjs` で高シグナル選別 → `npm run audit -- --id <CardNum>` で目視 → 「真no-op／シナリオ空振り／STUB未実装」に仕分け。**続き133 で高シグナル22件を精査して真 no-op バグ0件＝枯渇**。再開するなら**監査ツールの構造的盲点フィルタの実装が先**（低収量見込み）。
- [ ] **リリース判定**：fuzz 重め（`npm run fuzz -- --games 2000 --moves 80`）＋実機 PvP/CPU 通し対戦スモークをリリースゲートに。DESIGN §5 の手順（version bump→CI→push→`npx vercel --prod`）で本番反映。

---

## 6. 恒久指標（最新1ブロックのみ）

> **運用**＝この節は**「いまの数字」だけ**を置く。新しく作業したら ①上のブロックを [PLAN_DETAIL.md](./PLAN_DETAIL.md) の恒久指標アーカイブへ移す ②今回の値へ書き換える。⚠**溜め始めたら破綻する**（続き550 の整理時点で計測行15本＋ポインタ37本まで膨れ、cold start が最初に読む節が一番古い状態だった）。
> 🆕🔴**2026-09-01 改定＝3計器だけでは進捗が表示できなくなったので「在庫2本」を併記する**（理由は §3 の同日改定）。**3計器は底を打った＝これ以上は下がらないので、動かないことを「停滞」と読まない。**

- **2026-09-04（`O-60` 第64〜68バッチ・索引 A 第24〜28巡）＝A群最大 catch-all が live 0 に到達（Opus 5 単独／本ブロックが直近の正）**
  📊**進捗3計器**＝**Sheet1 要対応 17 / 863 (2.0%)**（据置・全部 `mech`）｜**台帳 残 OPEN 44**（据置）｜
  **census 高シグナル 3 / BASELINE 3**（据置）
  **この項目の計器 `census:enginetext` の A群**＝**13行 / 12ハンドラ（据置）**。**ただし live は 27 → 0。**
  📦**在庫2本**＝**機構 worklist 39項目**（`O-235` クローズ −1／🆕`O-236`〜`O-242` +7＝33→39）｜
  **実機 残 15件**（🆕`V-145`＝14→15）
  🔧**ゲート（全緑 ✅）**＝golden **3418 / 3418**（3404→3418＝+14本／既存の契約 golden 3本を理由つきで更新）／
  smoke **10725** 全異常0／fuzz 全0／census **3 / BASELINE 3**／`census:stubs` A群🔴0・C群0／manual-fields 0／
  `census:enginetext` A🔴 **13行**（据置）／`census:costtext` A🔴 **0規則**（据置）／
  lint 0 errors／`npm run regen` 完走後に再度 gates 全緑。
  🖥**実機＝機械判定では不要（§2.2）**＝`src/screens/` は0行。ただし `src/engine/` を2箇所触り
  **14効果の挙動が変わった（うち10効果は恒久 no-op から実際の効果／明示 defer へ）**ので `V-145` を登録（未実施）。
  🏁**主産物＝引用付与 catch-all の3綴りが live 0**＝`GRANT_ABILITY_INNER_TEXT` /
  `GRANT_QUOTED_ABILITY` / `GRANT_QUOTED_AUTO_ABILITY`（第63の較正時点で 27効果）を
  5バッチで**全部**受け皿（11効果）か**名前のある穴**（8効果）へ振り分けた（`O-235` クローズ）。
  🔴🔑**最重要の発見＝PLAN の記述が間違っていた**＝「A群の2行が減るのは live 0 になったとき」は**誤り**。
  この計器は **engine のコード行**を数えるので、**live 0 でもハンドラを消すまで行は落ちない**（実測）。
  ⇒ 次の一手は「parser の catch-all 生成地点5箇所を畳んでから、ハンドラごと撤去する」。
  🔴**実害（どれも計器が全部緑のまま壊れていた）**＝
  ①`WX22-Re04-E2` の**3択のうち2枝が無言 no-op**（engine の切り出しが「を得る」を要求し1本も当たらない）
  ②`WXDi-CP02-TK03A-E1` は**クラフト自身に＋5000**していた（「これの上にある」の綴りを見ておらず既定 else へ）
  ③`WXEX1-32-E2` は**デッキから2枚落とすだけ**で能力が付かなかった
  ④`WXDi-D04-011` / `WXK03-042` / `WXDi-P05-069` / `WXDi-P12-036` ほか**7効果が恒久 no-op**。
  🔑**一般則**＝①**engine では1本のハンドラでも parser の復元規則が綴りを1つしか見ていないことがある**
  ②**宣言型 STUB は CONTINUOUS として読まれて初めて意味を持つ**（引用は `GRANT_EFFECT` で包む）
  ③**live が `PARTIAL` の効果は `manualEffects.ts` ＋ `syncManualLive` でしか届かない**
  ④**「見出しだけ取れた」は宣言ではない**（`gameGrants` が `abilityBlockHeader` だけなら穴を宣言し直す）
  ⑤**近似で既存の受け皿へ寄せない**（寄せた4件はどれも過大実行になる形だった）。
  ✅**ブラスト半径＝効果 変更14・追加0・削除0、予定外0**。

- **2026-09-03（`O-60` 第61バッチ・索引 A 第21巡）＝モーダル選択 family の残り3件（Opus 5 単独／本ブロックが直近の正）**
  📊**進捗3計器**＝**Sheet1 要対応 17 / 863 (2.0%)**（据置・全部 `mech`）｜**台帳 残 OPEN 44**（据置）｜
  **census 高シグナル 3 / BASELINE 3**（据置）
  ⚠**据置は想定どおり**＝`O-60` は語彙の欠落ではなく「engine が原文を読む」形を潰す項目なので3計器には出ない。
  **この項目の計器 `census:enginetext` の A群**＝**17行 / 16ハンドラ → 13行 / 12ハンドラ**
  （`BASELINE_SELF_TEXT` も 13 へ払い戻し）。miss は 0 のまま。
  📦**在庫2本**＝**機構 worklist 32項目**（🆕`O-234` を登録＝31→32）｜**実機 残 12件**（🆕`V-142` を登録＝11→12）
  🔧**ゲート（全緑 ✅）**＝golden **3399 / 3399**（3398→3399＝+1本／既存の契約 golden 2本を理由つきで更新）／
  smoke **10725** 全異常0／fuzz 全0／census **3 / BASELINE 3**／`census:stubs` A群🔴0・C群0／manual-fields 0／
  `census:enginetext` A🔴 **13行**（`BASELINE_SELF_TEXT` も 13）／`census:costtext` A🔴 **0規則**（据置）／
  lint 0 errors／`npm run regen` 完走。
  🖥**実機＝機械判定では不要（§2.2）**＝`src/screens/` は0行・新しい型／payload キーも0（既存語彙のみ）。
  ただし**3効果の提示形が変わった**ので `V-142` を登録（未実施）。
  🔴**主産物＝「昇格」には2つの軸がある**＝`CHOOSE{betChoose/…}` は**選択数**の昇格だが、
  `WDK01-010` は**対象枚数**（3枚まで→4枚まで）の昇格で軸が違う。旧 `BET_CONDITION` は差分1枚を
  **追加で選ばせて**おり、その候補は `trash` の「シグニ」全部＝原文の絞り込みが**追加の1枚にだけ掛からない**
  過剰実行だった。⇒ **枚数だけ差し替えた同じ本文を解き直して `CONDITIONAL{IS_BETTING}` の両枝に置く。**
  🔴**実害・副産物**＝①`effectExecutor` の**専用先取り**がカード全文から ＜クラス＞ と枚数を読み直していた
  （A群1行＝`src/engine/` の catch-all だけ見ていると見落とす）②`CHOOSE_N_FROM_LIST` に**意味の違う
  catch-all が3本相乗り**（「以下のNつを**行う**」は**選ぶのと意味が逆**）③**見出しと①の間の文が落ちる**
  （`WX13-003` のコスト宣言）。
  🔑**一般則**＝①**昇格は選択数と対象枚数の2軸** ②**支払いの成否を後段が読む形は `OPTIONAL_COST` 経路へ**
  （ただし id を全部乗り換えず1文型だけ分ける） ③**受け皿を撤去したら その id を出す枝を全部消す**。
  🔑**`choiceTextParser.ts`（engine 第2の原文解析器・492行）の呼び出し元は残り1本**＝`O-234` に登録。
  ✅**ブラスト半径＝効果 変更3・追加0・削除0、予定外0**。

- **2026-09-03（`O-60` 第60バッチ・索引 A 第20巡）＝モーダル選択 family を `CHOOSE` へ寄せて受け皿4本を撤去（Opus 5 単独／本ブロックが直近の正）**
  📊**進捗3計器**＝**Sheet1 要対応 16 → 17 / 863 (2.0%)**（🔴増だが**簿記**＝`O-233` の登録で `SPK16-13E` が
  `mech` に入った。17件は**全部 `mech`＝即着手可能 0**）｜**台帳 残 OPEN 44**（据置）｜
  **census 高シグナル 3 / BASELINE 3**（据置）
  ⚠**据置は想定どおり**＝`O-60` は語彙の欠落ではなく「engine が原文を読む」形を潰す項目なので3計器には出ない。
  **この項目の計器 `census:enginetext` の A群**＝**22行 / 21ハンドラ → 17行 / 16ハンドラ**
  （`BASELINE_SELF_TEXT` も 17 へ払い戻し）。miss は 0 のまま。
  📦**在庫2本**＝**機構 worklist 31項目**（🆕`O-233` を登録＝30→31）｜**実機 残 11件**（🆕`V-141` を登録＝10→11）
  🔧**ゲート（全緑 ✅）**＝golden **3398 / 3398**（3397→3398＝+1本／既存の契約 golden 5本を理由つきで更新）／
  smoke **10725** 全異常0／fuzz 全0／census **3 / BASELINE 3**／`census:stubs` A群🔴0・C群0／manual-fields 0／
  `census:enginetext` A🔴 **17行**（`BASELINE_SELF_TEXT` も 17）／`census:costtext` A🔴 **0規則**（据置）／
  lint 0 errors／`npm run regen` 完走。
  🖥**実機＝機械判定では不要（§2.2）**＝`src/screens/` は0行・新しいアクション型／条件型／interaction 型も0
  （足したのは `StubAction` の payload キー1本）。ただし**18効果の提示形が `CHOOSE` へ変わった**ので
  `V-141` を登録（未実施）。
  🔴**主産物＝engine には原文解析器が「2つ」ある**＝`parseChoiceOptionsFromText`（`choiceTextParser.ts`・492行）は
  **原文を引数で受け取る**ので `census:enginetext` に1行も映らない（罠の3例目）。
  受け皿 STUB を撤去するときは**そこにしか無い規則を全部 parser へ移す**（移し忘れで3件が壊れた）。
  🔴**実害**＝①**ルリグの＜X＞をカード名と突き合わせていた**（正しくは `CardClass`）＝逆包含のまぐれ当たりでしか
  成立せず、通常のルリグ名では**昇格が永久に起きなかった**（3枚）②**同じ追加コストを2回提示**していた（3枚・
  🔑**既存 golden がその不具合を契約として固定していた**）③**`?` の後置 optional は直前1文字にしか掛からない**
  （`枚?以上?`＝条件が落ちて無条件に3枚エナチャージ）④**別名を両方書いていなかった3件**
  （`【ライフバースト】`/`《ライフバースト》`・「移動して」/「置かれて」・語順違い）。
  🔑**一般則**＝①**engine の原文解析器は1つとは限らない**＝撤去前に同じ仕事の別実装を grep する
  ②**STUB を撤去すると census の高シグナルが増える**（STUB 免除が外れる）＝「可視化」で片付けず1件ずつ割る
  ③**句を任意にするなら `(?:…)?` で囲む** ④**別名は必ず両方書く**。
  ⚠**見送り**＝`WX13-003`（`CHOOSE_N_FROM_LIST`）／`WDK01-010`（`BET_CONDITION`）＝**別文型**。
  ✅**ブラスト半径＝効果 変更18・追加0・削除0、予定外0**。

- **2026-09-03（`O-60` 第59バッチ・索引 A 第19巡）＝A群の小口4 family＝自己再帰する受け皿の payload 積み忘れ（Opus 5 単独／本ブロックが直近の正）**
  📊**進捗3計器**＝**Sheet1 要対応 16 / 863 (1.9%)**（据置）｜**台帳 残 OPEN 44**（据置）｜
  **census 高シグナル 3 / BASELINE 3**（据置）
  ⚠**据置は想定どおり**＝`O-60` は語彙の欠落ではなく「engine が原文を読む」形を潰す項目なので3計器には出ない。
  **この項目の計器 `census:enginetext` の A群**＝**28行 / 27ハンドラ → 22行 / 21ハンドラ**
  （`BASELINE_SELF_TEXT` も 22 へ払い戻し）。miss は 0 のまま。
  📦**在庫2本**＝**機構 worklist 30項目**（🆕`O-232` を登録＝29→30）｜**実機 残 10件**（🆕`V-140` を登録＝9→10）
  🔧**ゲート（全緑 ✅）**＝golden **3397 / 3397**（3393→3397＝+4本／既存の契約 golden 2本を更新）／
  smoke **10725** 全異常0／fuzz 全0／census **3 / BASELINE 3**／`census:stubs` A群🔴0・C群0／manual-fields 0／
  `census:enginetext` A🔴 **22行**（`BASELINE_SELF_TEXT` も 22）／`census:costtext` A🔴 **0規則**（据置）／
  lint 0 errors／`npm run regen` 完走。
  🖥**実機＝機械判定では不要（§2.2）**＝`src/screens/` は0行・新しい interaction 型も0。
  ただし**3効果が「原文と違う数値／落ちていたゾーン／並び順」で動いていた**ので `V-140` を登録（未実施）。
  🔴**主産物＝自己再帰する受け皿は payload を継続へ積み忘れる**＝`SELECT_TARGET` のあと自分自身へ再入する
  3ハンドラで、`continuation` の STUB に payload を積み忘れると**2周目が fail-closed＝
  「対象は選ばせるのに何も起きない」**無言 no-op になる（既存 golden の反転側が捕捉）。
  🔴**実害**＝①**外れたときの既定値が原文に無い数値**（`POWER_MOD_BY_COLOR_VARIETY` の `-3000` 焼き込み）
  ②**1つの id に2文型が同居**（`TRASH_ALL_SIGNI_AND_KEY`＝`WX07-017` は手札とエナが流れず、原文に無いキーを流していた）
  ③**engine のパターン表が静かな上限**（`GRANT_CHOSEN_ABILITY`＝並び順を無視し、表に無い語彙は無言 no-op）
  ④**原文にその句が無い regex**（`STUB{DRAW}` の `カードをN枚引く` は必ず外れて既定1だった）。
  🔑**一般則**＝①**自己再帰する受け皿は継続にも payload を積む** ②**具体値の既定は原文に無い焼き込みを疑う**
  ③**payload 化したら逆翻訳の固定文言も撤去する** ④**engine のパターン表は表ごと parser へ移す**。
  ⚠**見送り**＝`CONDITIONAL_POWER_BONUS`（live 0・リテラル9本）＝**標本が無いので検証できない。消してもいけない。**
  ✅**ブラスト半径＝効果 変更10・追加0・削除0、予定外0**（＝対象4 family の10効果ちょうど）。

## 付録A. 全体像と Definition of Done

| 層 | 内容 | 検証手段 |
|---|---|---|
| **① 表現 P1** | JSON がカード原文を正しく**表現**する | 逆翻訳一致／同型★0／BEHAVIOR_AUDIT キュー消化 |
| ② 実行 P2 | エンジンが各DSL構文を正しく**実行** | golden型網羅／smoke／fuzz／BEHAVIOR_AUDITで見つかる実行バグの解消 |
| ③ 挙動 P3 | 実ゲームで各カードがルールどおり動く | 実機/自動対戦テスト（`scripts/verifyBattleDrive.mjs`） |
| ④ 対戦体験 | CPU AI がメインフェイズで能動行動し、一人でも通しで遊べる | 実機通し対戦・fuzz重め |

**注意**：①の「逆翻訳一致」は "JSONがテキストを表す" ことのみ保証。実機での正しさ(③)は別。各コミットは**「要実機検証」**を付す。
- [x] 全シートで **同型★ = 0**（`docs/grouped_all.txt`・`node scripts/groupSimilar.mjs --all` で再生成）。**達成・維持中**。
- [x] 「⚠脱落疑い」リストの各カードが、**偽陽性**／**修正済**／**機構待ち（理由明記）** のいずれかに分類済み。**✅2026-06-28＝255枚を全分類**（偽陽性179／機構待ち72／修正済）。`node scripts/_dropTriage.mjs`・明細 `docs/_drop_triage.txt`。
- [x] 残る大型機構（付録C）が実装＋配線済み、または明確にスコープ外と合意。**✅2026-06-28＝B1-B4を全完了**。残るは **C（engine 実機配線・全 R5-R58 と B1-B4 は要実機検証・§5.1）＝P2/P3 スコープ**。
- [~] **逆翻訳機（`scripts/decompileEffects.ts`）の出力品質＝原文一致**（2026-06-30 着手・2026-07-03に主作業の座は次項へ譲った）。英語ID漏れ 582→367（BUGFIXES⑩〜㉒）、レンダラ5系統是正済。残＝§5.5（低優先のテール）。
- [x] **🏁 P1（表現）完了宣言（2026-07-23）**＝語彙センサス高シグナルの逓減限界到達を実測確認（バッチ2〜4の投入前実測枯渇＋生きた parser バッチ全消化）し、残 census 1581 効果を「**§5.3 正式送り 282／粗網のみ偽陽性 116／長テール単発・別節偽陽性 1183**」へ3分類。宣言本文・根拠・以後の運用は [P1_COMPLETION_ROADMAP.md](./P1_COMPLETION_ROADMAP.md) 冒頭の宣言節。以後 census は回帰ゲート（`BASELINE_HIGH＝**宣言当時 1580／現在は §6 恒久指標の1行が正**`）としてのみ運用し、主軸は P2/P3（§5.3 機構・§5.1 実機・BEHAVIOR_AUDIT 継続）へ移行。
- [ ] **BEHAVIOR_AUDIT の要レビュー・キューを逓減限界まで消化**（2026-07-03 着手）。指標＝`node scripts/_bqTriage.mjs` の高シグナル件数（811→285→261→169→129→30…と逓減中）。
- ⚠ **「脱落疑いの件数」は完了指標にしない**（メトリクスが粗く内容修正で減らないため。§4.1参照）。

---

## 付録B. 偽陽性パターン（脱落疑いに出るが**直さない**）— 毎回まず除外

1. **使用条件＋本体**（「このカードは〜の場合にしか使用できない」が前置き）＝条件として正しく表現済み。【真の偽陽性】
2. **CHOOSE/チェインの1文圧縮**＝「以下の[N]つから[M]つ（まで）を選ぶ」で改善済み（BUGFIXES③）。択肢が全部出ていれば機能的には正しい。
3. **REVEAL_AND_PICK / LOOK_AND_REORDER の文法崩れ**＝主要系統は是正済（BUGFIXES上部）。残りは§5.5の低優先テール。
4. **ルール注記**（「（コストのない【出】能力は発動しないことを選べない）」等）＝効果ではない。
5. **アンコール/ベット注記のみ**訳に出ない＝本体が合っていれば正しい。
6. **BET_MECHANIC STUB**＝§5.5（機構待ち）。
7. **owner:any の一括変換は禁止**：POWER_MODIFY/BANISH の `owner:'any'` は大半が正当（「シグニ1体を対象とし±N」＝自他選択／「すべてをバニッシュ」）。原文に明示主語があるものだけ個別是正。
8. **`[STUB:id]` を含むからとスキップしない**：実装済みハンドラのタグ表示。ただしハンドラがカード全体を覆うか（◎）／断片だけで残りを落としたか（実バグ）はタグでは区別不可＝各外れは個別検証。[[stub-means-implemented]]
9. **LIFE_BURST 内 `CONDITIONAL{IS_MY_TURN}`** は実害なし（常時true＋「そうした場合」特別処理）。修正不要。

## 付録C. 触らなくてよい/枯れた系統（調査済み）

- 強制アタック＝実装済み（未配線は WX12-010 複雑レゾナのみ）。BURST丸ごと欠落＝残0。保護系キーワードのowner誤り＝残0。
- 同型★（`grouped_all.txt`）＝**枯れた・常に0維持**。残1件 `WX04-056` は無害な表現差（任意）。
- 「あなたのアタックフェイズ開始時」系（self約407件）は**全再生成禁止**（約90枚退化）。個別にtiming/triggerScopeを直す。

### 付録C-2. 残・大型機構オーナー表（ほぼ完了の台帳）

着手前に**この表の「状態」を `着手中(担当名)` に更新してコミット**（重複防止）。実装の型は §3「機構実装の型」に従う。

| 機構 | 影響 | リスク | 状態 |
|---|---|---|---|
| 引用AUTO付与（`GRANT_QUOTED_AUTO_ABILITY`） | 中 | 中 | **表現完了＋engine精緻化(B4)着手済**＝引用【自】/【常】能力を実発火（自場シグニ・ターン限定・parse成功時のみ）。残＝permanent/相手付与対応・誤パース是正（約30枚は原文に引用無しparser案件）。⚠要実機検証 |
| ~~SET_TRAP／動的閾値フィルタ／遅延条件トリガー／《相手ターン》《自分ターン》AUTO基盤／ビート機構Phase1-7／傀儡場出し汎用化・levelLteLastProcessed~~ | — | — | **✅完了**（詳細 [PLAN_DETAIL.md](./PLAN_DETAIL.md) 付録C） |
| engine未配線 timing 群の実機配線 | 大（~15 timing・R33-R58） | 高 | **✅C1全配線完了**。残るは実機検証のみ（§5.1参照）。 |

実装済み機構の履歴：コスト増加・ライフクラッシュ履歴・LOOK_PICK_CHAIN field宛先・リコレクト系統・改造素材機構・引用能力付与型・保護/制限系STUB・アーツコスト軽減句 は `BUGFIXES.md` 参照。

---
**関連**：[DESIGN.md](./DESIGN.md)（設計方針）／[PLAN_DETAIL.md](./PLAN_DETAIL.md)（消化済み履歴）／[PLAN_PROGRESS.md](./PLAN_PROGRESS.md)（過去セッション要約）／[BUGFIXES.md](./BUGFIXES.md)（修正記録）／[BEHAVIOR_AUDIT.md](./BEHAVIOR_AUDIT.md)／[SEMANTIC_AUDIT.md](./SEMANTIC_AUDIT.md)／[CODEX_GUIDE.md](./CODEX_GUIDE.md)／[BATTLE_CONTROLLER.md](./BATTLE_CONTROLLER.md)／[effects-json-guide.md](./effects-json-guide.md)／[STUBS.md](./STUBS.md)
