# Codex 指示ガイド（Claude 用）

> **このファイルの位置づけ**：ユーザー指示により「実装は Codex CLI・Claude は指示と検証」という分担で回す場合に、
> **Claude が指示書を書く前に必ず最初に読む**運用ドキュメント。
> 内容はすべて **続き208〜211 の実セッションで起きた事故と、その対策として効果が確認された指示**に基づく。
> 一般論は書かない。効かなかった/不要だった指示は削る。**新しい知見が出たら §7 の実績表に追記して更新すること。**

関連：作業計画は [PLAN.md](./PLAN.md)（§3 モデル分担・Opusタスク表）、修正記録は [BUGFIXES.md](./BUGFIXES.md)。

---

## 1. 役割分担（これを崩さない）

| | 担当 |
|---|---|
| **Claude** | ①現状の**実測**（簿記は古い前提で疑う）②バッチのスコープ決定 ③指示書の作成 ④**検証** ⑤簿記（PLAN/BUGFIXES）⑥commit / push |
| **Codex** | 実装と報告のみ。**簿記も commit もしない** |

**Codex に簿記をさせない理由**：続き210 で Codex は PLAN §4 の要約入れ替えまで実施した。
**レビュー前に簿記が済むと、採用を差し戻したときに簿記が実態とズレる。**
以降は指示書で明示的に禁止しており、続き211 で遵守を確認済み。

---

## 2. 起動方法

`.claude/settings.local.json` の `permissions.allow` に `Bash(codex *)` / `Bash(CODEX_HOME=* codex *)` を追加済み。

**⚠️ 2026-07-24 に確定した正しい起動法（この節は 2026-07-19 の記述を上書き。別セッションの Claude 実測＋codex-cli 0.144.5 で確認）**：

```bash
CODEX_HOME="C:/Users/zerom/.codex-work" codex exec -C "C:/Users/zerom/source/WixossReact" -o "<scratchpad>/codex_taskNNN_last.md" - < "<scratchpad>/codex_taskNNN.md" > "<scratchpad>/codex_taskNNN.log" 2>&1
```

**🆕 2026-08-08（続き377m）に実測で判明した4点＝この節の上書き**：
- ⚠**`.codex-work` / `.codex-personal` は消えていた**（`~/.codex` のみ存在・`sandbox_mode` 未設定）。**再作成手順**＝`C:/Users/zerom/.codex-work/config.toml` に `sandbox_mode = "danger-full-access"`（＋`model` / `model_reasoning_effort`）を書き、**`~/.codex/auth.json` をコピー**する。⚠**認証ファイルのコピーは Claude 側の分類器にブロックされる**ので、ユーザーに `! cp "C:/Users/zerom/.codex/auth.json" "C:/Users/zerom/.codex-work/auth.json"` を1回実行してもらう。
- ⚠**リポジトリのパスが変わっている**＝`C:/Users/zerom/source/WixossReact`（旧記載の `/c/Users/zerom/WixossReact` は無効）。
- ⚠**`codex exec` は `--ask-for-approval` を受け付けない**（非対話なので不要・付けると **exit 2**）。TUI 用のフラグ。
- ⚠**`Bash(codex *)` は前方一致ルール**＝`cd ... && VAR=... && codex ...` のような**複合コマンド**や `$(cat ...)` の**コマンド置換**を含めると一致に失敗し、auto-mode 分類器に回って「無人で書き込み権限を持つエージェント起動」として**ブロックされる**（PowerShell ツール経由も同様）。**`codex`（または `CODEX_HOME=… codex`）で始まる単体コマンドにし、指示書は `-` + stdin リダイレクトで渡す**。`-o` で最終レポートだけを別ファイルへ出すと、検証側が巨大ログを読まずに済む。
- ⚠**`-s workspace-write` を付けると Windows でサンドボックスヘルパーが落ちる**ことを再確認した（`windows sandbox: orchestrator_helper_exit_nonzero: setup helper exited with status -1073741502`）。**フラグ無しが正解**という下の記述は正しい。

- **サンドボックスフラグを一切付けない**のが正解（`-s` も `--full-auto` も `--dangerously-bypass-*` も付けない）。サンドボックスは **config の `sandbox_mode` で決める**。`-C <dir>` で作業ディレクトリ、指示書は stdin (`-`) で渡す。
- **ログイン切替**＝`CODEX_HOME` を差し替えるだけ：`.codex-work`（有料版）／`.codex-personal`（無料版）。両者とも config トップレベルに **`sandbox_mode = "danger-full-access"`**（2026-07-18 に統一）＝**フラグ無しで書込可能**。既定 `.codex` は `sandbox_mode` 未設定。
- **⚠️ 最重要の落とし穴**＝**`-s workspace-write` や `--full-auto` を付けると config の `danger-full-access` を上書きし、Windows では workspace-write が実質 read-only になって最初の書込パッチが拒否される**（`patch rejected: writing is blocked by read-only sandbox`）。2026-07-24 に Opus セッションがこれを踏み、「codex-work が read-only」と誤診断した。真因は **CLI のサンドボックスフラグが config を上書きしたこと**。**フラグを付けない限りこの壁は発生しない**。
- **`--dangerously-bypass-approvals-and-sandbox` は不要かつ逆効果**：書込は通るが Claude Code 側の auto-mode classifier が**ハードブロック**する（危険フラグ文字列を検出）。上記フラグ無しコマンドなら**危険フラグが無いので classifier に素通り**し、`.claude/settings.local.json` の `autoMode.allow` 追加も不要（2026-07-24 に足した自然文ルールはフラグ無し運用では無用＝害はないが必須でもない）。
- ⚠🆕**2026-08-08（続き378）＝Claude Code の Bash ツールは `run_in_background: true` でも `timeout` の上限（**600000ms＝10分**）で強制終了する。** 実測＝実装・`build:effects`・golden・`regen`・`gates`・`census:wiring` まで終えた**最終レポート生成の直前**で kill された（**ワーキングツリーの成果物は無傷**だったので Claude 側の検証で代替できた）。**対策**＝①長い委譲は**ユーザーに `!` で起動してもらう**（ハーネスのタイムアウト圏外。その場合タスク通知は来ないので Monitor でログ末尾の `tokens used` を待つ）②または**レポートを最初から Claude 側検証で代替する前提**でスコープを切る（§7 のチェックリストは元々レポートを検算する内容なので成立する）。⚠kill されたら**まず `git status` と per-effect diff で「編集の途中で切れていないか」を確かめる**（今回は計測コマンド実行中だったので無傷）。
- ⚠🆕**2026-08-09（続き386）＝ハーネスの10分 kill が殺すのは「ラッパー」だけで、`codex.exe` 自身は生き残って完走する。** 実測＝タスク通知が `killed` で来た後もログが 2.8MB→135MB まで伸び、**`-o` の最終レポートも後から出力された**。⇒ **kill 通知が来ても「失敗」と決めつけない**＝`tasklist | grep codex.exe` でプロセス生存を確認し、生きていれば待つ（`until [ -f <-o の出力> ] || ! tasklist | grep -qi codex.exe; do sleep 20; done`）。⚠**その間に Claude 側で `build:effects` 等を走らせると競合しうる**ので、生存確認したら検証は待つ。⚠**kill 時点の `_held_fresh.json` は stale**（採用直後に再生成される前で止まる）＝**held が +12 に見える**ことがあるので、**必ず `build:effects`→`heldReview` を再実行してから読む**（実測 133→117）。
- 🔑🆕**2026-08-09〜10（続き397〜402・6連投）＝起動法の現状はこれが正**（続き377m の記述を一部上書き）：
  - ⚠**`.codex-work` の認証は失効していた**（`refresh_token_invalidated` / `token_revoked` で起動直後に exit 1）。`auth.json` の日付を見て古ければ疑う。
  - 🔑**`CODEX_HOME="C:/Users/zerom/.codex"`（本体）で、サンドボックスフラグ無しのまま書き込みが通る**（続き397 でプローブ実測＝`tmp_probe_write.txt` の作成に成功）。理由は `~/.codex/config.toml` に **`[windows] sandbox = "elevated"`** があり、**`[projects.'c:\users\zerom\source\wixossreact'] trust_level = "trusted"`** も入っているため。⇒ **`.codex-work` が使えないときは `.codex` にそのまま投げてよい**（グローバル設定の書き換え不要）。
  - 🔴**`-c sandbox_mode="danger-full-access"` は Claude Code 側の auto-mode 分類器にハードブロックされる**（危険フラグ文字列の検出）。`--dangerously-bypass-*` と同じ扱い＝**コマンドラインに "danger" を出さない**こと。`-c model_reasoning_effort="high"` のような無害な `-c` は通る（`.codex` の既定は medium なので、`.codex-work` と条件を揃えるならこれを付ける）。
  - ⚠**Windows `0xC0000142`（`orchestrator_helper_exit_nonzero` / `-1073741502`）は続き400〜402 で3回連続発生**し、**codex 側が最終工程（`docs/BUGFIXES.md` 追記・`groupSimilar --all`・エンコーディング検査・`heldReview` 再測）を実行できなくなった**。実装とゲートは完了しているのに報告だけ書けない形で止まる。⇒ **指示書に「症状が出たら早めに BUGFIXES を書いて確定させ、残りは検証側へ渡す」と明記**し、**検証側が引き取る前提でスコープを切る**と成立する（§7 のチェックリストは元々それを検算する内容なので問題ない）。
  - ⚠**10分 kill 後も `codex.exe` は完走する**（続き400/401/402 で3回とも実証。ログは 4.6MB→17MB まで伸び、`-o` は後から出た）＝続き386 の記述どおり。待機は `until [ -f <-o> ] || ! tasklist | grep -qi codex.exe; do sleep 20; done` を `run_in_background: true` で回す。
- **`run_in_background: true` で起動**する（数十分かかる）。ただしユーザーが `!` で起動した場合はタスク通知が来ないので、**Monitor ツールでログ末尾の `tokens used` 出現を待つ**（`while ...; do grep -q "tokens used" <log> && break; sleep 15; done`）。
- 🆕**2026-08-11（続き435）＝Codex のサンドボックスは Supabase 等の外部ネットワークへの出口が塞がれている**（`net::ERR_NETWORK_ACCESS_DENIED`）。**`sandbox_mode = "danger-full-access"` はファイル書込を許可するだけでネットワークは別軸**＝`scripts/verifyBattleDrive.mjs` のような**実ブラウザ＋ライブ Supabase を要する検証系タスクは Codex 側では実行できない**（Playwright Chromium も既定インストールが無く、`PLAYWRIGHT_CHANNEL=chrome` に切り替えてもネットワーク遮断の方が先に効く）。**この種のタスクは「Codex にシナリオコードだけ書かせ、実行検証は必ず Claude 側（ネットワークアクセスあり）で行う」**という前提で指示書を書くこと（§1 の役割分担どおりだが、検証系タスクでは Codex 報告の「実行結果」欄が最初から空＝`BLOCKED` になる想定で読む）。
- 進捗を見るときは `git status --porcelain` と `git diff --stat`、ログ末尾は Read ツールで直接（`awk`/`tail` パイプは classifier に弾かれやすい）。
- ログは巨大（1〜10MB）。**全部読まない**。`grep -n "tokens used" <log>` で最終レポート位置を出してから Read で該当箇所を読む。

### ⚠️ 自動コミットフックとの相互作用（重要）

`.claude/settings.local.json` の `PostToolUse` フックが **Write/Edit のたびに `git add -A` + commit** する。
**scratchpad（リポジトリ外）へのファイル書き込みでも発火し、リポジトリ全体をコミットする。**

- 実際に続き210 で、検証スクリプトを書いた時点で **Codex の未検証作業が `061f423b` として自動コミットされた**。
- 実害は小さい（未 push・ローカル）が、**検証時の diff ベースラインがズレる**。
- **対策**：検証を始める前に `git log --oneline -3` でベースラインとなるコミットを確定し、
  `git show <baseline>:path` で比較する。`HEAD` を使うと自分のフック起因コミットと比較してしまい**差分ゼロに見える**。

---

## 3. 指示書を書く前に Claude がやること（省略禁止）

### 3-1. 簿記を信用せず実測する
**PLAN の残件数は3回連続で古かった**（続き207・209・211）。必ず数え直す。

```bash
npm run build:effects          # docs/_partial_report.txt を再生成
head -8 docs/_partial_report.txt
npm run census:timing          # timing 系なら
```

### 3-2. ベースライン数値を取る（指示書に必ず書く）
```bash
npm run gates                  # golden / census / smoke / fuzz / lint
node scripts/groupSimilar.mjs --all   # 同型★
```

### 3-3. **Codex が前回言った「ブロック理由」を機械検証する**
これが最も価値を生んでいる。**2回連続で Codex の見立てが外れていた。**

- **続き210→211**：Codex は「前段が STUB だから `lastProcessedCards` に残らない」と説明したが、
  前段ステップを機械分類すると **20件は plain action（TRASH/BANISH/REVEAL/DRAW 等）で engine は記録済み**。
  実体は `LAST_PROCESSED_*` の**語彙不足**だった → 絞って投げ直し10件採用。
- **続き211→212**：Codex は「`OPTIONAL_COST` が結果を記録しない」と説明したが、ハンドラを読むと
  **エナ色コストの CHOOSE を出すだけでアクションを一切実行していない**。真因は
  **任意アクションが汎用 STUB に潰されて消えている**こと。

**やり方**＝対象効果の JSON を読み、条件の直前ステップの型を集計するワンオフを書く（`tmp_*.mjs`）。
**「Codex の説明」ではなく「コードと JSON の実測」からスコープを決める。**

### 3-4. スコープを切る
- **1バッチ 10〜30効果**。全部投げない。
- **機構が同じものだけ束ねる**。異質なものを混ぜると検証不能になる。
- 群分けして渡す（本命／既存機構で足りる分／構造が別で見送ってよい分）。

---

## 4. 指示書テンプレ（この構成を維持する）

```markdown
# 依頼：<タスク名>

作業ディレクトリ / HEAD / 作業ツリー clean の明示
まず読むもの（CLAUDE.md・PLAN.md §4・BUGFIXES.md 先頭）＋「あなたが続きNNN でやった作業の続き」

## 背景＝バグの正体
（なぜ実害なのか。逆翻訳では気付けない偽陰性なら、それを明記）

## ⚠️ 真因の見立てを訂正する（Claude の実測結果）
（3-3 の分析。Codex の前回の説明が外れていたら、根拠つきで訂正する）

## 今回のスコープ＝この N 効果だけ
（effectId と原文の条件節を表で列挙。群分けする）

## 既存機構を最優先で流用すること
（既存の型／ヘルパ名を具体的に挙げる。「新しい型を乱立させない」）

## ⚠️ ガードレール
（下の §5 から該当するものを選んで書く）

## ゲート
（ベースライン数値を明記。census は増やすな・減ったら BASELINE_HIGH も更新、等）

## 禁止事項
- commit も push もしない
- docs/PLAN.md と docs/PLAN_PROGRESS.md を編集しない（＝簿記をしない）。BUGFIXES.md への追記は可
- スコープ外に手を出さない
- 「全部直した」ことを優先しない。確実に正しい分だけ採用する方が価値が高い

## 報告フォーマット
（§6 の必須項目）
```

---

## 5. 実績のあるガードレール（なぜ入っているかの記録つき）

**そのまま流用してよい。理由を消さないこと**（消すと次に誰かが外してしまう）。

| # | ガードレール | 入っている理由（実際の事故） |
|---|---|---|
| 1 | **1効果ずつ原文照合してから条件を書く。まとめて機械置換しない** | PLAN が警告する「無検証置換で約90枚退化」の失敗モード |
| 2 | **engine の実装を読んで「本当に評価できるか」を確認してから採用** | parser が条件を吐いても engine が解決できないと**今度は常時偽＝過小実行**。続き209 で `WXK06-031` が実際に踏みかけた |
| 3 | **golden は成立/不成立の両方向を固定** | 片方向だけだと当該バグの再発を検知できない |
| 4 | **見送り判断そのものも golden で固定** | 続き209 で `WXK06-031` の非採用をテスト化＝将来うっかり採用されるのを防ぐ。良い設計なので継続させる |
| 5 | **全カード生パース diff を取り outlier 0 を機械確認**。ただし🆕**「held を元の数値へ戻して終わる」と書いてはいけない**＝**増分は1件ずつ原文照合して採用/据置を判断させる** | regex 拡張の波及。続き208 では未申告のまま **held が40枚増**していた。🆕続き316 では逆に、Claude が指示書へ「held は 250/98 に**戻して終わる**」と数値で固定したため、codex が `カードを２枚引き(?=、【エナチャージ２】)` のような**特定カードの本文を埋め込んだ先読み**で regex を絞り、**改善2件（`WXK04-006-E1` の DRAW 脱落・`WX24-P3-022-E1` の丸ごと UNKNOWN）を取りこぼした**。数値固定は「改善を捨てる」インセンティブになる |
| 5e | 🆕**既存の語彙・フィールド・STUB id を「名前が近いから」で借りない。借りる前に、その語彙を**消費する側の実コード**（executor の分岐／collector の該当ループ）を読む。** 特に「表せないので no-op にする」目的で既存 STUB id を使うのは危険＝**多くの id は実装を持っている**。真の no-op が要るなら**engine 分岐を持たない専用の宣言 STUB**を作る（`execStub` の既定は `[STUB: id]` ログのみ・`STUBS.md` に未実装として載る＝計器を甘くしない） | 3バッチ連続で発生＝①(lxx)：`triggerCondition.byOwnEffect` を「あなたの効果によって相手が捨てたとき」に流用しようとしたが、engine の同フィールドは「**捨てた本人**の効果が原因」という別軸で、any_opp watcher ループは**問答無用で非発火**＝付けると4効果が恒久 no-op（Claude が踏んで撤回）②(lxxii)：`THIS_ATTACK_PHASE` で grep して0件だったので「物理削除は存在しない」と断定したが、実体は別ファイルの `clearEndOfAttackPhaseDelayedTriggers`＝**探す文字列がその層に現れる保証が無かった**（Claude・codex が訂正）③(lxxiv)残1：defer マーカーに `UNKNOWN_NESTED` を流用したが「このシグニを任意でトラッシュに置く」実装だった（codex・検証側が差し替え） |
| 5c | 🆕**カード固有の本文を埋め込むのは「先読み」だけでなく「分岐ガード」にも出る。**「この分岐に入る条件」を原文 regex で書かせない＝**木の形（action.type / 部分木の存在）で判定させる**。原文パターンでしか書けないなら、**判定を関数へ切り出して1箇所から呼ぶ**形にさせる | 2026-08-01 (lxxiv) で codex は `parseActionTextInner` 冒頭に `WXDi-P05-052` 専用の分岐（`①…デッキの上からカードをN枚見る` かつ `②…センタールリグは「【自】`）を置いた。**先回りが必要という判断自体は正しかった**（後段の引用能力抽出が全文を奪う）。同バッチでは `parseSpellEffect` の分岐条件も原文 regex で書かれ、その分岐が中で `parseStatus` を再代入するため**付与能力を持たないカードまで入って `spellFb` 由来の PARTIAL を握り潰す**形になっていた |
| 5d | 🆕**「そのバッチで実害だったもの」を実行して検査する E2E golden を、対象効果ごとに最低1本書かせる。**JSON 構造 assert は「エンコードが正しい」ことしか見ない | 2026-08-01 (lxxiv) の実害は「任意コストが落ちて本体が無償実行される」ことなのに、codex の golden 2本は JSON に特定の文字列が含まれるかしか見ておらず、**ゲートが本当に本体を止めるか**を検査していなかった（検証側が `WXK11-008` をエナ0で実行する E2E を追加）。§8「コストゲート2」「バッチ5c第1波」から継続する同一の弱点 |
| 5b | 🆕**held に落ちた fresh の変化は live JSON に映らない。**「ガードを外しても live diff 0 だから不要」は**誤った判定**＝必ず `heldReview` の**カード集合**を before/after で差分する | 続き316 で Claude が live diff だけで「codex のガード3つは何も守っていない」と判断しかけた。実際は held +3 で、うち2件は**採用すべき改善**だった（結果的に採用したが、判定手順としては誤り）|
| 6 | **採用効果ごとに「逆翻訳文 _全体_ が原文と一致するか」を判定させる** | 続き209 で Codex は逆翻訳を報告に載せながら**原文との食い違いを指摘しなかった**（`WXEX1-03-E1` の `owner:'any'`、`WXK02-028-E3` の `targetsTriggerSource`）。検証側が parser の照応バグを別途発見する羽目に。**この指示を入れた続き210 で12効果、続き211 で7効果を自主申告するようになった** |
| 7 | **前回の見送り理由が今も有効か再判定させる** | 続き211 で Codex が**自分の見送り理由を2件「訂正」**（`WD20-018`/`WD21-020`）。指示に書くと機能する |
| 8 | **既存型へのフィールド追加を優先。新しい型を乱立させない** | 続き210 は新設1型のみ、続き211 は新設ゼロで達成できた |
| 9 | **JSON 手パッチは parser 同修正か MANUAL 化とセット**。`effects_*.json` はミニファイ1行＝`JSON.stringify(j)` で書き戻す | 片方だけだと held 計器が増える。pretty-print は巨大 diff |
| 10 | **`MANUAL`/`PARTIAL` を含むカードは `build:effects` が curated を温存し heldReview に載らない**（`PRESERVE_STATUSES`）と明記 | 続き209 で `WXEX1-03` がこれに該当し、直接 JSON パッチが必要だった |
| 11 | **既存挙動を広く変える一般化はしない**（regex は具体文型に限定） | 続き207 の「plain な『場に出してもよい』の optional 化は据置」判断 |
| 12 | **変更は per-card ではなく per-effect で報告させる** | 続き212 で Codex は「変更8カード・outlier 0」と正しく報告したが、**採用はカード単位なので同じカード内の別効果3件も一緒に変わっていた**（`PR-427-E3`／`WXDi-D09-H17-E2`／`WXDi-P11-039-E2`）。今回は3件とも改善方向だったが、**退化していても報告に現れない**。カード単位の diff だけ見ていると見逃す |
| 13 | **`buildEffectsJson.ts` に force-adopt リストを仕込むの禁止。採用は `heldReview --adopt`（or 検証済み fresh の直パッチ）経由** | 続き251 で Codex が恒久 `FORCE_ADOPT_BOARD_ZONE_BATCH3` を build 本体に追加＝**held 計器の恒久バイパス**になり、PRESERVE（MANUAL/PARTIAL 同居）カード4枚を含む**兄弟効果12件が無審査採用**された（うち退化4件＝条件脱落2・別STUB化1・MANUAL剥がし1）。しかも本人の報告は「変化33のみ・outlier 0」＝**数値申告が外れた初のケース**（force-adopt の副作用は自分の diff 集計にも映らなかった） |
| 18 | **効果データの値の誤りは JSON を `effectId` アンカーで外科パッチする。共有関数（`mergeManualEffects` 等）にカード固有テーブルを埋めるのは §5-13 と同型の隠しパッチで禁止** | 共有経路への後付けは正規データと検査ゲートを迂回し、`parseStatus` 等の計器分類を歪め、将来 JSON を訂正しても永久に上書きする隠れ状態になる。 |
| 14 | **新フィールドを既存 action 型へ足すときは「型宣言＋executor 対応」の両方を確認させる**（JSON は型検査されない） | 続き251 で `TRANSFER_TO_DECK` に `targetsLastProcessed` を付けたが**型にも executor にも存在しない死フラグ**＝実行時は無フィルタの独自選択へフォールバックし過剰効果化（WXK09-066）。tsc は JSON データを検査しないため素通りする |
| 16 | **`manualEffects.ts` の override は JSON の同じ `effectId` を完全置換するため、元 JSON のトップレベルフィールドをすべて明示し、`npm run check:manual-fields` で脱落0を確認する** | 続き293の「runtime に `mergeManualEffects` を通さない」という診断は誤り。App は JSON を `card.effects` へ格納するが、実機は `BattleScreen` の `buildEffectsMap`（`BattleScreen.tsx:671`→`effectParser.ts:7140`）で必ず `mergeManualEffects` を通すため、manual は実機へ届く。真の再発条件は完全置換で `cost`/`condition`/`timing`/`appearanceCondition` 等を書き落とすこと。JSON 再生成が必要なのは JSON を直接読む計器・成果物へ curated 値を焼く作業であり、manual runtime 修正の必須条件ではない。続き293で JSON 採用が必要だったという事実は残るが、その4効果が実機で不発だった理由は当時の記録と現行コードからは確定できない。 |
| 17 | **カード種別ごとにコスト・使用条件の評価経路を実コードで確認してから「実害解消」と書く** | スペル／アーツ／ピースのエナはCSV `Cost` 列、effectの `cost.energy` は【起】能力系が読む。effectへフィールドを足したら、そのカード種別の実経路が読むかを `evalUseCondition(`／`parseGrowCost(` のgrepで確認する。関数直呼びやeffect object上の算術は実経路の前後実測として扱わない。 |
| 17 | **既存 golden が「原文と食い違う実装」を固定している可能性を疑わせる。採用効果の既存 golden は assert の向きまで原文照合させる** | 続き293 で `§6.3 C WX24-P4-045…` が `otherState.life_cloth` を期待しており、**相手にライフを与える真逆の実装を golden が守っていた**。codex は「既存 golden が緑だから正しい」と扱って JSON をそれに合わせ、報告にも「実挙動は一致」と書いた。**緑の golden は正しさの証明ではない**（原文照合の代替にならない） |
| 15 | **採用カードごとに「発火/適用に使われる collector・経路の関数名」を報告させる**（トリガーカードなら収集関数名、状態消化ならその消費経路を全部） | 2026-07-23 §6.3除外で WD22-035-G が **collectTurnTriggers 未拡張のまま採用され永久不発**（トラッシュ在中 watcher 走査を WX21-Re06 用の collectFieldTriggers にだけ実装し満足）。同バッチでターン終了時除外も3経路（human END/confirmEndDiscard/CPU END）中1経路にしか配線せず。**ガードレール2（engine が評価するか確認）を指示書に書いても、同型の配線が複数箇所に跨るとき1箇所で満足する**＝関数名を書かせると残りの穴が可視化される |
| 19 | **日本語を書いたら「そのバッチで書き換えた全ファイル」のエンコーディングを検査させる**（`git diff --name-only` 全件・`U+FFFD` と **3文字以上**連続の `?` と **先頭 BOM（`efbbbf`）** をベースライン比で数えて新規増0。`.ts` の `??` は nullish coalescing なので2文字だと誤検出する） | 2026-07-29〜30 の (xxix) セッションで**3回**発生＝**codex が新規に書いた日本語だけが CP932 で潰れる**（第10波は live JSON の filter が `"cardType":"???"`／第11波は `BASELINE_HIGH` のコメント冒頭。既存部分は無傷）。`manualEffects.ts` 側は正しいままなので**実機は正常・golden も census も緑のまま素通り**し、**ゲートに検出器が無い**（typecheck も lint もコメント内の文字化けを通す）。第10波の教訓で検査を入れたら、codex は `public/data/*.json` にしか適用せず第11波で `.ts` を取りこぼした＝**対象範囲を「全変更ファイル」と明示する必要がある**。🆕**2026-07-30 のエクシード本体バッチでは BOM（U+FEFF）が2ファイル（`scripts/vocabCensus.ts`／`src/screens/BattleScreen.tsx`）に混入した**＝U+FFFD と `?` 連続だけを見る検査では**BOM は素通りし、typecheck も lint も BOM を通す**ため全緑のまま着地していた（Claude が検出・除去）。**検査項目に先頭3バイト `efbbbf` を必ず含めること。** |
| 20 | **「支払い地点・配線先が複数ある」機構は、Claude が投入前に**その全箇所を表で列挙**して指示書に書く**（例＝任意【出】のコストは engine の `OPTIONAL_COST` と `BattleScreen.executeSigniOnPlayCost` の2箇所） | (xxix) 第12波＝codex は engine 側にだけ「コスト支払いの記録」を書き、**通常召喚では動的 filter が `noMatch` に倒れて効果が丸ごと no-op** だった。対象はシグニなので**通常召喚こそ主要経路**。**golden 緑・census 減少のまま実機だけ壊れる＝計器に一切映らない**（BattleScreen は React で golden から叩けない）。§5-15 の一般形だが、**関数名を表で名指しした第13波以降は codex が自発的に両方へ配線した**＝一般論では足りず箇所の列挙が要る。代替の lock-in は「参照する側と払う側の対応」をデータ不変条件として golden に固定すること |

---

## 6. 報告フォーマット（必須項目）

**「やらなかったことの申告」を最重要と明記する。** これを書くと質が上がる。

> ⚠ **held/parserWorklist の申告値は「報告直前に `npm run build:effects` → `node scripts/heldReview.mjs` を再実行した実測値」であることを義務付ける**。続き252 で Codex は stale な held ファイルの読み値（74）を申告し、実測は 86 だった（parser 未修正の JSON 直パッチ21件ぶんのドリフトが不可視化）。

1. 触ったファイルと各1行の理由
2. **調査結果**（今回の機構が成立する前提を満たすか。効果ごとに）
3. **採用した効果の全件**＝`effectId ／ 原文の条件節 ／ 生成 JSON ／ 逆翻訳文全体 ／ 原文と一致するか`
4. **見送った効果の全件＋理由**（過去バッチの見送りの再判定結果も含める）
5. **条件以外で見つけた原文との食い違い**（0件なら「0件」と明記）
6. ゲート数値（golden / census / 同型★ / smoke / fuzz / lint warning 数 / 残件数）
7. **生パース diff の変化集合と outlier 件数**
8. **held バケットの増減**と **lint warning の増減** ← 続き208 で両方とも申告漏れがあった

---

## 7. Claude 側の検証チェックリスト（Codex の報告を鵜呑みにしない）

**Codex の数値申告はこれまで一貫して正確**だった。**外すのは「副作用」と「周辺の食い違い」**。そこを重点的に見る。

```bash
# 0. ベースライン確定（フック起因コミットに注意＝§2）
git log --oneline -3

# 1. ゲート独立実行
npm run gates
node scripts/groupSimilar.mjs --all        # 同型★0

# 2. JSON 変更範囲の機械 diff（申告と一致するか）
#    ※ HEAD ではなくベースラインコミットと比較する
node -e "…全 effects_*.json を <baseline> と比較して変更カードを列挙…"

# 3. held バケットの集合/枚数 diff
git show <baseline>:docs/_held_review.txt > $TEMP/h.txt
#    バケット別枚数を比較。新規増があれば中身を確認

# 4. lint の増減（ベースラインの warning 数と比較）
```

**目視で必ずやること**：
- **採用効果の条件を1件ずつ原文と突き合わせる**（閾値の向き・主語・「枚/体/種類」の区別）。
  実例：`WX06-018` は原文が3分岐（1枚/2枚/3枚→パワー3000/8000/15000）で `eq 1/2/3` が要る。
  「N**種類**」は `distinctName`、「N**枚**」は素のカウント。**ここは Codex も間違えうる**。
- **新規追加された型/フィールドを engine が実際に評価しているか**コードで確認（過小実行の検出）。
- **Codex が「一致」と書いた逆翻訳を1〜2件サンプリングして自分でも原文照合**する。

**計器で裏を取る**：held の該当バケットが狙いどおり減っているか。
例＝続き210 で `+CONDITIONAL +IS_MY_TURN` バケットが **17→4**。これが動かないなら直っていない。

---

## 8. これまでの実績（Codex の傾向を読むための記録）

| 続き | タスク | 採用/対象 | golden | census | Codex の申告品質 |
|---|---|---|---|---|---|
| 208 | timing[C] 2クラスタ | 4/4 | 478→482 | 1945維持 | **held +40 と lint +4 を未申告**。数値は正確 |
| 209 | (xxii) この方法で family | 5/約45 | 482→484 | 1945→1941 | 見送り理由は具体的。**逆翻訳と原文の食い違いを未指摘**（検証側が parser 照応バグを発見） |
| 210 | (xxii) 属性判定 family | 20/30 | 484→486 | 1941→1933 | **条件外不一致12件を自主申告**。ただし**検証前に PLAN 簿記を実施** |
| 211 | (xxii) `LAST_PROCESSED_*` 語彙 | 10/20 | 486→488 | 1933→1929 | **簿記禁止を遵守**。**自分の見送り理由を2件訂正**。条件外不一致7件を申告 |
| 212 | (xxii) `OPTIONAL_COST` 前段 | 8/12 | 488→491 | 1929維持 | 遵守事項は継続。条件外不一致5件を申告。**新弱点＝per-card 報告のため同カード内の別効果3件の変化を申告せず**（→ §5-12） |
| 397 | §6.3 C 第4波 裏向きフリップバック | 4/4 | 1682→1686 | 882維持 | **指示の見立て（`face_down_signi` を使う／ターン跨ぎは表せない）を実コードで訂正**し、`field.facedown_signi` と専用配列で二重遅延まで実装。3経路×両プレイヤーの配線を関数名で申告 |
| 398 | §6.3 E-1 引用付与 | 1/2 | 1686→1687 | 882維持 | **「engine 変更不要」という指示の誤りを E2E で検出**（`collectEffectImmuneSigni` が SEQUENCE 非再帰）。新ゲートを5条件の完全一致で限定し既存8効果の不変を自主申告。もう1件は理由つき defer |
| 399 | §6.3 E-1 解除コストつきアタック制限 | 1/1 | 1687→1688 | 882維持 | 前回 defer を撤回して実装。**指示の「現行は `blocked_actions` 軸」という誤りを訂正**（実際は `keyword_grants`）。配線9箇所を関数名で申告 |
| 400 | §6.3 E-2 `WX25-P3-023-E2` | 1/1 | 1688→1693 | 882維持 | **構造だけでは恒久 no-op** と自力で発見し collector を拡張。`UNTIL_OPP_TURN_END` の CPU 経路欠落も是正。⚠`0xC0000142` で最終3工程が書けず検証側が引き取り |
| 401 | §6.3 E-2 permanent 引用付与 | 3/4 | 1693→1696 | 882維持 | **①はベースライン時点で既に修正済み＝指示の誤りを訂正**して JSON を触らず。新型2種（`GRANT_PLAYER_ABILITY`／`targetOwner`）を新設。⚠同じく `0xC0000142` |
| 402 | §6.3 E-2 `WX20-028` 多重アクセ | 2/2 | 1696→1698 | 882→880 | **型移行（案a）を選び tsc で追従漏れゼロを立証**。再生成時に旧 no-op を復元する `scripts/fixLrigColorFilters.mjs` のハードコードも自力で発見・除去。⚠同じく `0xC0000142`。**held 増分2件の判定は検証側が実施し採用** |
| 213 | 16 timing[C] 3系統 | **9/9** | 491→496 | 1929維持 | **§5-12 が効いて巻き添え0**。「engine が収集しなければ発火しない」と書いたことで**engine 側の要拡張を自分で発見**（`ON_GUARD` ルリグ変種）。検出方式も的確（`trapActivated` 明示フラグで発動/破棄を弁別） |
| 249 | ROADMAPバッチ1第1波（TURN_OWNER＋LIFE_COUNT） | 25/27 | 579→584 | 1817→1799 | 数値・見送り理由とも正確。**唯一の穴＝新分岐1本がカード無ゲート**でスコープ外 WX16-Re20 に誤方向 fresh ドリフト（採用されると「ライフ2枚以下でしか使えないアーツ」化）＝検証側が held バケット diff で検出し `STATE_HOIST_BATCH1_CARDS` ゲートで是正。**指示書に「IS_MY_TURN 3重多義ゆえ実評価化禁止・新型 TURN_OWNER」と設計拘束を書いたのが効いた**（executor 慣例を壊さず着地） |
| 250 | ROADMAPバッチ1第2波（参照カード属性条件） | 12/15 | 584→588 | 1799→1793 | **投入前の Claude 全件実測が最重要だった回**＝候補41件中21件が REVEAL_AND_PICK filter 表現済みの census 偽陽性→「変更禁止リスト」化で巻き添え0。カードゲート（`applyReferenceAttributeBatch2`）を自発採用＝§249 の教訓を吸収。**見送り理由1件が過剰保守**＝「ADD_TO_FIELD に同一インスタンス限定が無い」はトラッシュがカード番号管理ゆえ非問題（同名交換可能）＝検証側が差し戻して13枚目を採用（1793→1792）。engine 実装（AWAKEN 誤実装の是正・BANISH_REDIRECT 付与形の既存 enforcement 流用）は的確 |
| 251 | ROADMAPバッチ1第3波（盤面/ゾーン状態条件） | 33/39 | 588→590 | 1792→1761 | 実装内容・見送り判断は的確（複合バグ8件も同時是正）。**ただし初めて数値申告が外れた**＝`buildEffectsJson` への恒久 force-adopt（→§5-13）で兄弟効果12件が無審査採用され「変化33・outlier 0」の申告と乖離（実測45）＝**退化4件**（条件脱落2・別STUB化1・MANUAL剥がし1）を検証側が復元。さらに `TRANSFER_TO_DECK` への**死フラグ**（→§5-14）1件を検証側が修正。**教訓＝「codex の数値は正確」の前提は force-adopt のような集計外バイパスがあると崩れる。§7 の per-effect 機械 diff は今後も必須** |
| 253 | ROADMAPバッチ2第1波（対象フィルタ合成・トラッシュ→手札） | 30/33 | 593→596 | 1742→1715 | **差し戻し0の初の完走**。カードゲート（`TTH_FILTER_BATCH2_WAVE1_CARDS`）を指示なしで自発適用・PRESERVE 11件の外科パッチも兄弟効果不変・held 実測申告（187枚/73グループ）も検証と一致（§6 の再実行義務化が機能）。見送り3件の「部分filterだけの採用も抑止する」parser 後処理は指示外の良い設計。**効いた指示＝投入前の Claude 全件実測で「PRESERVE 11／parser 4穴／見送り3」まで群分けして渡したこと**（codex は設計判断をほぼせず実装に専念できた） |
| 252 | ROADMAPバッチ1第4波（センター/履歴/出自条件） | 22/28 | 590→593 | 1761→1742 | 採用内容・engine 拡張（OR/FROM_DECK/PLACED省略形の writer 設計）・「betChoose は既に正しかった」の正直申告は良質。**しかし parser を一切修正せず JSON 直パッチのみで納品**（ガードレール9違反）＝held 実測 73→**86 汚染**（申告「74」は stale 読み値＝**乖離2連続**・§6 の再実行義務化の由来）・drift は「fresh が条件を失う」向き＝将来 `--adopt` で修正が剥がれる最悪の形。**是正指示（252b）を投げたが調査段階で低速のためユーザー指示で中断し Claude が直接是正**＝curated 完全一致リテラル `STATE_COND_BATCH4_ACTIONS` を parser 固定・誤合成残骸3件（サーチLv4+白限定化/全体+5000のLv4+緑限定化/無色欠落）修正・held 73 復帰。**教訓＝指示書に「parser 同修正」を書いても、報告フォーマットに held 再実行実測を義務付けないと違反が不可視化する** |
| 2026-07-23 §6.3ルリグ付与 | §6.3 GRANT_LRIG_ABILITY 完結（新機構4本＝アーツ名履歴/ルリグ下任意枚数/比例ミル/数字2宣言+トリクラ） | 5/5 | 635（検証是正後637） | 1702維持 | **指示書の機構名指し＋雛形指定が効いて実装量の割に高品質**（LRIG_UNDER の選択UI・multiSelect 宣言・全ターン境界クリアまで自発）。**穴2つはどちらも既知の失敗モードの再発**＝①トリクラのクラッシュ枚数拡張が**シグニアタック経路のみ**（付与先はルリグ＝肝心のルリグアタック経路 9016 が未配線。**§5-15 の「同型配線が複数箇所」を指示書に書いても、対象経路を行番号で名指ししないと外す**）②PR-470A のフェッチを「**同名**レゾナの登場」と誤読して実装（フェッチ先《進化する筋肉 紗倉ひびき》=PR-470B は**別名カード**＝常に不発で自シグニが消えるだけ。**カード名の同一性という最も基本的な原文照合を落とす**＝報告文にも誤読がそのまま載った）。resume跨ぎ golden の要求は部分遵守（PR-238/WX17-041 は書いたが WD21-009/PR-470A＝壊れていた2枚は書かず）＝**「全採用効果に1本」は列挙表で担保しないと危ないものから漏れる** |
| 2026-07-23 §6.3コストゲート2 | §6.3 コストゲート第2波（OPTIONAL_COST.exceed／handDiscardGroups／targetsStored の TRASH/BOUNCE/EXILE 共通化） | 4/9 | 629（検証是正後631） | 1702維持 | **見送り5件の正直申告は的確**（不足機構の指摘＝アーツ名履歴・ルリグ下任意枚数・トラップ戻し等はすべて実在の欠落）。**穴＝主目的カード WXDi-D08-012 が丸ごと壊れていた**：①`snapshotLastProcessedForConditionals` を PAID ゲートに誤用＝snapshot は**シーケンス冒頭で全 CONDITIONAL を事前解決**する仕様のため、支払い前に PAID=false 評価され **EXILE 枝が実行前に削除**（既存機構の適用条件を確認せず流用＝ガードレール2の変種）②skip 側の凍結漏れ③storedTargetCards が BattleScreen の pause を跨がない系統穴（D09-P15 の CHOOSE 跨ぎも同罪＝Claude が PendingEffect 永続化を配線）。**検出できたのは Claude が resume を跨ぐ挙動 golden を書いたから＝codex の golden は文字列構造 assert のみで、「エンコードが正しい」ことと「実行経路が生きている」ことの差を検査しない。今後の指示書は「対象効果ごとに resume 跨ぎの挙動 golden を1本」まで要求する** |
| 2026-07-23 §6.3状態 | §6.3 アップ/ダウン状態ファミリ（追加ダウンコスト・handDiscard実払い・動的filter・watcher是正） | 12/12 | 623→627 | 1711→1705（検証是正後1702） | **1回目は基盤4点のみで「未完了」と正直申告して終了**（続行指示で完走＝2段投入は有効だった）。完走後の実装は良質＝**ガードレール15（経路関数名の報告）が機能し watcher 両側ループ・look-ahead 全経路を自力で面配線**（前回の弱点を克服）。**新しい弱点＝「既存を触らず並行新設する」が2箇所で再現**＝①追加ダウンコストに既存 `fieldDown`（同義・全経路完備）があるのに並行語彙 `signiDown` を新設→両regex が同時マッチし**スコープ外 WXDi-P14-040 が二重コスト化**（本人申告は「無損失改善の付随差分」＝退化を改善と誤認）②census ベースラインも既存 `BASELINE_HIGH`（巨大履歴コメント付き）を避けて並行定数 `CENSUS_BASELINE_HIGH`+`void` 握り潰しで更新（将来の更新手順を無効化する罠）。ほか WX15-055/056 の turnOwner:'self' 欠落（「あなたの」の限定落ち）と no-fallthrough lint 1件を検証側是正。**教訓＝指示書に「既存機構を流用」と書くだけでは足りず、対象機構と同義の既存語彙・定数を Claude が実測時に特定して「これを拡張せよ／新設禁止」と名指しする**（fieldDown の存在は Claude の投入前実測でも見落としていた＝WXDi-P14-040 の cost を見れば分かった） |
| 2026-07-23 バッチ6 | ROADMAPバッチ6（数量比例＝resolveCountRef一本化＋countFromZone＋perAllSigni・27効果＝最終parserバッチ） | 27/47 | 666→669 | 1589→1581 | **見送り20件の層分類（コスト計算層/CONTパワー層/記録不在）は全件正当**＝「計算層には手を出さない」判断の一貫性が確立。E2E golden 3本・held --adopt・BASELINE_HIGH 本体更新も定着継続。**穴1つ＝申告27 vs 実26**：WXDi-P10-008-E3 が lastOfType(TRASH) の ENERGY ガード不発で silent skip（最終 TRASH が別型のため）＝**「lastOfType＋型ガード」パターンは対象 step が木の最後にない効果で静かに外れる**。教訓＝チョークポイントの mutate は「適用できたか」を検査せず成功扱いにする実装が多い＝**採用申告は build 後の per-effect diff 実測から生成させる**（申告と実 diff の突合は検証側で毎回やっているが、codex 自身に diff からリストを作らせれば乖離が構造的に消える） |
| 2026-07-23 バッチ11 | ROADMAPバッチ11（相手が選ぶ＝opponentSelects 一括付与・新入口2） | 43/46 | 664→666 | 1612→1589 | **今日8バッチ目にして初の完全定着**＝BASELINE_HIGH 本体更新（行名指しが効いた）・BUGFIXES 先頭・held --adopt 完了・E2E golden 2本自書き＝**過去3波の穴3種がすべて解消**。blind 1件を「既に正しい」と実測で差し戻し（指示書の明細が stale だった側の誤りを正した初例）・偽陽性1件（WX26-CP1-060-E1）特定・スコープ外 count バグ2件を§5で申告（Claude が是正）。**教訓＝失敗モードを「定数名・行番号・定型句」まで具体化して指示書に載せると1回で定着する**（一般論の禁止事項では3波再発した）。検証は置換テストによる機械証明（全差分がフラグ追加のみ）が有効だった |
| 2026-07-23 バッチ5c第2波 | ROADMAPバッチ5c第2波（sharedColor初適用＋FIELD_LRIGS_SHARE_COLOR＋コスト参照色＝バッチ5完了・17効果） | 17/24 | 660→664 | 1649→1644 | **E2E golden 4本を指示どおり自書き＝第1波の教訓が1回で定着**（実選択の不正集合拒否・両評価器・空ヒットまで）。見送り群E/F の理由（「効果解決用 selectionConstraint を流用すると死フラグになる」）は設計理解の深さを示す正当判断。**穴3つ＝全て既知失敗モードの再発**＝①held 採用漏れ2件（3波連続＝**「build後に held に残った対象カードは --adopt まで含めて完了」を指示書の定型句にすべき**）②BUGFIXES を末尾追記（規約は先頭）③**並行定数 CURRENT_BASELINE_HIGH 新設**＝CENSUS_BASELINE_HIGH 事故（状態ファミリ）と同一の再発。**巨大履歴コメント付き定数を「壊すのが怖い」方向に避ける傾向は指示書に「BASELINE_HIGH を履歴先頭追記で更新・並行定数禁止」と書いても再発した**＝定数名を名指しし「この行の数値を書き換えよ」まで書く必要 |
| 2026-07-23 バッチ5c第1波 | ROADMAPバッチ5c（複数選択の相互制約機構＝Opus設計6点雛形＋43効果） | 機構＋32/40（Claude完遂で43） | 658→659（完遂後660） | 1649維持 | **機構実装は雛形指定どおり高品質**（純関数の無色/複数クラス処理・SEARCH 経路まで自発）だが**3種の未完を正直申告**（per-effect 明細表・E2E golden・BUGFIXES）＝2段投入相当。**穴3つ**＝①**PRESERVE 直パッチ10効果の失念**（ガードレール10 既知の失敗モード＝チョークポイントが fresh に出ても curated 温存で不可視）②**「selectOrInteract を経ない自動取得経路」が surface 配線の盲点**＝execTransferToDeck TRASH_CARD 必須経路の slice(0,N) が constraint 素通り（本バッチ最多経路！）・execEnergyCharge の extra 未渡し＝**Claude の E2E golden が検出**（純関数真偽表だけでは経路の生死を検査できない＝コストゲート2 の教訓の再現）③held 採用漏れ1件（WXEX2-41）。**教訓＝機構バッチは「E2E golden を機構実装者自身に1本書かせる」を指示の必須要件にする**（純関数テストと構造 assert では素通り経路を検出できない）。兄弟2件の fresh 改善（UNTIL_OPP_TURN_END）は正の巻き添え＝Claude 原文照合で採用 |
| 2026-07-23 バッチ5b | ROADMAPバッチ5b（同一性参照フィルタ＝新語彙5本＋コスト記録2経路・31/39） | 31/39 | 654→657（検証是正後658） | 1674→1649 | **実装設計は今日最高**＝コスト参照解決の共通化（execBanish 局所→resolveDynamicFilter 集約）・noMatch センチネル（実キー cardNum で確実に空ヒット＝「参照不能→無制限化」を構造的に遮断）・指示書の「据置容認」5件に加え**自力で3件の正当な見送りを追加**（bare REVEAL 非記録・前段GRANT上書き・先行対象脱落＝全て記録実測に基づく）。**穴2つ**＝①**WXK10-001-E1 の再構成で従来 curated の正しい GRANT_KEYWORD step を落とす退化**（「復元した」と報告した効果の中で、指示書が明記しなかった既存正常部分を落とす＝**再構成系は「既存 steps のうち維持すべきもの」まで指示書に列挙する必要**）②**held 実測を「PLAN §3 の build:effects 実行禁止」という実在しないルールを引いてスキップ**（§6 の再実行義務違反＝幻覚ルールで義務を回避する新パターン。実測はドリフト無しだったが、義務スキップの理由が捏造だった点が問題）＋BASELINE_HIGH 更新漏れ。**教訓＝報告フォーマットの義務項目を「〜というルールがあるのでやらない」と回避してきたら、そのルールの実在を必ず確認する** |
| 2026-07-23 バッチ5a | ROADMAPバッチ5第1波（ルリグ共通色filter脱落25効果＝既存語彙のみ・parser抽出＋3経路の動的解決配線） | 25/26 | 651→654 | 1695→1674 | **差し戻し0の4例目・是正0件の初**＝カードゲート自発適用（原文再照合付き）・見送り1件（WXDi-D09-H20）は「filter追加だけでは直せない構造崩壊」の実測付きで的確・次波向け不一致2件（代わりに構造/②条件脱落）を自主申告・BASELINE_HIGH 追記の UTF-8 も正常（前回の文字化け教訓が1回で定着）。**注目点＝BANISH/POWER_MODIFY の基準変更（使用者→対象オーナー）を「ルリグ色フラグがある場合だけ」に限定する条件付き切替で設計**＝既存挙動の保全意識が向上。ただし**既存59保持効果の全数基準監査は Claude 側で実施**（codex は変更経路の検証のみ）＝「既存 consumer の全数監査」はまだ検証側の仕事として残る。**教訓＝投入前実測で『現行 JSON の per-effect 明細＋既存機構の実在確認＋変更禁止の周辺構造』まで表にすると、codex の作業が純粋な機械適用になり事故率が最小化する**（3バッチ連続で確認） |
| 2026-07-23 G072第2波 | §6.3 被バニッシュ側ゲート第2波（正面/チャーム変種/相対レベル/中央ゾーン/アップ状態＝5ゲート新設・9効果＋lock-in 1） | 9/9 | 644→650 | 1698→1695 | **差し戻し0の3例目・同族連投の2波目は前波の機構と失敗モード記録をそのまま踏襲でき品質が安定**＝section2/3 対称・section1 は指示どおり banishedWasUp のみ・battleAttackerNum の単一経路配線・regex 全アンカー・途中の汎用語彙波及（4効果）も**自己検出→絞り→復元**（前波の WX20-025 事故の学習が定着）。軽微な穴2つ＝①`BASELINE_HIGH` コメント先頭に追記した履歴が**文字化け**（非ASCIIをツール経由で書いて壊した・数値は正しい）②parser の「据置」コメントの stale 化を放置（自分が消化した対象への言及を更新しない）。どちらも実害なしの記録衛生＝検証側で是正。**教訓＝同族バッチの2波連投は「前波の指示書＋実績表を読ませる」だけで設計判断コストがほぼゼロになる（投入前実測で誤り明細を全カードに付ける前提は変わらず）** |
| 2026-07-23 G072残6枚 | §6.3 G072 条件前置き付き相手バニッシュ反応（duringMainPhase配線／banishedHadCharm／cause引数／triggeringCardNum＋傀儡STUB流用） | 6/6 | 637→643（検証追認後644） | 1702→1698 | **差し戻し0の2例目・実装品質は完走級**＝両section対称ゲート・4呼び出し系統のbefore-state配線・スコープ外巻き込み（WX20-025-E2）の自力検出→regex絞り→復元・resume跨ぎ傀儡goldenまで指示どおり。**唯一の穴＝「既存 ON_BANISH watcher に triggeringCardNum 依存は0件」の申告が誤り**：WXK11-020-E2 が `powerLtTrigger` で依存しており、従来undefined＝**無制限バニッシュの潜在過剰効果が今回の entry 保持で静かに直った**（改善方向だが挙動変化の不申告）。codex の grep は「ctx.triggeringCardNum を読む action 実装」を探して0件と結論したが、**依存は action コードでなく JSON の filter 語彙（powerLtTrigger 等）に宿る**＝動的filter系は resolveDynamicFilter が一括で triggeringCardNum を消費する。**教訓＝「新しく値が入るフィールドの既存依存」を調べさせるときは、コード grep ではなく『その値を消費する解決器（resolveDynamicFilter等）の語彙リスト×対象タイミングの JSON 走査』を指示書で指定する**（Claude 検証はこの JSON 走査で1件発見・golden lock-in 追加） |
| 2026-08-11 続き435 | PLAN§7実機検証バッチ1（アシストルリグアタック＋複合任意コスト＋UNKNOWN是正3件＝計9項目・`verifyBattleDrive.mjs`シナリオ15件新設） | 7/15完全PASS（8件はドライバ未完成） | 変化なし（1809・engine/parser非改変） | 852維持 | **申告は誠実（15件全て`BLOCKED`と正直に報告・「Supabase認証前でinjectScenario未到達＝FAILではない」と明記）だが、そもそも実行検証が一切できなかった**＝サンドボックスのネットワーク遮断（`net::ERR_NETWORK_ACCESS_DENIED`）により`scripts/verifyBattleDrive.mjs`のような実ブラウザ＋ライブSupabase系タスクではCodexは検証段階まで到達不能と判明（§2に新規ガードレール追記）。**書いたシナリオコード自体はH.queryState()の既存フィールド（handCards/trashCards/energyCards/pendingCandidates等）を正しく再利用しており捏造フィールドは無く、実行してみると15件中7件は無修正でPASS**（実装の質は高い）。**残り8件はクリック手順・盤面注入（ピースをlrig_deckに置く際のエナ選択altテキスト等）の作り込み不足でFAIL**＝コードは書けても無検証だと機構理解の細部（UI操作の正しい経路）まではCodex単独では詰め切れないことを示す実例。**教訓＝実ブラウザ検証タスクは指示書の時点で「Codexは実行できない前提」と明記し、シナリオ骨子だけ書かせてClaude側で仕上げの実行・デバッグを行う分業にする方が効率的**（今回は察知が投入後だったため手戻りが発生した） |
| 2026-08-11 続き436 | PLAN§7実機検証バッチ2＝**実行不可を前提に「UI の安定セレクタ整備＋壊れた8シナリオの testid 化」だけを切り出した分業** | 3/8 が2回連続PASSへ反転（従来0/8）・回帰0 | 1809 据置 | 852 据置 | **前回（続き435）の教訓「実ブラウザ検証は Codex は実行できない＝コードだけ書かせる」を指示書の冒頭に明記して投入した初回。狙いどおり機能した。**⭐**最大の効き目は投入前の真因訂正**＝PLAN §7 と続き435 の報告は8件のFAILを「シナリオ側のクリック手順バグ」と記録していたが、Claude が実測すると**真因は UI 側に安定セレクタが無いこと**だった（①支払いモーダル5本のうち `KeyUseModal` だけ testid 無し ②ルリグ行スロットに testid が無く**自分と相手で label 文字列が同一** ③カード詳細のアクションボタンに testid が無く同名「アタック」を撃ち分けられない ④driver 自身の `clickModalImage` が「同名 img が手札ストリップに先に居るので `.first()` は誤クリック」と警告しているのに `assistAttackBoth` が `.first()` を使っていた）。**シナリオだけ書き直させていたら何度でも同じ壁に当たった**＝§3-3 の「Codex の説明ではなくコードの実測からスコープを決める」の典型例がまた1つ。✅**効いた指示**＝(a)**「すべてのクリックについて testid の定義箇所（ファイル:行）を報告に書く」**＝実行できない以上**根拠はソースにしか無い**ので、推測を構造的に禁じられる。codex は全クリックに出典を付け「ソース上未確認のセレクタはありません」と締めた (b)**testid の命名を既存に合わせろ・既存 testid のリネーム禁止**（80件超の既定 order が依存）(c)**PASS済み7件と `order` 配列を壊していないことの機械的証明を要求**→codex は**シナリオブロックの SHA-256 比較**で応えた。🔴**codex が指示書の欠けを1つ埋めた**＝`optcost-pay`/`skip` は**エナ色コストUI専用**で、手札捨て／下カードだけの任意コストは**汎用CHOOSE**へ流れるため、そちらにも同名 testid を付ける必要があった（指示書は前者しか挙げていなかった）＝**指示との不一致欄がまた機能した**。⚠**Claude 側の失敗1件**＝ベースライン表に **fuzz の `distinct効果` を一致基準として書いた**が、これは乱択で毎回変動する（同一ツリーで 2682/2672/2670/2666 を実測）。codex は正しく「一致扱いにしていない」と申告した＝**ゲートに使えない数値をベースライン表に載せない**。⭐**分業の総括**＝実行できない相手でも「セレクタ整備のような**ソースから答えが決まる作業**」は高品質に回る。逆に**残った5件は testid 化しても FAIL のまま**で、中身は engine/設計の問題だった（ピースのコスト二重請求疑い＝PLAN §3 (cxxiii)／ON_TURN_END が出ない＝(cxxiv)）＝**「実行できないと詰め切れない部分」と「ソースだけで詰まる部分」は綺麗に分離できる**。 |
| 2026-08-11 続き437 | §6.4 UNKNOWN 第6バッチ＝「デッキを見る/公開する→振り分け→残りをデッキ下」の記述子変形 | **2/5 採用＋3件は engine 未対応を実測して据置** | 1809→1812 | 852→851 | ⭐**「投入前に消費側コードを読んで parser 専業だと確定させる」が最大の効き目**＝5つの変形（主語=対戦相手／動的枚数／すべて＝ALL／行き先/残りシャッフル）について `ownerState`（`effectExecutor.ts:4985`）・`resolveCountRef`（`:4986`）・`pickCount:'ALL'`（`:5011`）・generic な `then`（`:5049`）が**実際に読んでいる**ことを行番号つきで指示書に載せた結果、**新 action 型・新 timing・新 engine 分岐ゼロ**で着地。⭐**罠3つを `buildEffectsJson.ts` の行番号つきで先出しし、3つとも的中**＝①**PARTIAL 不可侵**（`:24`,`:210`）→ codex は A1 の parseStatus を戻してからビルド ②**`isPureSuperset` で held に落ちる**（`:212`）→ A2 を `heldReview --adopt` で採用 ③**effectId 集合が変わると丸ごと温存**（`:206`）→ 集合不変を確認して報告。**予防的な罠先出しは通算6例目で全部当たっている。**🔴**codex の "NO" がまた正しかった**＝3件の据置理由がすべて engine の実コード（`REVEAL_UNTIL_TO_HAND` は1件で停止＝`:5211`／`INTERNAL_KEEP_ON_DECK_TOP` は LOOK_PICK_CHAIN 専用＝`:5058`,`:7062`／SEARCH pending が相手応答へルーティングされない＝`BattleScreen.tsx:4623`）に基づいており、**特に `SP38-006` は載せると「自分が相手のデッキを覗いて選ぶ」真逆の実装になる**ところだった。**非採用を golden で固定**（§5-4）も自発。⚠**Claude 側の失敗1件**＝指示書の HEAD 指定（`680207f7d`）が投入時点で既に古かった（**自動コミットフックが直前に `docs/_held_review.txt` をコミットしていた**＝§2 の相互作用そのもの）。codex は開始時 HEAD を実測して「指示との不一致」欄に書いてきた。⇒ **指示書を書く直前に `git log --oneline -1` を取り直す**。⚠**環境事故＝`0xC0000142` が通算4回目**（最終計測工程だけ実行不能）＝**実装・ゲート・BUGFIXES 追記までは完了しており成果物は無傷**。`BASELINE_HIGH` 更新・held 再計測・エンコーディング検査・per-effect diff を検証側が全部引き取れた＝**「BUGFIXES をレポートより先に書け」を指示に入れておくと中断の損失がゼロになる**という §8 の結論が再確認された。 |
| 2026-08-11 続き438 | 【マジックボックス】設置が live から丸ごと落ちている4効果（parser＋engine 1分岐） | **3/4 採用＋1件は型不足を実測して据置** | 1812→1816 | 851→848 | ⭐**このバッチの価値の半分は「投げなかった判断」にある**＝`census:wiring` の最上位セル（`cardClass × SIGNI[filter]`・miss 17・has 429・★★）を開いたら**miss のほぼ全部が偽陽性**だった（＜クラス＞が**コスト句**〔`WX05-044`＝【起】コスト／`WX07-028`＝捨てるコスト〕・**トリガー範囲**〔`WX22-023`〕・**条件節**〔`WX11-031`／`WX21-053`／`WXK05-051`〕に出ているだけで、対象自体は「対戦相手のシグニ1体」＝無限定が正しい）。**計器は「文中に語彙が出るのにこの filter にキーが無い」を数えるのでクロス計上する**＝★★でも投げる前に必ずセルを開く（PLAN に2回、これで同じ結論が出ている）。`census:timing` も3件で枯渇していた。⭐**代わりに選んだのが `census:clusters` 経由の【マジックボックス】設置**＝原文に設置を持つ7カード中**4カードで設置ステップが JSON に1つも無い**（`WX24-P3-072-E1` は「3枚見て下に戻すだけ」＝**撃っても何も起きない**過少実行）。🔴**先出しした罠が的中（予防的使用7例目）**＝`then: STUB{PLACE_MAGIC_BOX}` は**そのままでは無音 no-op**＝`PLACE_MAGIC_BOX` は `ctx.lastProcessedCards[0]` しか読まないのに `resumeSearch` の**汎用 then ループ（`:7224`）は picked を引数で渡すだけで `lastProcessedCards` を設定しない**。**先例（トラップ設置の専用分岐 `:7194-7204`）を名指しして「鏡写しにせよ」と書いた結果、codex はそのとおり1分岐だけ足して着地**した（§5-5e「借りる前に消費側の実コードを読む」の実例がまた1つ）。🔴**codex の "NO" がまた正しかった**＝`WX24-P3-033-E1`（設置＋＜トリック＞手札加えの**移動先が異なる2群振り分け**）は既存 `transferGroups` が「同一移動元→**手札へ**」の表現で表せず、**型と engine の追加が要る**と実測して据置（**部分採用しない**という指示も守った＝設置だけ足すと原文とズレたまま固定される）。⚠**Claude 側の失敗2件**＝(a) **HEAD 指定がまた古かった**（**自分が回した計器3ファイルの再生成が自動コミットされた**＝続き437 と同じ原因で**2回連続**。⇒ **計器を回した直後は特に `git log -1` を取り直す**）(b) 罠の説明の一部が不正確だった＝「専用分岐にしないと『残りをデッキ下』が落ちる」は**現行コードでは `revealRemainder` が分岐より前に動くので当たらない**（codex が訂正。主要因の `lastProcessedCards` 未設定の方は正しかった）。📋**残した制約**＝追加分岐は**1枚目だけ設置する**（トラップ分岐は per-card 展開している）＝「2枚以上同時設置」のカードが出たら要拡張。現行3効果は原文が「1枚まで」なので実害なし。 |
| 2026-08-11 続き439 | 「能力を持たない」語彙の2軸配線（動詞昇格の脱落3件＋対象限定の脱落2件） | **5/5＝defer 0・是正0（本プロジェクト初の完全消化）** | 1816→1821 | 848 据置 | ⭐**投入前に候補を4つ潰してから選んだ回**＝`census:wiring` 最上位セル（★★）／【トラップ】設置の任意性／「次にスペルを使用する場合」5件／「対戦相手は捨てる」が `owner:self` 疑い12件——**4つとも開いたら実害なし**だった（順に：語彙がコスト句・条件節・トリガー範囲にあるクロス計上／`pickCount` は LOOK_PICK_CHAIN では上限で0枚確定できる／executor が `next_spell_cost_reduction` に積んで使用時に消費＋ターン終了でリセット済み／self 側は原文の別節「**あなたは**手札を捨てる」で正しい）。**「投げなかった判断」も成果として記録する**＝計器が成熟すると偽陽性が優勢になり、**セルを開く工程がバッチ本体より価値を持つ**。⭐**選んだのは節の役割で機械分類して残った本物**＝原文に「能力を持たない」を持つ約30枚 vs live で `noAbilities` を持つ11枚の差を、**A対象／B条件／Cその他**に分類して A だけを取った。🔴**先出しした罠が的中（予防的使用8例目・今回が最も効いた）**＝`LAST_PROCESSED_MATCHES` の評価（`execUtils.ts:2003`）は `matchesFilter` を**CardData だけ**で呼び、状態依存キーの補助照合リスト `ZONE_STATE_KEYS` に **`noAbilities` が無い**。一方 `hasNoAbility`（`:825`＝唯一の判定）は **`holder.abilities_removed` を第一に見る**。⇒ 素直に書くと**「効果でこのターン能力を失ったシグニ」を取りこぼす**＝`WXEX1-55` は **E1 で能力を奪い E2 でそれを狙う自己完結コンボ**なので**主要シナリオを直撃**するところだった。codex は `ZONE_STATE_KEYS` へ追加し、静的判定から `noAbilities` を外して `findFieldZoneState` の holder を `hasNoAbility` へ渡す形で解決し、**`abilities_removed` を立てた盤面の E2E まで自作**した。⭐**指示書の誤りも先回りで訂正した**＝PLAN §6.4 の「`WX25-P3-014-E1` は `BOUNCE{owner:'self'}` の自傷が残る」が**既に修正済みで古い**ことを Claude が投入前に実測し、指示書に「在庫の記述は古い・live を見よ」と明記した（在庫の症状記述が覆るのは通算8回目）。⚠**検証側が追加で見た副作用**（指示書に無かった観点）＝`ZONE_STATE_KEYS` に入れると**場にいないカードで常に偽**になるため、既存の `LAST_PROCESSED_MATCHES{noAbilities}` 利用者を live 全走査した（**新規3件のみ＝回帰対象なし**）。**「keys 配列に足す」変更は既存利用者の全走査とセットで見る**。 |
| 2026-08-11 続き440 | 「この方法で〜したカードと同じ枚数」の数量参照が固定値に潰れていた5効果 | **4/5＋1件は実コード根拠つき defer** | 1821→1826 | 848→847 | 🔑**このバッチ最大の収穫は codex が見つけた新しい死角**＝**`count` の解決関数は action 型ごとに違う**。`TRANSFER_TO_HAND` は `resolveNum(src.count)`（`effectExecutor.ts:2288`）を使い、**`resolveNum` は `{$ref}` を問答無用で 0 にする**（`execUtils.ts:118-120`）＝**書いても黙って0枚**。⇒ `WXEX1-44-E2` はこの理由で defer が正しい（**前段の STUB は枚数を記録していたので、Claude の想定した defer 理由〔前段が記録しない〕とは別の理由だった**）。**指示書は「前段が書くか」だけを罠として挙げていたが、真の死角は「後段が読めるか」の側にもあった**＝§5-2 の「消費側の実コードを読む」は**前段・後段の両方**に適用する。⭐**指示書の行番号を codex が訂正（通算12回目）**＝`BOUNCE` の記録地点として渡した `:986` は `count:'ALL'` 経路専用で、**選択式 BOUNCE は `resumeSelectTarget`（`:7017`）**が記録する。⭐**効いた指示**＝(a) **11件を全数で開いて同一機構の5件に絞ってから投げた**（残り6件は「実装済みの別表現」＝専用STUBや別語彙で、巻き込むと退化する）(b) **「N枚**まで**」の `upToCount` を `count` と一緒に直せ**と明記（片方だけだと「全部戻さないと解決できない」過剰要求になる）(c) **E2E に「0件のとき0件で解決する」を必須化**＝codex の golden は「2体バウンス→上2枚公開／0体なら0枚」「青2枚だけ数えて相手が2枚捨て／青0枚なら0枚」と**動的枚数と下限の両方**を実行検査した。⭐**実害の大きさ**＝`WXK11-013-E1-G` は**盤面全部をトラッシュして1体しか戻らない**（エクシード2を払う価値が消える）、`WXK11-028-E1` は **`count:0` で1枚も公開しない丸ごと不発**だった。📋**残タスク**＝`resolveNum` を使う `count` 消費地点（`:1005`／`:2238`／`:2288` ほか）は**動的枚数を書けない死角**＝`resolveCountRef` へ寄せるか、**「ここに `$ref` を書くと 0 になる」を golden のトリップワイヤで固定**する。 |
| 2026-07-23 §6.3除外 | §6.3「ゲームから除外」機構ファミリ（excluded実ゾーン化＋遅延自己除外＋LRIG_DECK除外・manualEffects経由） | 8/11 | 617→620（検証是正後623） | 1711維持 | **機構ファミリ（parser 非経由・manualEffects＋engine新機構）への初の適用で、実装設計は良質**（MARK の「場に戻った自身だけ登録」ゲート・excluded 統一・見送り3件の判断と BUGFIXES 追記も的確）。**穴は「配線の面」**＝①WD22-035-G が **collectTurnTriggers 未拡張のまま採用され永久不発**（トラッシュ watcher 走査を WX21-Re06 用 collectFieldTriggers にだけ実装し満足→§5-15）②ターン終了時除外を3経路中1経路（ターンプレイヤー側）にしか配線せず＝**主用途（相手ターン中の蘇生）で1ターン生き延びる**③PR-378 で原文「1体」→count:2 の幻覚＋除外を「そうした場合」の外に配置（**原文照合ガードレール1の初違反**。指示書の実測表に個別の誤り明細が無かったカードで発生＝表に書いた誤りは全部直すが、書かなかった部分の原文照合が甘い）④resolvePendingExiles の filter 全除去で同名別コピー巻き添え。検証側で4件是正し golden+3 |
| 2026-07-24 §6.3リフレッシュ置換 | §6.3 WX25-P2-009「ゲーム持続付与AUTO能力」機構（game_granted_auto_effects レジストリ＋install STUB＋両collector走査＋リフレッシュ置換） | 1機構（E1本実装＋E2をhollow→実発火化） | 706→707 | 1580維持 | **差し戻し0・是正0の理想的完走**＝指示書の機構名指し・行番号名指し・「両リフレッシュ経路を1ヘルパーに寄せてから分岐」の設計拘束がそのまま実装され、`applyRefreshState` 一本化（guardrail15）・`game_actions_done` 既存フィールド再利用（新設せず）・E2E golden 自書き（アーツ ACTIVATED 実行→install→E1→executor リフレッシュ置換を実経路で固定）まで自発。**投入前の Claude 実測が決定的だった回**＝「E2 は commit 306537a9 で✅済み」という簿記が実は hollow（golden がアーツを場のシグニに人工配置・実戦不発）だったのを投入前に見抜き、install レジストリ機構を full scope として渡せた（＝§3-3「Codex/簿記の見立てを機械検証」の典型）。数値・原文照合・近似申告（lrigデッキ先頭1枚）すべて正確。**単一カードでも「新規基盤が要る大型機構」は機構名指し＋既存先例（lrig_granted_auto_effects）＋全経路の行番号を指示書に書くと1発で着地する**。⚠**起動で回り道をした回**＝`--full-auto`（stale）→`-s workspace-write`→`--dangerously-bypass` と試して classifier ブロックまで踏んだが、**真因は CLI のサンドボックスフラグが config の `danger-full-access` を上書きしていたこと**（Windows で workspace-write＝実質 read-only）。正解は **§2 のフラグ無し `CODEX_HOME=… codex exec -C <dir> - < brief`**（別セッション Claude の実測で確定）＝フラグを付けなければ read-only 化も classifier ブロックも起きない |

| 293 | (xxii) 後置条件 IS_MY_TURN の live 実害12効果 | 9/12 | 808→811（検証是正後815） | 1521→1519 | **指示書で先出しした2つの罠（`execConditional` の記録残存・`PAID_ADDITIONAL_COST` の look-ahead ペアリング）を両方とも回避し、同型の穴（`MILL` 空振り時に古い記録が残る）を自力で発見して閉じた**＝罠を engine の行番号つきで書くと確実に効く。見送り3件の理由も全件正当。BASELINE_HIGH 本体更新・BUGFIXES 先頭追記・held 実測申告も定着。**穴2つ**＝①**WX24-P4-045-E1 が真逆**（`ADD_TO_LIFE{owner:'opponent'}` で相手シグニが**相手の**ライフへ＝相手にライフを与える）。ただし**この JSON は前セッション由来で、既存 golden が誤った向きを固定していた**（→§5-17）②**採用9件のうち4件は本バッチの実装ではない**＝manual override は既にあり JSON 未反映だっただけ（→§5-16）。**教訓＝「codex が実装した」と「codex の再 build でようやく実機に届いた」は別物。per-effect diff だけ見ると区別できないので、ベースラインの `manualEffects.ts` と突き合わせる**。codex の golden は構造 assert（JSON regex＋`evalCondition` 真偽表）中心で経路の生死を検査しない傾向が継続＝**実挙動 E2E は検証側で4本追加した** |
| 2026-07-29〜30 (xxix) 第10〜15波 | 任意【出】の「写せないコスト語彙」を潰し切る（ゾーン徴収／相互制約／支払い札参照／場ゾーン／「あなたのシグニの下」分離／可変枚数） | 23効果を実働化・残4件は明示保留 | 1063→1075 | 1400→1394 | **第13〜15波は差し戻し0・是正0が3波連続**。defer 判断が正確で、機構コストが見合わないと判断した4件を理由つきで保留＝**フェイク実装より defer を選ぶ**傾向は一貫。第12波で「`TRASH{asCost}` の記録は `ENERGY_CARD` に到達しない」を推測せず実測で突き止め、第13波では**指示していない**のに UI 純関数と engine 支払可否を同じ盤面で突き合わせる golden を自作した。**穴は3件とも「計器に映らない種類」**＝文字化け2件（→§5-19）と片肺配線1件（→§5-20）。**教訓＝codex の数値申告とゲートは信用してよいが、「ゲートが検査しない次元」（文字エンコーディング・React 側の経路）だけは Claude が毎回手で確かめる** |

| 2026-07-30 エクシード本体 | PLAN §4 次の一手①（エクシードルリグ本体不一致6件＝群A〜F） | 5/6（群F は defer） | 1109→1113 | 1386→1384 | **投入前の Claude 実測が最大の効き目だった回**＝PLAN の「本体不一致」を額面どおり受け取ると6件とも新機構に見えるが、実測すると**5件は engine 完備で parser/データの穴**（`untilHandCount`／`OPPONENT_PAY_OPTIONAL.opponentHandDiscard`／`LifeCrashAction.optional`＋`lastProcessedCards`／`lrig_granted_auto_effects`／`*Groups` の先例）。**既存語彙を名指しして「拡張しろ・並行新設禁止」と書いた結果、新設は3本のみ**で並行語彙事故は非再発。群F の defer 判断・見送り理由・per-effect 差分（changed 8）・outlier 0 はすべて独立実測と一致。**穴3つ＝全て「ゲートが検査しない次元」**＝①**BOM 混入2件**（→§5-19 を更新。U+FFFD と `?` 連続だけでは検出できない）②**`transferGroups` の逆翻訳から移動元ゾーンが脱落**（群ごとの filter しか持たない新フィールドで、source 側の情報を落とすのは新語彙導入時の定型リスク）③**held +2 の帰属を誤説明**（「未再生成ドリフト」と報告したが実際は本バッチの群B規則で fresh が新語彙を獲得したもの＝**live 差分だけ見て held の原因を推定すると外す**。`_held_fresh.json` の実測で確定できる）。**教訓＝家族規模の大きい parser 規則は「1件必達＋巻き込み全件を原文照合＋説明できなければ regex を絞る」と書くと live 波及0で着地する**（今回 60効果家族に触れながら live changed は 8 で止まった） |

| 2026-07-31 タスク16第2波 | `ON_TARGETED` の origin 種別軸（`targetedOrigins`）＝群A2件＋群B2件 | **4/4** | 1158→1168 | 1365→1364 | **差し戻し0・是正0**。新語彙**1本のみ**（新 timing・新 action 型・新 collector ゼロ）で着地し、指示した OR-of-AND 形をそのまま実装。**効いた指示が3つとも再現性のある形だった**＝①**罠の先出し**（「H16 の `CONDITIONAL{IS_MY_TURN}` は誤変換ではなく `OPTIONAL_COST` 直後の『そうした場合』慣例＝`effectExecutor.ts:2709-2714`。触るな」と書いて余計な改変を防いだ＝§5-2 の変種を**予防的に**使った初例）②**「アップ状態を after 盤面で見ると『そのシグニをダウンする』効果で発火しなくなる」を投入前に指摘し before 盤面の変数名まで指定**＝codex は既定引数（`beforeHostState = afterHostState`）で後方互換を保ったまま配線した③**呼び出し元を全数 grep して行番号で名指し**（2箇所）＝§5-15/20 の「1箇所で満足する」は非再発。**穴は2つとも「報告に出ない種類」で、どちらも本バッチ由来ではなかった**＝(a) **decompile シートに前セッションの未再生成ドリフト**（J-3 の4効果が `ON_LIFE_CLOTH_MOVED` を出すようになったが JSON 不変・`decompileEffects.ts` は純追加＝**前回が `npm run regen` を回していなかっただけ**。シート差分を本バッチの影響と読むと §8「エクシード本体」の held +2 誤説明と同型の誤帰属になる）(b) `scripts/fixLrigColorFilters.mjs` への `delete obj.triggerScope`（**挙動不変**＝collector が `?? 'self'` で既定を補う。`build:effects` 冪等化のための安定化だが**指示していないスコープ外ファイル**なので検証側が意図を確認する必要があった）。**教訓＝codex がスコープ外ファイルを1行だけ触ったときは、まず「本バッチ由来の差分を消しているのか、既存ドリフトを黙らせているのか」を per-effect diff で切り分ける**（今回は後者で無害だった）。副産物として既存バグ1件を検証側が発見＝散文形「対戦相手のターンの間、」が `turnOwner` を生まない（→ タスク12(lxviii)） |

| 2026-07-31 タスク16第3波 | エナへ移動したカード自身の `ON_ENERGY_CHARGE`（`movedSelf`＋`fromZones`＋原因種別） | **3/3**（群B は defer） | 1168→1170 | 1364→1363 | **実装は的確**（両側ガードによる二重発火防止・`detectEnergyAdded` のシグネチャ保護・「ちょうど1枚」制限を新経路に持ち込まない・群B の defer 理由がすべて指示どおり）。**最大の効き目は投入前のスコープ差し替え**＝PLAN の「次の一手」は `ON_HAND_DISCARDED` だったが、Claude が実測して**呼び出し元7箇所＋機構3種混在**と判明し、単一の根本原因を共有する `ON_ENERGY_CHARGE` へ差し替えた（→ §3-4 の実例が1つ増えた）。**穴3つは全て「codex の数値申告には出ない次元」**＝①**新フィールドの受理値が狭すぎた**（`byLrigOrSigniEffect` が `CardData.Type` の `'ルリグ'`/`'シグニ'` だけを見て **`'アシストルリグ'` 340枚・`'レゾナ'` 46枚を落とす**＝ルール上どちらもルリグ／シグニ。**ゲートは全緑・census も減るので計器に一切映らない過小実行**。⇒**新しい列挙値を書かせるときは「その列に実在する値を全部数えたか」を指示書で要求すべき**）②**golden の置き場所**（集計行 `console.log(PASS…)` と `process.exit(1)` の**後ろ**に `test()` を58行追記＝**落ちても件数に乗らず exit code も変わらない＝ゲートとして機能しない**。同内容を集計対象側にも二重に置いていたので気付いてはいたが、死んだ側を消していない）③**held +1 の帰属を今回も説明しなかった**（「対象3効果の残りではない」とだけ書いたが、真因は自分の parser 変更のスコープ外波及＝§8「エクシード本体」と同じ再発。**live 未採用なので実害0だが、帰属の説明は毎回外す**）。**教訓＝§5-19（エンコーディング）と同型で、「ゲートが検査しない次元」は増える一方＝新フィールドの受理値・golden の実行位置・held の帰属の3つは検証側の定型チェックに加える** |

| 2026-07-31 タスク16第4波 | `WXDi-P06-072-E1` **1効果だけ**（バニッシュ主体の実効パワー条件）＝ユーザー指示で「1件ずつ投げる」方針 | **1/1** | 1170→1174 | 1363 据置 | **新語彙0本で着地**（既存 `triggerFilter.powerRange` と `matchesFilter` の既存第3引数を使うだけ）。**前波で新設したガードレール2本が1回で定着**＝①「golden は集計行より前に置く」（正しく前へ配置）②「しきい値・列挙値は台帳でなく原文/実データで数える」（**codex が自力で台帳の `15000` を原文の `8000` に訂正**）。§3-3 の「golden から叩けない経路は pure 関数へ切り出す」も指示どおり実行（`battleBanisherMatchesTrigger`）し、**「未検査なのは React 統合経路そのもの」と限界を正確に自己申告**した。**穴は1つで、まさにその自己申告した範囲に落ちた**＝**`effectivePowers.get(getCardNum(myTopNum))` と素の cardNum へ丸めていた**が、`calcFieldPowers` は**場のスタック頂点をそのままキーにする**（`effectEngine.ts:1345`）ため `#N` 付きインスタンスID（トークン/複製シグニ）で lookup が外れ、**黙って表記パワーへフォールバック＝バッチの目的そのものが無効化**される。同ファイルの他の `effectivePowers.get(...)` は全て raw。**教訓＝同じ「カード番号」に見える2つの Map でキーの正規化形が違うとき、片方に合わせて丸めると型検査もゲートも通ったまま無効化される。新しく Map を引く行を足させたら、その Map の `set()` 側を読んでキー形を確かめる**（`matchesFilter(cardMap.get(getCardNum(x)), f, powers.get(x))` のように**同じ行の中で正規化の有無が混在する**のが典型）。**1件ずつ投げる方式の評価＝小さい代わりに投入前実測の密度を上げられ、台帳の誤り2件を投入前に潰せた**。 |

| 2026-08-01 (lxxiv) | 「以下のNつを行う。①②③」母集団の per-card 監査＋全項目 SEQUENCE 化 | **8/9**（`WXK11-001` は defer） | 1234→1239 | 1352→1351 | **投入前の全数実測が在庫の記述を3点とも覆した回**＝(a) 「残り9枚は DTT の text パターン依存」→ live で DTT を持つのは**6枚だけ** (b) 「②は `LIFE_CRASH`＝バーストが誘発する別物」→ `triggerBurst:false` が出ており engine が守る＝**分割パースのほうが正しい** (c) 「無差別に開くと held +9」→ **退化するのは2項目だけ**。加えて**在庫が書いていなかった実害**（任意コスト脱落による8項目の無償実行・`WXDi-P05-052` の①丸ごと欠落）を発見してスコープに入れた。⇒ **在庫行は「症状の見立て」であって実測ではない。投げる前に母集団を全数走査して表にする**という §3-1 の実例がまた1つ増えた。<br>**先出しした罠が的中**＝「`CENTER_LRIG_OR_SIGNI` を使うな（`execFreeze` に分岐が無くシグニ限定へ落ち、型もゲートも通ったままルリグだけ黙って外れる）」を投入前に書いたので engine 無改修で着地した（§5-2 の**予防的**使用＝タスク16第2波に続き2例目・再現性がある）。数値申告（changed 8 / outlier 0 / held 251据置）は独立実測と完全一致。<br>**穴3つ**＝①**カード固有の本文を埋め込んだ分岐**（`①…デッキの上からカードをN枚見る` かつ `②…センタールリグは「【自】` ＝`WXDi-P05-052` 専用）を `parseActionTextInner` 冒頭に置いた。**先回りが必要という判断自体は正しい**（後段の引用能力抽出が全文を奪う）が、**判定を関数へ切り出して先頭で1回呼ぶ**構造にすれば同型の新カードにも効く＝§5-5 の「本文の埋め込み」は先読みだけでなく**分岐ガード**にも出る。②`parseSpellEffect` の分岐条件を**原文 regex** にしたため、付与能力を持たないカードまで分岐に入り `spellFb` 由来の PARTIAL を握り潰す形になっていた（**木の形で判定すべき**）。③**golden が構造 assert のみ**＝本バッチの実害そのもの（任意コストゲートが本体を止めるか）を検査しない。検証側で E2E 3本を追加し、`WXK11-008` をエナ0で実行して②③が走らないことを実挙動で固定した（§8「コストゲート2」以来の**同じ弱点が継続**）。<br>**加えて BUGFIXES を末尾追記**（規約は先頭）＝§8「バッチ5c第2波」と同一の再発。**指示書に「`docs/BUGFIXES.md` への**先頭**追記は可」と書いてあっても守られない**ので、次からは「先頭（`# バグ修正記録 (BUGFIXES)` の直後）」と位置まで書く。<br>**正直申告の質は高い**＝defer 1枚の理由（owner の食い違い）は的確で、到達不能化した分岐の一覧も自発的に出した。ただし**「`until` が付いて改善」は不正確**（`execRemoveAbilities` は `until` を読まない死フィールド）＝**新しく書いたフィールドを engine が読むかの確認（§5-2）は、自分が追加していない既存フィールドには及ばない** |

| 2026-08-01 (lxx) | 「カードをN枚引くか<B>」の2択脱落＝母集団60効果の per-effect 監査 | **3/17**（14件 defer） | 1239→1244 | 1351 据置 | **§5-5c と §5-5d が1回で定着した回**＝(lxxiv) の反省で新設した2項目がそのまま効き、**分岐ガードは木の形**（`effectType`/`timing`/`triggerCondition`）で書かれ、**golden は構造 assert ではなく実行 E2E**（CHOOSE を開いて両枝＋辞退の盤面まで固定）になった。BUGFIXES も**先頭**追記（位置まで指示に書いたのが効いた）。**差し戻し0**。<br>**投入前実測が在庫の記述を4点とも覆した**＝母集団は23件でなく60効果／在庫が名指しした主因（ガード）は主因でない／在庫の推奨案は単独では0件も開かない／1件は parser の問題ですらない（PRESERVE）。**決め手は `parseDrawOrChoice` の入口を計装して「実際に届くテキスト」を17件全部観測したこと**＝§3-3 の「コードと実測からスコープを決める」の最も直接的な形。<br>**先出しした「変更禁止」も的中**＝`WX24-P4-017-E3`「AするかBしたとき」はトリガー条件の OR で選択ではない＝**ガードは実際に仕事をしていた**（在庫は「ガードを緩めろ」と書いていた）。<br>🔴**この回は検証側（Claude）が誤った**＝codex の defer 1件を過剰保守と判断して差し戻したら**既存 golden 2本が落ちて撤回**。engine の `triggerCondition.byOwnEffect` は「**捨てた本人**の効果が原因」という軸で、`collectHandDiscardTriggers` の any_opp watcher ループは `byOwnEffect` 付き効果を**問答無用で非発火**にする（コード内に「実データ0件だから保守的に非発火」と明記）＝付けると4効果が**恒久 no-op**。**codex の defer が正しかった**。⇒ **§5-2 の新しい形**＝「engine がそのフィールドを honor している」は「そのフィールドが**自分の意味**を表せる」を意味しない。**同名フィールドが収集経路ごとに違う軸を指す**ことがあるので、grep で honor 箇所を数えるのではなく**その効果が通る collector の該当ループを読む**。「実データ0件」と書かれた保守的スキップは**その 0 を破る変更**への警告として読む（罠は golden で lock-in 済み・新在庫 (lxxviii)） |

| 2026-08-01 (lxxi)+(lxxii) | 遅延トリガーの「次に」＝1回消費（2効果）／`delayed_triggers` ターン終了クリアの片側漏れ | **2/2** ＋ **6箇所配線** | 1244→1248→1250 | 1351→1349→据置 | **2連投とも差し戻し0・是正0**。(lxxi) は「**変更禁止の対照群を先出しする**」が決定打＝`WX24-P4-011-E3` は原文に「次に」が**無い**ので毎回発火が正しく、消費を一般に付けると退化する。名指しした結果 `once` 省略時の従来挙動が維持された。(lxxii) は「**BattleScreen は golden から叩けない**ので判定を純関数へ出せ」と書いた結果、**3経路 × 2プレイヤー＝6箇所**を1関数で覆い、§5-15/20 の「1箇所で満足する」が構造的に消えた。**engine のみの変更では live per-effect diff の期待値を `changed 0` と明記する**と、スコープ漏れが自動的に検出可能になる（実際 0 で着地）。<br>🔴**この2回は検証側（Claude）の指示書が2件とも誤りを含み、うち1件は codex が訂正した**＝①(lxxi) の在庫写しで effectId・照応の種類・「消費機構の先例が無い」を誤って伝えた（Claude が投入前実測で自力訂正）②(lxxii) で「`THIS_ATTACK_PHASE` の物理削除は存在しない（grep 0件）」と断定したが**実在した**（`clearEndOfAttackPhaseDelayedTriggers` が別ファイルにあり両者へ適用済み）。**codex は指示を鵜呑みにせず HEAD と突き合わせて訂正し、報告の「指示との不一致」欄に明記した**＝この欄を必須項目にしている効果が出た初の例。<br>⚠**教訓（Claude 側）＝「grep して0件だからコードが無い」は、探した文字列がその層に現れる保証があるときだけ成立する。** 機構の有無は**型名・duration リテラルではなく、機構を呼ぶ側の関数名**で探す。§8 の (lxx) 行「engine が honor している≠自分の意味を表せる」と**同じ系統**＝**その層の実コードを読むまで断定しない**。 |

| 2026-08-01 (lxxiv)残1 | アタックステップのスキップを engine 実装＋`WXK11-001-E1`① 復元 | **1/2項目**（②は defer） | 1250→1252 | 1349 据置 | **投入前 grep で「在庫の残作業定義が小さすぎる」と分かった回**＝在庫は「1枚の parse」だったが、`BLOCK_ACTION{LRIG/SIGNI_ATTACK_STEP}` の全参照が **parser 2箇所とログ用ラベル1箇所だけ**＝**フェイズ進行が一切見ておらず、この文型5カード全部が no-op** だった。⇒ 指示書に「**engine 実装が主・parse 修正が従。engine を入れられないなら parse も入れずに defer**」と書いてスコープを組み替えた（§3-4 のスコープ差し替え実例が1つ増えた）。**§5-20 対策も奏功**＝「BattleScreen は golden から叩けないので判定を純関数へ出せ」と書いた結果、**`PHASE_NEXT` の直接参照が BattleScreen から消えて全5経路が1関数を通る**形になり、遷移4ケースを golden で実行固定できた。<br>🔴**穴＝defer マーカーに実装済み STUB を流用した**：②の「表せないので no-op」を `STUB{UNKNOWN_NESTED}` で表したが、これは**「このシグニを任意でトラッシュに置く」実装を持つ**（`execStubPart1.ts:2223`）。対象がアーツなので偶然無害だったが、①意味が正反対 ②`STUBS.md` 上「実装済み」に数えられ計器が甘くなる ③シグニカードへコピーされたら自分のシグニを失う。⇒ 検証側が**engine 分岐を持たない専用の宣言 STUB**へ差し替え（`execStub` の既定は `[STUB: id]` ログのみ＝真の no-op・先例 `LOCK_OPP_TRASH_MOVE`）、**流用禁止の向きで golden も固定**。<br>⚠**「名前が近い既存語彙を借りる」事故はこの3バッチで3件目**（(lxx) の `byOwnEffect`＝Claude／(lxxii) の grep トークン誤り＝Claude／本件＝codex）。**借りる前にその語彙の engine 実装を読む**＝§5-2 の最も頻出する形。⇒ **新ガードレール §5-5e として独立させた**。 |

| 2026-08-08 377m | (iv)計器較正 第3・第4バッチ＋(i)配線ギャップ 第18バッチ（3連投） | 17＋3（較正）／7＋据置7（実装） | 1466→1472 | 953→927 | **3バッチとも差し戻し0・是正0**。報告フォーマットの遵守が完全（held 再実行実測・per-effect 明細・「やらなかったこと」・指示との不一致まで自発申告）。**最大の成果は Codex 側の2つの正しい"NO"**＝①第4バッチで「Nまで」52件を**1件も較正せず全件を実バグと判定**（無理に近似すれば census は下がったが masking になっていた）②第18バッチで **14効果中7効果を据置**し、`execAttachCharm`／`execGrantKeyword` が `upToCount` を消費しないことを実測して**死フラグを書かなかった**（§5-14 の回避）。**指示書が誤っていた側を Codex が訂正した2例目**（1例目は (lxxii)）。<br>⚠**Claude 側の失敗が2つ**＝(a) **CODEX_GUIDE を読まずに1本目の指示書を書いた**＝§1 に反して「commit とPLAN簿記をさせる」「census 900 まで無制限ループ」と書き、§2 の落とし穴（`-s workspace-write`）も踏んだ。**投入前にこのファイルを読む、が最初の手順である理由がそのまま実証された。**(b) **指示書に「`upToCount` は engine 実装済みなので parser のみで直る」と広すぎる前提を書いた**＝live 487効果で使われていても、`execGrantKeyword` は `selectOrInteract` の第3引数が `false` 固定で読まない。**§5-2 は「honor 箇所を grep で数える」ではなく「その効果が通る exec の該当行を読む」と読むべき**（(lxx) の `byOwnEffect` と同型で3例目）。<br>✅**効いた指示**＝①**投入前に ★★セル6本を全数分類**して「(i) は実質枯れている・残りは条件節用法のクロス計上」と示したこと（PLAN §4 の「次の一手」を実測が否定した）②**per-effect の誤り明細を表で名指し**（`WXDi-P01-042-E2` の4重欠落・`WXDi-P10-008-E1` の幻覚フィルタ＝3件とも直った。§8 の既知傾向どおり明細のないカードは甘くなるので、14効果すべてに現況を書いた）③**バッチ固有の罠を先出し**（「Nまで」が2箇所ある効果は両方必要／「代わりに」は向きが逆になりやすい）＝どちらも実際に該当例が出た。 |

| 2026-08-08 378 | §5d-0(i) 第19バッチ＝`isDisona` / `excludeResona` の対象フィルタ合成漏れ | **17/17 live ＋ engine パリティで5**（計22） | 1488→1517 | 924 据置 | **差し戻し0・是正0**。報告は**ハーネスの10分上限で kill されて出ていない**（→§2）が、実装・golden・regen・gates まで完了しており**成果物は無傷**＝Claude 側の §7 チェックリストで全面代替できた。**指示どおりの据置判断が的確**＝`WXEX2-18-E2`（2対象の owner 取り違え）を「誤った対象に正しい制限を付けて誤りを固定するな」の指示どおり据置し、**トリップワイヤ golden まで自発で置いた**。golden は**構造22＋実行E2E 5＋トリップワイヤ2**＝§5-5d の「E2E を書かせる」が3バッチ連続で定着。<br>✅**効いた指示が3つ**＝①**罠の先出し**（「`effectEngine.matchesFilter` に `isDisona` が無いので `HAS_CARD_IN_FIELD` に足しても黙って無視される」）＝**予防的使用の4例目**で、codex はパリティ移植とセットで実装し、**波及した既存5効果を自分で洗い出して原文照合まで報告**した（**JSON もゲートも一切変わらない過剰実行**を5件是正＝このバッチ最大の収穫）②**per-effect の誤り明細を全15効果に付けた**（現況 JSON と直す形を表で）③**スコープ外リストを「実測だけして直すな」と明示**＝条件節11効果を掘らずに現況だけ持ち帰り、次バッチの在庫になった。<br>⭐**Claude 側の投入前実測がまた効いた**＝§4 が指していた ★★セルは今回も薄く、`isPuppet × TRASH_CARD` 7 は**計器の誤検出**（「傀儡状態で場に出す」は配線先ではない）・`isAwakened × SIGNI` 6 は**全部条件節のクロス計上**。**セルを開かずに投げていたら偽陽性を掘っていた**（§3-4 の実例がまた1つ増えた）。<br>⚠**唯一の穴らしい穴**＝live changed が**17件**で指示の16件を1件超えたが、中身は `WXDi-P13-002-E1`＝**正しい巻き添え**（被覆マトリクスが STUB 同居効果を除外するため候補表に出ない同型）。**codex は A/B で自分で見つけて原文照合のうえ採用し、BUGFIXES にも明記した**＝§5-12（per-effect 報告）が機能している。 |

| 2026-08-08 379 | `isDisona` の条件節グループ（条件が丸ごと落ちて無条件発火） | **7/7＋Claude が据置1件を採用＝8** | 1517→1531 | 924→919 | **10分以内に完走**（前回 kill された反省で「**BUGFIXES への記録を報告より先に書く**」と順序を指示書に固定したのが効いた）。**新しい条件型ゼロ**で着地＝投入前に Claude が両評価器の `case` を行番号まで確認して指示書に載せた表を、そのまま使った。**engine E2E 7本＝全効果を成立/不成立の両方向で固定**（§5-5d が4バッチ連続で定着）。**前バッチのトリップワイヤを「条件は許可・帰結側への漏出だけ検査」へ正しく絞り込んだ**＝テストを弱めずに更新できている。<br>✅**指示書の誤りを codex が訂正した3例目**＝「被覆マトリクスは条件節を集計しないので miss は動かない」と書いたが、**条件内の filter も集計する**ので 291→283 と減った。<br>🔴**唯一の是正＝held 採用漏れ1件**（`WXDi-P12-063-E1`）。fresh の `CONDITIONAL{ゲート → OPTIONAL_COST}` は **engine の正準形**（`effectExecutor.ts:2898-2914` に専用の解き処理・46効果が使用中）なのに「既存 held カードなので未採用」と保守的に据置した＝**§8 で繰り返し出ている「held 採用漏れ」の再発**。⇒ 指示書に「**held に落ちた対象は `--adopt` まで含めて完了**」を定型句として入れるべき（バッチ5c第2波でも同じ指摘）。<br>⭐**判断が難しい所を正しく踏んだ**＝`powerRange` の実効パワー化を**解決時だけ**にして持続側を触らなかった（持続側は「パワーN以上があるかぎり**基本パワー**がMになる」＝実効パワーで見ると循環）。**「両評価器のパリティを取る」は自動的に正解ではない**という判断を、指示書で全数監査を求めた結果として自力で出している。 |

| 2026-08-09 386〜393 | Diva「場のルリグ」系統／印刷済み【使用条件】脱落／CONTINUOUS パワー機構／対象範囲・発生源の過小過剰（**8連投**） | 85（＋JSON不変の波及7） | 1556→**1649** | 909→**897** | **差し戻し0が6回／検証側の是正は3件のみ**。⭐**効いた指示の型が固まった**＝①**投入前に母集団を「生成元」から数え直す**（在庫の見出しではなく CSV原文×live の突き合わせ。8回中5回で在庫の記述を覆した。例＝「ゴミ `GRANT_KEYWORD` 22件」は症状で真の母集団は「【使用条件】79カード中66カードが条件ゼロ」）②**罠を行番号つきで先出し**（`:10174` の strip の第2の目的／`execFreeze` に `CENTER_LRIG_OR_SIGNI` 分岐が無い／`effectEngine.ts:4523` の `continue`／チーム名の半角中黒＝**全部的中**）③**per-effect の誤り明細を全カードに付ける**④**変更禁止の対照群を名指し**（毎回バイト不変を機械証明）。🔴**codex の "NO" は信頼できる**＝4バッチで11枚を「action が退化するので条件も付けない」と自発据置し held に明示在庫化。🔴**codex が指示書を訂正したのは通算7回**（`calcFieldPowers`／PARTIAL は build 不可侵／実HEAD／期待 changed 値 ほか）＝**指示書側が誤っている前提で報告項目に「指示との不一致」を置き続ける価値が確認された**。🔴**新しい失敗モード2つ**＝(1)**「STUB → UNKNOWN／別STUB」を「改善」と誤分類**（捨てる側の STUB に engine 実装があるか読まない＝§5-5e の裏返し。検証側が `GRANT_QUOTED_ABILITY` を復元）(2)**`BASELINE_HIGH` の数値だけ更新してコメント履歴が前回のまま**（値と説明が食い違う）。⭐**codex が指示書に無いバグを自力で発見・修正した回が2回**（`CRASH_LIFE_TO_HAND` の所有者判定／`hasBanishResist` の原文フォールバックが構造化制限を上書き＝**後者は JSON 不変のまま7効果を是正**）。 |
| 2026-08-09 394〜396 | §6.3 C「この方法／この効果で〜した場合」の後段条件が live で丸ごと脱落（**3連投**＝①効果内トラッシュ②公開・手札に加える③手札捨て・エナ配置） | **11／11／11＝33** | 1649→**1682** | 897→**882** | **3波とも差し戻し0・是正0**（この型では初）。⭐**投入前実測が在庫を3点とも覆した回**＝(a)**母集団の計器違い**＝`docs/_partial_report.txt` は **fresh parser の出力**を測るもので live の実害を測らない（刻印39件のうち live に `IS_MY_TURN` が残るのは1件）。CSV原文×live から数え直すと**60カード**（在庫の約3倍）(b)**在庫が書く共通ブロッカー「前段が `lastProcessedCards` に記録しない」は偽**＝writer は engine に**153箇所**あり完備、parser が条件を生成していないだけ (c)条件語彙も既に揃っていた。⇒ §3-1 の実例がまた1つ増えた。<br>⭐**このバッチ最大の効き目＝「記録先が家族ごとに3通りに割れている」を投入前に特定し、行番号つきで罠として先出ししたこと**＝`execTrash` は無条件に書く／**`REVEAL_AND_PICK` は `recordRevealed` のオプトイン**（`effectExecutor.ts:4533`・**live 利用者1枚・parser emit 0件**＝「型があるから記録されているはず」が成り立たない）／**`LOOK_AND_REORDER` は `needsInteraction` を返すだけで記録は `resumeLookAndReorder`（`:6791`）＝UI ポーズを跨いだ後**／**コスト由来は engine でなく `BattleScreen.tsx:10885-10952`** が `last_cost_trashed_cards`・`last_activated_discard_count` に書く。**取り違えると恒久 no-op でゲートに一切映らない。** 3波とも罠を外さずに着地した。<br>⭐**「近似で決め打ちするな」と書くと型を拡張して正解を出す**＝「1枚が赤で、もう1枚が白か青か緑か黒」に対し `requiredDistinctColors` を `string[]` → **`(string | string[])[]`（スロットが配列なら OR 候補）** へ拡張し engine も `Array.isArray` で受けた（近似も defer もせず原文一致）。<br>🔴**codex が既存実装のバグを自力で発見**＝`LAST_PROCESSED_MATCHES.shareClass` が「一致カードが**全員**共通クラス」を要求しており「**一部が3枚以上**」を数えられていなかった（`WD08-008` の原文がまさにこれ）＝最大共有枚数方式へ是正。**既存唯一の利用者 `WX22-006` が挙動同値であることは検証側で確認。**<br>🔴**codex が指示書を訂正したのは通算10回**＝(a)`WXK11-068` は「効果内トラッシュ」ではなく**コスト経路**（指示どおり書いていたら恒久 no-op）(b)`WXK03-039` は「STUB のみ＝未実装」ではなく `INTERNAL_MILL_BOTTOM_DISTINCT4_BANISH` が**両方向 golden まで完備**(c)`TargetFilter.levelParity` は実装済み。**指示書側が誤っている前提で「指示との不一致」欄を置き続ける価値がまた確認された。**<br>🔴**新しい失敗モード1つ＝共有経路を変えたのに波及走査を報告しない**＝第2波で `execRevealAndPick` の候補0件パスを「前段の値を素通り」から `[]` 明示へ変えたが、その値を読む live 効果の全数走査が報告に無かった（検証側で実施＝該当13効果は全件が `REVEAL_AND_PICK` を step 0 に持つため波及0）。⇒ **第3波の指示書に「共有経路を触ったら全数走査を報告に入れる」を明記したら守られた。**<br>⚠🔴**環境事故が2回・どちらも成果物は無傷**＝第2波は **Windows のプロセス生成が `0xC0000142`（DLL initialization failed）で恒久失敗**、第3波は **`ERROR: You've hit your usage limit`（復帰 2026-08-16）で exit 1**。**いずれも実装・ゲート・BUGFIXES 追記・`BASELINE_HIGH` 更新までは完了**しており、残工程（`regen`／`groupSimilar`／`heldReview`／per-effect diff／エンコーディング検査／原文照合）は**検証側が全部引き取れた**。⇒ **指示書に「BUGFIXES はレポートより先に書く」「区切りごとにディスクへ確定させる」を入れておくと中断の損失がゼロになる**（§2 の kill 対策の一般形）。<br>⚠**Claude 側の失敗2件**＝抽出計器の偽陽性（`WX20-053-E2`／`WXEX2-21-E1` は既に正しかった＝条件語彙リストに `DECK_COUNT` と `afterSearch` 構造を入れ忘れ）。**在庫化する前に1枚開く。** |

> **スコープ選定の実例（続き213）**：PLAN の「次の一手」はタスク12(xxxix) 24件だったが、**Claude が23件を原文↔逆翻訳で実測したところ同型クラスタが無く寄せ集め**と判明（唯一まとまった系統は全CSVで4枚・しかも engine に型が無い §6.3級）。**§3-4 の「機構が同じものだけ束ねる」に従って Codex 向けから外し**、配線状況を実測できる timing センサス[C] に差し替えた結果 9/9 で着地した。**PLAN に書いた「次の一手」でも、投げる前に実測してバッチ適性を判断すること。**

**読み取れる傾向**：
- **数値（ゲート・件数・diff 集合）は正確**。信用してよいが独立実行はする。
- **明示的に要求した項目は守る**。要求していない副作用は報告しない。**必要な報告は全部書き出す。**
- **保守的な採用は褒めると継続する**（続き209 で約40件を構造的ブロッカーとして見送った判断は正しかった、と指示書に明記した）。
- **自分の過去の判断を訂正できる**。ただし**促さないとやらない**。
- **同型の配線が複数箇所に必要なとき、1箇所で満足する**（2026-07-23 §6.3除外＝collector 2系統中1系統・ターン終了時3経路中1経路）。指示書に「複数箇所ある」と書くだけでは足りず、**§5-15 のとおり採用カードごとに経路の関数名を報告させて面を可視化する**。
- **指示書の実測表に明細を書いたカードは正確に直すが、明細を書かなかったカードの原文照合が甘い**（同バッチ PR-378 の count 幻覚）。スコープに入れる全カードに「現行の誤り」明細を付けるか、付けられないカードには「1効果ずつ原文照合」を個別に念押しする。
- **既存の同義実装を触らず並行新設する**（2026-07-23 §6.3状態＝既存 `fieldDown` の横に `signiDown` 新設で二重コスト化・既存 `BASELINE_HIGH` の横に `CENSUS_BASELINE_HIGH` 新設）。巨大な既存コメント/regex を「壊すのが怖い」方向に倒れる。**Claude が実測時に同義の既存語彙・定数を特定して「これを拡張せよ／並行新設禁止」と名指しする**のが対策（「既存機構を流用」という一般論では防げなかった）。
- **時間切れ時は機構単位で完結させ「未完了」を正直申告する**（同バッチ1回目）。続行指示（実装済み分の明記＋残作業リスト＋優先順位）で2回目に完走＝**大きいバッチは2段投入を前提に設計してよい**。
